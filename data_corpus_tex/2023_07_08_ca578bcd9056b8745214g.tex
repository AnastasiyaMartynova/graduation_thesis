\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{mathrsfs}

\begin{document}
семейство линейных операторов на $X$, все $\|F(t)\| \leqslant 1$, $F(0)=I$ и

\[
\lim _{t \rightarrow+0}\left\|t^{-1}(F(t)-I) f-A f\right\|=0
\]

для $f \in \mathscr{M}$ (сушественной области определения $A$, то есть $\left.\overline{\left.A\right|_{\mathscr{Q}}}=A\right)$. Тогда

\[
T(t) f=\lim _{n \rightarrow \infty} F(t / n)^{n} f
\]

для всех $f \in X$. При $F(t)=U(t) V(t)$, где $U(\cdot)$ и $V(\cdot)$ - полугруппы сжатий, получается Троттера формула.

Jum.: [1] Cher noff P., «J. Funct. Anal.», 1968, v. 2, p. 238-42; [2] Браттели У., Робинсон Д., Операторные алгебры и квантовая статистическая механика, пер. с англ., М., 1982.

А. В. Булинский.

ЧЕTAEBA ПРИНЦИП максим ума работы - один из вариационных принципов классической механики: работа

\[
A_{\delta}=\sum_{i}\left(F_{i}-m_{i} w_{i}\right)\left(v_{i}+\frac{\delta v_{i}}{d t} \frac{d t}{2}\right) d t+\ldots
\]

на элементарном цикле, состояшем из прямого движения в поле заданных сил и движения обратного в поле сил, которых было бы достаточно для создания действительного движения, если бы механическая система была совершенно свободной; здесь $F_{i}\left(t, r_{j}, \dot{r}_{j}\right) \in C^{1}-$ активные силы, $m_{i}-$ масса точки $r_{j}$ системы, $v_{i}=\dot{r}_{i}, w_{i}=\ddot{r}_{i}$. Ч. п. позволяет расширить характер обычно рассматриваемых механич. систем путем привлечения принципа Карно из термодинамики.

Принцип предложен Н. Г. Четаевым (1941). В. В. Румянцев.

ЧЕТАЕВА ФУНКЦИЯ - см. Устойчивость.

ЧETЫPEXMEPHЫЙ BEKTOP в п р о с т р а с т в е - в р е м е н и - объект, определяемый четырьмя действительными величинами, преобразующимися соответствуюшим образом при Лоренца преобразованиях. Так, пространственные и временная координаты мировой точки (события) в галилеевой (псевдоевклидовой) системе координат образуют к о н травариантный четырехмерный вектор

\[
\left(x^{0}, x^{1}, x^{2}, x^{3}\right) \text {. }
\]

Здесь $x^{0}=t-$ время, $\left(x^{1}, x^{2}, x^{3}\right)=\boldsymbol{r}-$ пространственные координаты. Аналогично преобразуются и дифференциалы. Другой пример - ко вар и ан т ный че ты рехмерны й в е к т ор

\[
\left(x_{0}, x_{1}, x_{2}, x_{3}\right), x_{0}=t, x_{s}=-x^{s} .
\]

Ме т р ический (псевдоевклидов) тензор $\eta_{i j}$ пространства-времени устанавливает соответствие

\[
\eta_{i j}=\operatorname{diag}(1,-1,-1,-1)
\]

между этими двумя типами векторов, а именно:

\[
x_{i}=\eta_{i j} x^{j} ; i, j=0,1,2,3 .
\]

Скалярное произведение двух Ч. в. $\boldsymbol{a}$ и $\boldsymbol{b}$ получается при свертке контравариантных компонент одного с ковариантными компонентами другого, то есть

\[
a_{i} b^{i}=a^{i} b_{i}=(\boldsymbol{a}, \boldsymbol{b}),
\]

при этом, очевидно, квадрат нормы

\[
(\boldsymbol{a})^{2}=(\boldsymbol{a}, \boldsymbol{a})=a_{0}^{2}-a_{1}^{2}-a_{2}^{2}-a_{3}^{2} .
\]

Ч. в. разделяются на три класса в соответствии со знаком их нормы $|\boldsymbol{a}|$ :

\begin{enumerate}
  \item $a_{i} a^{i}<0, \quad a-$ пространственноподобный ве к т о р, $s$ (space)-вектор,

  \item $a_{i} a^{i}>0, \quad a-$ времениподобный вектор, $t$ (time)-вектор,

  \item $a_{i} a^{i}=0$, светоподобный, или изотропный, в е к т о р, $l$ (light)-вектор.

\end{enumerate}

Эта классификация соответствует положению Ч. в. по отношению к световому конусу $x_{i} x^{i}=0$. Первый и второй случай различаются еще знаком временной компоненты: если $a^{0}>0$, то вектор направлен в будущее, если $a^{0}<0$, то вектор направлен в прошлое.

Два $s$-вектора могут быть ортогональны: $(\boldsymbol{a}, \boldsymbol{b})=0$, однако два ненулевые $t$-вектора таковыми быть не могут; ортогональность $s$-векторов равносильна их линейной зависимости.

Если Ч. в., соединяющий мировые точки двух событий, времениподобен, то существует система отсчета, в к-рой события происходят в одной и той же точке трехмерного пространства $\boldsymbol{r}_{1}=\boldsymbol{r}_{2}$. Время, разделяющее события в этой системе отсчета, равно $\tau=\left|t_{2}-t_{1}\right|$, где $\tau-$ собственное время. Ни в какой системе отсчета эти события не могут быть одновременными (то есть иметь равные координаты $t$ ). Если Ч. в., соединяющий мировые точки двух событий, пространственноподобен, то сушествует система отсчета, в К-рой эти два события происходят одновременно $t_{1}=t_{2}$; они не связаны причинно-следственной связью, и модуль интервала $\left|\boldsymbol{r}_{2}-\boldsymbol{r}_{1}\right|$ (также всегда положительный) определяет пространственное расстояние между этими двумя точками в этой системе отсчета.

Лит.: [1] Фе д о р о в Ф. И., Группа Лоренца, М., 1979.

М. И. Войцеховский.

ЧИСТАЯ ФАЗА - см. Статистической механики математические задачи.

ЧИСТОЕ ПРЕОБРАЗОВАНИЕ ЛОРЕНЦА - СМ. БУс

ЧИСТОЕ СОСТОЯНИЕ - состояние физической системы, плотности матрица $\rho$ которого является проекционным оператором. В этом случае оператор $\rho$ представим в виде:

\[
\rho=\frac{|\psi\rangle\langle\psi|}{\langle\psi \mid \psi\rangle},
\]

где $\psi$ - нек-рый ненулевой вектор в гильбертовом пространстве. Вектор $\psi$ определяется по матрице плотности с точностью до множителя и называется вектором состояния.

Состояние с матрицей плотности $\rho$ является чистым тогда, когда выполняется хотя бы одно из следующих четырех утверждений

\begin{enumerate}
  \item Существует наблюдаемая с чисто дискретным спектром, дисперсия к-рой в состоянии $\rho$ равна нулю.

  \item $\rho^{2}=\rho$.

  \item Оператор $\rho$ не представим в виде $\alpha_{1} \rho_{1}+\alpha_{2} \rho_{2}$, где $\alpha_{1}$, $\alpha_{2}>0$, а $\rho_{1}, \rho_{2}-$ положительные операторы с единичным следом.

  \item Энтропия системы $W=-\mathrm{Sp}(\rho \ln \rho)=0$.

\end{enumerate}

См. также Плотности оператор. $\quad Ф$. А. Лунев.

ЧИСТЫЙ АНСАМБЛЬ - см. Плотности матрица.

ЧИСТЫЙ ПРОЦЕСС РОЖДЕНИЯ - см. Рождения и гибели процесс.


\end{document}