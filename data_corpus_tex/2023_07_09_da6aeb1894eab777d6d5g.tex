\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
При этом кручение не зависит от базисов $b_{i}$ в групшах границ, а только от $c_{i}$ и $h_{i}$.

Пусть дана пара $(K, L)$, состоящая из конечного связного клеточного разбиения $K$ п подкомплекса $L$, являющегося деформационным ретрактом $K$. Пусть $\Pi \cong \pi_{1}(K) \cong \pi_{1}(L)$. Если $\tilde{K}$ и $\tilde{L}$ - универсальные накрывающие разбиений $K$ и $L$, то $\sigma \in \Pi$ определяет клеточное отображение $\sigma:(\tilde{k}, \tilde{i}) \rightarrow(\tilde{K}, \tilde{L})$, а следовательно, и отображение групп цепей $\sigma_{*}: C(\tilde{K}, \tilde{L}) \rightarrow C(\tilde{K}$, $\tilde{L})$, т. е. $C_{p}(\tilde{K}, \tilde{L})$ является $\mathbb{Z}[\Pi]$-модулем. Получается свободный цепной комплекс $C_{n}(\tilde{K}, \tilde{L}) \rightarrow G_{n-1}(\tilde{K}, \tilde{L}) \rightarrow$ $\rightarrow \ldots C_{0}(\tilde{K}, \tilde{L})$ над $\mathbb{Z}[\Pi]$. Гомологии этого комплекса тривиальны, т. е. $\tilde{L}$ - деформационный ретpakт $\tilde{K}$.

Пусть $e_{1}, \ldots, e_{\alpha}$ суть $p$-клетки в $K \backslash L$. Для каждой клетки $e_{i}$ выбирается клетка-представитель $\tilde{e}_{i}$ в $\tilde{K}$, лежащая над $e_{i}$, и фиксируется ее ориентация. Тогда $c_{p}=\left(\tilde{e}_{1}, \ldots, \tilde{e}_{\alpha}\right)$ - базис в $C_{p}(\tilde{K}, \tilde{L})$. Следовательно, определено подмножество $\tau C(\tilde{K}, \tilde{L}) \subset \bar{K}_{1} \mathbb{Z}[\Pi]$, т. к. кручение, вообще говоря, зависит от выбора базиса $c_{p}$. Однако уже образ этого множества в группе Уайтхеда $W h(\Pi)$ состоит из одного әлемента $\tau(K, L)$ и наз. к р $\mathbf{y}$ ч е ни е м $\boldsymbol{\nabla}$ а й т х е д а п а ры $(K, L)$.

Важным свойством У.к. является его комбинаторная инвариантность. Является ли $\tau(K, L)$ топологич. инвариантом, неиввестно (1984).

Пусть $f: X \rightarrow Y$ - гомотопич. эквивалентность $(X$ и $Y$ - клеточные комплексы). Тогда кручение отображения $f$ определяется как $\tau(f)=f_{*} \tau\left(M_{f}, X\right) \in W h\left(\pi_{1} Y\right)$, где $M_{f}$ - цилиндр отображения $f$. Если $\tau(f)=0$, то $f$ наз. П р о с т о Й го мот о п и ч с с ко ̆ в а л е н т но с т ь ю. Свойства кручения $\tau(f):$ 1) если $i: L \rightarrow K-$ включение, то $\tau(i)=\tau(K, L) ; 2)$ $\left.\tau(g \circ f)=\tau(g)+g_{*} \tau(f) ; 3\right)$ если $f$ гомотопно $f^{\prime}$, то $\tau(f)=$ $=\tau\left(f^{\prime}\right)$; 4) если $I$ - тождественное отображение односвязного комплекса с эйлеровой характеристикой $\chi$, то $\tau(I \times f)=\chi \cdot \tau(f)$.

JIum.: [1] W w i t e h e a d J. H. C., "Amer. J. Math.", 1950, v. 72, ${ }^{1967}$, т. 11 , № 1, с. $3-42$.

УАЙТЕДА ПРОИЗВЕДЕНИЕ элементов гомотопич. групп пунктированного топологич. пространства - см. Уайтхеда умножение.

УАЙТХЕДА УМНОЖЕНИЕ - Умножение в гомотопических группах $\pi_{m}(X) \times \pi_{m}(X) \rightarrow \pi_{n+m-1}(X)$, определенное Дж. Уайтхедом [1]. Пусть в $S^{k}$ фиксировано разбиение на две клетки $e^{0}$ и $e^{k}$. Тогда в произведении сфер $S^{m} \times S^{n}$ индуцируется разбиение на клетки $e^{0}, e^{m}, e^{n}, e^{m+n}$. Поэтому характеристич. отображение $\varphi_{n, m}$ :

\[
\partial e^{n+m}=S^{n+m-1} \longrightarrow S^{m} \times S^{n}
\]

разлагается в композицию

\[
S^{n+m-1} \stackrel{W(m, n)}{\longrightarrow} S^{m} \vee S^{n} \longrightarrow S^{m} \times S^{n}
\]

где $S^{m} \vee S^{n}-$ букет сфер. Пусть, теперь, клаесы $\alpha \in \pi_{m}(X)$ и $\beta \in \pi_{n}(X)$ представляются отображениями $f$ и $g$. Тогда п р о и в е д е ние у а й тхе д а $[\alpha, \beta] \in \pi_{n+m-1}(X)$ представляется композицией отображений

\[
\stackrel{W(n, m)}{\longrightarrow} S^{m} \vee S^{n} \stackrel{f \vee g}{\longrightarrow} X
\]

Для этого умножения выполняются следующие свойства:

\begin{enumerate}
  \item $[\alpha, \beta]=(-1)^{\operatorname{deg} \alpha \operatorname{deg} \beta}[\beta, \alpha]$

  \item если $\alpha, \beta \in \pi_{1}(X)$, то $[\alpha, \beta]=\alpha \beta \alpha^{-1} \beta-1$

  \item если $X \quad n$-просто, то $[\alpha, \beta]=0$ для $\alpha \in \pi_{1}(X) ; \beta \in$ $\in \pi_{n}(X)$ 4) если $[\alpha, \beta]=0$ для любых $\alpha \in \pi_{1}(X), \beta \in \pi_{n}(X)$, то $X \quad n$-просто;

  \item если $\alpha \in \pi_{n}(X), \beta \in \pi_{m}(X), \gamma \in \pi_{k}(X), n, m, k>1$, то $(-1)^{n k}[[\alpha, \beta], \gamma]+(-1)^{m n}[[\beta, \gamma], \alpha]+(-1)^{m k}[[\gamma, \alpha]$, $\beta]=0$;

  \item элемент $\left[i_{1}, i_{2}\right] \in \pi_{3}\left(S^{2}\right)$, где $i_{2} \in \pi_{2}\left(S^{2}\right)=\mathbb{Z}-$ образующая, равен удвоенной образующей групшы $\pi_{3}\left(S^{2}\right)=\mathbb{Z}$

  \item ядро эпиморфизма $\Sigma: \pi_{4 n-1}\left(S^{2 n}\right) \rightarrow \pi_{4 n}\left(S^{2 n+1}\right)$ порождается одним әлементом $\left[i_{2 n}, i_{2 n}\right] \in \pi_{4 n-1}\left(S^{2 n}\right)$, где $i_{2 n} \in \pi_{2 n}\left(S^{2 n}\right)$ - канонич. образующая.

\end{enumerate}

Jum.. [1].W h i t e h e a d G. W., "Ann. Math.», 1946, v. 47, У. 460-75; 1950, v. 51, p. 192-237. ранство расслоения $p_{n}:(X, n) \rightarrow X$, для к-рого гомотопич. группы $\pi_{i}(X, n)=0$ при $i<n$ и такое, тто отображение $p_{n *}: \pi_{i}(X, \quad n) \rightarrow \pi_{i}(X)$ - изоморфиям при $i \geqslant n$. Пространство $(X, n)$ строится по индукции по $n$. Если пространство $(X, n-1)$ уже построено, то за $(X, n)$ принимается гомотопич. слой естественного вложения в Эйленбераа - Маклейна пространство $(X, n-1) \longrightarrow K\left(\pi_{n-1}(X), n-1\right)$.

Последовательность пространств $(X, n)$ и отображений $p_{n}$ является системой Мура - Постникова отображения $* \rightarrow X$. A. Ф. Харшиладзе.

УБЫВАЮЩАЯ ПОСЛЕДОВАТЕЛЬНОСТЬ - такая последовательность $x_{n}$, что для всех $n=1,2, \ldots$ выполняется неравенство $x_{n}>x_{n+1}$. Иногда такие последовательности наз. с т р о г о у б ы в а ю Щ и м, а термин «У. п.» применяется к последоватөльностям, удовлетворяющим для всех $n$ лишь условию $x_{n} \geqslant x_{n+1}$. Такие последовательности наз. также н е в о з р а ст а ю щ и м и. Всякая ограниченная снизу невозрастающая последовательность имеет конечный предел, а всякая не ограниченная снизу имеет бесконечный предел, равный - $\infty$. л. Д. Кудрлвчев.

УБЫВАЮЩАЯ ФУНКЦИЯ - такая функция $f(x)$, определенная на нек-ром числовом множестве $E$, что из условия

\[
x^{\prime}<x^{\prime \prime}, x^{\prime}, x^{\prime \prime} \in E
\]

следует

\[
f\left(x^{\prime}\right)>f\left(x^{\prime \prime}\right)
\]

Иногда такие функции наз. с т р о г о у б ы в а ющ и м и, а термин «У. ф.» применяется к функциям, удовлетворяющим для указанных $x^{\prime}, x^{\prime \prime}$ лишь условию $f\left(x^{\prime}\right) \geqslant f\left(x^{\prime \prime}\right) \quad$ (не в о з р а с т а ю щ а я ф у нк ци я). У всякой строго У. ф. обратная функция является однозначной и также строго убывающей. Если $x_{0}-$ правосторонняя (соответственно левосторонняя) предельная точка множества $E, f(x)-$ невозрастающая функция и множество $\left\{y: y=f(x), x>x_{0}, x \in E\right\}$ ограничено сверху (соответственно $\left\{y: y=f(x), \quad x<x_{0}\right.$, $x \in E\}$ ограничено снизу), то при $x \rightarrow x_{0}+0$ (соответственно $\left.x \rightarrow x_{0}-0\right), x \in E$, у функции $f(x)$ существует конечный предел; если же указанное множество не ограничено сверху (соответственно снизу), то $f(x)$ имеет бесконечный предел, равный $+\infty$ (соответственно $-\infty)$. УГЛОВАЯ ТОЧКА - то же, что излома точка.

УГЛОВОЕ ГРАНИЧНОЕ ЗНАЧЕНИЕ, Г р а Н И ч-

\includegraphics[max width=\textwidth, center]{2023_07_09_da6aeb1894eab777d6d5g-1}
ны м п у т я м,- значение комплексной функции $f(z)$, определенной в единичном круге $D=\{z \in \mathbb{C}:$ $|z|<1\}, \quad$ в граничной точке $\zeta=e^{i \theta}$, равное пределу

\[
\lim _{z \rightarrow \zeta, z \in S(\zeta, \varepsilon)} f(z)=f^{*}(\zeta)
\]

фұнкции $f(z)$ по множеству точек угловой области

\[
S(\zeta, \varepsilon)=\left\{z=r e^{i \varphi} \in D:\left|\arg \left(e^{i \theta}-z\right)\right|<\frac{\pi}{2}-\varepsilon\right\}
\]


\end{document}