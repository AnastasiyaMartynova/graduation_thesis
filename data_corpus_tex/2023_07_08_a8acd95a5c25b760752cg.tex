\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
круг $K_{1}$; граничная точка $\zeta$ называется о с о бо й точ ко й, если А. П. в нее невозможно. На границе круга сходимости любого степенного ряда имеется, по крайней мере, одна особая точка.

Выбрав на плоскости $\mathbb{C}$ какую-либо кривую $L$, соединяющую центр исходного элемента $z_{0}$ с нек-рой точкой $z_{N}$ (см. рис. 2), и постепенно осушествляя процесс непосред-

\begin{center}
\includegraphics[max width=\textwidth]{2023_07_08_a8acd95a5c25b760752cg-1(1)}
\end{center}

Рис. 2. Аналитическое продолжение вдоль кривой $L$.

ственного А. П. вдоль кривой $L$, получают цепочку канонич. элементов $\left(K_{0}, f_{0}\right),\left(K_{1}, f_{1}\right), \ldots,\left(K_{N}, f_{N}\right)$, осуществляющую А. П. исходного элемента $\left(K_{0}, f_{0}\right)$ в обшую часть всех кругов цепочки $K_{0}, K_{1}, \ldots, K_{N}$ и позволяющую, в частности, вычислить значение функции $f_{\text {в точке }} z_{N}$. Следует иметь, однако, в виду, что этот процесс А. П. осуществим не всегда (не для всех кривых $L$ и не для всех точек $z_{N}$ ). Во-первых, все граничные точки исходного круга сходимости $K_{0}$ могут оказаться особыми; в этом случае говорят, что окружность $\left|z-z_{0}\right|=R_{0}$ круга сходимости $K_{0}$ есть е с тест в е ная г р а н и ц а функции $f_{0}$. Во-вторых, при А. П. вдоль кривой $L$ точка $z_{N}$ может оказаться особой, то есть радиусы сходимости $R_{k}$ при приближении к $z_{N}$ будут настолько быстро уменьшаться, что $z_{N}$ не попадает ни в один из кругов $R_{k}$. В-третьих, при А. п. вдоль различных кривых $L$ в одну и ту же точку $z_{N}$ могут получаться различные канонич. элементы $\left(K_{N}, f_{N}\right)$ с центром $z_{N}$, то есть при А. П. исходной однозначной функции $f_{0}$ может возникнуть многозначная аналитич. Функция $f$.

Несмотря на свою теоретич. простоту, процесс А. П. с помощью канонич. элементов малоэффективен. Поэтому с давних пор в вопросах А. п. прибегают к самым разнообразным иным методам А. П. Сущность этих методов сводится к тому, что используются различные аналитич. представления исходной функции, позволяющие так или иначе расширить ее область определения (в виде рациональных дробей; интегралов, зависящих от параметра; различных функциональных рядов), или же используются функциональные и дифференциальные уравнения, характерные для данной функции, свойства периодичности, четности и т. П.

Напр., степенной ряд

\[
f_{0}(z)=\sum_{n=0}^{\infty} z^{n}
\]

определяет аналитич. функцию $f_{0}(z)$ в его круге сходимости $K_{0}=\{z:|z|<1\}$. Пользуясь формулой суммы бесконечной геометрич. прогрессии, получают выражение для $f_{0}$ в виде рациональной дроби

\[
f_{0}(z)=1 /(1-z)
\]

К-рое в данном простейшем примере и дает А. П. функции $f_{0}$ на всю плоскость $\mathbb{C}$ с выброшенной одной особой точкой $z=1$. Несколько более сложный пример доставляет гамма-функция $\Gamma(z)$, обобщающая понятие факториала $n$ ! на комплексные числа z и первоначально определяемая эйлеровым интегралом второго рода:

\[
\Gamma(z)=\int_{0}^{z} x^{z-1} e^{-x} d x
\]

всюду в правой полуплоскости $\operatorname{Re} z>0$ (заштрихована на рис. 3). А. П. гамма-функции можно постепенно осушест-

\section{6 АНАЛИТИЧЕСКОЕ}
вить, используя характерное для нее функциональное уравнение

\[
\Gamma(z)=\Gamma(z+1) / z
\]

для вычисления значений $\Gamma(z)$ сначала в точках полуплоскости $\operatorname{Re} z>-1$, затем $\operatorname{Re} z>-2$ и т. Д. При этом гамма-функция оказывается уже определенной всюду на плоскости $\mathbb{C}$, кроме точек $z=0,-1,-2, \ldots$, к-рые для нее являются особыми.

Рис. 3. Аналитическое продолжение гамма-функции $\Gamma(z)$.

\begin{center}
\includegraphics[max width=\textwidth]{2023_07_08_a8acd95a5c25b760752cg-1}
\end{center}

Вообще, вопрос об определении положения особых точек при А. П. является одним из основных в этой теории.

Лит.: [1] М а р к уш е в и ч А. И., Теория аналитических функций, 2 изд., т. 2, М., 1968; [2] Б и бе рб а х Л., Аналитическое продолже$\begin{array}{ll}\text { ние, пер. с нем., М., } 1967 . & \text { Е. Д. Соломенцев. }\end{array}$ АНТАРМОНИЧЕСКИЙ ОСЦИЛЛЯТОР - см. Осцилляmop.

АНДРОНОВА - ХОПФА БИФУРКАЧИЯ - см. Турбулентности возникновение.

AННУЛЯтОР (ле в ы й) м ноже с т в а $X$ в $R-$ множество $3_{l}(X)$ всех таких элементов $y$ из $R$, что $y X=0$. Здесь $R-$ кольцо, или полугруппа с нулем. Аналогично определяется п р а в Ы Й А. множества $X$ в $R$ : это есть множество

\[
3_{r}(X)=\{z \in R \mid X z=0\} .
\]

Д в усторон н и й А. множества $X$ есть множество

\[
3(X)=3_{l}(X) \cap 3_{r}(X) .
\]

В ассоциативном кольце (или полугруппе) $R$ левый $\mathrm{A}$. любого множества $X$ будет левым идеалом, а если $X$ является левым идеалом $R$, то $3_{l}(X)-$ двусторонний идеал $R ;$ в неассоциативном случае эти утверждения, вообще говоря, не имеют места.

К. А. Жевлаков.

AнОмАлия в квантовой теории поля- свойство, состоящее в том, что некоторые законы сохранения, справедливые в классической теории, перестают выполняться при правильном учете квантовых эффектов. Происхождение А. связано с ультрафиолетовыми расходимостями квантовой теории поля, нуждающимися в регуляризации. Конкретный выбор процедуры регуляризации в квантовой теории поля, как правило, неоднозначен. При этом в нек-рых случаях регуляризацию невозможно провести так, чтобы удовлетворить одновременно всем требованиям симметрии исходной классич. теории поля. В результате нек-рые симметрии оказываются нарушенными. Напр., в квантовой электродинамике выполняется закон сохранения векторного тока $\partial j_{\mu} / \partial x_{\mu}=0$, где $j_{\mu}(x)=\bar{\psi}(x) \gamma_{\mu} \psi(x)-4$-вектор тока, $\mu=0,1,2,3, \psi(x)$ - Дирака поле электрона ( $x-$ пространственно-временная точка), $\gamma_{\mu}-$ Дирака матрицы, $\bar{\psi}=\psi^{+} \gamma_{0}-$ эрмитово сопряженное поле. Наряду с векторным полем, в квантовой электродинамике можно также рассмотреть аксиальный ток $j_{\mu 5}=\bar{\psi}(x) \gamma_{\mu} \gamma_{5} \psi(x)$, где $\gamma_{5}=i \gamma_{0} \gamma_{1} \gamma_{2} \gamma_{3}$. В силу Дирака уравнения дивергенция аксиального тока

\[
\partial j_{\mu 5} / \partial x_{\mu}=2 i m \bar{\psi} \gamma_{5} \psi
\]

где $m$ - масса электрона (используется система единиц, в $\mathrm{K}-$ рой $\hbar=c=0$ ). Из этого уравнения следует, что в пределе нулевой массы электрона аксиальный ток сохраняется, что является отражением теории киральной симметрии. Однако более аккуратное исследование показывает, что этот вывод неверен. Действительно, в определении аксиального тока стоит произведение антикоммутирующих операторов $\psi$ и $\bar{\psi}$, взятых в одной точке $x$. Такое произведение нуждается в доопределении (регуляризации). Если ее провести так, чтобы не нарушить закон сохранения векторного тока, то


\end{document}