\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\def\AA{\mathring{\mathrm{A}}}

\DeclareUnicodeCharacter{0131}{$\imath$}

\begin{document}
Ф. Схема любого численного метода решения уравнения (1) состоит в следующем. Пусть $\tilde{\Phi}$, вообще говоря, отличное от Ф банахово пространство, нек-рым образом связанное с $\Phi, \tilde{A}$ - линейный оператор из $\tilde{\Phi}$ в Ф. Уравнение

\[
(E-\lambda \tilde{A}) \tilde{\varphi}=\tilde{f}
\]

наз. аппроксимирующим уравнением для (2). Обычно аппроксимирующий оператор $\tilde{A}$ подбирается так, чтобы либо было возможно непосредственное вычисление $\tilde{\varphi}$ из (3), либо (более общо) можно было бы найти приближенное решение (3) вида

\[
\tilde{\tilde{\varphi}}=\psi(\tilde{A}, \tilde{f})
\]

так, чтобы правую часть (4) можно было вычислить за конечное число арифмметич. действий. Выражение $\psi(\tilde{A}, \tilde{f})$ означает проведение нек-рых действий над $\tilde{A}$ и $\tilde{f}$, в частности $\psi$ может быть просто операторной функцией от $\tilde{A}$ (напр., $\left.\psi(\tilde{A}, \tilde{f})=(E-\tilde{A})^{-1} f\right)$. Выбор $\tilde{A}, \psi$ ı $\tilde{f}$, а также пространства $\tilde{\Phi}$ прежде всего подчінен требованию близості (в каком-либо смысле) $\tilde{\tilde{\varphi}}$ ІІ точного решения $\varphi$ уравнения (1), (2) и, вообще говоря, неоднозначен. Тично также для конкретного чІсленного метода (конкретной формулы ашпроксимации для $A$ ) выбор пространства $\tilde{\Phi}$ также неоднозначен. Конкретный выбор Ф ІІ П диктуется требованиями «близости» $\tilde{\bar{\varphi}}$ и $\varphi$, таюже удобствами исследования. Специфика численных методов реттения Ф. у. 2-го рода заключена в основном именно в той или иной конкретной аппроксимации оператора $A$ при помощи $\tilde{A}$. Поэтому обычно способ апшроксимации и дает название тому или иному методу численного решения уравнения (1).

После того как выбраны $\Phi, \tilde{\Phi}, \tilde{A}$ и $\tilde{f}$, близость $\varphi$ и $\tilde{\varphi}(\tilde{\bar{\varphi}})$ устанавлівается с помощью теорем общей теории приближенных методов решения операторных уравнений.

В случае $\Phi=\tilde{\Phi}$ для установления близости $\tilde{\varphi}$ и $\varphi$ достаточно показать, что $\AA \tilde{A}-A \|$ мала. При подходящем выборе Ф это удается сделать для большинства класспч. методов приближенного решения Ф. у. 2-го рода.

В большинстве конкретных методов решение уравнения (3) легко редудируется $\kappa$ решению системы линейных алгебраич. уравнений; для построения $\psi$ в (4) можно воспользоваться нек-рым алгоритмом решения систем линейных алгебраич. уравнений (см. Личейная алгебра; численные методы).

Основные сшособы построения аппроксимируюцих операторов:

М етоды к в а д р а т у р ных с ум м и и х о бобще ни я являются наиболее распространенными методами аппроксимации интегрального оператора $A$ в уравнении (1). Основной из этих методов, применение к-рого возможно в случае непрерывных $K^{\prime}(x, s), \varphi(s)$ и $f(x)$, состоит в замене интеграла (по $s$ ) в (1) какой-либо квадратурноӥ формулой по сетке узлов $\left\{s_{i}\right\} \in D$.

II рї этом

\[
\tilde{A} y=\sum_{i=1}^{N} a_{i}^{(N)} K\left(x, s_{i}\right) y\left(s_{i}\right),
\]

\begin{center}
\includegraphics[max width=\textwidth]{2023_07_09_a9e0c792731aa2a3ead4g-1}
\end{center}

Аппроксимир ующее уравноние (3) можно рассматривать как операторное в том же самом пространстве, что И основное уравнение (1) (напр., в простравстве $C(D)$ непрерывных вектор-функций на $D)$. В этом случае оно будет пметь вид

\[
\tilde{\varphi}(x)-\lambda \sum_{i=1}^{N_{-}} a_{i}^{(N)} K\left(x, s_{i}\right) \tilde{\varphi}\left(s_{i}\right)=f(x) .
\]

Уравнение (6) редуцируется к системе линейных алгебраич. уравнений относительно $\tilde{\varphi}\left(s_{i}\right), i=1, \ldots, N$ :

\[
\tilde{\varphi}\left(s_{i}\right)-\lambda \sum_{i=1}^{N} a_{i}^{(N)} K\left(s_{j}, s_{i}\right) \tilde{\varphi}\left(s_{i}\right), j=1, \ldots, N .(7)
\]

Решение (точное или приближенное) системы (7) дает $\tilde{\tilde{\varphi}} \cdot$

Иногда само уравнение (7) считают аппроксимирующим уравнение (1), тогда уравнение (7) соответствует уравнению (3).

При таком подходе пространство $\tilde{\Phi}$ не совпадает с Ф. Пространство $\tilde{\Phi}$ можно, напр., естественно отождествить с факторпространством Ф по поднространству функций из $\Phi$, обращающихся в нуль в точках $\left\{s_{i}\right\}$, $i=1, \ldots, N$. Метод (5) допускает различные обобщения, к-рыми удобно пользоваться, напр., в случае разрывных $K(x, s)$. В этих обобпенных методах оператор $\tilde{A}$ имеет вид

\[
\tilde{A} y=\sum_{i=1}^{N} a_{i}^{(N)}(x) y\left(s_{i}\right)
\]

где $a_{i}^{(N)}(x)$-функции, связанные с ядром $K(x, s)$.

См. также Квадратурных сум.и метод.

Методы замены ядрана близкоеиспользуют аппроксимирующий оператор $\tilde{A}$ вида

\[
\tilde{A} y=\int_{D} \tilde{K}(x, s) y(s) d s
\]

где $\tilde{\boldsymbol{K}}$-нек-рая функция, близкая к $K$, но более просто устроенная. Чаще всего $\tilde{K}-$ вырожденное ядро, т. е.

\[
\tilde{K}(x, s)=\sum_{i=1}^{N} a_{i}(x) b_{i}(s) .
\]

Уравнение (3) в данном случае - интегральное Ф. у. с вырожденным ядром. Его решение сводится к решению системы линейных алгебраич. уравнений. Однако элементы матрицы полученной системы уравнений будут выражаться интегралами от известных функций и при численном решении их, вообще говоря, ғужно аппроксимировать квадратурными суммами. Существует много способов конкретного выбора $\widetilde{K}$ по формуле (8) (см., напр., полос метод). Теоретич. псследование близости решений уравнений (3) Іі (1) в этих методах обычно значительно проще, чем, напр., в методах квадратурных сумм, т. к. в большинстве случаев можно положить $\Phi=\tilde{\Phi}$ и выбор $\Phi$ естественно определяется непосредственно постановкой задачи. Близость $\tilde{K}$ и $K$, как правило, обеспечивает близость $A$ и $\tilde{A}$ по норме Ф. Однако практич. реализация этих методов в болышинстве случаев значительно более трудоемка по сравнению с методами квадратурных сумм и их обобщениями.

П р о е к ц о н ны е ме то ды приводят к аппроксимирующему уравнению (3) вида $\tilde{\varphi}+P A \tilde{\varphi}=P f$, причем $\tilde{\Phi}$-подпространство $\Phi$ и $\boldsymbol{P}$-проектор на әто подпространство. Произвол в выборе $\Phi, \tilde{\Phi}$ и даже самого $P$ приводит к многочисленным конкретным проекционным методам решения интегральных Ф. у. 2-го рода. Типичным примером проекционного метода является Галеркина метод. Для получения конкретных расчетных формул этого метода нужно (если это возможно) интегральное уравнение (1) трактовать как операторное уравнение в гильбертовом пространстве $L_{2}(D)$ функций, интегрируемых с квадратом в $D$, II взять в качестве $P$ ортопроектор, сопоставляющий функции из $L_{2}(D) N$-членный отрезок ее ряда Фурье


\end{document}