\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
то говорят о с р е д е с т п п ен ом п ри ближе ни и, к-рое при $p=1$ наз. п ри ил и же ни е м в с р е д н е м. Особое значение имеет случай $p=2-$ среднеквад ратическое при ближен и е, когда погрешность наилучшего приближения функции $f$ конечномерным подпространством может быть точно выражена через нек-рые определители. В нек-рых задачах требования близости функций $f$ и $\varphi$ в различных точках различны; для учета этой неоднородности вводят весовую функцию $\rho(t) \geqslant 0$ и рассматривают взве шен ное при бли жени е с мерой погрешности

\[
\mu(f, \varphi)=\sup _{t \in Q}|f(t)-\varphi(t)| \rho(t)
\]

или

\[
\mu(f, \varphi)=\int_{Q}|f(t)-\varphi(t)|^{p} \rho(t) d t, p>0 .
\]

Весовая функция позволяет также обеспечить конечвость погрешности, если, напр., $f(t)$ неограничена. Если погрешность должна учитывать близость $f$ и $\varphi$ только в отдельных точках $t_{k}(k=1, \ldots, N)$ из $Q$, то в качестве $\mu(f, \varphi)$ можно выбрать одну из величин

\[
\max _{1 \leqslant k \leqslant N}\left|f\left(t_{k}\right)-\varphi\left(t_{k}\right)\right|
\]

Іли

\[
\sum_{k=1}^{N}\left|f\left(t_{k}\right)-\varphi\left(t_{k}\right)\right| p, p>0
\]

в к-рые также могут вводиться весовые коәффициенты.

При решении вопроса о том, по какому правилу выбирать из множества $\mathfrak{N}$ приближающую функцию

\includegraphics[max width=\textwidth, center]{2023_07_08_c076bf3211634ad39d90g-1}
же в и я), естественно стремление обеспечить по возможности более высокую точность приближения и одновременно простоту построения $\varphi(f, t)$ по имеющейся информации о приближаемой функции $f(t)$. Первое требование ориентирует на «ближайшую» к $f(t)$ функцию $\varphi_{f}(t)$ из $\mathfrak{l}$, т.е. такую, что

\[
\mu\left(f, \varphi_{f}\right)=\inf _{\varphi \in \Re} \mu(f, \varphi) .
\]

Здесь сразу же возникают вопросы о существовании ■ единственности такой функции (ф у н к ц и и н а и-

\includegraphics[max width=\textwidth, center]{2023_07_08_c076bf3211634ad39d90g-1(1)}
характеристич. свойствах (см. [5]). Существование гарантируется, если $\mathfrak{N}-$ замкнутое локально компактное множество, в частности конечномерное подпространство. Единственеость зависит как от свойств приближающего множества $\mathfrak{l}$ (см. Хаapa условие, पебышева система функций), так и от метрики, определяющей меру погрешности $\mu(f, \varphi)$. Известен ряд веобходимых и достаточных условий, к-рым должна удовлетворлть фушкция наилучшего шриближения $\varphi_{f}(t)$ в той или иной ситуации (см. Наилучщего приближения многочлен, Чебышева теорема, Маркова критерий). Однако эти критерии, как правило, не дают способов әффективного построения функции $\varphi_{f}(t)$. Поэтому большое значение имеют методы, к-рые позволяют по ивформации о приближаемой функции $f(t)$ эффективно построить нек-рую функцию $\varphi(f, t)$ из $\mathfrak{k}$, обеспечивающуг приемжемое приближение. Здесь, в первую очередь, надо говорить 0 ли н е й н ы $\mathbf{x}$ м т од а X (когда $\varphi\left(\alpha_{1} f_{1}+\alpha_{2} f_{2}, t\right)=\alpha_{1} \varphi\left(f_{1}, t\right)+\alpha_{2} \varphi\left(f_{2}, t\right)$ ), к к-рым, в частности, относится метод интерполяции. Зафиксировав точки $t_{1}, \ldots, t_{N}$ из $Q$, можно выбирать $\varphi(f, t)$ среди тех функций $\varphi(t)$ из $\mathfrak{i}$, к-рые удовлетворяют условию интерполяции

\[
\varphi\left(t_{k}\right)=f\left(t_{k}\right), k=1,2, \ldots, N .
\]

Если $\mathfrak{N}$ - линейное многообразие и в нем существует система функший $\varphi_{1}, \ldots, \varphi N$ такая, что $\varphi_{i}\left(t_{i}\right)=1$, $\varphi_{i}\left(t_{k}\right)=0 \quad(k \neq i)$, то функция

\[
\varphi(f, t)=\sum_{k=1}^{N} f\left(t_{k}\right) \varphi_{k}(t)
\]

принадлежит $\mathfrak{N}$ и удовлетворяет условиям (2); ова задает ин т е р по ля ц и о ны ы й м тод п р пбл и ж е н и я, являющийся, очевидно, линөйным. Функцию $\varphi(f, t)$ можно выбирать, требуя совпадения в точках $t_{k}$ не только $f(t)$ и $\varphi(f, t)$, но и нек-рых их производных; в әтом случае говорят об и н т е р поли р о в а пи и с р а тным и узл а й. Если $Q=[a, b], a \leqslant t_{1}<t_{2}<\ldots<t_{N} \leqslant b$, то существует едшвственный алгебраич. многочлен степени $N-1$, а в непериодич. случае $\left(b-a=2 \pi, t_{N}=t_{2 n-1}<b\right)-$ едивственный тригонометрич. полином порядка $n-1$, совпадающий с $f(t)$ в точках $t_{k}$. Кратное интерполирование осуществляют интерполяционные полиномы Эрмита, частным случаем к-рых является многочлен Тейлора, когда в одной точке алгебраич. многочленом степени $n$ интерполируются значения функции и еe первых $n$ производных.

Интерполирование сплайнами имеет свои особенности, связанные с выбором точек интерполяции и краевых условий, обеспечивающих существование и единственность интерполяционного сплайна. Напр., сплайн $s(t)$ порядка $r \geqslant 2$ дефекта 1 по разбшению (1), принимающий заданные значения в $N$ различных точках $t_{i}$ интервала $(a, b)$ таких, что $\tau_{i-1} \leqslant t_{i} \leqslant \tau_{i}$, $i=$ $=1,2, \ldots, N$, существует и единствен, если задать определенным образом краевые условия в виде $m_{a}$ чисел $s^{(v)}(a)(0 \leqslant v \leqslant r-1)$ и $m_{b}$ чисел $s^{(\mu)}(b)(0 \leqslant \mu<r-1)$, причем $m_{a}+m_{b}=r$. Функции $f \in C^{k-1}[a, b]$ можно однозначно сопоставить сплайн $s(f, t)$ порядка $2 r-1$ дефекта $k(1 \leqslant k \leqslant r)$ по разбиению (1), потребовав выполнения равевств $s^{(v)}\left(f, \tau_{i}\right)=f^{(v)}\left(\tau_{i}\right), \quad v=0,1, \ldots$, $k-1 ; i=0,1, \ldots, N$, а при $k<r$ также нек-рых краевых условий. При $k=r$ этот сплайн наз. э р м и т о в м м, а также л о к а л ь ны м, т. к. его поведение на интервале $\left(\tau_{i-1}, \tau_{i}\right)$ определяется только значениями функции $f(t)$ и ее производных $f^{(v)}(t)(v=1, \ldots, k-1)$ в точках $\tau_{i-1}$ и $\tau_{i}$.

В П. ф. важную роль играют также линейные мөтоды, построенные на базе разложения приближаемой функции в ряд Фурье по нек-рой ортогональной системе. В частности, в периодич. случае широко распространенным аппаратом приближения являются суммы Фурье по тригонометрич. системе и их различные усреднения (см. Приближение бункций; линейныө методы приближения).

Ис следование и оценка пог р е шно с т и п р и ли ж е и я- важный с практич. точки зрения и в то же время наиболее содержатөльный в идейном отношении этап П. ф. Именно разработка методов оценки погрешности, изучение зависимости ее от гладкостных характеристик приближаемой функции, исследование и сравнение аппроксимативных свойств различных аппаратов приближения привели к формированию теории приближения функций одного из наиболее интенсивно развивающихся разделов математич. анализа.

Фундамент теории П. Ф. был заложен работамд П. Л. Чебышева в 1854-59 (см. [1]) о наилучшем равномерном приближении непрерывных функций многочленами и рациональными дробями, а также работами К. Вейерштрасса [2], доказавшего в 1885, что для любой непрерывной на отрезке $[a, b]$ или непрерывной на всей оси с периодом $2 \pi$ функции $f(t)$ существует последовательность алгебраических (соответственно тригонометрических) полиномов $\boldsymbol{P}_{n}(f, t)$ по-


\end{document}