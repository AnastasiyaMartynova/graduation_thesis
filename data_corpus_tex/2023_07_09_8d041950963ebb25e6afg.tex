\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }
\usepackage{mathrsfs}
\usepackage{bbold}

\def\AA{\mathring{\mathrm{A}}}

\begin{document}
лен оператор $A^{*} g=g^{*}$, пеӥствуюций из $D_{A *}$ в $X^{*}$. Если $D_{A}=X$ и $A$ - линейінйй непрерывный оператор, то $A^{*}$ - также линейныї непрерывный оператор. Если, кроме того, $X$ и $Y$ - линейные нормированные пространства, то $\left\|A^{*}\right\|=\|A\|$. Если $A$ - вполне непрерывный оператор, то таков же и $A$ * . Наиболее подробно изучены свойства С. о., когда $X$ и $Y$ - гильбертовы пространства.

Лит.: [1] И о с и д а К., Функциональный анализ, пер. с англ., М., 1967; [2] Р и с с Ф., С ёке ф а ль в и - Н а д ь Б, Лекции по функциональному анализу, пер. с франц., 2 изд., М., СОПРЯЖЕННЫЙ ТРИГОНОМЕТРИЧЕСКИЙ РЯТ к $\mathrm{p}$ я Д $\mathbf{y}$

\[
\sigma=\frac{a_{0}}{2}+\sum_{n=1}^{\infty} a_{n} \cos n x+b_{n} \sin n x
\]

\begin{itemize}
  \item ряд
\end{itemize}

\[
\bar{\sigma}=\sum_{n=1}^{\infty}-b_{n} \cos n x+a_{n} \sin n x .
\]

Эти ряды являются соответственно действительної п мнимой частями ряда

\[
\frac{a_{0}}{2}+\sum_{n=1}^{\infty}\left(a_{n}-i b_{n}\right) z^{n}
\]

при $z=e^{i x}$. Формула для частных сумм $\bar{\sigma}[f]$ сопряженного к ряду Фурье функции $f(x)$ тригонометрич. ряда

\[
\bar{S}_{n}(x)=-\frac{1}{\pi} \int_{-\pi}^{\pi} f(t) \bar{D}_{n}(t-x) d t,
\]

где $\bar{D}_{n}(x)$ - сопряженное Дирихле ядро. Если $f(x)-$ функция ограниченной вариации на $[-\pi, \pi]$, то необходимым и достаточным условием сходимости ряда $\bar{\sigma} \mid f]$ в точке $x_{0}$ является существование сопряженной бункции (см. п. 3) $\bar{f}\left(x_{0}\right)$, к-рая представляет тогда сумму ряда $\bar{\sigma}[f]$. Если $f(x)$ - суммируемая на $[-\pi, \pi]$ функция, то ряд $\bar{\sigma}[f]$ суммируется почти всюду методами $(C, \alpha), \alpha>0$, и методом Абеля - Пуассона и почти всюду совпадает с сопряженной функцией $f(x)$. Если функция $\bar{f}(x)$ суммируема, то сопряженный ряд $\bar{\sigma}[f]$ является ее рядом Фурье. Функция $f(x)$ может быть несуммируемой; для таких обобщений интеграла Лебега, как $A$-интеграл и Бокса интеграл, сопряженный ряд $\bar{\sigma}[f]$ всегда является рядом Фурье сопряженной функции.

Jum.: [1] T a u b e r A., "Monatsch. Math. Phys.», 1891, naturwiss. Hb. Bayerischen Akad. Wiss. München», 1911, Bd 41, S. 361-71; [3] P r i w a l of f I., "Bull. Soc. math. France», 1916, t. 44, p. $100-103 ;$ [4] П p " в а л о в И. И., Интеграл Cauchy, Саратов, 1919, с. $61-104 ;$ [5] Л у з и н Н. Н., Интеграл и тригонометрический ряд, М.- Л., 1951; [6] Б а р и Н. К.,

\includegraphics[max width=\textwidth, center]{2023_07_09_8d041950963ebb25e6afg-1}
мат ический анализ. 1970, М., 1971, с. $65-107 ;$ [8] Ж и ж и аш в и л и Л. В., Сопряженные функции и тригонометрические ші в и л и л. В., Сопряженные функции и тригонометрические
ряды, тбилиси, 1969.

СОПРЯЖЕННЫЙ ФУНКТОР - понятие, выражающее универсальность и естественность многих важных математич. конструкций: свободных универсальных алгебр, различных пополнений, прямых и обратных пределов и т. Д.

Пусть $F: \mathscr{\Re} \rightarrow(5-$ одноместный ковариантныї функтор из категории $\Re$ в категорию (5. Функтор $F$ индуцирует функтор

\[
H^{F}(X, Y)=H_{\Theta}(F(X), Y): \mathfrak{\Re}^{*} \times(5 \longrightarrow \Subset,
\]

\includegraphics[max width=\textwidth, center]{2023_07_09_8d041950963ebb25e6afg-1(1)}
категория множеств, $H_{\S}(X, Y):(5 * \times(5 \rightarrow \Im-$ основной теоретико-множественный функтор. Функтор $H^{F}$ контравариантен по первому аргументу и ковариантен по второму. Аналогично, любой ковариантный функтор $G:(S \rightarrow \AA$ индуцирует функтор

\[
H_{G}(X, Y)=H_{J_{i}}(X, G(Y)): \mathfrak{H}^{*} \times \subseteq \longrightarrow €
\]

также контравариантный по первому аргументу и ковариантный по второму. Функторы $F$ и $G$ с о п р яж е ны, или образуют с о п р я ж ен н у ю п а р у, если функторы $H^{F}$ и $H_{G}$ изоморфны, т. е. существует естественное преобразование $\theta: H^{F} \rightarrow H_{G}$, к-рое устанавливает взаимно однозначное соответствие между множествами морфизмов $H_{\complement}(F(X), Y)$ и $H_{\Re}(X, G(Y))$ для любых объектов $X \in \mathrm{Ob}) \overline{\mathrm{t}}$ и $Y \in \mathrm{Ob}(5$. Преобразование $\theta$ наз. с о п р я ж е ни е м $F$ с $G$; функтор $F$ наз. ле в ы с о о р я ж е н н м к функтору $G$, а $G-$ р а в ы м с о п р я ж е н н ы м к $F$ (что обозначается $\theta: F-G$ или просто $F-G)$. Преобразование $\theta^{-1}: H_{G} \rightarrow H^{F}$ наз. к о с о п р я ж е н и е м.

Пусть $\theta: F-G$. Для любых объектов $X \in \mathrm{Ob} \mathfrak{\AA}$ и $Y \in \mathrm{Ob}(5$, пусть

\[
\varepsilon_{X}=\theta\left(1_{F(X)}\right), \quad \eta_{Y}=\theta^{-1}\left(1_{G(Y)}\right) .
\]

Семейства морфизмов $\left\{\varepsilon_{X}\right\}$ и $\left\{\eta_{Y}\right\}$ огределяют естественные преобразования $\varepsilon: \mathrm{Id}_{\mathfrak{A N}} \rightarrow F G$ и $\eta: G F \rightarrow \mathrm{Id}_{\mathfrak{C}}$, к-рые наз. соответственно е д и н и це й и к о е диниц е ӥ сопряжения $\theta$. Для преобразованиӥ $\varepsilon$ и $\eta$ справедливы следующие равенства:

\[
\varepsilon_{G(Y)} G\left(\eta_{Y}\right)=1_{(i(Y)}, F\left(\varepsilon_{X}\right) \eta_{F(X)}=1_{F(X)} .
\]

Вообще, пара естественных преобразований $\varphi: \operatorname{Id}_{\mathscr{R}} \rightarrow$ $\rightarrow F G$ и $\psi: G F \rightarrow \mathrm{Id}_{\mathfrak{S}}$ состоит из единицы и коединицы нек-рого сопряжения, когда выполнены равенства

\[
\varphi_{G(Y)} G\left(\psi_{Y}\right)=-1_{G(Y)}, F\left(\varphi_{X}\right) \psi_{F(X)}=1_{F(X)}
\]

для любых объектов $X$ и $Y$. Естественное преобразование $\varphi: \mathrm{Id}_{\mathfrak{N}} \rightarrow F G$ является единицей нек-рого сопряжения тогда и только тогда, когда для любого морфизма $\alpha: X \rightarrow G(Y)$ из категории $\Re$ существует такой единственный морфизм $\alpha^{\prime}: F(X) \rightarrow Y$ в категории (5, что $\alpha=\varepsilon_{X} G\left(\alpha^{\prime}\right)$. Последнее свойство выражает тот факт, что объект $F(X)$ свободен над $X$ относительно функтора $G$ в смысле следующего определения. Объект $Y \in \mathrm{Ob}(5$ вместе с морфизмом $\varepsilon: X \rightarrow G(Y)$ с в о б о д е н на д о б ъ е к т о м $X \in \mathrm{Ob}$, если всякий морфизм $\alpha: X \rightarrow$ $\rightarrow G\left(Y^{\prime}\right)$ однозначно представим в виде $\alpha=\varepsilon G\left(\alpha^{\prime}\right)$ для нек-рого морфизма $\alpha^{\prime}: Y \rightarrow Y^{\prime}$. Функтор $G: \mathbb{S} \rightarrow \mathbb{R}$ тогда и только тогда обладает левым С. ф., когда для каждого $X \in \mathrm{Ob}$ существует объект $Y$, свободный над $X$ относительно $G$.

П р и м е р ы С. ф.

\begin{enumerate}
  \item Если $G:(5 \rightarrow €$, где G-категория множеств, то $G$ обладает левым С. ф. тогда и только тогда, когда он представим. Представимыї функтор $G \simeq H^{A}=$ $=H_{\Im}(A, Y)$ обладает левым С. ф. тогда и только тогда, когда в (5, имеются любые копроизведения $\Pi_{x \in X^{*}}^{*}$, где $X \in \mathrm{Ob} \Im$ и $A_{x}=A$ для всех $x \in X$.

  \item В категории множеств $\subseteq$ для любого множества $A$ основной функтор $H^{A}(Y)=I I(A, Y)$ сопряжен слева функтору $X \times A$.

  \item В категории абелевых групп функтор $\operatorname{Hom}(A, Y)$ сопряжен слева функтору $X \otimes A$ тензорного умножения на $A$, а функтор вложения полной подкатегории периодич. групп сопряжен справа функтору взятия периодич. части произвольної абелевой групшы.

  \item Пусть $P: \mathfrak{A} \rightarrow \Xi-$ пренебрегающий функтор из произвольного многообразия универсальных алгебр в категорию множеств. Функтор $P$ обладает левым С. Ф. $F: \Im \rightarrow \mathfrak{X}$, к-рый каждому множеству $X$ сопоставляет свободную алгебру многообразия $\mathfrak{A}$ с множеством $X$ свободных образующих.

  \item Функтор вложения $\operatorname{Id}_{\left(\delta_{n}, \mathfrak{\Re}\right.}:(S \rightarrow \Re$ произвольної реф̆лективной подкатегории (5) категории $\mathfrak{R}$ сопряжен слева (5-рефлектору. В частности, функтор вложения категории абелевых груши в категорию групп

\end{enumerate}

\end{document}