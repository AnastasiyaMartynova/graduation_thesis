\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}
\usepackage{mathrsfs}

\begin{document}
на $\Gamma$ существуют угловые граничные значения $F^{ \pm}\left(\varepsilon_{0}\right)$ интеграла типа Коши - Стилтьеса

\[
F(z)=\frac{1}{2 \pi i} \int_{\Gamma} \frac{e^{i \varphi(s)} d \psi(s)}{\zeta-z}, z \in D^{ \pm},
\]

соответственно из областей $D^{ \pm}$, причем почти всюду справедливы Сохочкого борлулы:

\[
F^{ \pm}\left(\zeta_{0}\right)=\frac{1}{2 \pi i} \int_{\Gamma} \frac{e^{i \varphi(s)} d \psi(s)}{\zeta-\zeta_{0}} \pm \frac{1}{2} \psi^{\prime}\left(s_{0}\right) .
\]

Обратно, если почти всюду на $\Gamma$ существуют угловые граничные значения $F+\left(\zeta_{0}\right)$ (или $F-\left(\zeta_{0}\right)$ ) интеграла $(2)$, то почти всюду на Г существуют сингулярный интеграл (1) и граничные значения с другой стороны $F-\left(\zeta_{0}\right)$ (соответственно $F+\left(\zeta_{0}\right)$ ), причем выполняются равенства (3). Эта теорема была установлена И. И. Приваловым для пнтегралов типа Коши - Лебега (т. е. для случая абсолютно непрерывной функции $\psi(s)$, см. [2]), а затем и для общего случая [3]. Она играет основную роль в теории сингулярных интегральных уравнений и разрывных граничных задач аналитич. функций (см [6]).

\begin{enumerate}
  \setcounter{enumi}{3}
  \item П. т. о г р а и ич ны грал а типа Коши-Л е б е г а: если жорданова кривая $\Gamma$ кусочно гладкая и без точек заострения, а комплексная функция $f(\zeta), \zeta \in \Gamma$, удовлетворяет условию Липшица
\end{enumerate}

\[
\left|f\left(\zeta_{1}\right)-f\left(\zeta_{2}\right)\right|<C\left|\zeta_{1}-\zeta_{2}\right|^{\alpha}, 0<\alpha \leqslant 1,
\]

то интеграл типа Коши - Лебега

\[
F(z)=\frac{1}{2 \pi i} \int_{\Gamma} \frac{f(\zeta) d \zeta}{\zeta-z}, z \in D^{ \pm},
\]

есть непрерывная функция в замкнутой области $\bar{D}+$, причем для граничных значений $F^{ \pm}(\zeta)$ выполняются условия:

\[
\left|F^{ \pm}\left(\zeta_{1}\right)-F^{ \pm}\left(\zeta_{2}\right)\right|<C_{1}\left|\zeta_{1}-\zeta_{2}\right|^{\alpha},
\]

если $0<\alpha<1$, и

$\left|F^{ \pm}\left(\zeta_{1}\right)-F^{ \pm}\left(\zeta_{2}\right)\right|<C_{2}(\delta)\left|\zeta_{1}-\zeta_{2}\right| \ln \frac{1}{\left|\zeta_{1}-\zeta_{2}\right|}$, если $\alpha=1,\left|\zeta_{1}-\zeta_{2}\right| \leqslant \delta<1$ (см. [2]).

Jum.: [1] П р и в а ло в И. И., «Вull. Soc. math. France», 1916 , t. 44, p. 100-03; [2] е г о ж е, Интеграл Cauchy, Саратов, $1918 ;$ [3] е г о ж е, Граничные свойства однозначных аналитических функций, М., 1941; [4] е г о ж е, Граничные свойства аналитических функций, 2 изд., М. М Л., 1950; [5] 3 и г м у н д аналитических функций, 2 изд., М. - Л., 1950; [5] 3 и Г М У н д д е ли и д з е Б. В., в кн.: Итоги науки и техники. Современные проблемы математики, т. 7, М., 1975, с. 5-162.

Е. Д. Соломенцев. ІІ РИввило логич. вЫвода, позволяющее заключить, что если из списка утверждений $\Gamma, A$ следует как утверждение $B$, так и утверждение $\left.{ }^{-}\right\rceil$, то из списка $\Gamma$ следует $\rceil A$. Правило П. к а. записывают, напр., в виде

\[
\frac{\Gamma, A \rightarrow B ; \Gamma, A \rightarrow \neg B}{\Gamma \rightarrow 7 A}
\]

и наз. также п р а в и л о м в в е д е н и я о т р и цан и я. П. к а. является допустимым правилом для подавляющего большинства логико-математич. исчислений.

ПРИВЕДЕННАЯ СИСТЕМА ВЫЧЕТОВ П $о$ м о д $\mathbf{y}-$ л ю $m$ - набор, составленный из всех чисел полной системы вычетов по модулю $m$, взаимно простых с $m$. П. с. в. по модулю $m$ состоит из $\varphi(m)$ чисел, где $\varphi(m)$ функция Эйлера. В качестве П. с. в. по модулю $m$ обычно берутся взаимно простые с $m$ числа полной системы вычетов $0,1, \ldots, m-1$.

C. А. Степанов.

ІІИВЕДЕННАЯ СХЕМА - схема, локальное кольцо любой точки к-рой не содержіт ненулевых ныльпотентных элементов. Для любой схемы $\left(X, \widehat{G}_{X}\right)$ сущест- вует наибольшая замкнутая приведенная подсхема $\left(X_{\text {red }}, \widehat{\sigma} X_{\text {red }}\right)$, характеризуемая соотношениями

\[
\sigma_{X \text { red }} x=\hat{O}_{x, x} / r_{x}
\]

где $r_{x}$ - идеал, состоящий из всех нильпотентных элєментов кольца $G_{x}, x$. Групповая схема над полем характеристики 0 всегда приведена [3]. П. с.- классич. объект изучения в алгебраич. геометрии.

Лит.: [1] А р т и н М., “Математика", 1970, т. 14, № 4, c. 3-47; [2] $\Gamma$ р о т е н д и к А., Д ь ё дон н е Ж., «Успехи матем. наук", 1972 , т. 27, Nㅡㄴ, с. $135-48$; ᄂ3] М а м ф о р д англ., М., 1968. С. Г. Танкеев.

ІРИВИЛЕГИРОВАННЫЙ КОМПАКТ - понятие, часто используемое в теории комплексных пространств, в особенности в теории модулей комплексных структур. Пусть $K$ - компакт в $\mathbb{C}^{n}, \widehat{G}_{K}$ - ограничение на $K$ пучка ростков голоморфных функций в $\mathbb{C}^{n}$. Компакт $K$ наз. пр и и ле гированным о тносите льно коге гентного аналитического пучка $\mathscr{T}$, заданного на $K$, если существует точная последовательность отображений $\widehat{O}^{\text {-пучков }}$

$0 \longrightarrow \mathscr{L}_{n} \longrightarrow \mathscr{L}_{n-1} \longrightarrow \ldots \rightarrow \mathscr{L}_{1} \longrightarrow \mathscr{L}_{0} \stackrel{\varphi}{\longrightarrow} \mathscr{F} \longrightarrow 0$, (1) в к-рой $\mathscr{L}_{i}=\sigma_{K}^{r}$ с нек-рыми $r_{i} \geqslant 0, i=0,1, \ldots, n$, такая, что порожденная ею последовательность непрерывных операторов

\[
\begin{gathered}
\mathrm{C} \longrightarrow B\left(K, \mathscr{L}_{n}\right) \stackrel{d}{\longrightarrow} B\left(K, \mathscr{L}_{n-1}\right) \longrightarrow \ldots \\
\cdots \longrightarrow B\left(K, \mathscr{L}_{1}\right) \stackrel{d}{\longrightarrow} B\left(K, \mathscr{L}_{0}\right)
\end{gathered}
\]

точна и расщепляема. Здесь

\[
B\left(K, \mathscr{L}_{i}\right)=B(K, \hat{0})^{r_{i}},
\]

a $B(K, 6)$ есть банахово пространство непрерывных на $K$ функций, голоморфных внутри $K$, наделенное равномерной нормой. $\mathrm{P}$ а с щ е п л я е м о с т ь последовательности (2) означает, что ядро и образ дифференциала $d$ в каждом члене имеет прямое замкнутое дополнение. Это условие расщепляемости эквивалентно следующему: существует линейный непрерывный оператор $h$ в (2), переводящий $B\left(K, \mathscr{L}_{i}\right)$ в $B\left(K, \mathscr{L}_{i+1}\right)$, такой, что $d h d=d$ (о п е р а т о р г о м о т о п и и). Свойство точности и расщепляемости последовательности (2) не зависит от выбора последовательности (1).

Пусть точка $z$ принадлежит внутренности компакта $K$. Тогда существует морфизм $\pi$ комплекса (2) в слой комплекса (1) над точкой $z$, переводящий элемент $B\left(K, \mathscr{L}_{i}\right)$, т. е. Функцию на $K$ со значениями в $\mathbb{C}^{r} i$ в ее росток в точке $z$. Отсюда вытекает, что последовательность

\[
B\left(K, \mathscr{L}_{1}\right) \stackrel{d}{\longrightarrow} B\left(K, \mathscr{L}_{0}\right) \stackrel{\pi \varphi}{\longrightarrow} \mathscr{F}_{z}
\]

полуточна. Компакт $K$ наз. $\mathscr{F}$ п р и в и л е г и р ов а н но й о к р е с т нос т ь ю то чк и $z$, если он $\mathscr{F}$-привилегирован и последовательность (3) точна. Это свойство также не зависит от выбора последовательности (1).

Для всякого когерентного аналитич. пучка $\mathscr{T}$ всякая точка его области определения обладает фундаментальной системой $\mathscr{F}$-привилегированных окрестностей. В каqестве таких окрестностей выбираются поликруги с определенными соотношениями типа неравенств для радиусов. Известно достаточное условие $\mathscr{T}$-привилегированности полицилиндра, связывающее пучок $\mathscr{F}$ с устройством границы (см. [1]).

Рассматриваются также привилегированные компакты по отношению к пучку, заданному на произвольном комплексном пространстве $X$, при әтом имеют в виду компакты, привилегированные о'тносительно пучков $f_{*}(\mathscr{T})$, где $f$ карта на $X$.


\end{document}