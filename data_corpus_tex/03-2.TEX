\documentstyle[%
righttag%
,11pt%
,amssymb%
,russian%               remove in Eng.
,izvru%                 Set izven% in Eng.
]{amsart}

% {=}

\newcommand{\diag}{\operatorname{diag}}
\newcommand{\col}{\operatorname{colon}}
\newcommand{\la}{\langle}
\newcommand{\ra}{\rangle}
\newcommand{\tts}[1]{}

\theoremstyle{plain}
\theorembodyfont{\it}
\newtheorem{assrt}{\hskip\parindent Утверждение}
\renewcommand{\theassrt}{}


%\hoffset=-15mm%                  For Eng.
\setcounter{page}{30}
\god{2005}
\nomer{2~(513)} %                        Remove in Eng.
%\nomer{Vol.\,49, No.\,2}%               Set in Eng.
%\str{\pageref{begin}--\pageref{end}}%  Set in Eng.
\udk{517.984}

\author{\it Г.Г.\,Исламов}
% NB:   ^^ remove in Eng.
\title{Об одном классе одноранговых возмущений}
\thanks{}

\date{}
%\pagestyle{myheadings}%         Set in Eng.
\pagestyle{plain} %              Remove in Eng.
\begin{document}
%\thispagestyle{plain}%          Set in Eng.
\maketitle
\label{begin}


Спектральные свойства матриц с вещественными неотрицательными элементами,
установленные Перроном и Фробениусом в начале прошлого столетия, были
обнаружены сначала у интегральных операторов, а затем и у более общих
операторов в полуупорядоченных пространствах [1].

Интересны свойства периферического спектра линейных операторов.
Периферический спектр оператора $A$, некоторая степень которого вполне
непрерывна, состоит из тех собственных значений этого оператора, которые
лежат на спектральной окружности $\{\lambda : |\lambda | = r(A)\}$, где
$r(A)$ --- спектральный радиус оператора $A$.

Сначала рассмотрим конечномерный случай. Пусть $A$ --- вещественная квадратная
матрица порядка $n$ с неотрицательными элементами $a_{ij}$. Следуя ([2],
с.\,129), выберем на плоскости $n$ различных точек $p_1,\dotsc,p_n$.
Для каждого ненулевого элемента $a_{ij}$ матрицы $A$ соединим точку $p_i$
с точкой $p_j$ направленной линией (звеном) $\overline{p_i p_j}$. Полученная
в результате фигура называется направленным графом матрицы $A$.
Матрица $A$ называется неразложимой, если ее направленный граф сильно связен,
т.\,е. для любых двух точек $p_i$ и $p_j$ существует связывающий их 
ориентированный путь $\overline{p_i p_{i_1} },\dotsc,\overline{p_{i_l} p_j}$.
Число звеньев пути называется длиной этого пути.
Однозвенный путь из $p_i$ в $p_i$ (петля) существует тогда
и только тогда, когда $a_{ii}>0$.

Согласно теореме Фробениуса ([3], с.\,334) спектральный радиус $r(A)$
неразложимой матрицы $A$ с неотрицательными элементами является положительным 
простым собственным значением этой матрицы и ему соответствует вектор $u$
с положительными координатами. Кроме того, периферический спектр матрицы $A$
состоит из простых собственных значений	
\begin{equation}\label{1}
\lambda_k = r(A)\exp\left(\frac{2\pi}{m}(k-1)\sqrt{-1}\right),
\quad k = \overline{1,m}.  
\end{equation}
При $m > 1$ число $m$ собственных значений на спектральной окружности 
называется индексом импримитивности (периодом) матрицы~$A$. 
В ([3], с.\,355) показано, что если
$\Delta(\lambda) = \lambda^n + a_1 \lambda^{n_1} + \dotsb +a_t \lambda^{n_t}$
--- характеристический многочлен матрицы $A$, то $m$ равно наибольшему
общему делителю разностей
$n - n_1, n_1 - n_2,\dotsc, n_{t-1} - n_t$.
Известна и другая характеристика индекса импримитивности ([2], с.\,131): 
$m$ есть наибольший общий делитель длин замкнутых
путей (контуров) направленного графа матрицы $A$.

Как показано в ([3], с. 339), найдется такая диагональная матрица
$D=\diag (\mu_1,\dotsc,\mu_n)$ с $\mu^m_i = 1$, $i=\overline{1,n}$,
что $\varphi_k = D^{k-1}u$ будет собственным вектором, отвечающим $\lambda_k$.

\begin{assrt}
Транспонированная матрица $A^*$ $($как и $A)$ неотрицательна, неприводима и имеет
тот же периферический спектр. Пусть $v$ --- положительное решение уравнения
$A^* v = r(A) v$, нормированное условием $(u,v) = 1$, где $(\cdot,\cdot)$ ---
скалярное произведение векторов в $\Bbb C^n$.
Положим
$S = \diag(1/\ov\mu_1,\dotsc,1/\ov\mu_n)$.
Тогда $\psi_j = S^{j-1}v$ будет единственным с точностью до постоянного
множителя собственным вектором матрицы $A^*$, отвечающим 
комплексно--сопряженному собственному значению 
$\ov\lambda_j$, $j=\overline{1,m}$,
причем $(\varphi_k,\psi_j) = \delta_{kj}$ --- символ Кронекера.
\end{assrt}

Действительно, имеет место представление ([3], с.\,339)
\begin{equation}\label{2}
A =\frac{\lambda_j}{r(A)}D^{j-1}AD^{-j+1},\quad j = \overline{1,m}.
\end{equation}
Перехoдя к комплексно-сопряженным матрицам, получим
\begin{equation}\label{3}
A^* = \frac{\ov\lambda_j}{r(A)}S^{j-1}A^*S^{-j+1},\quad j = \overline{1,m}.
\end{equation}
Отсюда и из равенства $A^* v = r(A) v$ следует, что $\psi_j = S^{j-1} v$
удовлетворяет уравнению $A^* \psi = \ov\lambda_j \psi_j$.
Утверждение следует из простоты собственного значения 
$\ov\lambda_j$.
Ортогональность $\varphi_k$ и $\psi_j$ при $k\ne j$ очевидна, а равенство 
$(\varphi_k,\psi_k) = 1$ следует из того, что $S^* = D^{-1}$ 
и $(u,v) = 1$.

Понятно, что собственные значения оператора 
$$
A_1 = A - \sum_{k=1}^m \lambda_k \varphi_k (\cdot, \psi_k)
$$
лежат внутри спектрального круга $\{\lambda : |\lambda| \le r(A) \}$.
Оператор $\sum\limits_{k=1}^m\lambda_k \varphi_k (\cdot, \psi_k)$ 
приводится к нильпотентной матрице $H$
одноранговым возмущением  $K = a (\cdot, b)$ (напр., [4]), где
$a = \sum\limits_{k=1}^m \alpha_k \varphi_k$, 
$b = \sum\limits_{k=1}^m \ov\beta_k \psi_k$, причем
\begin{equation}\label{4}
\alpha_k \beta_k =\frac{\lambda_k^m}
{\prod\limits_{j=1,\ j\ne k}^m (\lambda_k - \lambda_j)},\quad k=\overline{1,m}.
\end{equation}
Так как для спектральных множеств
$\Sigma(A-K) = \Sigma(A_1 + H) = \Sigma(A_1)\cup \{0\}$,
то $r(A-K) < r(A)$.

Оценим сверху норму однорангового возмущения $K$. Пусть
$u=\col(u_1,\dotsc,u_n)$, $v = \col (v_1,\dotsc,v_n)$.
Так как
$$
\sum_{k=1}^m \alpha_k D^{k-1} = \diag (R(\mu_1),\ldots,R(\mu_n)),
$$
где $R(t) = \sum\limits_{k=1}^m \alpha_k t^{k-1}$, то
$a = \col(R(\mu_1) u_1,\dotsc,R(\mu_n) u_n)$.
В силу $|\mu_j| =1$ имеем $|R(\mu_j)| \le 
\sum\limits_{k=1}^m |\alpha_k|$, $j=\overline{1,n}$. Поэтому
$\|a\| = (a,a)^{1/2} \le \|u\| \sum\limits_{k=1}^m |\alpha_k |$.
Аналогично
$\sum\limits_{k=1}^m \ov\beta_k S^{k-1} = \diag
(Q(1/\ov\mu_1),\dotsc,Q(1/\ov\mu_n))$,
где $Q(t) = \sum\limits_{k=1}^m \ov\beta_k t^{k-1}$, причем
в силу $|1/\ov\mu_j| = 1$ верно 
$|Q(1/\ov\mu_j)| \le \sum\limits_{k=1}^m |\ov\beta_k| =
\sum\limits_{k=1}^m |\beta_k|$ и, значит,
$\|b\| = (b,b)^{1/2} \le \|v\|\sum\limits_{k=1}^m |\beta_k|$.
Так как $|\alpha_k|\, |\beta_k| = r(A)/m$, $k = \overline{1,m}$,
то для нормы однорангового возмущения $K$ имеем оценку
$$
\|K\| = \|a\|\, \|b\| \le r(A) \|u\|\, \|v\|
\frac{1}{m}\sum_{k=1}^m |\alpha_k| \sum_{k=1}^m |\alpha_k|^{-1}.
$$
При $r(A) = 1$, положив $|\alpha_k| =1$, $k = \overline{1,m}$, отсюда получаем
$\|K\| \le m \|u\|\, \|v\|$,
где $u$, $v$ --- собственные векторы матриц $A$ и $A^*$ соответственно,
отвечающие единице и нормированные условием $(u,v) = 1$. При этом
$r(A - K) < 1$.

Заметим, что обычно индекс импримитивности $m$ меньше
размерности $n$ матрицы $A$. Так, например, если $a_{ii} > 0$ для
некоторого $i = \overline{1,n}$, то $m = 1$. Если же $a_{ij} > 0$
и $a_{ji} > 0$ для некоторой пары $(i,j)$ с $i\ne j$, то существует
двухзвенный контур направленного графа матрицы $A$, и, следовательно,
$m \le 2$, т.\,к. 2 должно делиться на $m$. Понятно, что возможен
случай, когда $m = n$ (если в качестве $A$ взять матрицу перестановок
([2], c.\,27)).

Будем следовать терминологии, принятой в [1], [5].

Аналогом неприводимой матрицы с неотрицательными элементами 
в бесконечномерном случае является потенциально компактный положительный
оператор $A$, действующий в комплексной банаховой решетке $B$
и обладающий следующим свойством:
s)~не существует нетривиальных $A$-инвариантных замкнутых идеалов
решетки $B$.
Как известно [6], для оператора $A$ с перечисленными выше свойствами 
спектральный радиус $r(A)$ является положительным собственным значением;
все собственные значения, лежащие на спектральной окружности
$\{\lambda: |\lambda| = r(A)\}$, простые и задаются формулой (\ref{1}),
где $m$ --- число различных собственных значений, совпадающих с $r(A)$
по модулю. Далее, для сопряженного оператора $A^*$ найдется такой строго
положительный функционал $v\in B^*$, что $A^*v = r(A)v$.
Кроме того, существует такое $u\in B$, что $u > 0$, $Au = r(A)u$ и $u$ 
--- квазивнутренняя точка решетки $B$.
Известно ([1], с.\,337), что свойство~s) эквивалентно следующему свойству:
существует такой скаляр $\lambda > r(A)$, что для всякого $x > 0$
вектор $y = \sum\limits_{k=1}^\infty \lambda^{-k}A^k x$ является
квазивнутренней точкой решетки $B$.

Обозначим $B_0 = \{x\in B : |x| \le \lambda u$ для некоторого
$\lambda$, $0 \le \lambda < \infty\}$. Это линейное подпространство является
минимальным идеалом, содержащим собственный вектор $u > 0$. Кроме того,
$B_0$ плотно в $B$ (т.\,к. $u$ --- квазивнутренняя точка) и инвариантно
относительно оператора $A$. Согласно теореме 8 из [6] существует такой
биективный ортоморфизм $D$ банаховой решетки $B$, что $|Dx| = |x|$
для всех $x\in B_0$ и имеют место представления (\ref{2}) и (\ref{3})
для оператора $A$ и сопряженного оператора $A^*$,
где $S = (D^*)^{-1}$. Таким образом, элементы $\varphi_k = D^{k-1}u$ и
$\psi_k = S^{k-1}v$ будут единственными с точностью до постоянного 
множителя собственными векторами, отвечающими соответственно
$\lambda_k$ и $\ov\lambda_k$, операторов $A$ и $A^*$.

Если ведущие собственные векторы $u$ и $v$ выбраны так, что $(u,v) = 1$,
то $(\varphi_k,\psi_j) = \delta_{ij}$ --- символ Кронекера.

Из равенства $|D^{k-1}x| = |x|$, имеющего место при всех $k = \overline{1,m}$
и $x\in B_0$, следует $|D^{k-1}u|{=}u$. Кроме того, для любого 
$y > 0$, $y\in B$, имеем
$(y,|S^{k-1}v|) = \sup \{|(x,S^{k-1}v)| : |x| \le y \} = 
\sup \{|(D^{k-1}x,v)| : |x| \le y \} \le 
\sup \{(|x|,v) : |x| \le~ y \} = (y,v)$.  
Поэтому $|S^{k-1}v| \le v$.

Аналогично конечномерному случаю можно показать, что $r(A - K) < r(A)$,
если в одноранговом операторе
$$
K x = \bigg(\sum_{k=1}^m \alpha_k D^{k-1}u\bigg)
\bigg(x, \sum_{k=1}^m \ov\beta_k S^{k-1}v\bigg), \quad x\in B,
$$
коэффициенты $\alpha_k$ и $\beta_k$ связаны соотношением (\ref{4}).

При фиксированном $x\in B$ имеем
$$
|Kx| \le \bigg(\sum_{k=1}^m |\alpha_k|\,|D^{k-1}u|\bigg)
\bigg(|x|,\sum_{k=1}^m |\ov\beta_k|\, |S^{k-1}v|\bigg).
$$
Так как
$$
|D^{k-1}u| = u,\ \ |S^{k-1}v| \le v, 
\ \ |\alpha_k|\,|\beta_k| = \frac{r(A)}{m},\ \ k = \overline{1,m},
$$
то $|Kx| \le u (|x|,v)\frac{r(A)}{m}
\sum\limits_{k=1}^m |\alpha_k|\sum\limits_{k=1}^m |\alpha_k|^{-1}$.
В силу монотонности нормы банаховой решетки $\|\, |x|\, \| = \|x\|$ и
$$
\|Kx\| \le \|u\|\,\|v\|\,\|x\|
\frac{r(A)}{m}\sum_{k=1}^m |\alpha_k|\sum_{k=1}^m |\alpha_k|^{-1}.
$$
Следовательно, $\|K\| \le \|u\|\,\|v\|
\frac{r(A)}{m}\sum\limits_{k=1}^m |\alpha_k|
\sum\limits_{k=1}^m |\alpha_k|^{-1}$,
где $u$, $v$ --- ведущие собственные векторы операторов $A$ и  $A^*$,
нормированные условием $(u,v) = 1$. Из этого неравенства при $r(A) = 1$
и $|\alpha_k| = 1$, $k=\overline{1,m}$, следует $\|K\| \le m\|u\|\,\|v\|$.

Эти оценки для нормы однорангового возмущения $K$, уменьшающего
спектральный радиус оператора $A$, аналогичны 
конечномерным неравенствам.

В общем случае для потенциально компактного положительного оператора $A$,
неприводимого над замкнутым идеалом банаховой решетки $B$, нет правил для
подсчета числа $m$ различных собственных значений, лежащих на спектральной
окружности (для конечномерного случая такие правила были приведены выше).
Однако известно ([6], c.\,391; [1], c.\,340), что $m = 1$, если выполнено одно
из следующих условий:
\begin{itemize}
\item[а)] $Ax$ --- квазивнутренняя точка решетки $B$, если $x > 0$;
\item[б)] $A$ принадлежит операторному идеалу [7] со следом $\tau$
и $\tau(A) \ne 0$;
\item[в)] $B = C(X)$ --- банахова решетка непрерывных функций, заданных на 
связном хаусдорфовом компакте $X$.
\end{itemize}

Ниже предполагается, что $r(A) < 1$. В случае, когда обычный метод 
последовательных приближений нахождения единственного решения уравнения 
$x = Ax + f$, где $f\in B$, сходится медленно, можно предложить cледующее
ускорение метода простой итерации. Выберем произвольную 
последовательность комплексных чисел 
$\beta_1,\dotsc,\beta_m$ и положим 
$b = \sum\limits_{k=1}^m \ov\beta_k\psi_k$, 
где $\ov\beta_k$ --- комплексно-сопряженное с $\beta_k$ число. Обозначим
$$
a = \sum_{k=1}^m \varphi_k\lambda_k^m \Big/
\bigg(\beta_k\prod_{j=1,j\ne k}^m (\lambda_k - \lambda_j)\bigg),\quad
c = \sum_{k=1}^m \psi_k \ov\beta_k/(1 - \ov\lambda_k).
$$
Положим $g = f + a(f,c)$. Следуя схеме работы [8], можно
показать, что в некоторой эквивалентной норме $\|\cdot\|_*$
пространства $B$ итерационный процесс
$$
x_0 = 0,\ \ x_k = Ax_{k-1} - a(x_{k-1},b) + g,\ \ k=1,2,\dotsc,
$$
сходится к решению $x$ уравнения $x = Ax + f$ со скоростью
$\|x_k - x \|_* < q^k \|x\|_*$, где $q$ меньше  $r(A)$, 
но больше радиуса второй спектральной окружности оператора $A$.


\begin{thebibliography}{9}

\bibitem{1}
Шефер Х. {\em Топологические векторные пространства}. 
-- М.: Мир, 1971. -- 359~с.
\bibitem{2}
Воеводин В.В., Кузнецов Ю.А. {\em Матрицы и вычисления}. 
-- М.: Наука, 1984. -- 318~с.
\bibitem{3}
Гантмахер Ф.Р. {\em Теория матриц}. -- М.: Наука, 1988. -- 552~с.
\bibitem{4}
Исламов Г.Г. {\em Экстремальные возмущения замкнутых операторов} // 
Изв. вузов. Математика. -- 1989. -- \No\,1. -- C.\,35--41.
\bibitem{5}
Канторович Л.В., Акилов Г.П. {\em Функциональный анализ}. 
-- М.: Наука, 1977. -- 742~с.
\bibitem{6}
Grobler J.J. {\em A note on the theorem of Jentzsch--Perron and 
Frobenius} // Proc. Koninkl. Nederl. Akad. wetensch. -- 1987. --
Ser.\,A90. -- \No\,4. 
-- P.\,381--391.
\bibitem{7}
Пич А. {\em Операторные идеалы}. -- М.: Мир, 1982. -- 536~с.
\bibitem{8}
Исламов Г.Г. {\em О границе применимости итерационного метода} //
Межвуз. сб. науч. тр. ``Матем. моделир. и информационные
технологии''. -- Ижевск: Изд-во Удмурт. ун-та, 1991. -- С.\,14--18.

\end{thebibliography}

\vskip 2cm

\post{\it Удмуртский государственный}{\it Поступила}
\post{\it университет}{06.05.2002}

\label{end}
\end{document}
