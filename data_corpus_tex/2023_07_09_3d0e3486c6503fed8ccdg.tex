\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }
\usepackage{bbold}

\DeclareUnicodeCharacter{0131}{$\imath$}

\begin{document}
ЭКСПОНЕНТА - то же, что әкспоненциальнал функчия.

ӘКСПОНЕНТА г р у п п - ваименьшее число $n$ такое, что в данной группе выполняется тождество $x^{n}=1$. $\quad$ o. А. Уванова.

ЭКСПОНЕНЦИАЛЬНАЯ ТОПОЛОГИЯ - слабейшая тонология на множестве $\exp X=2^{X}$ всех замкнутых подмножеств топологич. пространства $X$, в к-рої множества $\exp A$ открыты (в $\exp X$ ) для открытых $A$ и замкнуты (в $\exp X)$ для замкнутых $A$. Если $A \subset X$, то через $\exp A$ обозначается множество всех замкнутых подмножеств множества $A$.

П р и м е р. Тополіогия метрич. пространства замкнутых ограниченных подмножеств метрич. пространства, наделенное Хаусдорфова метрикой. Общее определевие: гуусть $U_{1}, \ldots, U_{n}$ - произвольный конечный набор непустых открытых в $X$ множеств; базу Э. т. образуют множества вида

\[
\begin{gathered}
\left\langle U_{i}, \ldots, U_{n}\right\rangle=\left\{\hat{F^{\prime}} \exp X: F \subset \bigcup_{1}^{n} U_{i} \& F \cap U_{i} \neq \xi,\right. \\
i=1, \ldots, n\},
\end{gathered}
\]

где $\hat{F}$ обозначает точку $\exp X$, соответствующую данному замкнутому множеству $F \subset X$. Пространство $\exp X$, наделенное Э. т., наз. э к с п् о н е н т о й п् $\mathrm{p}$ о с тр а в с т в а $X$. Если $X$ есть $T_{1}$-пространство, то $\exp X$ также $T_{1}$-пространство. Если $X$ регулярно, то $\exp X$ хаусдорфово. Если $X$ нормально, то $\exp X$ вполне ре гулярно. Для Э. т. нормальность эквивалентна ее бикомпактности. Если пространство $X$ бикомпактно, то $\exp X$ также бикомпактно. Если $X$ - диадический бикомпакт и вес $X$ не превосходит $\boldsymbol{\aleph}_{1}$, то $\exp X-$ также диадический бикомпакт. С другой стороны, экспонента любого бикомпакта, вес к-рого больше или равен $\aleph_{2}$, не является диадическим бикомпактом. Экспоневта континуума Пеано является абсолютвым ретрактом в классе метрич. компактов и, следовательно, непрерывным образом отрезка. Однако экспонента несчетного веса не является непрерывным образом тихововского куба $I^{\tau}$. Пусть $f: X \rightarrow Y-$ замкнутое отображение пространства $X$ на пространство $Y$. Отображение $\exp f: \exp X \rightarrow \exp Y, \quad$ действующее по правилу $(\exp f)(\widehat{F})=\widehat{f(F)}$, наз. ә к с п о н ент о й о т об р аже ни я. Если $f: X \rightarrow Y-$ непрерывное отображение бикомшакта $X$ на бикомпакт $Y$, то оно открыто тогпа и только тогда, когда открыто отображение $\exp f$. Функтор $\exp X$, действующий из категории бикомпактов и непрерывных отображений в ту же самую категорию, является ковариантным функтором экспоненциального тина, если морфизму ставится экспонента этого морфизма $\exp f$.

Ллит.: [1] $\mathrm{K}$ у $\mathrm{p}$ а т т в с к и й К., Топология, [пер. с англ.],

\includegraphics[max width=\textwidth, center]{2023_07_09_3d0e3486c6503fed8ccdg-1}
т е ль н а я ф у нкц и я,- функция $y=e^{x}$; обозначается также $y=\exp x$. Иногда Э. ф. ваз. и функцию

$y=a^{x}$ при любом основании $a>0$.
ЭКСПОНЕНЦИЛЬНОГО ТИПА ФУНКЦИЯ - целая функция $f(z)$, удовлетворнющая условию:

\[
|f(z)|<A e^{a|z|},|z|<\infty, A<\infty, a<\infty .
\]

Если $f(z)$ представить рядом

тo

\[
f(z)=\sum_{k=0}^{\infty} \frac{a_{k}}{k !} z^{k}
\][

\textbackslash varlimsup\_\{k \textbackslash rightarrow \textbackslash infty\} \textbackslash sqrt[k]\{\textbackslash left|a\_\{k\}\textbackslash right|\}<\textbackslash infty \textbackslash text \{. \}
]

Простейтие примеры Э. т. ф.: $e^{c z}, \sin \alpha z, \cos \beta z$, $\sum_{k=1}^{n} A_{k} e^{a_{k}^{z}}$ Э. т. Ф. имеет интегральное представление

\[
f(z)=\frac{1}{2 \pi i} \int_{C} \gamma(t) e^{z t} d t,
\]

где $\gamma(t)$ - функция, ассоциированная по Борелю $\mathrm{c} f(z)$ (см. Бореля преобразование), а $C-$ замкнутый контур, охватывающий все особенности $\gamma(t)$.

Јича:: [1] Л е в и н Б. Я., Распределение корней целых Функций, М., 1956.

ЭКСПОНЕНЦИАЛЬНОЕ ОТОБРАЖЕНИЕ - отображение касательного простравства многообразия $M$ в $M$, определяемое заданной на $M$ сөязностью и являющееся далеко идущим обобщением обычной эксповенциальной функции, рассматриваемой гак отображение прямой в себя.

\begin{enumerate}
  \item Пусть $M$ - многообразие класса $C^{\infty}$ с аффинной связностью, $p$ - нек-рая точка из $M, M_{p}$ - касательное пространство к многообразию $M$ в точке $p$ и $X-$ ненулевой вектор из $M_{p}, t \rightarrow \gamma_{\boldsymbol{X}}(t)-$ геодезическая, шроходящая через точку $p$ в направлении вектора $X$. Существует такая открытая окрестность $N_{0}$ точки 0 в $M_{p}$ и такая открытая окрестность $N_{p}$ точки $p$ в $M$, что отображение $X \rightarrow \gamma_{X}(1)$ является диффеоморфизмом $N_{0}$ на $N_{p}$. Это отображение называется Э. о. в точке $p$ и обозначается $\operatorname{Exp}$. Окрестность $N_{0}$ наз. н о р м а л ьн о й, если: 1) отображение $\operatorname{Exp}$ диффеоморфно отображает $N_{0}$ на $\left.N_{p}, 2\right)$ если $X \in N_{0}$ и $0 \leqslant t \leqslant 1$, то $t X \in N_{0}$. В этом случае $N_{p}$ - нормальная окрестность точки $p$ в многообразии $M$. Каждая точка $p \in M$ обладает выпуклой нормальной окрестностью $N_{p}$ точки $p:$ любые две точки такой окрестности можно соединить в точности одним геодезич. отрезком, содержащимся в $N_{p}$. Если $M$ - полное риманово многообразие, то Ехр есть сюръективное отображение $M_{p}$ на $M$.

  \item Пусть $G$ - группа Ли с единицей $e$ и $g$ - соответствующая алгебра Ли, состоящая из касательных векторов к $G$ в точке $e$. Для каждого вектора $X \in g$ существует единственный аналитич. гомоморфизм $\theta$ групшы $\mathbb{R}$ в $G$ такой, что касательный вектор к $\theta(\mathbb{R})$ в точкө $e$ совпадает с $X$. Отображенис $X \rightarrow \exp X=\theta(1)$ и наз. Э. о. алгебры $g$ в групшу G. Существует такая открытая окрестность $N_{0}$ точки 0 в $g$ и такая открытая окрестность $N_{e}$ точки $e$ в $G$, что $\operatorname{exp~является~аналитич.~диффеомор-~}$ физмом окрестности $N_{0}$ на $N_{e}$. Пусть $X_{1}, \ldots, X_{n}-$ нек-рый базис алгебры g. Отображение $\exp \left(x_{1} X_{1}+\right.$ $\ldots-\left(-x_{n} X_{n}\right) \rightarrow\left(x_{1}, \ldots, x_{n}\right)$ есть система координат на $N_{e}$, әти координаты наз. к а н о в и ч е с к и ми.

\end{enumerate}

$\kappa$ понятию Э. о. для групшы Ли $G$ можно подойти и с другой точки зрения. Существует взаимно однозначное соответствие между множеством всех аффинных связностей на $G$, инвариантных относительно групшы левых сдвигов, и множеством билинейных функций $\alpha: g \times$ $\times g \rightarrow g$. Оказывается, что Э. о. ехр алгебры $g$ в групгу $G$ совпадают с отображением $\operatorname{Exp}$ касательного ıространства $g$ к многообразию $G$ в точке $e$ в это многообразие относительно левоинвариантной аффинной связности, отвечающей любой кососимметричной билинейвой функции $\alpha$.

ЈІит.: [1] Х е лг г а с о н С., Дифферендиальная геометрия и симметрические пространства, пер. с англ,, М., 1964. . Ф. Фенко,

ЭКСІОНЕНЦИАЈЬНОЕ РАСПРЕДЕЛЕНИЕ - то же, что показательное распределение.

ЭКСТРАПОЛИРОВАНИЕ, э к с т р а п о ля ц П я, функции - продолжение функции за пределы ее области определения, при к-ром продолженная функция (как правило, аналитическая) принадлежит заданному классу. Э. функций обычно производится с помощью формул, в к-рых использована информация о поведении функции в нек-ром конечном наборе точек (у з л а $\mathbf{x}$ и н те ределения.


\end{document}