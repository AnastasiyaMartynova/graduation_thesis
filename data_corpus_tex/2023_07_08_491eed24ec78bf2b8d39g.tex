\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
О с п о в ны е у р а в н е н іл я. Коэффициенты первой и второй квадратпчных форм не являются независимыми. Они связаны уравнениями Гаусса и Петерсона - Кодацци - Майнарди.

У ра в нения

$R(X, Y) Z=\bar{R}(X, Y) Z-H\left(X, \nabla_{X} Z\right)+H\left(Y, \nabla_{X} Z\right)+$

\[
+H([X, Y], Z)
\]

выражают преобразование кривизны поверхности $F$ через преобразование кривизны $N^{n}$ и коәффициенты квадратичных форм (здесь [., . ] - скобка Пуассона).

Инвариантная форма уравнений Петерсона - Кодацци - Майнарди связана с понятием риманова расслоения над $M_{g}^{m}$. Расслоение $E(l) l$-мерных плоскостей над римановым многообразием $M_{\mathrm{g}}^{m}$ наз. р и м аШ о в ы м, если в $E(l)$ задана риманова метрика $\langle\cdot, \cdot\rangle_{E}$ и риманова (т. е. согласованная с метрикой) связность $D: T M^{n} \times E \rightarrow E$. Билинейное $g$-симметричное отображение

\[
A_{\xi}: T M^{m} \times E \rightarrow T M^{m}
\]

наз. в т о рым фундаме пталь ным тензор о м в $E ;$ равенство

\[
\langle H(X, Y), \xi\rangle_{E}=\left\langle A_{\xi} X, Y\right\rangle_{\mathrm{g}}
\]

определяет вторую фундаментальную форму в $E$, acсоцишрованную с $A_{\xi}$. Уравнения

\[
\begin{gathered}
\nabla_{X} A_{\xi} Y-\nabla_{Y} A_{\xi} X=A_{D_{X}} Y-A_{D_{Y}} X, \\
\tilde{R}(X, Y) \xi=H\left(A_{\xi} X, Y\right)-H\left(X, A_{\xi} Y\right),
\end{gathered}
\]

где $\tilde{R}$ - форма кривизны связности $D$, наз. у р а внения ми П е те р с онан а р д и. Уравнения (3) часто наз. у р а в ш е н и я м и $\mathrm{P}$ и ч ч и (координатную запись уравнений $(2)-(3)$ см. в [1]). Уравнения (2) - (3) всегда выполнены, если $E(l)$ - нормальное расслоение $M^{m}$ в $N^{n}$ и $D_{X} \xi$ равно проекции $\bar{\nabla}_{X} \xi$ на $\left(\nu M^{m}\right)_{p}$.

Имеется следующее обобщение Бонне теоремы (см. [2]). Пусть $E(l)$ - риманово расслоение над односвязным $M_{\mathrm{g}}^{m}$ и пусть на $E(l)$ задан второй фундаментальный тензор $A_{\xi}$. Если при әтом выполняются соотношения (1) - (3), то существует изометрич. погружение $M_{g}^{m}$ в евклидово пространство $\mathbb{R} m+l$ с нормальным расслоением $E(l)$. Такое погружение единственно в следующем смысле: если $f, f^{\prime}$ - изометрич. погружения $M_{g}^{m}$ в $\mathbb{R}^{m+l}$ с нормальными расслоениями $E, E^{\prime}$, оснащенными, как и выше, и нек-рая изометрия $\varphi: M_{\mathrm{g}}^{m} \rightarrow M_{\mathrm{g}}^{m}$ накрывается отображением $\bar{\varphi}: E \rightarrow E^{\prime}$, согласованным с оснащенным, то существует такая изометрия Ф пространства $\mathbb{R} m+l$, что Ф。f $=f^{\prime} \circ \varphi$.

К л а с с ы п г г у ж е и и й. Многомерная П. м. г. возникла и долгое время развивалась как теория существования изометрич. погружений римановых многообразий как правило в $\mathbb{R}^{n}$, реже - в пространство постоянной кривизны $K$ (см. Изометрическое погружение). Что касается внешне геометрич. свойств и связей между внешней и внутренней геометрией поверхностей, то она подробно изучена только для двумерных поверхностей в $\mathbb{R}^{3}$. В этом случае существует классификация точек поверхности, с помощью к-рой двумерные поверхности разбиваются на классы: выпуклые поверхности, седловые поверхности и развертывающиеся поверхности. Именно эти классы являются основным объектом изучения в дифференциальной геометрии в целом. В многомерном случае такая классификация точек поверхности неизвестна (1983). Известны только нек-рые классы многомерных поверхностей: $k$-выпуклые, $k$-седловые и $k$-развертывающиеся. $k$-в ы П у л ы е п о в е р х о о с т и. Поверхность $F^{m}$ в $\mathbb{R}^{n}$ наз. $k-$ в ы п у к л о й, если для кажцой точки $p \in F^{m}$ существует нормаль $\xi_{p} \in\left(v F^{m}\right)_{p}$, для к-рой $h_{p}(\xi)$ положительно определена, и для любого $k$-мерного направления $\sigma_{k} \in\left(T F^{m}\right)_{p}, 2 \leqslant k \leqslant m$, найдется в $\sigma_{k}$ двумерное направление $\sigma_{2}$ такое, что $h_{p}(\xi)(X, Y)>0$ (либо $\left.h_{p}(\xi)(X, Y) \equiv 0\right)$ для каждого $\xi_{p} \in\left(\nu F^{m}\right)_{p}$ при $X, Y \in \sigma_{2}, X, Y \neq 0.2$-выпуклая поверхность $F^{m}$ в $\mathbb{R}^{n}$ есть выпуклая гиперповерхность в $\mathbb{R}^{m+1} \subset \mathbb{R}^{n}$ (см. [4]). Внутренняя метрика $k$-выпуклой поверхности обладает следующим свойством: в каждой точке $p$ для каждого $k$-мерного направления $\sigma_{k}$ касательного простравства найдется двумерное направление $\sigma_{2} \subset \sigma_{k}$, в к-ром риманова кривизна строго положительна.

$k$-с е д л о в ы е по в е р х но с т и. Поверхность $F^{m}$ в $\mathbb{R}^{n}$ наз. $k$-с е д л о в о й, если каждой ее точке $p$ д.тя каждой нормали $\xi \in\left(v F^{m}\right)_{p}$ число собственных значений $h_{p}(\xi)$ одного знака не превосходит $(k-1), 2 \leqslant k \leqslant m$. Двумерная $k$-седловая поверхность есть обычная седловая поверхность в $\mathbb{R} n$, от к-рой нельзя отсечь «горбушку" гиперплоскостью. Внутренняя метрика $k$-седловой поверхности обладает следующим свойством: в каждой точке $p$ для каждого $k$-мерного направления $\sigma_{k}$ касательного пространства найдется двумерное направление $\sigma_{2} \subset \sigma_{k}$, в к-ром риманова кривизна не положительна. Если $k$-седловая поверхность полна в $\mathbb{R}^{n}$, то ее гомологии $H_{i}\left(F^{m}\right)=0$ при $i \geqslant k$ (см.[4], [5]). Полная $m$-мерная $k$-седловая поверхность $F^{m}$ с неотрицательной кривизной Риччи есть цилиндр $\mathbf{c}(m-$ $-k+1)$-мерной образующей.

$k-\mathrm{p}$ а з в е р т ы в а ю щ и е с я $(k-\Pi$ а р а б о ли ч ес ки е ) По в е рх ности. Поверхность $F^{m}$ в $\mathbb{R}^{n}$ наз. $k-p$ а 3 в е р т ы в а ю е й с я, если в каждой ее точке $p$ существует $k$-мерное направление $\sigma_{k} \subset\left(T F^{m}\right)_{p}$, к-рое состоит из собственных векторов, принадлежащих нулевому собственному значению матрицы второй квадратичной формы относительно каждой нормали в данной точке. Внутренняя метрика $k$-развертывающейся поверхности обладает следующим свойством: в каждой точке $p$ найдется подпространство $\sigma_{k}$ касательного пространства $\left(T F^{m}\right)_{p}$ размерности $k$ такое, что $R_{X Y}=0$ для любого вектора $X \in \sigma_{k F}$, где $Y \in\left(T F^{m}\right)_{p}-$ любой вектор касательного пространства, $R_{X Y}$ оператор кривизны. Если $k$-развертывающаяся поверхность $F^{m}$ полна в $\mathbb{R}^{n}$ и несет на себе внутреннюю метрику неположительной кривизны Риччи, то она является цилиндром с $k$-мерной образующей [6].

С в о б о д ны е по г р у же ния. Если образ $H_{p}(X, Y)$ в каждой точке $p \in F^{m}$ имеет максимально возможную размерность $m(m+1) / 2$, то погружение наз. с в о б о д н ы м. В этом случае первые и вторые производные радиус-вектора погружения $F^{m}$ образуют линейно независимую систему. В классе свободных погружений существует изометрич. погружение в размерность $n>m(m+1) / 2+3 m+5$, но при этом полностью теряется связь между внутренней и внешней геометрией. Напр., два свободных изометрич. погружения $m$-мерного многообразия $M^{m}$ в $\mathbb{R}^{n}, n>m(m+$ $+1) / 2+3 m+5$, можно соединить гомотопией, состоящей из свободных изометрич. погружений $M^{m}$ (см. [7]).

\includegraphics[max width=\textwidth, center]{2023_07_08_491eed24ec78bf2b8d39g-1}
с т ю ю. Если коразмерность $q$ погружения мала, то из условий на внутреннюю метрику многообразия вытекают условия на вторую квадратичную форму поверхности. А из свойств второй квадратичной формы далее выводятся топологические и внешне геометрич. свойства поверхности. В частности, получаются теоремы непогружаемости. Так, если $M^{m}$ с секционной кривизной $K_{\sigma} \leqslant 0$ изометрически погружено в $\mathbb{R} m+q$ с $q<m$, то $M^{m}$ есть $(q+1)$-седловая поверхность и ее гомологии (в случае полноты) $H_{k}=0$ при $k \geqslant q+1$ (см. [5]). В частности, компактное $M^{m}$ с $K_{\sigma} \leqslant 0$ не


\end{document}