\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
инвариантное уравнение, описывающее свободное скалярное (или псевдоскалярное) физическое поле. Впервые получено в 1926 Э. Шредингером (Е. Schrödinger) (как релятивистское обобшение Шредингера уравнения) и независимо О. Клейном (O. Klein), В.А. Фоком и В. Гордоном (W. Gordon). В квантовой теории поля применяется для описания частиц со спином 0. В Минковского пространстве-времени К.-Г.У.- линейное однородное дифференциальное уравнение 2-го порядка:

\[
\left(\square+m^{2}\right) \varphi(x)=0,
\]

где $\square$ - оператор Д'Аламбера, $m$ - масса частицы, $\varphi$ полевая функция или ее компоненты в пространстве внутренней симметрии $\left(x=\left(x^{0}, \boldsymbol{x}\right)\right.$ - точка пространства-времени; используется система единиц, в к-рой $\hbar=c=1$ ). Решение К.-Г.у. записывают в виде разложения по плоским волнам:

\[
\begin{gathered}
\varphi(x)=(2 \pi)^{-3 / 2} \int d p\left(2 p^{0}\right)^{-1 / 2}\left\{e^{i p x} a^{+}(p)+e^{-i p x} a^{-}(p)\right\}, \\
p^{0}=\left(p^{2}+m^{2}\right)^{1 / 2},
\end{gathered}
\]

где $p=\left(p^{0}, \boldsymbol{p}\right)-4$-импульс, $p x=p^{0} x^{0}-p x, a^{+}(p)$ и $a^{-}(p)-$ положительно- и отрицательно-частотные компоненты Фурье. При каноническом квантовании $a^{+}$и $a^{-}$интерпретируются как операторы рождения и уничтожения частицы с импульсом $\boldsymbol{p}$ и энергией $p^{0}$. В их терминах гамильтониан свободного поля имеет вид:

\[
H=\int d \boldsymbol{p} p^{0}(\boldsymbol{p}) a^{+}(\boldsymbol{p}) a^{-}(\boldsymbol{p}) .
\]

К. -Г. у. удовлетворяют компоненты любого свободного поля (спинорного, векторного и др.). В римановом пространстве с метрикой $R^{\mu \nu}$ (напр., в присутствии гравитационного поля с такой метрикой) К.-Г. у. имеет вид:

\[
(-g)^{-1 / 2} \frac{\partial}{\partial x^{\mu}}\left[(-g)^{1 / 2} g^{\mu \nu} \frac{\partial}{\partial x^{v}} \varphi\right]+m^{2} \varphi=0,
\]

где $g$ - определитель матрицы $\left\|g^{\mu \nu}\right\|, \mu, v=0,1,2, \dot{3}$.

Изучены К.-Г.у. с различными видами нелинейности (напр., синус-Гордона уравнение).

Лит.: [1] Б о гол ю бо в Н. Н., Ш и рков Д. В., Квантовые поля, $\begin{array}{ll}\text { М., 1980. } & \text { В. П. Павлов. }\end{array}$

КЛЕЙНОВА ПОВЕРХНОСТЬ - неориентируемая поверХность или поверхность, имеющая край, покрытая подмножествами $U_{i}$, параметризованными комплексными числами. К таким поверхностям приводит метод алгебро-геометрич. интегрирования уравнений математич. физики и нек-рые модели конформной теории поля. Параметризация подмножеств $U_{i}$ задается с помощью непрерывных обратимых отображений $z_{i}$ множеств $U_{i}$ на единичный круг $\{z \in \mathbb{C}|| z \mid<1\}$ или полукруг $\{z \in \mathbb{C}|| z \mid<1, \operatorname{Im} z>0\}$ комплексной плоскости. При этом отображения выбираются таким образом, чтобы каждая функция $z_{i j}=z_{i} z_{j}^{-1}$ была голоморфна $\partial z_{i j} / \partial \bar{z}=0$ или антиголоморфна $\partial z_{i j} / \partial z=0$. Пары $\left(U_{i}, z_{i}\right)$ называются ди аналитическими картами, а их совокупность - дианалитическим атласом.

Обратимое отображение $\varphi$ К. П. $A$ в К. П. $\tilde{A}$ называется и зом ор ф и змом, если описывающие ее в дианалитич. атласах $\left\{U_{i}, z_{i}\right\},\left\{\widetilde{U}_{i}, \tilde{z}_{i}\right\}$ функции $\varphi_{i j}=\widetilde{z}_{i} \varphi z_{j}^{-1}$ голоморфны или антиголоморфны. Всякая К. п. изоморфна $A=A_{+} /\langle\tau\rangle$, где $A_{+}$- риманова поверхность и $\tau: A_{+} \rightarrow A_{+}$- антиголоморфная инволюция, то есть $\tau^{2}=1$ и $\frac{\partial}{\partial z}\left(z_{i} \tau z_{j}^{-1}\right)=0$ для любой пары карт $\left(U_{i}, z_{j}\right),\left(U_{j}, z_{j}\right)$ голоморфного атласа римановой поверхности $A_{+}$. Для антиголоморфной инволюции $\tau$ римановой поверхности $A_{+}$факторповерхность $A_{+} /\langle\tau\rangle$ является множеством неупорядоченных пар $(a, \tau a)$, где $a \in A_{+}$. Проекция $a \mapsto(a, \tau a)$ переводит голоморфный атлас римановой поверхности в дианалитич. атлас на $\boldsymbol{A}$. Если $A=A_{+} /\langle\tau\rangle-$ ориентируемая К. п. (с границей), то риманова поверхность $A_{+}$называется ком плексным д у б л е м.

В е е с т в н ной аналитической функцией называется аналитич. функция, заданная рядом

\[
f(z)=\sum_{n=0}^{\infty} \alpha_{n} z^{n}
\]

с вещественными коэффициентами. Как и любой аналитич. функции, ей отвечает риманова поверхность $A_{f}$ и пара меро-

\includegraphics[max width=\textwidth, center]{2023_07_08_1b87669a8f9c8ade08a8g-1}
ция $z \rightarrow \bar{z}$ индуцирует антиголоморфную инволюцию $\tau_{f}$ : $A_{f} \rightarrow A_{f}$. В ее неподвижных точках функции $\varphi_{f}$ и $\varphi^{\prime}$ вещественны. К. П. $A_{f} /\left\langle\tau_{f}\right\rangle$ называется К. П. вешественной аналитич. функции. Она компактна, если и только если аналитич. функция определяется уравнением $F(x, y)=0$, где $F(x, y)$ многочлен с вещественными коэффициентами.

Компактная К. П. $A=A_{+} /\langle\tau\rangle$ называется в е е с т в е н ной алге б ра ическо й кри вой. Неподвижные точки инволюции $\tau$ называются в е ще с т ве н ны м и точка м и к р и в о й. Им отвечают вещественные решения уравнения $F(x, y)=0$, определяющего кривую. Вещественные кривые изоморфны, если и только если отвечающие им уравнения $F(x, y)=0$ и $\tilde{F}(\tilde{x}, \tilde{y})=0$ можно перевести друг в друга бирациональной заменой вида $x \rightarrow \tilde{x}(x, y), y \rightarrow \tilde{y}(x, y)$ с вещественными коэффициентами.

Риманова поверхность $A_{+}$, связанная с вешественной алгебраич. кривой $A=A_{+} /\langle\tau\rangle$, называется ком пле кси фикацией кри вой $A$. Род поверхности $A_{+}$называется р од о м к р и в о й $A$. Группа автоморфизмов вещественной кривой рода $g$ бесконечна при $g=1$ и конечна порядка, не превосходящего $12(g-1)$, если $g>1$ и кривая имеет вещественные точки. Вещественные точки кривой распадаются на простые замкнутые контуры, называемые о валам и. Согласно теореме Гарнака, их число $k$ не превосходит $g+1$. Кривые с $g+1$ (соответственно с $g$ ) овалами называются $M$-кривыми (соответственно $(M-1)$-кривыми). Вещественная кривая считается гиперэллиптической, если ее комплексификация - гиперэллиптич. комплексная кривая. Негиперэллиптич. $M$ - или $(M-1)$-кривые изоморфны, если изоморфны их комплексификации.

Т и пом вещественной алгебраич. кривой рода $g \mathrm{c} k$ овалами называется набор $(g, k, \varepsilon)$, где $\varepsilon=0$, если К. П. неориентируема и $\varepsilon=1$, если ориентируема. Множество $M(g, k, \varepsilon)$ классов изоморфности вещественных алгебраич. кривых типа $(g, k, \varepsilon)$ называется пространством модуле й веше с твенных кривых. Оно не пусто, если и только если $\varepsilon=0,0 \leqslant k \leqslant g$, или $\varepsilon=1, \quad 1 \leqslant k \leqslant g+1$, $k \equiv g+1(\bmod 2)$. В этих случаях пространство $M(g, k, \varepsilon)$ связно. Кривые рода нуль изоморфны К. п. $\overline{\mathbb{C}} /\langle\tau\rangle$, где $\overline{\mathbb{C}}-$ сфера Римана и $\tau(z)=\bar{z}$ или $\tau(z)=-\frac{1}{z}$. Множество кривых рода $g$ распадается на три компоненты связности $M(1,0,0)$, $M(1,1,0), M(1,2,1)$, каждая из к-рых гомеоморфна $\mathbb{R}$. Все остальные пространства $M(g, k, \varepsilon)$ представляются в виде $\mathbb{R}^{3 g^{-3}} / \operatorname{Mod}(g, k, \varepsilon)$, где группа $\operatorname{Mod}(g, k, \varepsilon)$ действует дискретно. Аналогичное описание пространств модулей имеется и для супераналогов К. п. (клейновых суперповерхностей).

Jum.: [1] Alling N., Green le af N., Foundations of the theory of Klein surfaces, B.- Hdlb., 1971; [2] Натанзон С. М., «Успехи математич. наук», 1990, т. 45 , в. 6, с. 47-90.

КЛЕТКА - см. Эйлерова характеристика.

КЛИФФОРДА АЛГEБРА, спинорная алгебра,ассоциативная алгебра $K_{n} \mathrm{c} n$ образуюшими $k_{1}, \ldots, k_{n}$, то есть совокупность линейных комбинаций из произведений $k_{i}$, причем выполняются соотношения:

\[
k_{i} k_{j}+k_{j} k_{i}=\left\{k_{i}, k_{j}\right\}=0 \text { при } i \neq j, k_{i}^{2}=1 .
\]

K. а. названа по имени У. Клиффорда (W. Clifford, 1876).


\end{document}