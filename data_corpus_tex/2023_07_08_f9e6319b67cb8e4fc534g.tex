\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
Алгоритм регуля рных точе к. Этот алгоритм на каждом шаге осматривает окрестность $S_{2}(\mathfrak{A}, \mathfrak{A})$ конъюнкции $\mathfrak{A}$ в д. н. Ф. $\mathfrak{X}$ и удаляет конъюнкции, не входящие ни в одну тупиковую и, следовательно, ни в одну минимальную д. н. ф. В описании алгоритма используется понятие $\tilde{\alpha}-\Pi$ у ч к а в д. н. ф. $\mathfrak{l}$, представляющего собой для рассматриваемой точки $\tilde{\alpha}$ из $N_{\mathfrak{R}}$ множество $M(\tilde{\alpha}, \mathfrak{R})$ интервалов $N_{\mathfrak{\vartheta}}$ таких, что $\mathfrak{\Re}_{j} \in \mathfrak{\Re}, \tilde{\alpha} \in N_{\mathfrak{\Re}_{j}}$ Для конъюнкции $\mathfrak{X}$ из д. н. ф. $\mathfrak{N}$ точка $\tilde{\alpha}$ из $N_{\mathfrak{A}}$ наз. р е г у ля р но й о тнос и те ль но $(\mathfrak{H}, \mathfrak{R})$, если существует точка $\tilde{\alpha}^{\prime}$ такая, что $\tilde{\alpha}^{\prime} \in N_{\mathfrak{R}} \backslash N_{\text {१্ }}$ и $M(\tilde{\alpha}, \mathfrak{N}) \subseteq M(\tilde{\alpha}, \mathfrak{N})$. Множество $M \subseteq N_{\Re}$ наз. р е-

\begin{center}
\includegraphics[max width=\textwidth]{2023_07_08_f9e6319b67cb8e4fc534g-1(5)}
\end{center}

\includegraphics[max width=\textwidth, center]{2023_07_08_f9e6319b67cb8e4fc534g-1(7)}
мый алгоритм основывается на том, что конъюнкция $\mathfrak{A}$ из сокращенной д. н. ф. Функции $f$ не входит ни в одну тупиковую д. н. ф. функции $f$ тогда и только тогда, когда $N_{\mathfrak{A}}$ есть множество, регулярное относительно $(\mathfrak{A}, \mathfrak{R})$. Алгоритм проверяет в нек-ром порядке, являются ли интервалы конъюнкций, входящих в д. н. ф., регулярными множествами, и удаляет те из них, интервалы к-рых регулярны. Свойство интервала $N_{\Re}$ быть

\includegraphics[max width=\textwidth, center]{2023_07_08_f9e6319b67cb8e4fc534g-1(6)}
ляется окрестностью $S_{2}(\mathfrak{H}, \mathfrak{R})$ и, вообще говоря, не определяется окрестностью $S_{1}(\mathfrak{A}, \mathfrak{R})$.

$A$-а л г о р и т м. Здесь используется понятие д. н. ф.,

\includegraphics[max width=\textwidth, center]{2023_07_08_f9e6319b67cb8e4fc534g-1}
т. е. такой д. н. Ф., к-рая минимальна среди всех д. н. Ф.,, эквивалентных $\mathfrak{R}$ и получающихся из $\mathfrak{R}$ опусканием нек-рых әлементарных контюнкций. Рассматриваются два свойства элементарной конъюнкции в д. н. Ф.:

\includegraphics[max width=\textwidth, center]{2023_07_08_f9e6319b67cb8e4fc534g-1(9)}
относительно গ̊, и

$P_{2}(\mathfrak{A}, \mathfrak{R})-\ll \mathfrak{Y}$ не входит ни в одну д. н. ф., минимальную относительно $\mathfrak{\Re} »$.

Считается, что $P_{i}=1$, если свойство $P_{i}$ имеет место, п $P_{i}=0$ в противном случае. Предполагается, что д. н. Ф. составлены из конъюнкций с информационными отметками $\left(\omega_{1}, \omega_{2}\right)$, где $\omega_{i} \in\{0,1, \Delta\}$. При этом равенство $\omega_{i}=\Delta$ означает, что свойство $P_{i}$ не вычислено $(i=1,2)$, а равенства $\omega_{i}=1$ означают, что $P_{i}=\omega_{i}(i=1,2)$. Конъюнкция $\mathfrak{A}$ с информационными отметками $\left(\omega_{1}, \omega_{2}\right)$ обозначается через $\mathfrak{U}^{\omega_{1} \omega_{2}}, A$-алгоритм вычисляет значения свойств $P_{1}$ и $P_{2}$ для конъюнкций $\mathfrak{A}$ из д. н. ф. $\mathfrak{\Re}$, используя для этого лишь конъюнкции из окрестности $S_{2}(\mathfrak{A}, \mathfrak{\Re})$ и их информационные отметки. Для конъюнк-

\includegraphics[max width=\textwidth, center]{2023_07_08_f9e6319b67cb8e4fc534g-1(4)}
же с твом перво го типа относ итель-

\includegraphics[max width=\textwidth, center]{2023_07_08_f9e6319b67cb8e4fc534g-1(2)}
$\mathfrak{A}_{1}, \ldots, \mathfrak{U}_{m}$ рангов $r_{1}, \ldots, r_{m}$ такие, что $M \subseteq \bigcup_{j=1}^{m} N_{\mathfrak{N}_{j}}$ и $r>\sum_{j=\mathbf{1}}^{m} r_{j}$.

Р а 3 н о с т ь ю $\mathfrak{R}_{1} \backslash \mathfrak{N}_{2}$ д. н. Ф. $\mathfrak{l}_{1}$ и $\mathfrak{R}_{2}$ наз. д. н. ф., состоящая из элементарных конъюнкций, входящих в $\mathfrak{N}_{1}$ и не входящих в $\mathfrak{R}_{2}$.

До применения $A$-алгоритма все конъюнкции из рассматриваемой д. н. ф. $\mathfrak{l}$ имеют отметку $(\Delta, \Delta)$. Если выполнены $i$ шагов И отметку $(1, \Delta)$ получили конъюнкции $\mathfrak{A}_{1}^{\prime}, \ldots, \mathfrak{A}_{s}^{\prime}$, а отметку $(\Delta, 1)$ - конъюнкции $\mathfrak{A}_{1}^{\prime \prime}$, . . ., $\mathfrak{A}_{t}^{\prime \prime}$, то $(i+1)$-й шаг состоит в следующем. Упорядочивают нек-рым способом конъюнкции из д. н. ф. Если д. н. ф. $\mathfrak{X} \backslash\left(\vee{ }_{j=1}^{s} \mathfrak{A}_{j}^{\prime} \vee \vee{ }_{j=1}^{t} \mathfrak{Y}_{j}^{\prime \prime}\right)$ пустая, то $A$-алгоритм заканчивает свою работу. В противном случае после упорядочивания к.-л. способом конъюнкций из этой д. н. ф. выделяются первая по порядку конъюнкция $\stackrel{\mathfrak{X}}{\mathfrak{X}}$ и еe окрестности $S_{1}$ и $S_{2}$ в д. н. ф. $\tilde{\mathfrak{l}}=\mathfrak{\Re} \backslash\left(\vee{ }_{j=1}^{t} \mathfrak{X}_{j}^{\prime \prime}\right)$ и проверяется соотношение $N_{\mathfrak{R}} \equiv \bigcup_{\mathfrak{B} \in\left(S_{1}(\mathfrak{F}, \tilde{\mathfrak{R}}) \backslash \mathfrak{R}\right)} N_{\mathfrak{B}}$.

Если оно выполнено, то отметка $(\Delta, \Delta)$ над $\mathfrak{A}$ заменяется на $(1, \Delta)$ и $(i+1)$-й шаг $A$-алгоритма заканчивается. Если оно не выполнено, то проверяется, можно ли $N_{\Re}$ представить в виде $M_{1} \cup M_{2}$, где $M_{1}$ - множество, ре-

\begin{center}
\includegraphics[max width=\textwidth]{2023_07_08_f9e6319b67cb8e4fc534g-1(1)}
\end{center}

\includegraphics[max width=\textwidth, center]{2023_07_08_f9e6319b67cb8e4fc534g-1(8)}
ставить в таком виде, то отметка $(\Delta, \Delta)$ над $\mathfrak{\mathcal { X }}$ заменяется на отметку $(\Delta, 1)$, и $(i+1)$-й шаг $A$-алгоритма заканчивается; если - нельзя, то описанная процедура применяется ко второй по порядку конъюнкции, и т. д. Если при этом ни у одной из конъюнкций отметка не изменится, то после просмотра всех конъюнкций работа $A$-алгоритма заканчивается.

Если за д. н. ф. $\mathfrak{N}$ принять сокращенную д. н. ф. $\mathfrak{N}_{f}$ функции $f$, то все конъюнкции, получившие в алгоритме отметку $(\Delta, 1)$, не входят ни в одну минимальную д. н. ф. функции $f$; они удаляются из $\mathfrak{N}_{f}$. Конъюнкции, получившие отметку $(1, \Delta)$, входят во все минимальные д. н. Ф. функции $f$. Рассматривались также различные частные случаи $A$-алгоритма.

К о л ц це в о й а л г о р и т расставляет над конъюнкциями информационные отметки $\left(\omega_{1}, \omega_{2}\right)$, значение к-рых то же, что и в $A$-алгоритме. На каждом шаге кольцевой алгоритм использует конъюнкции, входящие в окрестность $k$-го порядка нек-рой конъюнкции, и их информационные отметки. Ниже излагается упрощенный вариант этого алгоритма. В полном виде кольцевой алгоритм - наилучший в классе локальных алгоритмов со специальной памятью по окрестностям $S_{k}(\mathfrak{H}, \mathfrak{R})$. Описанные выше алгоритмы являются частными случаями о б щ е го ко льце в о го а л г ор и т м а. Если

\[
S_{k-1}(\mathfrak{A}, \mathfrak{R})=\left\{\mathfrak{A}, \mathfrak{A}_{1}, \ldots, \mathfrak{A}_{l}\right\},
\][

S\_\{k\}(\textbackslash mathfrak\{A\}, \textbackslash mathfrak\{N\})=\textbackslash left\{\textbackslash mathfrak\{A\}, \textbackslash mathfrak\{A\}\textit{\{1\}, ···, \textbackslash mathfrak\{A\}}\{l\}, \textbackslash mathfrak\{A\}\textit{\{l+1\}, ···, \textbackslash mathfrak\{A\}}\{m\}\textbackslash right\}
]

\[
\begin{gathered}
N_{S_{k-1}}=N_{\mathfrak{A}} \cup \cup_{i=1}^{l} N_{\mathfrak{A}_{j}}, \quad N_{S_{k}}=N_{\mathfrak{A}} \cup \cup_{j=1}^{m} N_{\mathfrak{G} j}, \\
Q\left(S_{k}\right)=N_{S_{k}} \backslash N_{S_{k-1}},
\end{gathered}
\]

то каждому подмножеству $N \subseteq Q\left(S_{k}\right)$ сопоставляется не всюду определенная булева функция $f\left(x_{1}, \ldots, x_{n}\right)$,

\includegraphics[max width=\textwidth, center]{2023_07_08_f9e6319b67cb8e4fc534g-1(3)}
$M_{0}^{f}$ нулей есть $E_{n} \backslash N_{S_{k}}$; на множестве $N$ функция $f$ не определена. Множество таких функций обозначается через $F_{k}(\mathfrak{X})$. До начала работы алгоритма все конъюнкции из рассматриваемой д. н. ф. $\mathfrak{N}$ имеют отметку $(\Delta, \Delta)$. Если выполнены $i$ шагов и отметку $(1, \Delta)$ получили конъюнкции $\mathfrak{A}_{1}^{\prime}, \ldots, \mathfrak{A}_{s}^{\prime}$, а отметку $(\Delta, 1)-$ конъюнкции $\mathfrak{H}_{1}^{\prime \prime}, \ldots, \mathfrak{A}_{t}^{\prime \prime}$, то $(i+1)-\mathbf{и}$ шаг состоит в следующем. Упорядочивают нек-рым способом конъюнкции из д. н. ф. Если д. н. ф. $\mathfrak{N} \backslash\left(\vee \stackrel{s=1}{s} \mathfrak{A}_{j}^{\prime} \vee \vee\left(\mathfrak{H}_{j=1}^{t} \mathfrak{a}_{j}^{\prime \prime}\right)\right.$ пустая, то алгоритм заканчивает свою работу. В противном случае после упорядочения к.-л. способом всех конъюнкций этой д. Н. Ф. для конъюнкции $\mathfrak{H}$, являющейся первой из них, и для каждой функции $f$ из множества $F_{k}(\mathfrak{\Re})$ находятся все д. н. Ф., реализующие $f$ на ее области определения, к-рые составлены из конъюнкций окрестности $S_{k}(\mathfrak{A}, \mathfrak{\Re})$ и содержат по сравнению с другими такими д. н. ф. наименьшее число символов переменных. Среди них выделяются все те д. н. ф., к-рые, во-первых, не содержат конъюнкций $\mathfrak{U}_{1}^{\prime \prime}$, . ., $\mathfrak{A}_{t}^{\prime \prime}$ и, во-вторых, содержат все конъюнкции $\mathfrak{A}_{j}^{\prime}, j=1,2$, ..., $s$, удовлетворяющие условию $\quad N_{\mathfrak{a}_{j}^{\prime}} \cap\left(N_{S_{k}} \backslash N\right)=\varnothing$.


\end{document}