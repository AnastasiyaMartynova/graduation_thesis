\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\DeclareUnicodeCharacter{0131}{$\imath$}

\begin{document}
обознатаются через $p_{1}^{s}, p_{2}^{s}, \ldots, p_{n}^{s}$ и наз. с и м п л е ктичес кими классам и Понтря гина.

Пусть, по-прежнему $F$-одно из тел $\mathbb{R}, \mathbb{C}, \mathbb{H}, h^{*}-$ теория когомологий, удовлетворяющая требуемым выше условиям. П р и н ц и п $\mathrm{p}$ а с щ $\mathbf{e}$ п л $\mathbf{e}$ в и я: для произвольного векторного (в смысле $F$ ) расслоения $\xi$ над $B$ существуют пространство $B^{\prime}$ и отображение $f: B^{\prime} \longrightarrow B$, для к-рых расслоение $f^{*} \xi$ над $B^{\prime}$ распадается в прямую сумму одномерных расслоений, и гомоморфизм $f^{*}$ : $h^{*}(B) \longrightarrow h^{*}\left(B^{\prime}\right)$ мономорфен.

В частности, если $\xi$-универсальное комплексное расслоение над $B U_{n}$, то в качестве $B^{\prime}$ можно взять пространство $B T_{n}=\mathbb{C} P^{\infty} \times \ldots \times \mathbb{C} P^{\infty}(n$ сомножителей $)$, где $T_{n}$-максимальный тор в $U_{n}$, а в качестве $f: B T_{n} \longrightarrow$ $\longrightarrow B U_{n}$-отображение, индуцированное включением
$T_{n} \subset U_{n}$. Отображение

\[
f^{*}: H^{* *}\left(B U_{n}\right) \longrightarrow H^{* *}\left(B T_{n}\right)=\mathbb{Z}\left[\left[x_{1}, \ldots, x_{n}\right]\right]
\]

мономорфно, и образ $f^{*}$ совпадает с кольцом всех симметрических формальных степенных рядов, зависящих от переменных $x_{1}, \ldots, x_{n}, \operatorname{dim} x_{i}=2$.

Для произвольной топологич. групшы $G$ множество всех X. к., определенных для главных $G$-расслоений и принимающих значения в теории когомологий $h^{*}$, находится во взаимно однознатном соответствии с $h^{*}(B G)$, где $B G-$ классифицирующее пространство группы $G$. В частности, для векторных расслоений и для теории $H^{*}$ задача описания всех $\mathrm{X}$. к. сводится к вычислению колец когомологий $H^{*}\left(B O_{n}\right), H^{*}\left(B S O_{n}\right), H^{*}\left(B U_{n}\right)$ И т. Д.

Пусть $G$-компактная группа Ли, и $T$-максимальный тор в $G$. Включение $T \subset G$ индуцирует отображение $B T \longrightarrow B G$ классифицирующих пространств. Пространство $B T$ гомотопически эквивалентно произведениіо $\mathbb{C} P^{\infty} \times \ldots \times \mathbb{C} P^{\infty}$, в к-ром число сомножителей равно размерности тора $T$. Поэтому $H^{* *}(B T)=\mathbb{Z}\left[\left[x_{1}, \ldots, x_{n}\right]\right]$, где $n=\operatorname{dim} T, \operatorname{dim} x_{i}=2$. На торе $T$ действует группа Вейля $\Phi(G)=N(T) / T$, где $N(T)$-нормализатор $T$, следовательно, групша Вейля действует также и на $B T$. Если группа $G$ связана и пространства $G, G / T$ не имеют кручения в гомологиях , то гомоморфизм $\rho^{*}: H^{* *}(B G) \longrightarrow$ $\longrightarrow H^{* *}(B T)$ мономорфен, и образ $\rho^{*}$ совпадает с подкольцом всех инвариантных относительно групты Вейля элементов кольца $H^{* *}(B T)=\mathbb{Z}\left[\left[x_{1}, \ldots, x_{n}\right]\right]$ (т е о р е м а Бор е ля).

Группа $U_{n}$ удовлетворяет условиям теоремы. Диагональные унитарные матрицы образуют максимальный тор $T_{n}$ группы $U_{n}$. Если элементы диагональной матрицы обозначить через $t_{1}, \ldots, t_{n}$, то группа Вейля состоит из всех перестановок переменных $t_{1}, \ldots, t_{n}$. Следовательно, $H^{* * *}\left(B U_{n}\right)=\mathbb{Z}\left[\left[c_{1}, \ldots, c_{n}\right]\right]$, где $c_{1}, \ldots$ $\ldots, c_{n}-$ элементарные симметрич. функции переменных $x_{1}, \ldots, x_{n} \in H^{2}\left(B T_{n}\right)$, совпадающие с классами Чжәня. Группа $S p_{n}$ также удовлетворяет требованиям теоремы Бореля. Групга Вейля порождена всеми перестановками $x_{1}, \ldots, x_{n}$ и произвольными переменами знаков. Следовательно, $H^{* *}\left(B S p_{n}\right)=\mathbb{Z}\left[\left[\sigma_{1}, \ldots, \sigma_{n}\right]\right]$, где $\sigma_{i}, \ldots$ $\ldots, \sigma_{n}$ - элементарные симметрич. функции переменных $x_{1}^{2}, \ldots, x_{n}^{2}$. Группа $S O_{n}$ не удовлетворяет требованиям теоремы Бореля, однако, если рассматривать в качестве кольца коәффициентов произвольное кольцо $\Lambda$, содержащее $1 / 2$, напр. $\mathbb{Z} / p \mathbb{Z}$ при нечетных $p$ или $\mathbb{Q}$, то видоизмененная таким образом теорема будет справедлива. Максимальный тор группы $S O_{n}$ образован матрицами вида

\[
\left\|\begin{array}{cr}
\cos \alpha_{1} & -\sin \alpha_{1} \\
\sin \alpha_{1} & \cos \alpha_{1}
\end{array}\right\| \oplus\left\|\begin{array}{cr}
\cos \alpha_{2} & -\sin \alpha_{2} \\
\sin \alpha_{2} & \cos \alpha_{2}
\end{array}\right\| \oplus \ldots
\]

и имеет размерность [n/2]. Группа Вейля порождена всеми перестановками $\alpha_{1}, \ldots, \alpha_{[n / 2]}$ ї перемснами знаков у четного числа этих символов при четном $n$ и у произвольного числа символов при нечетном $n$ Поотому $H^{* *}\left(S O_{2 k} ; \Lambda\right)=\Lambda\left[\left[p_{1}, \ldots, p_{k-1}, e\right]\right]$, где $p_{1}, \ldots$ ..,$p_{k-1}$-элементарные симметрич. функции перемен. ных $x_{1}^{2}, \ldots, x_{k}^{2}$, кроме последней, $e=x_{1}, \ldots, x_{k}$. Классы $p_{1}, \ldots, p_{k-1}, p_{k}=e^{2}$ совпадают с классами Понтрягина (см. ниже), $e-$ класс Эйлера; $H^{* *}\left(S O_{2 k+1} ; \Lambda\right)=\Lambda\left[\left[p_{1}, \ldots\right.\right.$ $\left.\left.\ldots, p_{k}\right]\right]$.

Классы $x_{i} \in H^{2}\left(B T_{n}\right), 1 \leqslant i \leqslant n$ наз. о б $\mathrm{p}$ а з у ю щ и м и В у. Сами они не являются Х. к. (так как не лежат в $\left.H^{* *}\left(B U_{n}\right)\right)$, но любой Х. к. выражается через них как симметрический формальный степенной ряд и любой симметрический формальный степенной ряд от $\left\{x_{i}\right\}$ задает Х. к. Напр., классу Эйлера $c_{n}$ соответствует ıроизведение $\prod_{i=1}^{n} x_{i}$.

Элемент (формальный степенной ряд) $e^{x_{1}}+\ldots+e^{x_{n}} \in$ $\in \mathbb{Z}\left(x_{1}, \ldots, x_{n}\right)=H^{* *}\left(B T_{n}\right)$ симметричен, и задает Х. к.- неоднородный элемент кольца $H^{* *}\left(B U_{n}\right)$, обозначаемый сh, наз. х а р а к т е р о м Ч ж эн я. Характер Чжәня «аддитивно-аддитивен» и «мультипликативномультипликативен», т. е.

\[
\begin{aligned}
\operatorname{ch}(\xi \oplus \eta) & =\operatorname{ch}(\xi)+\operatorname{ch}(\eta) . \\
\operatorname{ch}(\xi \otimes \eta) & =\operatorname{ch}(\xi) \cup \operatorname{ch}(\eta) .
\end{aligned}
\]

К ла с с ы Ч ж эн я и к р и в и 3 на. Пусть база $B n$-мерного векторного расслоения $\xi=(E, p, B)$ является гладким многообразием. Пусть в расслоении $\xi$ задана произвольная аффиннал сөязность. Если фиксировать локальную тривиализацию расслоения $\xi$ в окрестности нек-рой точки базы, то в этой окрестности кривизна данной связности задается 2 -формой $\Omega$ со значениями в векторном пространстве $g l(\mathbb{C}, n)$ комплексных $(n \times n)$-матриц. При замене локальной тривиализации расслоения значения формы $\Omega$ преобразуются по правилу $m \rightarrow g m g^{-1}$, где $g \in G L(\mathbb{C}, n)-$ матрица перехода от одной тривиализации к другой. Если $\varphi: \operatorname{gl}(\mathbb{C}, n) \rightarrow \mathbb{C}-$ однородный многочлен степени $j$, то $\varphi \circ \Omega-\mathbb{C}$-значная внешняя форма степени $2 j$. Если, кроме того, многочлен $\varphi$ инвариантен относительно действия

\[
\mathrm{GL}(\mathbb{C}, n) \times g l(\mathbb{C}, n) \longrightarrow g l(\mathbb{C}, n),(g, m) \longrightarrow g m g^{-1},
\]

то форма $\varphi \circ \Omega$ не зависит от локальных тривиализаций и является $\mathbb{C}$-значной внешней формой на всем многообразии $B$. Можно показать, что $d(\varphi \circ \Omega)=0$ и что изменение связности меняет $\varphi \circ \Omega$ лишь на точную форму. Так как коэффициенты $\operatorname{tr}\left(m^{i}\right)$ характеристического многочлена матрицы $m$ инвариантны, то, полагая $\varphi_{i}(m)=$ $=\operatorname{tr}\left(m^{i}\right)$, получаются классы когомологий $\left[\varphi_{i} \circ \Omega\right] \in$ $\in H^{2 i}(B ; \mathbb{C})$. Имеет место равенство $\left[\varphi_{i} \circ \Omega\right]=\left(C_{i}(\xi)\right)^{\mathbb{C}}$, где $\left(C_{i}(\xi)\right)^{\mathbb{C}}$-классы Чжәня с комплексными коэффициентами.

К л а с с а м и П о н т р я г и на действительного векторного расслоения $\xi$ наз. классы $p_{i}(\xi)=(-1)^{i} C_{2 i}(\xi \otimes$ $\otimes \mathbb{C}) \in H^{4 i}(B)$, здесь $\xi \otimes \mathbb{C}$ - комплексификация расслоения ६. (Другое определение см. в [5].) Пусть база $B$ $n$-мерного расслоения $\xi=(E, p, B)$ есть $N$-мерное многообразие с краем и $\sigma-$ целочисленная неубывающая функция аргумента $h=0, \ldots, n-1$. Система векторов $u_{1}, \ldots, u_{m} \in \mathbb{R}^{\boldsymbol{h}}$ наз. по о д ч и не вн о й функции $\sigma$;

\includegraphics[max width=\textwidth, center]{2023_07_09_c5308a5560badc268ed6g-1}
Пусть в расслоении выбраны сечения $v_{1}(x), \ldots, v_{m}(x)$ в общем положении. Подмножество базы $\left\{x \in B \mid v_{1}(x), \ldots\right.$ $\ldots, v_{m}(x)$ подчинена $\left.\sigma\right\}$ есть псевдомногообразие коразмерности $\sigma(0)+\ldots+\sigma(n-1)$. Оно реализует относительный класс гомологий в $H_{N-\Sigma_{0}^{n-1} \sigma(i)}(B, \partial B)$, а двойственный ему класс когомологий в $H^{\Sigma_{0}^{n-1} \sigma(i)}(B)$ будет


\end{document}