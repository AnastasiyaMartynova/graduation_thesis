\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
Построение полной системы неприводимых П. с. г. производится с помощью Юнга симметризаторов, повволяющих получить разложение регулярного представления. Если $d$ - диаграмма Юнга, отвечающая разбиению $\lambda$, то представление $T_{\lambda}$ эквивалентно П. с. г. $S_{m}$ в левом идеале групповой алгебры $Q S_{m}$, порожденном симметризатором Юнга $e_{d}$. Апостериорное описание элемента $e_{d}$ состоит в следующем: $T_{\mu}\left(e_{d}\right)=0$ при $\mu \neq \lambda$, а $T_{\lambda}\left(e_{d}\right)$ - оператор ранга 1 , действующий по формуле $T_{\lambda}\left(e_{d}\right) u=\left(u_{d}, u\right) u_{d}^{\prime}$ для любого $u \in U_{\lambda}$, тде ( , ) - подходящим образом нормированное инвариантное скалярное умножение в пространстве $U_{\lambda}$. При әтом

\[
\left(u_{d}, u_{d}^{\prime}\right)=\frac{m !}{\operatorname{dim} U_{\lambda}}
\]

Проивводящая функция для характеров представлений $T_{\lambda}$ дается Фробениуса бормулой. Однако для вычисления отдельных значений характеров удобнее пользоваться рекуррентными соотношениями. Наиболее әфффективным из них является п р а в и л о М у рна г а на - Н а к а я мы: пусть $a \lambda_{\mu}=a \lambda_{\mu}^{(m)}-$ значение характера представления $T_{\lambda}$ на классе $[\mu]$ сопряженных әлементов группы $S_{m}$, определенном разбиением $\mu$ числа $m$, и пусть разбиение $\mu$ содержит число $p$. Через $\bar{\mu}$ обозначается разбиение числа $m-p$, получаемое из $\mu$ выбрасыванием числа $p$. Тогда

\[
a_{\lambda \mu}^{(m)}=\Sigma_{\bar{\lambda}}(-1)^{i(\bar{\lambda})} a \frac{(m-p)}{\lambda \frac{\mu}{\mu}}
\]

где сумма берется по всем разбиениям $\bar{\lambda}$ числа $m-p$, получаөмым удалением косого крюка длины $p$ из таблицы Юнга $t_{\lambda}$, а $i(\bar{\lambda})$ обозначает высоту удаленного косого крюка.

Имеется также метод (см. [5]), позволяющий найти целиком таблицу характеров группы $S_{m}$, т. е. матрицу $A=\left\|a_{\lambda \mu}\right\|$. Пусть $M_{\lambda}$ есть П. с. г. $S_{m}$, индуцированное тривиальным одномерным представлением подгруппы $R_{\lambda}=R_{d}$, где $d$ - диаграмма Юнга, отвечающая разбиению $\lambda$. И пусть $M_{\lambda}=\Sigma_{\mu} m_{\lambda \mu} T_{\mu}$, а $M=\left\|m_{\lambda \mu}\right\|$. Если считать, что строки и столбцы матрицы $M$ расположены в порядке лексикографич. убывания индексов (разбиений), то это будет нижняя треугольная матрица с едивицами на диагонали. Значение характера представления $M_{\lambda}$ на классе $[\mu]$ равно

\[
b_{\lambda \mu}=\frac{\mathbf{c}_{\boldsymbol{\mu}}\left|\boldsymbol{R}_{\lambda} \cap[\mu]\right|}{\left|R_{\lambda}\right|}
\]

где $c_{\mu}-$ порядок централизатора подстановки из класca $[\mu]$. Матрица $B=\left\|b_{\lambda \mu}\right\|$ является верхней треугольной, и имеет место соотношение $M M^{t}=B C{ }^{-1} B^{T}$, где $C=\operatorname{diag}\left(c_{\mu}\right)$, из к-рого однозначно определяется матрица $M$. После әтого матрица $A$ находится по формуле

\[
A=M^{-1} B .
\]

Ограничение представления $T_{\lambda}$ группы $S_{m}$ на подгруппу $S_{m-1}$ находится по п р а в и л у ве т в ле н и я:

\[
T_{\lambda} \mid S_{m-1}=\Sigma_{i} T_{\left(\lambda_{1}, \ldots, \lambda_{i}-1, \ldots, \lambda_{r}\right)},
\]

где суммирование распространяется на те $i$, для к-рых $\lambda_{i}>\lambda_{i+1}$ (включая $r$ ). Ограничение представления $T_{\lambda}$ на подгруппу $A_{m}$ при $\lambda \neq \lambda^{\prime}$ абсолютно неприводимо, а при $\lambda=\lambda^{\prime}$ распадается над квадратичным распииением поля $\mathbb{Q}$ в сумму двух неэквивалентных абсолютно неприводимых представлений одинаковой размөрности. Получаемые таким образом представления группы $A_{m}$ исчерпывают все ее неприводимые представлөния над $\mathbb{C}$.

О II. с. Г. в тензорах см. в ст. Представления классических әpynn. Разработана также теория модулярных П. с. г. (см., напр., [5])

Лuт.: [1] В е й л ь $Г .$, Классические группы, их инварианты к представления, пер. с англ., М., 1947; [2] М у р н а-

\includegraphics[max width=\textwidth, center]{2023_07_08_846674d0b9a88d2fc555g-1}
к физическим проблемам, пер. с англ., М., 1966; [4] К э р т и с ч., Р а и в е р и., Теория представлений конечных группи ассоциативных алгебр, пер. с англ., М., 1969; [5] д ж е й м с г., Теория представлений симметрических групп, пер. с англ.,

ПРЕДСТАВЛЕНИЕ СО СТАРІІИМ ВЕКТОРОМ линейное представление $\rho$ конечномерной полупростой расщепляемой алгебры Ли $g$ над полем $k$ характеристики нуль с расщепляющей Картана подаләеброй t, удовлетворяющее следующим условиям.

\begin{enumerate}
  \item В пространстве $V$ представления $\rho$ существует ци к ли ч е ски й в е к т о р $v$ (т. е. $V$ - наименьшеө $\mathfrak{g}$-инвариантное подпространство, содержащее $v$ ).

  \item $\rho(h)(v)=\lambda(h) v$ для всех $h \in \mathrm{t}$, где $\lambda$ - некоторая фиксированная линейная форма на $\mathfrak{t}$ co значениями в $k$.

  \item Если $\alpha_{1}, \ldots, \alpha_{r}$ - система простых корней, определенная нек-рым лексикографич. упорядочением множества $\Delta$ всех корней алгебры $\mathfrak{g}$ относительно (см. Корневая система), а $e_{\alpha_{i}}, \mathrm{t}_{\alpha_{i}}, h_{\alpha_{i}}-$ соответствуюпие корню $\alpha_{i}$ векторы из базиса Шевалле алгебры $\mathfrak{g}, \quad i=1, \ldots, r$, то $\rho\left(e_{\alpha_{i}}\right)(v)=0$ для всех $i=1, \ldots, r$. Таким образом, $\lambda$ является весом относительно сужения $\rho$ на $\mathrm{t}$ (см. ши и в е с ом. Пространство $V$ наз. ц и к л и ч еским g-модулем с о с с т р ш им в е сом $\lambda$ и об ра а ую е й $v$, а $v$ наз. с т а ршим в е к тоp o $\mathrm{M}$.

\end{enumerate}

Для всякой линейной формы $\lambda$ на $t$ существует единственное с точностью до эквивалентности неприводимое представление $\rho_{\lambda}$ алгебры $g$ со старшим весом $\lambda$. g-модуль $V(\lambda)$, определяемый $\rho \lambda$, является прямой суммой весовых подпространств относительно сужения $\rho_{\lambda}$ на t. Их веса имеют вид

\[
\lambda-\sum_{i=1}^{r} n_{i} \alpha_{i}
\]

где $n_{i}$ - целые неотрицательные числа. Весовое подпространство $V_{\mu}(\lambda)$ веса $\mu$ конечномерно, натягивается над $k$ на векторы вида

\[
\left(\rho_{\lambda}\left(f_{\alpha_{i_{1}}}\right) \rho_{\lambda}\left(f_{\alpha_{i_{8}}}\right) \cdots \rho_{\lambda}\left(f_{\alpha_{i_{s}}}\right)\right)(v)
\]

и для любого $h \in \mathrm{t}$ ограничение $\rho_{\lambda}(h)$ на $V_{\mu}(\lambda)$ является скалярным оператором умножения на $\mu(h)$. Пространство $V_{\lambda}(\lambda)$ одномерно; вес $\lambda$ является единственным старшим весом представления $\rho_{\lambda}$ и может быть охарактеризован как единственный вес $\mathfrak{t}$ модуля $V(\lambda)$ такой, что любой другой вес имеет вид

\[
\lambda-\sum_{i=1}^{r} n_{i} \alpha_{i}
\]

где $n_{i}$ - целые неотрицательные числа.

Представление $\rho_{\lambda}$ конечномерно тогда и только тогда, когда $\lambda-$ д о ми на ат н а я ли не й ная фо рм a на $t$, то есть $\lambda\left(h_{\alpha_{i}}\right)$ - целое неотрицательное число для всех $i=1, \ldots, r$. Всякое неприводимое конечномерное линейное представление алгебры $g$ имеет вид $\rho \lambda$ для нек-рой доминантной линейной формы $\lambda$ ва $\mathrm{t}$ (так что все такие представления классифицируются с точностью до эквивалентности доминантными линейными формами на t). Множество всех весов конечномерного представления $\rho \lambda$ относительно $t$ инвариантно относительно Вейля группы алгебры $\mathfrak{g}$ (рассматриваемой как группа линейных прөобразований пространства t), и если веса $\mu$ и $\gamma$ лежат в одной орбите группы Вейля, то размерности пространств $V_{\mu}(\lambda)$ и $V_{\nu}(\lambda)$ совпадают. Для всякого веса $\mu$ и всякого корня


\end{document}