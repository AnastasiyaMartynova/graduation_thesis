\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}
\usepackage{mathrsfs}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\DeclareUnicodeCharacter{0131}{$\imath$}

\begin{document}
Ли g группы $G$ с помощью $d$-мерной коммутативной алгебры Ли, где $d=\operatorname{dim} H^{2}(\mathfrak{g}, \mathbb{R})$.

Лит.: [1] К и р и л л о в А. А., Элементы теории представлений, 2 изд., М., 1978; [2] К е е р т и с Ч., Р а й н е p И., Теория представлений конечных групп и ассоциативных алгебр, пер. с англ., M., 1969; [3] M a c k e y G. W., "Acta math.", 1947, v. 48, p. 568-640.

ПРОЕКТИВНОЕ ПРЕОБРАЗОВАНИЕ - вЗаИмНо однозначное отображение $F$ проективного пространства $\Pi_{n}$ на себя, сохраняющее отношение порядка частично упорядоченного (по включению) мвожества всех подпространств $\Pi_{n}$, т. е. отображение $\Pi_{n}$ в себя такое, что 1) если $S_{p} \subset S_{q}$, то $F\left(S_{p}\right) \subset F\left(S_{q}\right)$;

$2)$ для каждого $S_{p}$ существует $S_{p}$ такое, что $F\left(S_{p}\right)=$ $=\tilde{S}_{p}$

\begin{enumerate}
  \setcounter{enumi}{2}
  \item $S_{p}=S_{q}$ тогда и только тогда, когда $F\left(S_{p}\right)=F\left(S_{q}\right)$.
\end{enumerate}

При П. П. сохраняются сумма и пересечение подпространств, точки отображаются в точки, независимость точек сохраняется. П. п. образуют группу, наз. п р ое к т и в в о й г р у П п о й. Примеры П. п.: коллинеация, перспектива, гомология.

Пусть пространство $\Pi_{n}$ интерпретируется как совокупность подпростравств $P_{n}(K)$ левого векторного пространства $A_{n+1}(K)$ над телом $K$; п о л у ли не й ны м п р е о б р а 3 о а ни ем $A_{n+1}$ в себя наз. пара $(\bar{F}, \varphi)$, состоящая из автоморфизма $\bar{F}$ аддитивной группы $A_{n+1}$ и автоморфизма $\varphi$ тела $K$ такого, что для любых $a \in A_{n+1}$ и $k \in K$ имеет место: $\bar{F}(k a)=\varphi(k) \bar{F}(a)$; в частности, полулинейное преобразование $(\bar{F}, \varphi)$ является л ин е й ны м, если $\varphi(k) \equiv k$. Полулинейное преобразование $(\bar{F}, \varphi)$ индуцирует П. П. $F$. Обратное утверждение первая основная теорем а проект и в н о ї г е о м е т р и и: если $n \geqslant 2$, то каждое П. п. $F$ индуцируется нек-рым полулинейным преобразованием $(\bar{F}, \varphi)$ пространства $A_{n+1}(K)$.

Лuт.: [1] Б э р Р., Линейная алгебра и проективная геометрия, пер. с англ., М., 1955; [2] Х о д ж В., П и д о Д., Методы алгебраической геометрии, пер. с англ., т. 1, М., 1954.

ПРОЕКТИВНОЕ ПРОСТРАНСТВО - совокупность всех подпространств инцидентностной структуры $\pi=$ $=\{\mathscr{P}, \mathscr{L}, I\}$, где элементы множества $\mathscr{P}$ наз. т оч к ам и, а элементы множества $\mathscr{L}-$ п р ямы м и, Iотношение инци де п т ности. П о д п р о с тр а н с т в о м инцидентностнои структуры $\pi$ наз. подмножество $S$ множества $\mathscr{P}$, для к-рого справедливо условие: если $p, q \in S$ и $p \neq q$, то множество точек прямой, проходящей через точки $p$ и $q$, также принадлежат $S$. Инцидентностная структура $\pi$ удовлетворяет следующим требованиям:

\begin{enumerate}
  \item для любых двух различных точек $p$ и $q$ существует единственная прямая $L$ такая, что $p I L, q I L$;

  \item каждая прямая инцидентна по крайней мере с тремя точками;

  \item если две различные прямые $L, M$ пересекаются в точке $p$ и выполнено $q I L$ и $r I L$, а $s I M, t I M$, то прямые, проходяıцие через пары точек $r, t$ и $s, q$, пересекаются.

\end{enumerate}

Подпространство $S$ п о р о ж д е н о можеством $s$ точек из $\mathscr{P}$ (пишут $S=\langle s\rangle$ ), если $S$ является пересечением всех подпространств, содержащих $s$. Множество точек $s$ наз. независимым, если для любого $x \in s$ имеет место $x \notin\langle s \backslash\{x\}>$. Упорядоченное максимальное и независимое множество точек подпространства $S$ наз. б а 3 и с о м $S$, а число его элементов $d(S)-$ р а 3м е р н о с т ь ю подпространства $S$. Подпространство размерности 0 является гочкой, размерности 1 - проективной прямой. Подпространство размерности 2 наз. проективной плоскостью.

В П. п. определены операции сложения и пересечения подпространств. Суммой $P_{m}+P_{k}$ подпространств $P_{m}$ и $P_{k}$ наз. наименьшее из подпространств, содержащее и $P_{m}$, и $P_{k}$. Пересечением $P_{m} \cap P_{k}$ подпространств $P_{m}$ и $P_{k}$ наз. наибольшее из подпространств, содержащееся и в $P_{m}$, и в $P_{k}$. Размерности подпространств $P_{m}$, $P_{k}$, их суммы и пересечения связаны соотношением

\[
m+k=d\left(P_{m} \cap P_{k}\right)+d\left(P_{m}+P_{k}\right) .
\]

Для любого $P_{m}$ существует $P_{n-m-1}$ такое, что $P_{m} \cap$ $\bigcap_{n-m-1}=P_{-1}=\varnothing$ и $P_{m}+P_{n-m-1}=P_{n}\left(P_{n-m-1}-\right.$
дополнение $P_{m}$ в $\left.P_{n}\right)$, и если $P_{m} \subset P_{r}$, то

\[
\left(P_{m}+P_{k}\right) \cap P_{r}=P_{m}+P_{k} \cap P_{r}
\]

для любого $P_{k}$ (д е д е к и н д о в о п р а в и ло), т. е. относительно введенных операций П. П. является дедекиндовой решеткой с дополнениями.

П. П. размерности больуе двух дезаргово (см. Дезарга предложение), а следовательно, изоморфно П. П. (левому или правому) над подходящим телом $k$ (см. [1]). $P_{n}^{l}(k)$ (напр., левое) размерности $n$ над телом $k-$ совокупность линейных подпространств нек-рого $(n+1)$ мерного левого линейного пространства $A_{n+1}^{l}(k)$ над телом $k$; точками $P_{n}^{l}(k)$ являются прямые $A_{n+1}^{l}(k)$, т. е. множества классов эквивалентности слева строк $\left(x_{0}\right.$, $\left.x_{1}, \ldots, x_{n}\right)$, составленных из элементов тела $k$ и не равных одновременно вулю (строки $\left(x_{0}, x_{1}, \ldots, x_{n}\right)$ и $\left(y_{0}, y_{1}, \ldots, y_{n}\right)$ эквивалентны слева, если существует такое $\lambda \in k$, что $\left.x_{i}=\lambda y_{i}, i=0,1, \ldots, n\right)$; подпространствами $P_{m}^{l}(k), m=1, \ldots, n$, являются $(m+1)$-мерные подпространства $A_{m+1}^{l}(k)$. Можно установить нек-рое соответствие между левым $P_{n}^{l}(k)$ и правым $P_{n}^{r}(k) \Pi$. п., при к-ром подпространству $P_{s}^{l}(k)$ соответствует $P_{n-s-1}^{r}(k)$ (подпространства $P_{s}^{l}(k)$ и $P_{n-s-1}^{r}(k)$ наз. д у а л ьн ы м и друг другу), пересечению подпространств соответствует сумма, а сумме - пересечение. Если нек-рое утверждение, основанное только на свойствах линейных подпространств, их пересечений и сумм, справедливо для $P_{n}^{l}(k)$, то справедливо и соответствующее утверждение для $P_{n}^{r}(k)$. Это соответствие между свойствами пространств $P_{n}^{l}(k)$ и $P_{n}^{r}(k)$ наз. п р и н ц и по м д в о й с т в е н ност и для П. П. (см. $|2|)$.

Конечное тело необходимо коммутативно, следовательно, конечное П. п. размерности больше двух и порядка q изоморфно П. П. над Галуа полем $P G(n, q)$. Конечное П. п. $P(n, q)$ содержит $\left(q^{n+1}-1\right) /(q-1)$ точек и $\prod_{i=0}^{r}\left(q^{n+1-i-1) /} \prod_{i=0}^{r}\left(q^{r+1-i}-1\right)\right.$ подпространств размерности $r$ (см. [4]).

К о л ли е а и е й П. П. является перестановка ее точек, к-рая отображает прямые в прямые, при этом подпространства отображаются на подпространства. Нетривиальная коллинеация П. П. имеет не более одного центра и не более одной оси. Группа коллинеаций конечного П. п. $P G\left(n, p^{h}\right)$ имеет порядок, равный

\[
h p^{h n(n+1) / 2} \prod_{i=1}^{n+1}\left(p^{h i}-1\right) .
\]

Каждое П. п. $P G(n, q)$ допускает циклическую транзитивную группу коллинеаций (см. [3]).

К о р р е л я и е й $\delta$ П. п. является перестановка подпространств, к-рая меняет включения, т. е. если $S \subset T$, то $S^{\delta} \sqsupset T^{\delta}$. П. П. допускает корреляцию, только если оно конечномерно. Важное значение в проективной геометрии играет корреляция порядка два, наз. поляритетол.

Лит.: [1] А р т и н Э., Геометрическая алгебра, пер. с

\includegraphics[max width=\textwidth, center]{2023_07_08_d071f80248c90c003416g-1}
s k i P., Finite geometries, B.- [a.o.], 1968; [4] S e g r e B., Lectures on modern geometry, Roma, 1961., B. B. Аgанасъев.

ПРОЕКТИВНЫЕ КООРДИНАТЫ - взаИмНо однозначное соответствие между элементами проективного пространства $\Pi_{n}(K)$ (проективными подпространствами $S_{q}$ ) и классами эквивалентных упорядоченных ко-


\end{document}