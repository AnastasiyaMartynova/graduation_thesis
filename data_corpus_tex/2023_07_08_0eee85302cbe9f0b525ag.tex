\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }
\usepackage{bbold}

\begin{document}
$G_{t}\left(z, z^{\prime}\right), t=1, \ldots, T$. Достроив фувкцию Грина до сеточно аналитич. функцип $f_{T}(z)=G_{T}\left(z, z^{\prime}\right)+i H_{T}\left(z, z^{\prime}\right)$ согласно уравнениям типа Коши - Римана

\[
(-1)^{k} \nabla_{1-k} H=\rho_{k} \nabla_{k} G,
\]

получают однолистное сеточно-квазиконформвое отображение $w=\exp \left[2 \pi f_{T}(z)\right]$ области $Q_{T}$ в единичный круг. Ближайшим к началу координат будет образ точки $z^{\prime}$. В пределе при $N \rightarrow \infty$ отображение сеточноконформно и образом области $Q_{T}$ служит круг с концентрич. разрезами. Получен вепрерывный авалог формулы (2) (см. [6]). В случае, когда все области $G_{t}$ односвязны и канович. областью служит единичный круг $B$, удается, используя дробно-линейвые автоморфизмы круга $B$, представить функцию Грина в явном виде

\[
G_{t}\left(z, z^{\prime}\right)=\ln \left|1-f_{t}(z) \overline{f_{t}\left(z^{\prime}\right)}\right|-\ln \left|f_{t}(z)-f_{t}\left(z^{\prime}\right)\right|
\]

через функцию $f_{t}(z)$, отображающую $Q_{t}$ на $B$ с нормировкой $f(0)=0,0 \in Q_{t}$ для всех $t \in[0, T)$.

В терминах отображения $w=f_{t}(z)$ вариация Адамара сводится к обыквовенному дифференциальному уравнению (Лёвнера). По сравнению с вариацией Адамара это уравнение значительво проще, однако информация о гравице области $Q_{t}$ представлена в нем неявно - черөз управляюощий параметр $\alpha(t)=\arg f_{t}\left(z_{t}\right)$, поскольку функция $f_{t}(z)$ заравее неизвестна. Тем не менее уравнение Лёвнера - основной инструмент П. ІІ.

Были рассмотрены более общие однопараметрич. семейства областей $Q_{t}, 0<t<T$, ве обязательво вложенных друг в друга (см. [7]). Возникающие при таких П. п. уравнения наз. у р в в вевия м и К уф ар е в a - Л ̈̈ в в ө р а. Существуют также модификации уравнений Лёвнера и Куфарева - Лёвнера на те случаи, когда области $Q_{t}$ обладают различного рода симметриями или иными геометрич. особенностями (см. [1]).

Jum.: [1] Гол у в и н Г. М., Геометрическая теория функций комплексного переменного, 2 изд., М., 1966; [2] А л е $\mathrm{K}^{-}$ суницй комплексного переменного, 2 изд., М., 1966; [2] А л е К-

\includegraphics[max width=\textwidth, center]{2023_07_08_0eee85302cbe9f0b525ag-1}
problème d'analyse relatif à l'équilibri des plaques élastiques encastrées, P., 1908; [4] e r o ж e, Leçons sur le calcul des varia tions, v. 1, P., 1910; [5] Бе л л м а н Р., Э т д ж е л Э., Динамическое программорование и уравнения в частных производных, пер. с англ., М., 1974; [6] П о п о в В. И., "Докл. АН ¿ССР", 1972, т. 207, 수 5, с. 1048-50; [7] К у Ф а р е в П. П.,
"Матем. сб.», 1943, т. 13, №1, c. 87-118.

ПАРАМЕТРИЧЕСКОЕ ПРЕДСТАВЛЕНИЕ Ф У Н Кц и и - аадание функции $y=f(x)$, опредөленной, напр., на отрезке $[a, b]$ с помощью пары функций $x=\varphi(t)$, $y=\psi(t), t \in[\alpha, \beta]$, таких, пто у функции $\varphi:[\alpha, \beta] \rightarrow$ $\rightarrow \mid a, b]$ существует такая однозначная обратная функция $\varphi^{-1}:\left[\begin{array}{ll}a, & b\end{array}\right] \rightarrow[\alpha, \beta]$, что $f=\psi \circ \varphi^{-1}, \quad$ т. $е$. для любого $x \in[a, b]$ имеет место

\[
f(x)=\psi\left[\varphi^{-1}(x)\right]
\]

П р и м е р. Пара функций $x=\cos t, y=\sin t, 0 \leqslant t \leqslant \pi$, является П. п. функции $y=\sqrt{1-x^{2}},-1 \leqslant x \leqslant 1$.

Если в точке $t_{0} \in[a, b]$ П. п. функции $f$ дифференцируемо, т. е. фувкции $\varphi$ и $\psi$ дифференцируемы, и $\varphi^{\prime}\left(t_{0}\right) \neq$ $\not 0$, то параметрпчески представленная функция $f$ дифференцируема в точке $x_{0}=\varphi\left(t_{0}\right)$ и $f^{\prime}\left(x_{0}\right)=\psi^{\prime}\left(t_{0}\right) / \varphi^{\prime}\left(t_{0}\right)$. Если, кроме того, у функций $\varphi$ и $\psi$ в точке $t_{0}$ существуют производные порядка $n, n=2,3, \ldots$, то и у фувкции $f$ в точке $x_{0}$ существует производная порядка $n$, причем она является дробно-рациональной функцией от производных функций $\varphi$ и $\psi$ порядков $k, k=1$, $2, \ldots, n$, в знаменателе к-рой стоит $(2 n-1)$-я степень значения производной $\varphi^{\prime}\left(t_{0}\right)$, напр.,

\[
f^{\prime \prime}\left(x_{0}\right)=\frac{\psi^{\prime \prime}\left(t_{0}\right) \varphi^{\prime}\left(t_{0}\right)-\psi^{\prime}\left(t_{0}\right) \varphi^{\prime \prime}\left(t_{0}\right)}{\left[\varphi^{\prime}\left(t_{0}\right)\right]^{3}} .
\]

Л. Д. Кудрявцев.

ПАРАМЕТРИЧЕСКОЕ ПРОГРАММИРОВАНИЕ раздел математического программирования, посвящен- ный исследованию задач оптимизации, в к-рых условия допустимости и (или) целевая функция зависят от нек-рых детерминированных параметров. (Задачи, в к-рых эти параметры являются случайвыми, составляют предмет стохастического проераммирования.)

В общем виде задача П. п. заключается в максимизации целевой функции $f(x, \lambda)$ по всем $x=\left(x_{1}, \ldots\right.$, $\left.x_{n}\right) \in \mathbb{R}^{n}$, удовлетворяющим ограничениям

\[
g_{i}(x, \lambda) \leqslant b_{i}(\lambda), i=1, \ldots, m,
\]

где $\lambda$ - вектор параметров, принадлежащий нек-рому заданному множеству параметров $\Lambda \subset \mathbb{R}^{p}$. При любом фиксированном $\lambda$ эта задача представляет собой обычную задачу математич. программирования. Пусть $\Lambda^{\prime} \subset \Lambda$ - мвожөство тех звачений $\lambda$, при к-рых әта задача разрепима (м в 0 ж е с т в 0 р а 3 p e II и м ос т и). Оптимальное решение $x^{*}=x_{\lambda}^{*} \quad$ өстественным образом является функцией от $\lambda$. Под $\mathrm{p}$ е ш е н и е м з а д а ч и П. п. понимается семейство $\left\{x_{\lambda}^{*}\right\}$ при всех $\lambda \in \Lambda^{\prime}$.

Источники задач П. п. довольно разнообразны. Это прежде всего стремление отразить определенный произвол, с к-рым нередко бываот определены все или нек-рые исходные данные практической оптимизационной задачи, либо охватить единой формулировкой несколько связанных между собой вариантов задачи (или целое семейство задач, напр., зависящих от времени). П. п. является наиболее адекватным способом постановки важной проблемы устойчивости реппений задач оптимизации относительно вариаций тех или иных исходных данных. Наконец, с задачами П. п. тесно связана проблема нахождения множества оптимумов Парето в задачах многокритериальной оптимизации. Если при любом фиксированном $\lambda$ задача П. п. представляет собой задачу линейного програмяирования (выпуклого программирования и т. п.), то говорят о задаче л и н е й н о г о (соответственно в ы п у к л о г о и т. п.) п а рам т т ич еско го програм м ир о в а н я. В общем виде проблематику П. п. можно охарактеризовать следующим образом. 1) Нахождение и выяснение свойств множеств разрешимости $\Lambda^{\prime}$. 2) Нахождение областей устойqивости решений, характеризация их строения; анализ поведения неустойчивых задач. 3) Характеризадия зависимости оптимального значения целөвой функции от вектора параметров.

В полном своем объеме (т. е. для произвольных целевых функций, ограничений и областей изменения параметров $\Lambda$ ) әти задачи весьма трудны. Достаточно продвинуты в теоретичөском и вычислительном отношении лишь нек-рые частные их классы. В основном это касается задач линейного П. п., в к-рых: либо a) целевая функция линейно зависит от одного скалярного $\left(\Lambda=\mathbb{R}^{1}\right)$ параметра, либо б) правые части ограничений линейно зависят от одного параметра, либо в) целевая функция и правые части ограничений линейно зависят от двух независимых скалярных параметров или от одного и того же параметра, г) целевая функция линейно зависит от векторного шараметра, д) правые части огравичений линейно зависят от векторного параметра. Случай зависимости от параметров матрицы ограничений задачи ливейного программирования весьма сложен и пока (1983) исследован недостаточно. Для случая а), напр., решение указанных проблем 1) - 3) характеризуется следующим образом. Пусть требуется максимизировать

\[
\sum_{l=1}^{n}\left(c_{j}+\lambda c_{j}^{\prime}\right) x_{j}
\]

пріг условиях

$\sum_{j=1}^{n} a_{i}, x_{j} \leqslant b_{i}, i=1, \ldots, m ; x_{j} \geqslant 0, j=1, \ldots, n$,


\end{document}