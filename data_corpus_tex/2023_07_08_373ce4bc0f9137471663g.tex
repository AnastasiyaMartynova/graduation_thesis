\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}
\usepackage{mathrsfs}

\begin{document}
$(x-i 0)^{-1}$, где

\[
\begin{gathered}
\left(p \frac{1}{x}, \varphi\right)=\text { v.p. } \int \frac{\varphi(x)}{x} d x=\lim _{\varepsilon \rightarrow+0}\left\{\int_{-\infty}^{-\varepsilon}+\int_{+\varepsilon}^{+\infty} \frac{\varphi(x)}{x} d x\right\}, \\
\left(\frac{1}{x+i 0}, \varphi\right)=\lim _{\varepsilon \rightarrow+0} \int \frac{\varphi(x)}{x+i 0} d x, \quad \varphi \in D\left(\mathbb{R}^{1}\right),
\end{gathered}
\]

(v.p. означает главное значение интеграла). Остальные Р. функции $x^{-1}$ получаются линейными комбинациями приведенных.

Р. применяется также для представления данного распределения в виде предела последовательности регулярных распределений. Напр., дельта-функция Дирака имеет Р.

\[
(\delta(x), \varphi(x))=\varphi(0)=\lim _{\varepsilon \rightarrow+0} \frac{1}{\pi} \int \frac{\varepsilon}{x^{2}+\varepsilon^{2}} \varphi(x) d x, \quad \varphi \in D\left(\mathbb{R}^{1}\right) .
\]

Обычно Р. распределений используется при перемножении распределений. Напр.,

\[
\left(\frac{1}{x+i 0} \frac{1}{x+i 0}, \varphi\right)=\left(\frac{1}{(x+i 0)^{2}}, \varphi\right)=\lim _{\varepsilon \rightarrow+0} \int \frac{\varphi(x) d x}{(x+i \varepsilon)^{2}}, \quad \varphi \in D\left(\mathbb{R}^{1}\right) .
\]

Известный физич. пример - перемножение одночастотных функций в квантовой теории поля. Часто, напр. при перемножении причинных функций, такая процедура не приводит к однозначному ответу и требует доопределения, согласованного с физич. контекстом задачи (см. Ультрафиолетовал расходимость, Перенормировка). Пример подобного доопределения - Боголюбова - Парасюка R-операция. O других конкретных приемах Р., применяемых в физич. приложениях, см. в ст. Регуляризация расходимостей в квантовой теории поля.

Лит.: [1] Боголюбов Н. Н., Ширков Д. В., Введение в теорию квантованных полей, 4 изд., М., 1984; [2] В ла а и м и ро в В. С., Обобщенные функции в математической физике, 2 изд., М., 1979. B. В. Жаринов.

PЕгУЛЯРИЗАЦИЯ об обще н но й Функц и и - представление ее в виде предела последовательности гладких функций. Именно, последовательность функций $f_{\mathbf{v}}(x)$ класca $C^{(\infty)}, \quad v=1,2, \ldots$, называется регуля р и з а и е й об обще н но й функци и $f \in \mathscr{F}^{\prime}$, если $f_{v}(x)$ задают регулярные обобщенные функции $f_{\mathrm{v}} \in \mathscr{F}^{\prime}$, слабо сходящиеся к $f$ при $v \rightarrow \infty$, то есть если

\[
\lim _{v \rightarrow \infty} \int f_{v}(x) \varphi(x) d^{n} x=(f, \varphi), \quad \varphi \in \mathscr{F} .
\]

Обычно используют Р. вида

\[
f_{v}(x)=\left(f(y), \delta_{v}(x-y)\right) \equiv f * \delta_{v}(x),
\]

где $f \in \mathscr{F}^{\prime}-$ данная обобщенная функция, $\delta_{v}(x) \in \mathscr{F}$, $v=1,2, \ldots,-$ дельта-последовательность, то есть такая последовательность, что

\[
\delta_{v^{*}} \varphi(x)=\int \delta_{v}(x-y) \varphi(y) d^{n} y \in \mathscr{F}
\]

и сходится к $\varphi$ в топологии Ғ̈. В стандартных пространствах обобщенных функций каждая обобщенная функция имеет Р. Этот факт лежит в основе секвенщиального подхода к теории обобщенных функций, где обобщенная функция определяется как класс эквивалентности своих Р. В. В. Жаринов.

РЕГУЛЯРИЗАЦИЯ РАСХОДИМОСТЕЙ в К в а Н Т О В О Й те о р и и поля - вспомогательная операция, заключающаяся в замене пропагаторов или интегралов от их произведений (соответствующих локальной квантовой теории поля) на некоторые аппроксимирующие их выражения, не содержащие ультрафиолетовых расходимостей или соответствуюших им в координатном представлении сингулярностей на световом конусе. Такие регуляризованные интегралы явно вычисляют (в импульсном представлении), а затем уже в вычисленных выражениях производят операцию, обратную введению регуляризации, то есть переходят к реальному физич. пределу. Ультрафиолетовые сингулярности при этом выделяются в виде аддитивных составляющих, имеющих простую (напр., полиномиальную) структуру по внешним импульсам.

32 Математическая физика Необходимость Р. p. наиболее просто увидеть в $x$-представлении. В квантовополевых расчетах приходится иметь дело с произведениями пропагаторов $\Delta(x)$, обладающих сингулярностями типа полюса $1 / x^{2}$ и дельта-функции Дирака по квадрату 4-мерного интервала $x^{2}=\left(x^{0}\right)^{2}-x^{2}$ [здесь $x\left(x^{0}, \boldsymbol{x}\right)$ - точка пространства-времени; используется система единиц, в к-рой $\hbar=c=1]$. Ясно, что квадраты и более высокие степени таких сингулярностей [напр., $\delta^{2}\left(x^{2}\right)$ ] не определены математически даже в смысле обобщенных функций. Для соответствующего доопределения удобно иметь регулярные (то есть не имеющие особенностей) приближения к $\Delta$ или к произведениям нескольких $\Delta$. Такие приближения и получают посредством вспомогательной Р.p.

В квантовополевых вычислениях по теории возмущений получили распространение несколько различных регуляризаций. Среди них наиболее употребительны следующие.

Регуляризация обрезанием состоит во введении конечного верхнего предела $\Lambda$ (называемого также и мпульсом обрезания) при интегрировании по 4-импульсам виртуальных частиц. Так, напр., фейнмановский интеграл, отвечающий простейшей (однопетлевой) диаграмме поляризации вакуума в квантовой электродинамике

\[
\Pi^{\mu v}(k)=\left(i / \pi^{2}\right) \int d^{4} p \operatorname{Sp}\left\{\gamma^{\mu} S^{c}(p) \gamma^{v} S^{c}(p-k)\right\},
\]

при регуляризации обрезанием принимает вид:

\[
\operatorname{reg}_{\Lambda} \Pi^{\mu v}(k)=\left(i / \pi^{2}\right) \int_{|p|<\Lambda} d^{4} p \operatorname{Sp}\left\{\gamma^{\mu} S^{c}(p) \gamma^{v} S^{c}(p-k)\right\} \text {, (2) }
\]

где символ $|p|<\Lambda$ под знаком интеграла обозначает, что по всем четырем компонентам 4-импульса $p$ интегрирование проводится в пределах от $-\Lambda$ до $+\Lambda$. В приведенных формулах $\Pi^{\mu \nu}-$ поляризационный оператор, $\gamma^{\mu}-$ Дирака матрицы, $\mu=0,1,2,3, S^{c}(p)$ - пропагатор электрона в импульсном представлении (см. Фейнмана диаграммы).

Вычисление по формуле (2) с помощью стандартной техники дает явное выражение, к-рое в пределе больших (по сравнению с массой электрона $m$ и модулем внешнего импульса $k=\sqrt{k^{2}}$ ) значений $\Lambda$ имеет вид:

\[
\operatorname{reg}_{\Lambda} \Pi^{\mu v}(k)=P^{\mu v}(k, \Lambda)+\tilde{\Pi}^{\mu v}(k)
\]

где $P^{\mu v}$ - многочлен 2-й степени по компонентам 4-вектоpa $k^{v}$ с коэффициентами, пропорциональными $\Lambda^{2}$ и $\ln \left(\Lambda^{2}\right)$, а $\tilde{\Pi}^{\mu \nu}-$ конечная функция от $k^{\mu}$ и $m^{2}$. Ее явный вид несуществен. Отметим лишь, что при больших $k^{2}$ она имеет логарифмич. асимптотику

\[
\tilde{\Pi}^{\mu v}(k) \sim\left(g^{\mu v} k^{2}-k^{\mu} k^{v}\right) \frac{1}{3} \ln k^{2},
\]

где $g^{\mu v}$ - метрич. тензор пространства-времени Минковского. Представление (3) оказывается удобным для проведения перенормировки, то есть устранения бесконечностей. Результативно она сводится к вычитанию из правой части (3) первого, сингулярного в пределе $\Lambda \rightarrow \infty$ слагаемого. Поскольку разбивание $\operatorname{reg}_{\Lambda} \Pi$ на слагаемые $P$ и $\tilde{\Pi}$ содержит произвол, то возникает вопрос о степени однозначности определения конечной части $\tilde{\Pi}^{\mu \nu}$ поляризационного оператора. Одно из условий, к-рому должно удовлетворять $\tilde{\Pi},-$ условие поперечности $k_{\mu} \tilde{\Pi}^{\mu v}(k)=0$, вытекающее из требования калибровочной инвариантности. Это условие диктует тензорную структуру матрицы $\tilde{\Pi}$ :

\[
\tilde{\Pi}^{\mu v}(k)=\left(g^{\mu v} k^{2}-k^{\mu} k^{v}\right) \pi\left(k^{2}\right),
\]

где $\pi\left(k^{2}\right)$ - нек-рая скалярная функция от $k^{2}$. Как можно показать, после этого остается еще однопараметрич. произвол, к-рый, напр., можно фиксировать уеловием $\pi(0)=0$.

РЕГУЛЯРИЗАЦИЯ 497


\end{document}