\documentstyle[%
righttag%
,11pt%
,amssymb%
,russian%               remove in Eng.
,izvru%                 Set izven% in Eng.
]{amsart}

%
\newcommand{\tts}[1]{} 

\theoremstyle{definition}
\theorembodyfont{\rm}
\newtheorem{cond}{\hskip\parindent Условие}

%\hoffset=-15mm%                  For Eng.
\setcounter{page}{59}
\god{2002}
\nomer{4~(479)} %                        Remove in Eng.
%\nomer{Vol.\,46, No.\,4}%               Set in Eng.
%\str{\pageref{begin}--\pageref{end}}%  Set in Eng.
\udk{517.956.6}

\author{\it Р.С.\,Хайруллин }
% NB:   ^^ remove in Eng.
\title{Аналог задачи Франкля для уравнения второго рода}
\thanks{Работа выполнена при поддержке внебюджетного фонда НИОКР Академии
наук Татарстана (договор \No\,05-5.1-79/2001(\&)).}

\date{}
%\pagestyle{myheadings}%         Set in Eng.
\pagestyle{plain} %              Remove in Eng.
\begin{document}
%\thispagestyle{plain}%          Set in Eng.
\maketitle
\label{begin}


\section{Постановка задачи}

Рассмотрим уравнение
\begin{equation}
u_{xx}+yu_{yy}+ (-n+1/2) u_y=0, \quad n \in N,
\end{equation}
в смешанной области $D$, эллиптическая часть которой $D_1$
совпадает со всей верхней полуплоскостью, а гиперболическая
часть состоит из двух бесконечных треугольников: $D_2$,
ограниченного характеристиками $y=0$ и $x-2\sqrt {-y}=0$, и $D_3$,
ограниченного характеристиками $y=0 $ и $x+2\sqrt {-y}=0 $.

В [1]--[4] исследовались задачи типа Трикоми и Бицадзе--Самарского
для уравнения (1) в случае неограниченных областей.
В данной статье рассматривается некоторый аналог задачи Франкля.
\medskip

{\bf Задача $F$.} {\it В области $D$ найти функцию $u(x,y)$
со свойствами
\begin{itemize}
\item[1)] $u(x,y) $ принадлежит $C(D_1\cup\{y=0\}) \cap
C(D_2 \cup \{y=0\} \cup \{x-2\sqrt {-y}=0\}) \cap
C(D_3 \cup \{y=0\} \cup \{x+2\sqrt {-y}=0\});$
\item[2)] имеют место соотношения
\begin{equation}
u=o(R^{2n+1}), \quad u_x=o(R^{2n}), \quad u_y=o(R^{2n-1})
\end{equation}
при $R \rightarrow +\infty $, где $R^2=x^2+4y$, $(x,y)\in D_1;$
\item[3)] $u(x,y)$ принадлежит $C^2(D_1 \cup D_2 \cup D_3) $ и
удовлетворяет уравнению $(1)$ в $D_1 \cup D_2 \cup D_3;$
\item[4)] существуют пределы $(x \neq 0)$
\begin{equation}
\nu_i(x)=\lim_{y \rightarrow 0,\ (x,y)\in D_i} |y|^{-n+1/2}
[u(x,y)-A(x,y,\tau _i)]_y,\ \ i=1,2,3, 
\end{equation}
и выполняются равенства
\begin{gather}
\nu_1(x)=(-1)^n\nu_2(x), \quad x>0, \\
\nu_1(x)=0, \quad \nu_3(x)=0, \quad x<0,
\end{gather}
где
\begin{gather}
\tau _i(x)=\lim_{y \rightarrow 0,\ (x,y)\in D_i}u(x,y), \\
A(x,y,\tau)=\sum \limits _{s=1}^n \frac{\tau^{(2s)}(x)(-1)^s}
{(-n+1/2)_ss!}y^s,\notag
\end{gather}
$(\alpha )_0=1$,
$(\alpha )_s=\alpha (\alpha +1)(\alpha +2)\dotsb (\alpha +s-1);$
\item[5)] $u(x,y) $ удовлетворяет краевому условию $(y<0)$
\begin{equation}
u(x,y)\vert_{x=2\sqrt{-y}}=u(x,y)\vert_{x=-2\sqrt{-y}};
\end{equation}
\item[6)] выполняются условия склеивания
\begin{alignat}{2}
\tau_1(x)&=\tau_2(x), &\quad x &\ge 0,\\
\tau_1(x)&=\tau_3(x)+g(x), &\quad x &\le 0,
\end{alignat}
где $g(x)$ --- заданная функция$;$
\item[7)] выполняются равенства
\begin{equation}
\tau_i^{(s)}(0) = 0, \quad s= \overline{0,n}.
\end{equation}
\end{itemize}
}

Предполагается выполненным

\begin{cond}
Функция $ g(x) \in 
C^n( -\infty , 0] \cap C^{2n+1,\gamma}(-\infty,0)$,
$\gamma >0$,
$$
g^{(s)}(0)=0, \quad s=\overline{0,n},
$$
производная $g^{(n+1)}(x)$ может иметь особенность при
$x=0$ порядка ниже единицы и должна иметь представление
$g^{(n+1)}(x)=O(|x|^{-\beta})$ при $x\rightarrow \infty$,
где $\beta > 1/2$.
\end{cond}

Будем решение искать в классе функций, для которых
$\tau_i(x) $ удовлетворяют условиям, аналогичным условию 1 с
учетом области определения, а для $\nu_i(x)$ выполняется

\begin{cond}
Функции $\nu_i(x)$ непрерывны в соответствующих
областях определения, могут иметь особенность при $x=0$
порядка ниже $n+1$ и ограничены на бесконечности.
\end{cond}

\section{Основное соотношение из эллиптической подобласти}

Для решения задачи методом интегральных уравнений
понадобятся основные соотношения между $\tau$ и
$\nu$. В эллиптической подобласти используем решение задачи Дирихле с
краевым условием (6) при $i=1$ ([5], с.\,588)
\begin{equation}
u(x,y) = \frac{n!2^{2n+1}}{\sqrt{\pi}\Gamma(n+1/2)}y^{n+1/2}
\int_{-\infty}^{+\infty} \tau_i(\xi)[(x-\xi)^2+4y]^{-n-1}d\xi.
\end{equation}

Нетрудно проверить, что эта формула верна в случае, когда заданная
функция обладает на бесконечности особенностью порядка ниже $2n+1$,
при этом полученная функция (11) будет удовлетворять условиям (2).
Подставляя представление (11) в (3), аналогично работе [3]
доказываeм, что справедлива

\begin{lem}
Основное соотношение из эллиптической
подобласти имеет вид
\begin{equation}
\Gamma(n+1/2)\nu_1(x)=\frac{n!(n+1/2)2^{2n+1}}{\sqrt{\pi}(2n+1)!}
\frac{d^n}{dx^n}\int \limits^{+\infty}_{-\infty}
\frac{\tau_1^{(n+1)}(\xi)}{\xi-x}d\xi.
\end{equation}
\end{lem}

\section{Основное соотношение из гиперболических областей}

Используем решение задачи типа Коши с начальными условиями
(6), (3) при $ i=2,3$, которые в соответствующих областях имеют вид
\begin{multline}
u(x,y) = \frac{1}{2} \sum \limits_{s=0}^n \frac{n!(2n-s)!2^{2s}}
{s!(n-s)!(2n)!}(-y)^{s/2}[\tau_i^{(s)}(x-2\sqrt{-y})
+ (-1)^s\tau_i^{(s)}(x+2\sqrt{-y})] - \\
%
- \frac{2(2n+1)!}{(n+1)!^2}(-y)^{n+1/2}
\int \limits_0^1\nu_i(\zeta)[\xi(1-\xi)]^nd\xi,
\end{multline}
где $\zeta = x-2\sqrt{-y}(1-2\xi)$.

Условие (7) запишем в виде
\begin{equation}
u\bigg(x,-\frac{x^2}{4}\bigg) = u\bigg(-x,-\frac{x^2}{4}\bigg).
\end{equation}

Подстановкой (13) в (14) аналогично [1] доказывается

\begin{lem}
Основное соотношение из гиперболических
подобластей имеет вид
\begin{equation}
\Gamma(n+1/2)\nu_2(x) - \Gamma(-n+1/2)\tau_2^{(2n+1)}(x)=
\Gamma(n+1/2)\nu_3(-x) +
\Gamma(-n+1/2)\tau_3^{(2n+1)}(-x).
\end{equation}
\end{lem}

\section{Вывод интегральных уравнений и решение задачи $F$}

Из соотношений (12), (15) с учетом условий (4), (5), (8), (9) и равенства
$$
\frac{n!(n+1/2)2^{2n+1}(-1)^n}{\sqrt{\pi}(2n+1)!\Gamma(-n+1/2)}
=\frac{1}{\pi}
$$
получим
\begin{gather}
\tau_1^{(2n+1)}(x) + \tau_1^{(2n+1)}(-x) -
\frac{1}{\pi}\frac{d^n}{dx^n} \int_{-\infty}^{+\infty}
\frac{\tau_1^{(n+1)}(\xi)d\xi}{\xi-x}
=g^{(2n+1)}(-x),\quad x>0, \\
%
\frac{1}{\pi}\frac{d^n}{dx^n} \int_{-\infty}^{+\infty}
\frac{\tau_1^{(n+1)}(\xi)d\xi}{\xi+x}=0, \quad x>0.
\end{gather}

В обоих соотношениях (16) и (17) разобьем интегралы на два слагаемых:
от $-\infty$ до 0 и от 0 до $+\infty$.
В первых слагаемых изменим знак переменной интегрирования.
В результате уравнения (16), (17) примут вид
\begin{gather}
\tau_1^{(2n+1)}(x) + \tau_1^{(2n+1)}(-x)-
\frac{1}{\pi}\frac{d^n}{dx^n}\int^{+\infty}_0
\frac{\tau_1^{(n+1)}(\xi)d\xi}{\xi-x}+
\frac{1}{\pi}\frac{d^n}{dx^n}\int^{+\infty}_0
\frac{\tau_1^{(n+1)}(-\xi)d\xi}{\xi+x}= g^{(2n+1)}(-x), \\
%
\frac{1}{\pi}\frac{d^n}{dx^n}\int^{+\infty}_0
\frac{\tau_1^{(n+1)}(\xi)d\xi}{\xi+x}
-\frac{1}{\pi}\frac{d^n}{dx^n}\int^{+\infty}_0
\frac{\tau_1^{(n+1)}(-\xi)d\xi}{\xi-x}= 0.
\end{gather}

Равенства (18), (19) представляют собой систему уравнений относительно
$\tau_1(x)$ и $\tau_1(-x)$ при $x>0$.
 
Итак, доказaна

\begin{thm}
Решение задачи $F$
редуцируется к решению системы уравнений $(18)$, $(19)$.
\end{thm}

Решим эту систему. Умножим уравнение (19) на $(-1)^n$ и прибавим
к уравнению (18). В результате получим
\begin{equation}
\frac{d^n}{dx^n}\biggl[\rho(x) - \frac{1}{\pi}\int
^{+\infty}_0
\biggl(\frac{1}{\xi-x}-\frac{(-1)^n}{\xi+x}\biggr)\rho(\xi)
d\xi- (-1)^ng^{(n+1)}(-x)\biggr]=0,
\end{equation}
где
\begin{equation}
\rho(x)=\tau_1^{(n+1)}(x)+(-1)^n\tau_1^{(n+1)}(-x).
\end{equation}
Уравнение (20) эквивалентно следующему:
$$
\rho(x) - \frac{1}{\pi}\int^{+\infty}_0
\biggl(\frac{1}{\xi-x}-\frac{(-1)^n}{\xi+x}\biggr)\rho(\xi)
d\xi= (-1)^ng^{(n+1)}(-x) + \sum \limits_{s=0}^{n-1} c_sx^s,
$$
где $ c_s$ --- произвольные постоянные.

Выполним замену $\xi^2=\sigma$, $x^2=\eta$.
Получим при нечетном $n$
\begin{gather}
z(\eta)- \frac{1}{\pi}\int^{+\infty}_0\frac{z(\sigma )d\sigma}
{\sigma -\eta}=f(\eta)+\sum \limits_{s=0}^{n-1}c_s\eta^{s/2}, \\
%
z(\eta)=\rho(x), \quad f(\eta)=(-1)^ng^{(n+1)}(-x);\notag
\end{gather}
при четном $n$
\begin{gather}
z(\eta)- \frac{1}{\pi}\int^{+\infty}_0\frac{z(\sigma )d\sigma}
{\sigma -\eta}=f(\eta)+\sum \limits_{s=0}^{n-1}c_s\eta^{(s-1)/2}, \\
%
z(\eta)=\rho(x)/x, \quad f(\eta)=(-1)^ng^{(n+1)}(-x)/x.\notag
\end{gather}

Оценим поведение функций на концах промежутка интегрирования. Из
условия 1 следует, что если $n$ нечетное, то функция $f(\eta)$
может иметь особенность при $\eta = 0$ порядка ниже $1/2$ и при
$\eta \rightarrow +\infty$ должна иметь нуль порядка выше $1/4$;
если $n$ четное, то функция $f(\eta)$
может иметь особенность при $\eta = 0$ порядка ниже 1 и при
$\eta \rightarrow +\infty$ должна иметь нуль порядка выше $3/4$.
Такое же поведение предполагается у функции $z(\eta)$.

Уравнения вида (22), (23) разрешимы, когда правая часть обращается в
нуль на бесконечности [6]. Отсюда следуют равенства
$c_s=0$, $s=\overline{0,n-1}$, в случае уравнения (22) и
$c_s=0$, $s=\overline{1,n-1}$, в случае уравнения (23).
Теперь в этих уравнениях выполним подстановку [6]
\begin{gather*}
\eta=\zeta /(1-\zeta ), \quad \sigma = t/(1-t),\\
z(\eta)=v(\zeta)(1-\zeta),\quad f(\eta)=b(\zeta)(1-\zeta).
\end{gather*}
Уравнения примут вид
\begin{equation}
v(\zeta)-\frac{1}{\pi} \int^1_0\frac{v(t)dt}
{t -\zeta}=b(\zeta)+ \frac{c_0}{\sqrt{\zeta(1-\zeta)}},
\end{equation}
причем $c_0=0$ при нечетном $n$.

Отметим, что решения уравнения вида (24), неограниченные при $\zeta=0$,
имеют особенность порядка $1/4$, а неограниченные при $\zeta=1$
имеют особенность порядка $3/4$ в указанной точке.

Пусть $n$ нечетное. У функции $b(\zeta)$
могут быть особенности при $\zeta = 0$ порядка ниже $1/2$,
при $\zeta = 1$ --- ниже $3/4$. Такие же особенности допускаются у
решения $v(\zeta)$. Поэтому необходимо использовать формулу решения,
ограниченного при $\zeta=1$.

Пусть $n$ четное. У функции $ b(\zeta)$
могут быть особенности при $\zeta = 0$ порядка ниже $1$,
при $\zeta = 1$ --- ниже $1/4$. Следовательно, опять необходимо
использовать формулу решения, ограниченного при $\zeta=1$.
Причем должно быть $c_0=0$. В противном случае функция $v(\zeta)$
будет иметь при $\zeta=1$ особенность порядка $1/2$, что выше
допустимой.

Указанное решение единственно. Отсюда найдем $v(\zeta)$, а
следовательно, и функцию $\rho(x)$. Теперь, когда функция $\rho(x) $
найдена, из формулы (21) выразим
\begin{equation}
\tau_1^{(n+1)}(-x) = (-1)^n\rho(x)-(-1)^n
\tau_1^{(n+1)}(x)
\end{equation}
и подставим в уравнение (19). В результате получим
$$
\frac{d^n}{dx^n}\biggl[\frac{1}{\pi}\int^{+\infty}_0
\biggl(\frac{1}{\xi-x}+\frac{(-1)^n}{\xi+x}\biggr)\tau_1^{(n+
1)}(\xi)d\xi-
\frac{1}{\pi}\int^{+\infty}_0\frac{\rho(\xi)}{\xi-x}d\xi\biggr]=0.
$$

Это уравнение имеет такую же структуру, как и уравнение (20). Поэтому
после последовательных замен получим
$$
\frac{1}{\pi}\int^1_0\frac{p(t)dt}
{t -\zeta}=q(\zeta)+\frac{d_0}{\sqrt{\zeta(1-\zeta)}},
$$
причем $d_0=0$ при четном $n$.

У функций $p(\zeta)$ и $q(\zeta)$ допускаются следующие
особенности: если $n$ четное, то
при $\zeta=0$ особенность порядка ниже $1/2$, при $\zeta = 1$ --- ниже $3/4$;
если $n$ нечетное, то при $\zeta = 0$ --- ниже $1$, при
$\zeta = 1$ --- ниже $1/4$.

Решение, неограниченное при $\zeta=0$ или $\zeta =1$, имеет в этой
точке особенность порядка $1/2$. Поэтому необходимо использовать
формулу решения, ограниченного при $\zeta=0$, если $n$ четное, и
ограниченного при $\zeta=1$, если $n$ нечетное. Кроме того, отсюда 
же следует, что во втором случае $d_0=0$. Указанные решения единственны.

Найдем $ p(\zeta) $, а следовательно, и
$\tau_1^{(n+1)}(x)$, $x>0$.
Используя формулу (25), вычислим
$\tau_1^{(n+1)}(x)$, $x<0 $. Затем с учетом равенств (10)
восстановим функцию $\tau_1(x)$. После этого из соотношений
(8), (9), (15) последовательно найдем функции $\tau_2(x)$,
$\tau_3(x)$ и $\nu_2(x)$. Если запишем решение в каждой из
подобластей, используя формулы (11) и (13), то это решение
единственно в силу однозначности определения вспомогательных функций и
единственности решений задач Дирихле и типа Коши.

Итак, доказана

\begin{thm}
Задача $F$ имеет единственное решение.
\end{thm}

\begin{thebibliography}{9}

\bibitem{1}
Крикунов Ю.М. {\em Видоизмененная задача Трикоми для уравнения
$ u_{xx}+yu_{yy}+(-n+1/2)u_y=0$} // Изв. вузов. Математика. --
1979. -- \No\,9. -- С.\,21--28.
\bibitem{2}
Салтыкова Н.М., Смирнов М.М. {\em Об одной краевой задаче типа
Бицадзе-Самарского для уравнения смешанного типа второго рода в
неограниченной области} // Вестн. Ленингр. ун-та. Сер. матем., механ.,
астр. -- 1985. -- \No\,1. -- С.\,43--49.
\bibitem{3}
Хайруллин Р.С. {\em Задача Трикоми для уравнения смешанного
типа второго рода в случае неограниченной области} // Дифференц. 
уравнения. -- 1994. -- Т.\,30. -- \No\,11. -- С.\,2010--2017.
\bibitem{4}
Хайруллин Р.С. {\em К задаче Трикоми для уравнения
смешанного типа второго рода} // Сиб. матем. 
журн. -- 1994. -- Т.35. -- \No\,4. -- С.\,927--936.
\bibitem{5}
Самко С.Г., Килбас А.А., Маричев О.И. {\em Интегралы и производные
дробного порядка и некоторые их приложения}. -- Минск: Наука и техника,
1987. -- 688 с.
\bibitem{6}
Гахов Ф.Д. {\em Краевые задачи}. -- 3-е изд. -- М.: Наука,
1977. -- 640 с.

\end{thebibliography}

\vskip 2cm

\post{\it Казанская государственная}{\it Поступила}
\post{\it архитектурно-строительная академия}{04.05.2001}

\label{end}
\end{document}



\begin{equation}
\frac{d^n}{dx^n}\biggl[\tau_1^{(n+1)}(x) +(-1)^n \tau_1^{(n+1)}(-x)-
\frac{1}{\pi}\int\limits^{+\infty}_0
\frac{\tau_1^{(n+1)}(\xi)d\xi}{\xi-x}+
$$

$$
+\frac{1}{\pi}\int\limits^{+\infty}_0
\frac{\tau_1^{(n+1)}(-\xi)d\xi}{\xi+x}
-(-1)^n g^{(n+1)}(-x)\biggr]=0,
\eqno(21)
$$

$$
\frac{d^n}{dx^n}\biggl[\frac{1}{\pi}\int\limits^{+\infty}_0
\frac{\tau_1^{(n+1)}(\xi)d\xi}{\xi+x}
-\frac{1}{\pi}\int\limits^{+\infty}_0
\frac{\tau_1^{(n+1)}(-\xi)d\xi}{\xi-x}\biggr]= 0.
\eqno(22)
$$
