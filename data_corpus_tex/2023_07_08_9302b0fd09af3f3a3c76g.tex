\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\def\AA{\mathring{\mathrm{A}}}

\begin{document}
$\left(a_{i j}=-a_{j i}\right)$. В векторной форме абсолютная нуль-система может быть записана в виде $u=A x$, где $A-$ кососимметрич. оператор, матрица к-рого надлежащим выбором базиса приводится к виду

\begin{center}
\includegraphics[max width=\textwidth]{2023_07_08_9302b0fd09af3f3a3c76g-1}
\end{center}

В этом случае абсолютная нуль-система принимает канонич. вид:

\[
u_{2 i}=x^{2 i+1}, \quad u_{2 i+1}=-x^{2 i} .
\]

Абсолютная пуль-система порождает билинейную форму, к-рая записывается в канонич. виде:

\[
\boldsymbol{x} A \boldsymbol{y}=\sum_{i}\left(x^{2 i} y^{2 i+1}-x^{2 i+1} y^{2 i}\right)
\]

Коллинеации пространства $\mathrm{Sp}_{2 n+1}$, перестановочные с его нуль-системой, наз. с и м п л е к т и ч е с к и м и п р е о б р а о в а н и я и; операторы, определяющие әти коллинеации,- с и м п л е к т и ч е с к им и. Для указанной выше канонич. формы матрицы $\|A\|$ определяется $(2 n+2)$-матрица симплектич. оператора $U$, әлементы к-рой удовлетворяют условиям

\[
\sum_{i}\left(U_{j}^{2 i} U_{k}^{2 i+1}-U_{j}^{2 i+1} U_{k}^{2 i}\right)=\delta_{j}, k-1-\delta_{j}, k+1,
\]

где $\delta_{a, b}$ - символ Кронекера, а матрица такого оператора $U$ наз. с и мп л е к т и ч е с к о й; ее определитель равен единице. Симплектич. преобразования образуют группу, являющуюся группой Ли.

Всякая точка пространства $\mathrm{Sp}_{2 n+1}$ лежит в $(2 n-1)$ плоскости, соответствующей ей в абсолютной нульсистеме. Можно определить также и нулевые $m$-плоскости в $\mathrm{Sp}_{2 n+1}$. Многообразие нулевых прямых пространства $\mathrm{Sp}_{2 n+1}$ наз. его а б с о л ю т ны м ли н е йны м ко м п л е к с о м. В связи с этим симплектич. группа наз. также г р у пा п о ї ли н е й н о г о комп п е к с а, или ком пле к с - гру ппо й.

Всякая пара прямых и соответствующих в нульсистеме двух $(2 n-1)$-плоскостей определяют единственный в пространстве $\mathrm{Sp}_{2 n+1}$ симплектич. инвариант относительно групшы симплектич. преобразований этого пространства. Через каждую точку обеих прямых проходит трансверсаль этих прямых и $(2 n-1)$-плоскостей так, что определяет проективные четверки точек. Это составляет геометрический смысл с и м п л е к т ич е с к о го ин в а р и а т т а, который утверждает равенство двойных отношений получаемых четверок точек.

Симплектич. 3-пространство допускает интершретацию в гиперболич. пространстве, что указывает, в частности, на связь симплектич. пространств с гиперболическими. Так, группа симплектич. преобразований пространства $\mathrm{Sp}_{3}$ изоморфна группе движений гиперболич. пространства ${ }^{2} S_{4}$. В әтой интерпретации симплектич. инвариант связан с расстоянием между точками гиперболич. пространства.

Лит.: [1] Р о з е н ф е л ь д Б. А., Неевклидовы пространства, М., 1969. Л. А. Сидоров.

СИМПЛЕКТИЧЕСКОЕ ПРОСТРАНСТВО ОДНОРОД-

НОЕ - симплектическое многообразие $(M, \omega)$ вместе с транзитивной группой Ли $G$ его автоморфизмов. Элементы алгебры Ли $\mathfrak{g}$ группы $G$ можно рассхатривать как симплектические векторные поля на $M$, т. е.поля $X$, сохраняющие симплектическую 2-форму $\omega$ :

\[
X \cdot \omega=d i_{X} \omega=0
\]

где точкой обозначена производная Ли, $i_{X}$ - оператор внутреннего умножения на $X, d$ - внешний диффференциал. С. п. о. наз. с т р о г о с и м п л е к т и т ес к и м, если все поля $X \in \mathfrak{g}$ гамильтоновы, то есть $i_{X} \omega=d H_{X}$, где $H_{X}$ функция на $M$ (гамильтониан поля $X)$, причем гамильтониан $H_{X}$ можно выбрать так, чтобы отображение $X \mapsto H_{X}$ было гомоморфизмом алгөбры Ли $g$ в алгебру Ли функций на $M$ относительно скобки Пуассона. Примером строго С. п. о. является орбпта $M_{\alpha}=\left(\operatorname{Ad}^{*} G\right) \alpha$ группы Ли $G$ относительно коприсоединенного представления $\mathrm{Ad} * G$ групшы $G$ в пространстве $\mathfrak{g}^{*}$ линейных форм на $\mathfrak{g}$, проходящая через произвольную точку $\alpha \in \mathfrak{g}^{*}$. Инвариантная симплектическая 2-форма $\omega$ на $M_{\alpha}$ задается формулой

\[
\omega\left(X_{\beta}, Y_{\beta}\right)=d \beta(X, Y) \equiv \beta([X, Y]),
\]

гдө $X_{\beta}, Y_{\boldsymbol{\beta}}$ - значөния векторных полей $X, Y \in \mathfrak{g}$ в тотке $\beta \in M_{\alpha}$. Поле $X \in \mathfrak{g}$ имеет гамильтониан $H_{X}(\beta)=$ $=\beta(X)$.

Для произвольного строго С. п. о. $(M, \omega, G)$ определено $G$-эквивариантное отображение момента

\[
\mu: M \longrightarrow \mathfrak{g}^{*}, x \mapsto \mu_{x}, \mu_{x}(X)=H_{X}(x),
\]

к-рое отображает $M$ на орбиту $\mu(M)$ групшы $G$ в $g^{*}$ и является локальным изоморфизмом симплектич. многообразий. Таким образом, любое строго С. П. о. группы $G$ является накрытием над орбитой группы $G$ в коприсоединенном представлении.

Односвязные С. П. о. с односвязной, но не обязательво әффективно действующей группой автоморфизмов $G$ находятся во взаимно однозначном соответствии с орбитами естественного действия группы $G$ в пространстве $Z^{2}(\mathfrak{g})$ замкнутых 2-форм на ее алгебре Ли $\mathfrak{g}$. Соответствие определяется следующим образом: ядро $\mathfrak{f}^{\sigma}$ любой 2-формы $\sigma \in Z^{2}(\mathfrak{g})$ является подалгеброй алгебры Ли $\mathfrak{g}$. Соответствующая $\AA^{\sigma}$ связная подгруппа $K^{\sigma}$ группы Ли $G$ замкнута и определяет односвязное однородное пространство $M^{\sigma}=G / K^{\sigma}$. Форма $\sigma$ задает невырожденную 2-форму в касательном пространстве $T_{0} M^{\sigma} \simeq \mathfrak{g} / \Re^{\sigma}$ точки $o=e K^{\sigma}$ многообразия $M^{\sigma}$, к-рая продолжается до $G$-инвариантной симплектич. формы $\omega^{\sigma}$ на $M^{\sigma}$. Таким образом, форме $\sigma$ отвечает односвязное С. п. о. $\left(M^{\sigma}, \omega^{\sigma}\right)$. Если $\mathfrak{R}^{\sigma}$ не содержит идеалов алгебры Ли $\mathfrak{g}$, то действие $G$ на $M^{\sigma}$ локально эффективно. С. п. о. $M^{\sigma}$ и $M^{\sigma^{\prime}}$ изоморфны тогда и только тогда, когда формы $\sigma, \sigma^{\prime}$ принадлежат одной орбите группы $G$ в $Z^{2}(\mathfrak{g})$. Для точной 2 -формы $\sigma=d \alpha$ С. п. о. $M^{\sigma}$ отождествляется с универсальной накрывающей С. п. о. $M_{\alpha}$, являющегося орбитой точки $\alpha$ в коприсоединенном представлении. Если $[\mathfrak{g}, \mathfrak{g}]=\mathfrak{g}$, то орбита $G \sigma$ любой точки $\sigma \in Z^{2}(\mathfrak{g})$ канонически снабжается структурой C. П. о. и любое С. п. о. односвязной группы $G$ изоморфоно накрытию над одной из таких орбит. В частности, $M^{\sigma}$ есть универсальная накрывающая орбиты $G \sigma$.

Пусть $(M, \omega)$ - компактное С. П. о. односвязной связной групшы $G$, действующей локально әффективно. Тогда $G$ есть прямое произведение полупростой компактной группы $S$ и разрешимой группы $R$, разлагающейся в полупрямое произведение абелевой подгруппы и абелева нормального делителя, а С. п. о. $(M, \omega)$ разлагается в прямое произведение С. п. о. с группами автоморфизмов $S$ и $R$ соответственно.

Частным случаем C. п. о. является симплектическое групповое пространство - группа Ли вместө с левоинвариантной симплектич. формой $\omega$. Известно, что из редуктивности группы Ли, допускающей левоинвариантную симметрич. форму, следует ее коммутативность, а из унимодулярности - разрешимость. Все такие группы размерности $\leqslant 4$ разрешимы, но начиная


\end{document}