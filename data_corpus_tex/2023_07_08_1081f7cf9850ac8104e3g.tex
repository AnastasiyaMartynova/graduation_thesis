\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}

\begin{document}
ШІАТ в и н т - см. Винтовое движение.

ШАРКОВСКОТО ПОРяДОК - линейное упорядочение множества натуральных чисел вида:

\[
1,2,2^{2}, \ldots, 2^{n}, 2^{n+1}, \ldots, 2^{n+1} \cdot 5,2^{n+1} \cdot 3, \ldots, 2^{n} \cdot 5,2^{n} \cdot 3, \ldots
\][

···, 9,7,5,3 \textbackslash text \{. \}
]

Введен в Шарковского теореме. Задает направление бифуркаций в одномерных динамич. системах и многих других системах, связанных с одномерными. В однопараметрич. семействах бифуркации в соответствии с Ш. П. (в частности, бифуркации удвоения периода) обладают при определенных условиях рядом универсальных свойств, таких, как универсальная скорость бифуркаций по параметру $\lambda$ и универсальные масштабные соответствия между точками вновь возникающих циклов. Ю. Л. Майстренко.

IIAPKOBCKOТО TEOPEMA - теорема, устанавливающая закономерности сосушествования циклов различных периодов для одномерных отображений (цикл периода $m$ отображения $f$ - множество, состоящее из $m$ различных точек $x_{i}$, для которых $x_{i+1}=f\left(x_{i}\right), i=1, \ldots, m-1$ и $\left.x_{1}=f\left(x_{m}\right)\right)$. Если непрерывное отображение $f: \mathbb{R}^{1} \rightarrow \mathbb{R}^{1}$ имеет цикл нек-рого периода $m$, то $f$ имеет циклы всех других периодов, предшествующих $m$ в Шарковского порядке. Из Ш.т. вытекает простой критерий сложного динамич. поведения: если у отображения $f$ есть цикл нек-рого периода $m_{0} \neq 2^{n}, n=0$, $1, \ldots$ (напр., периода 3 ), то у этого отображения есть бесконечное число циклов других, в том числе сколь угодно больших периодов. В этом (и только в этом) случае топологическая энтропия рассматриваемой системы положительна.

Имеются многочисленные обобщения Ш. т., учитывающие понятие типа траектории (порядка, в к-ром точки цикла переставляются под действием $f$ ). На нек-рые классы отображений Ш. т. переносится без изменений. Это отображения окружности $f: S^{1} \rightarrow S^{1}$ с нулевым числом врашения, и отображения $f: \mathbb{R}^{k} \rightarrow \mathbb{R}^{k}$ вида

\[
\begin{gathered}
f:\left(x^{(1)}, x^{(2)}, \ldots, x^{(k)}\right) \mapsto\left(f_{1}\left(x^{(1)}\right), f_{2}\left(x^{(1)}, x^{(2)}\right), \ldots,\right. \\
\left.\ldots, f_{k}\left(x^{(1)}, x^{(2)}, \ldots, x^{(k)}\right)\right),
\end{gathered}
\]

называемые треугольными.

Лum.: [1] Шарко в ск и й А. Н., «Украинский математич. журнал», 1964, № 1, с. 61-71. Ю.Л. Майстренко. ШАРОВЫЕ ФУНКЦИИ - однородные функции $u_{n}$ степени $n$ от прямоугольных координат $x, y, z$, удовлетворяющие уравнению Лапласа:

\[
\frac{\partial^{2} u_{n}}{\partial x^{2}}+\frac{\partial^{2} u_{n}}{\partial y^{2}}+\frac{\partial^{2} u_{n}}{\partial z^{2}}=0
\]

для областей, ограниченных сферическими поверхностями. Сушествует $2 n+1$ линейно независимых однородных многочленов от $x, y, z$ целой положительной степени $n$, являюшихся Ш. Ф.; их линейная комбинация представляет обший вид такого многочлена степени $n$.

Так, напр.,

\[
\begin{gathered}
u_{0}=a, u_{1}=a x+b y+c z \\
u_{2}=a\left(x^{2}-z^{2}\right)-b\left(y^{2}-z^{2}\right)+c x y+d y z+e z x
\end{gathered}
\]

( $a, b, c, d, e-$ произвольные постоянные) представляют общий вид однородных многочленов степеней $0,1,2$, являющихся Ш. ф. Задача об отыскании общего вида простейших Ш. ф. степени $n$ приводит к понятию сферических функций. Ш. Ф. применяются при нахождении общего решения уравнения Лапласа и при решении задач математич. физики для областей, ограниченных сферич. поверхностями.

Н.Я. Виленкин.

IIBAPЧА ИНТЕГPAЛ - зависяший от параметра интеграл, даюший решение задач и Ш ва рца о выражении аналитической функции $f(z)=u(z)+i v(z)$ в круге $D$ по граничным значениям ее действительной (или мнимой) части $u$ на граничной окружности $C$.

Пусть на единичной окружности $C=\left\{z: z=e^{i \varphi}, 0<\varphi<2 \pi\right\}$ дана непрерывная действительная функция $u(\varphi)$. Тогда интегральны е формулы Ш варца, выражающие аналитич. функцию $f(z)=u(z)+i v(z)$, граничные значения действительной части к-рой совпадают с $u(\varphi)$ (или граничные значения мнимой части совпадают с $v(\varphi)$ ), имеют вид:

\[
\left.\begin{array}{rl}
f(z) & =S u(z)=\frac{1}{2 \pi i} \int_{C} u(t) \frac{t+z}{t-z} \frac{d t}{t}+i c= \\
& =\frac{1}{2 \pi} \int_{0}^{2 \pi} \frac{e^{i \varphi+r e}}{e^{i \varphi}-r e} u(\varphi) d \varphi+i c, \\
& f(z)=\frac{1}{2 \pi} \int_{C} v(t) \frac{t+z}{t-z} \frac{d t}{t}+c_{1}= \\
& =\frac{i}{2 \pi} \int_{0}^{2 \pi} \frac{e^{i \varphi_{+}} e^{i \theta}}{e^{i \varphi}-r e^{i \theta}} v(\varphi) d \varphi+c_{1},
\end{array}\right\}
\]

где $z=r e^{i \theta}, t=e^{i \varphi}, c$ и $c_{1}-$ произвольные действительные постоянные. Ш. и. (*) тесно связан с Пуассона интегралом. Выражение

\[
\frac{1}{2 \pi} \cdot \frac{e^{i \varphi_{+}}+e^{i \theta}}{e^{i \varphi}-r e^{i \theta}}
\]

часто называется я д р о м Ш в а р ц а, а интегральный оператор $S$, фигурирующий в первой формуле $(*),-$ о пе ра то ром Ш в а ца. Эти понятия обобщаются и на области произвольного вида комплексной плоскости.

Лum.: [1] Б и цадзе А. В., Основы теории аналитических функций комплексного переменного, 2 изд., М., 1972; [2] Гахо в Ф.Д.. Краевые задачи, 3 изд., М., $1977 . \quad$ Е. Д. Соломенцев.

ШВАРЦА ЛЕММА: пусть голоморфная в $U=\{z \in \mathbb{C}|| z \mid<1\}$ функция $f$ удовлетворяет условиям $f(0)=0$ и $|f(z)|<1$ при $z \in U$, тогда $|f(z)| \leqslant|z|$ при всех $z \in U$. Кроме того, ес.ти $\left|f\left(z_{0}\right)\right|=\left|\left(z_{0}\right)\right|$ при нек-ром $z_{0}$, то $f(z)=e^{i \alpha} z, \alpha \in \mathbb{R}$.

Дальнейшее развитие этого результата с целью придания ему конформно инвариантной формулировки приводит к гиперболической метрики принципу.

Jum.: [1] S c warz H. A., Ges. math. Abh., Bd 1-2, B., 1890.

В. В. Горяйнов.

ШВАРЦА НЕРАВЕНСТВО: если для комплексного векторного пространства $V$ существует комплекснозначная функция $(\cdot, \cdot)$ на $V \times V$, удовлетворяюшая условиям:

\begin{enumerate}
  \item $(x, x) \geqslant 0$, причем $(x, x)=0$ эквиватентно $x=0$;

  \item $(x, y+z)=(x, y)+(x, z)$

  \item $(x, \alpha y)=\alpha(x, y)$

\end{enumerate}

\end{document}