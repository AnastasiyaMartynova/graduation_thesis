\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}

\begin{document}
(варивнт П. к. типа I); частный случай - равномерное распределение.

Т и П III:

$p(x)=k\left(1+\frac{x}{a}\right)^{\mu a} e^{-\mu x},-a \leqslant x \leqslant \infty, \mu>0, a>0$ частные случаи - гамма-распределение, «xи-квадрат»распределение.

Т и п IV:

\[
p(x)=k\left(1+\frac{x^{2}}{a^{2}}\right)^{-m} e^{-\mu \operatorname{arctg}\left(\frac{x}{a}\right)}
\]

Т и п V:

\[
-\infty \leqslant x \leqslant \infty, a>0, \mu>0
\]

\[
p(x)=k x^{-q} e^{-\frac{\alpha}{x}}, 0 \leqslant x<\infty, \alpha>0, q>1
\]

(сводится преобразованием к типу III).

T и II VI:

\[
p(x)=k x^{-q_{1}}(x-a)^{q_{2}}, a \leqslant x<\infty, q_{1}>q_{2}-1 ;
\]

частные случаи - бета-распределение 2-го рода, Фuшера F-распределение.

T и II VII:

\[
p(x)=k\left(1+\frac{x^{2}}{a^{2}}\right)^{-m},-\infty<x<\infty, m>\frac{1}{2}
\]

частный случай - Cтьюдента распределение.

Т и п VIII:

\[
p(x)=k\left(1+\frac{x}{a}\right)^{-m},-a \leqslant x \leqslant 0,0 \leqslant m \leqslant 1 .
\]

T и п IX:

\[
p(x)=k\left(1+\frac{x}{a}\right)^{m},-a \leqslant x \leqslant 0, m>-1 .
\]

$\mathrm{T}$ и п $\mathrm{X}$ :

\[
p(x)=k e^{-\frac{x-m}{\sigma}}, m \leqslant x<\infty, \sigma>0,-
\]

показательное распределение.

Т и II XI:

\[
p(x)=k x^{-m}, b \leqslant x<\infty, m>0 ;
\]

частный случай - Парето распределение.

Т и П XII:

\[
p(x)=\left(\frac{1+\frac{x}{a_{1}}}{1-\frac{x}{a_{2}}}\right)^{m},-a_{1} \leqslant x \leqslant a_{2},|m|>1
\]

(вариант типа I).

Наиболее важны в приложениях типы I, III, VI, VII.

Всякая П. к. однозначно определяется своими первыми четырымя моментами

\[
\alpha_{k}=\int_{-\infty}^{+\infty} x^{k} p(x) d x
\]

если они конечны. Это свойство семейства П. к. исшользуется для приближенного описания эмпирических распределений.

Метод подгонки П. к. к нек-рому әмпирич. распределению состоит в следующем. По независимым результатам наблюдений вычисляют первые четыре выборочных момента, затем определяется тип подходящей П. к. и методом моментов находятся значения неизвестных параметров искомой П. к. В общем случае метод моментов не является әффективным методом шолучения оценок П. к. Проблема более точной апщроксимации распределений с помощью П. к. получила вовое решөние в работах Л. Н. Большева (1963) по аспмптотич. преобразованиям.

П. к. были введены К. Пирсоном (К. Pearson, 1894). Лum.: [1] E I d e r t o n W. P., Frequency curves and correlation, 4 ed., Сamb., 1953; [2] K е н д а л л M., С т ь ю а p т A., Теория распределений, пер. с англ., М., $1966 ;$;
Математические методы статистики, пер. с англ., 2 изд., М., 1975 ; [4] Б о л Б ш е в Л. Н., “Теория вероятн. и ее примен.», 1963, T. 8, 2, c. $129-55$.
A. В. Прохоров. ПИРСОНА РАСШРЕДЕЛЕНИЕ - см. Пирсона кривble.

IИТМЕНА ОЦЕНКА - эквивариантная статистич. оценка параметра сдвига относительно группы вещественных сдвигов, имеющая минималыный риск при квадратичной функции потерь.

Пусть компоненты $X_{1}, X_{2}, \ldots, X_{n}$ случайного вектора $X=\left(X_{1}, X_{2}, \ldots, X_{n}\right)$ суть независимые случайные величины, подчиняющиеся одному и тому же вероятностному закону, плотность вероятности к-рого принадлежит семейству

$\{f(x-\theta),|x|<\infty, \theta \in \Theta=(-\infty,+\infty)\}$,

причем

\[
\mathrm{E}_{\theta} X_{1}^{2}=\int_{-\infty}^{+\infty} x^{2} f(x-\theta) d x<\infty
\]

для любого $\theta \in \Theta$. Далее, пусть $G=\{g\}$ - группа вещественных сдвигов, действующая в пространстве реализаций $\mathbb{R}^{1}=(-\infty,+\infty)$ случайной величины $X_{i}$ $(i=1,2, \cdots, n)$ :

\[
G=\left\{g: g X_{i}=X_{i}+g,|g|>\infty\right\} .
\]

В таком случае задача оценивания параметра $\theta$ будет инвариантной относительно квадратичной функции потерь $L(\theta, \hat{\theta})=(\theta-\hat{\theta})^{2}$, если в качестве $\hat{\theta}=\hat{\theta}(X)$ выбрать эквивариантную оценку. Э. Питмен [1] показал, что эквивариантная оценка $\hat{\theta}(X)$ параметра сдвига $\theta$ относительно группы $G$, имеюгая минимальный риск при квадратичной функции потерь, имеет вид

\[
\hat{\theta}(X)=X_{(n 1)}-\frac{\int_{-\infty}^{+\infty} x f(x) \prod_{i=2}^{n} f\left(x+Y_{i}\right) d x}{\int_{-\infty}^{+\infty} f(x) \prod_{i=2}^{n} f\left(x+Y_{i}\right) d x},
\]

где $Y_{i}=X_{(n i)}-X_{(n 1)}, \quad X_{(n i)}$ есть $i$-я порядковая статистика, построенная по вектору наблюдекий $X$. II. о. - несмещенная оценка, она является минимаксной в классе всех оценок параметра сдвига $\theta$ при квадратичной функции потерь, если все эквивариантные оценки параметра $\theta$ имеют конечные функции риска [2].

П р и м е р 1. Если

\[
f(x-\theta)=e^{-(x-\theta)}, x \geqslant \theta,
\]

то есть $X_{i}, i=1,2, \ldots, n$, подчиняется показательному закону с неизвестным параметром сдвига $\theta$, то П. о. $\hat{\theta}(X)$ для $\theta$ выражается формулой

\[
\hat{\theta}(X)=X_{(n 1)}-\frac{1}{n},
\]

причем ее дисперсия равна $1 / n^{2}$.

П р и м е р 2. Если

\[
f(x-\theta)=\frac{1}{\sqrt{2 \pi}} e^{-\frac{(x-\theta)^{2}}{2}},|x|<\infty,
\]

то есть $X_{i}, i=1,2, \ldots, n$, подчиняется нормальному $N(\theta, 1)$ закону с неизвестным математич. ожиданием $\theta$, то в этом случае среднее арифмметическое

является П. о.

\[
\bar{X}=\frac{1}{n}\left(X_{1}+X_{2}+\ldots+X_{n}\right)
\]

Jum.: [1] P i t m a n E. J., "Biometrika", 1939, v. 30, p. 391421; [2] G i r s h i c k M. A., S a v a g e L. J., "Proc. Second Ber-
keley Symp. Math. Statist. Prob.", 1951, p. 53-73; [3] 3 a c c III., Теория статистических выводов, пер. с англ., М., 1975.

ПИФАГОРА ТЕОРЕМА - теорема геометрии, устанавливающая связь между сторонами прямоугольного треугольника. П. т. была, по-видимому, известна до


\end{document}