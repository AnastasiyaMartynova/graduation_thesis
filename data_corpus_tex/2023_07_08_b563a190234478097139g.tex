\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
расслоения $\xi$, а $c_{k}-$ Чжәня классы. П о л н ы м П. к. наз. неоднородный характеристич. класс $p=1+p_{1}+$ $+p_{2}+\ldots$. Иначе говоря, П. к. определяются как классы когомологий $p_{i} \in H^{4 i}\left(B O_{n}\right)$, задаваемые равенством $p_{i}=f^{*}\left((-1)^{i} c_{2 i}\right)$, где $f: B O_{n} \rightarrow\left(B U_{n}\right)$ - отображение, соответствующее комплексификации унирерсального расслоения, а $c_{n} \in H^{2 k}\left(B U_{n}\right)$ - классы Чжэня.

Пусть $\left(x_{n}\right)_{\mathbb{R}}$ - овеществление универсального расслоения $x_{n}$ над $B U_{n}$. Полный П. к. $p\left(\left(x_{n}\right)_{\mathbb{R}}\right)$ расслоения $\left(x_{n}\right)_{\mathbb{R}}$ совпадает с $\Pi_{i=1}^{n}\left(1+x_{i}^{2}\right) \in H^{*}\left(B U_{n}\right)$, где $x_{1}, \ldots, x_{n}-$ образующие Ву (см. Характеристический класс).

Частичное описание кольца когомологий $H^{*}\left(B O_{n}\right)$ может быть получено в терминах образующих Ву следующим образом. Отображение $g: B U_{[n / 2]} \rightarrow B O_{n}$, соответствующее расслоению $\left(x_{[n / 2]}\right)_{\mathbb{R}}$ либо $\left(x_{[n / 2]}\right)_{\mathbb{R}} \otimes \theta_{1}$, где $\theta_{1}$ - одномерное тривиальное расслоение, индуцирует гомоморфизм колец $g^{*}: H^{*}\left(B O_{n}\right) \rightarrow H^{*}\left(B U_{[n / 2]}\right)$, при к-ром подкольцо кольца $H^{*}\left(B O_{n}\right)$, порождаемое П. к. $p_{1}, p_{2}, \ldots, p^{[n / 2]}$, отображается мономорфно на подкольцо кольца $H^{*}\left(B U_{n}\right)$, состоящее из всех четных симметрич. полиномов от образующих Ву. Четность понимается в том смысле, что каждая переменная $x_{i}$ должна входить в полином в четной степени. Тем самым получается выражение любого әлемента кольца $\mathbb{Z}\left(p_{1}, \ldots\right.$ $\left.\ldots, p_{[n / 2]}\right) \subset H^{*}\left(B O_{n}\right)$ через образующие Ву, что важно для практич. вычислений с П. к. Характеристич. класс, определяемый четным симметрич. полиномом от образующих Ву, может быть выражен через П. к. следующим образом. Полином выражается через элементарные симметрич. функции переменных $x_{1}^{2}, \ldots, x_{n}^{2}$, а затем вместо элементарных симметрич. функций подставляется П. к.

Если $\xi, \eta$ - два действительных векторных расслоения над общей базой, то класс когомологий $p(\xi(\ominus \eta)-$ - $p(\xi) p(\eta)$ имеет ІІорядок не большіе двух; это связано c тем, что для порвого класса Чжәня $c_{1}(\lambda)=-c_{1}(\bar{\lambda})$. Пусть нек-рое кольцо $\Lambda$, содержащее $1 / 2$, рассматривается в качестве кольца коәффициентов и пусть $p_{i}-$ П. к. со значениями в $H^{*}(\cdot ; \Lambda)$. В этом случае пмеет место равенство

или

\[
p(\xi \bigoplus \eta)=p(\xi) p(\eta)
\]

\[
p_{k}(\xi \oplus \eta)=\sum_{i} p_{k-i}(\xi) p_{i}(\eta), p_{0}=1 \text {. }
\]

Кольцо $H^{* *}\left(B O_{n} ; \Lambda\right)$ мономорфно отображается в $H^{* *}\left(B U_{[n / 2]} ; \Lambda\right)$, и образ этого отображения совиадае' с подкольцом всех четных симметрич. рядов с образующими Ву в качестве переменных. При әтом полный П. к. переходит в полином $\Pi_{i=1}^{[n / 2]}\left(1+x_{i}^{2}\right)$, а П. к.- в элементарные симметрич. функции от переменных $x_{1}^{2}, \ldots, x_{n}^{2}$. Теорема:

\[
H^{* *}\left(B O_{n} ; \Lambda\right)=\Lambda\left[\left[p_{1}, \ldots, p_{[n / 2]}\right]\right] .
\]

Кольцо когомологий $H^{*}\left(B S O_{n}\right)$ содержит кроме П. к. также эйлеров класc $e \in H^{n}\left(B S O_{n}\right)$. Теорөма:

\[
\begin{aligned}
& H^{* *}\left(B S O_{2 k+1} ; \Lambda\right)=\Lambda\left[\left[p_{1}, \ldots, p_{k}, e\right]\right], \\
& H^{* *}\left(B S O_{2 k} ; \Lambda\right)=\Lambda\left[\left[p_{1}, \ldots, p_{k-1}, e\right]\right],
\end{aligned}
\]

для пространства $B S O_{2 k}$ имеет место равенство $p_{k}=e^{2}$.

Отображение $g: B U_{[n / 2]} \rightarrow B O_{n}$ пропускается через $B U_{[n / 2]} \rightarrow B S O_{n}$. Индуцированнов отображение $H^{* *}\left(B S O_{n}\right) \rightarrow H^{* *}(B U)_{[n / 2]}$ переводит $e$ в нуль при нечетном $n$ и в $\Pi_{i=1}^{n / 2} x_{i}$ при четном $n$.

Пусть $f(t) \in \mathbb{Q}[[t]]$ - четный формальный степенной ряд над полем $\mathbb{Q}$. Тогда ряд $\Pi_{i=1}^{[n / 2]} f\left(x_{i}\right)$ определяет не- к-рый неоднородный элемент кольца $H^{* *}\left(B O_{n} ; \mathbb{Q}\right)$, т. е. характеристич. класс. Допуская нек-рую вольность, можно записать

\[
x=\prod_{i=1}^{[n / 2]} f\left(x_{i}\right) \in H^{* *}\left(B O_{n} ; \mathbb{Q}\right) .
\]

Характеристич. класс $x$ стабилен (то есть $x(\xi \oplus \theta)=$ $=x(\xi)$, где $\theta$ - тривиальное расслоение) тогда и только тогда, когда свободный член ряда $f(t)$ равен единице. Если положить $f(t)=t /$ th $t$, то построенный описанным способом характеристич. класс обозначается через $L$ и наз. $L-к$ л а с с о м Х и р ц е б р у $\mathbf{x}$,

\[
L=\prod_{l=1}^{[n / 2]} x_{i} / \text { th } x_{i} \in H^{* *}\left(B O_{n} ; \mathbb{Q}\right) \text {. }
\]

Стандартная процедура выражения ряда П $f\left(x_{i}\right)$ через элементарные симметрич. функции переменных $x_{1}^{2}, \ldots$ $\ldots, x_{n}^{2}$ приводит к представлению класса $L$ в виде ряда от П. к. Другой важный для приложений характеристич. класс получается, если положить

\[
f(t)=\frac{t / 2}{\operatorname{sh}(t / 2)}=\frac{t}{e^{t / 2}-e^{-t / 2}} .
\]

Класс, задаваемый четным симметріч. рядом

\[
\prod f\left(x_{i}\right)=\prod \frac{x_{i} / 2}{\operatorname{sh}\left(x_{i} /{ }^{2}\right)},
\]

наз. $\hat{A}$ - к л а с с о м. Аналогично, $A$ - к л а с с о м наз. характеристич. класс, задаваемый рядом $\Pi f\left(x_{i}\right)$, где $f(t)=\frac{2 t}{\operatorname{sh}(2 t)}$. Оба эти класса, как и $L$, могут быть выражены через П. к.

То пологичес кая инва ри ант ность. В 1965 С. П. Новиков [2] доказал, что П. к. с рациональными коэффициентами двух гомеоморфных многообразий совпадают. До этой работы было известно, что рациональные П. к. кусочно линейно инвариантны, т. е. совпадают для двух кусочно линейных гомеоморфных многообразий. Более того, были определсны (см. [4]) рациональные П. к. для кусочно линейных многообразий (возможно с краем). Был дан пример (см. [5]), показывающий, что пелочислениые П. к. не являются топологич. инвариантамі.

В 1969 было доказано (см. [7]), что слой Top/PL pacслоения $B P L \rightarrow B$ Тор имеет гомотопич. тип пространства Эйленберга - Маклейна $K\left(\mathbb{Z}_{2}, 3\right)$. Отсюда следует топологич. инвариантность рациональных П. к., а также вытекает опровержение о с н о в н о й ги по тезы комбинаторно й топо логи и (Hauptvermutung).

\includegraphics[max width=\textwidth, center]{2023_07_08_b563a190234478097139g-1}
Пусть $h^{*}$ - обобщенная теория когомологий, в к-рой определены классы Чжәня $\sigma_{i}$. Если для одномерного комплексного векторного расслоения $\lambda$ выполнено равенство $\sigma_{1}(\lambda)=-\sigma_{1}(\bar{\lambda})$, то П. к. со значениями в теории $h^{*}$ можно определить прежней формулой $P_{i}(\xi)=$ $=(-1)^{i} \sigma_{2 i}(\xi \otimes \mathbb{C})$. Определенные таким образом классы будут обладать свойством $P(\xi \bigoplus \eta)=P(\xi) P(\eta)$, где $P=$ $=1+P_{1}+P_{2}+\ldots-$ полный П. к., рассматриваемый в теории $h^{*} \otimes \mathbb{Z} \quad[1 / 2]$.

Однако во многих практически используемых обобщенных теориях (напр., в $K$-теории) приведенное равенство для $\sigma_{1}$ не имеет места. В таких теориях нецелесообразно определять П. к. описанным способом, т. к. при таком определении не выполняется обычная формула для полного класса суммы двух расслоений, даже после включения $1 / 2$ в коэффициенты. Можно определять обобщенные П. к. следующим образом. Пусть $h^{*}-$ мультипликативная теория когомологий, в к-рой универсальным образом задана ориентация $u(\xi \otimes \mathbb{C}) \in$


\end{document}