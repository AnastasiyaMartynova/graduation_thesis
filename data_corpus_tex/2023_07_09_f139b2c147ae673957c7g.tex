\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{mathrsfs}

\begin{document}
Для формального описания у.Д.П. применяется язык

\includegraphics[max width=\textwidth, center]{2023_07_09_f139b2c147ae673957c7g-1}
ное вероятностное п ространство, $\left\{\mathscr{F}_{t}, t \geqslant 0\right\}$-семеӥство расширяющихся полных $\sigma$-алгебр $\mathscr{F}_{t}$, вложенных в $\mathscr{F}$. Пусть $w_{t}=\left(w_{t}^{i}, i=1, \ldots, d_{1}\right)-d_{1}$-мерный винеровский процесс относительно $\left\{\mathscr{\mathscr { F }}_{t}\right\}$, определенный на $(\Omega, \mathscr{F}, \mathrm{P})$ при $t \geqslant 0$ (т. е. процесс, для к-рого $w_{t}^{i}$ являіотя одномерными неп рерывными стандартными винеровскими процессами при каждом $i$, процессы $w_{t}^{1}, \ldots, w_{t}^{d_{1}}$ независимы, $w_{t}^{i} \mathscr{J}$-измеримо при всяких $t$, $i$ и при $t, h \geqslant 0$ величины $\left\{w_{t+h}^{i}-w_{t}^{i}, i=1, \ldots, d_{1}\right\}$ не зависят от $\mathscr{F}_{t}$ ). Пусть $A$-нек-рое сепарабельное метрич. пространство. При $\alpha \in A, \quad t \geqslant 0, \quad x=\left(x^{i}\right.$, $i=1, \ldots, d) \in E_{d}$ предполагаются заданными функции $\sigma(\alpha, t, x), b(\alpha, t, x)$, причем $\sigma(\alpha, t, x)$-матрица размера $d \times d_{1}, b(\alpha, t, x)$ есть $d$-мерный вектор. Считают, что $\sigma, b$-борелевские функции $\alpha, t, x$, удовлетворяющие условию Липшица по $x$ с постоянной, не зависящей от $\alpha, t$, и такие, что $\left|\sigma^{i j}(\alpha, t, 0)\right|$ и $\mid b^{i}(\alpha, t, 0)$ ограничены. ПІ роизвольный процесс $\alpha_{t}=\alpha_{t}(\omega), t \geqslant 0$, $\omega \in \Omega$, прогрессивно измеримый относительно $\left\{\mathscr{F}_{t}\right\}_{\text {и }}$ п ринимающий значения из $A$, наз. с т р а т е г и е й; $\mathfrak{A}$ - множество всех стратегий. Для всякого $s \geqslant 0$, $x \in E_{d}, \quad \alpha \in \mathfrak{H}$, существует и единственно решение стохастич. уравнения Ито

\[
\begin{gathered}
d x_{t}=b\left(\alpha_{t}, s+t, x_{t}\right) d t+\sigma\left(\alpha_{t}, s+t, x_{t}\right) d w_{t}, \\
x_{0}=x
\end{gathered}
\]

(т е о р е м а И т о). Это решение, обозначаемое $x_{t}^{\alpha, s, x}$, наз. упр а ля емы м диффузионны м процесс ом (управля емым іл р цес сом диффузионного ти п а); оно управляется с помощью выбора стратегии $\alpha=\alpha_{t}$. Кроме стратегий из $\mathfrak{U}$ рассматриваются другие классы стратегий. Пусть $C\left([0, \infty), E_{d}\right)-$ пространство непрерывных функций на $[0, \infty)$ со значениями в $E_{d}$. Полуось $[0, \infty)$ интерпретируется как множество значений времени $t$. Элементы $C\left([0, \infty), E_{d}\right)$ обозначаются через $\left.x_{r 0, \infty}\right)$. И пусть $N_{t}$-наименьшая $\sigma$-алгебра подмножеств $C\left([0, \infty), E_{d}\right)$, относительно к-рой при $s \leqslant t$ измеримы функции $x_{s}$ (являющиеся вначением әлемента $\left.x_{[0, \infty)}\right)$ пространства $C\left([0, \infty), E_{d}\right)$ в момент времени $s$. Функция $\alpha=\alpha_{t}\left(x_{[0, \infty)}\right)$ со значениями в $A$ наз. естеств енной стратегией, допустимой в точке $(s, x)$, если она прогрессивно измерима относительно $\left\{N_{t}\right\}$ и при $\alpha_{t}=\alpha_{t}\left(x_{(0, \infty)}\right)$ существует хотя бы одно решение уравнения (9), прогрессивно измеримое относительно $\left\{\mathscr{J}_{t}\right\}$. Множество всех естественных стратегий, допустимых в точке $(s, x)$, обозначается $\mathfrak{A}_{E}(s, x)$, его подмножество, состоящее из всех естественных стратегий вида $\alpha_{t}\left(x_{t}\right)$, обозначается $\mathfrak{H}_{M}(s, x)$ и наз. множеством м а $\mathbf{p}$ к о в с к и $\mathbf{x}$ с т р а т е г и й, допустимых в точке $(s, x)$. Принято говорить, что естественная стратегия определяет управление в момент времени $t$ на основании наблюдений за процессом $x_{r}$ на участке времени $[0, t]$, марковская стратегия-на основании наблюдений за процессом только в момент времени $t$. При $\alpha \in \mathfrak{A}_{E}(s, x)$ (даже при $\alpha \in \mathfrak{Y}_{M}(s, x)$ ) решение (9) может не быть единственным. Поэтому для каждых $s \geqslant 0, x \in E_{d}, \alpha \in \mathfrak{T}_{E}(s, x)$, произвольным образом фиксируется к.-н. решение уравнения (9) и оно обозначается $x_{t}^{\alpha, s, x}$. После этого по формуле $\beta_{t}(\omega)=\alpha_{t}\left(x_{[0, \infty)}^{\alpha,{ }^{\alpha}{ }^{x}}(\omega)\right)$ определяется вложение $\mathfrak{U}_{E}(s, x) \subset \mathfrak{U}$, при к-ром $x_{t}^{\beta, s, x}=x_{t}^{\alpha, s, x}$ (п. н.).

Целью управления, как правило, является максимизация или минимизация математич. ожидания того или иного функционала от траекторий $x_{t}^{\alpha}, s, x$. Более общей является следующая задача. Пусть на $A \times[0, \infty) \times E_{d}$ определены борелевские функции $C^{\alpha}(t, x) \geqslant 0, f^{\alpha}(t, x)$ и на $[0, \infty) \times E_{d}$ определена бо- релевская функция $g(t, x)$. Для $\alpha \in \mathfrak{H}, s \geqslant 0, x \in E_{\boldsymbol{d}}$, номент первого выхода $\left(s+t, x_{t}^{\alpha, \varsigma, x)}\right.$ из $Q$,

\[
\left.\begin{array}{c}
\varphi_{t}^{\alpha, s, x}=\int_{0}^{t} C^{\alpha}\left(s+r, x_{r}^{\alpha, s, x}\right) d r \\
v^{\alpha}(s, x)=\mathrm{E}_{s, x}^{\alpha}\left[\int_{0}^{\tau} Q e^{-\varphi_{t}} f^{\alpha_{t}}\left(s+t, x_{t}\right) d t+\right. \\
\left.+g\left(s+\tau_{Q}, x_{\tau_{Q}}\right) e^{-\varphi_{\tau}} \tau\right],
\end{array}\right\}
\]

где индексы $\alpha, s, x$ у знака математич. ожидания означают, что их нужно вставить под знаком математич. ожидания всюду, где әто возможно. Тогда возникает задача о нахождении стратегии $\alpha$, максимизирующей $v^{\alpha}(s, x)$, и $о$ нахождении ф у н к й и в и г ры Іп а (це н ы)

\[
v(s, x)=\sup _{\alpha \in \mathfrak{A}} v^{\alpha}(s, x) .
\]

Стратегии $\alpha$, для к-рых $v^{\alpha}(s, x) \geqslant v(s, x)-\varepsilon$, наз. E-о п т и м а ль ь Ы м и для точки $(s, x)$. О п т и ма л ь-

\begin{center}
\includegraphics[max width=\textwidth]{2023_07_09_f139b2c147ae673957c7g-1(1)}
\end{center}

\includegraphics[max width=\textwidth, center]{2023_07_09_f139b2c147ae673957c7g-1(2)}
няя грань обозначается $v_{(E)}(s, x)\left(v_{M}(s, x)\right)$. Поскольку имеют место вложения $\mathfrak{A}_{M}(s, x) \subset \mathfrak{A}_{E}(s, x) \subset \mathfrak{A}$, то $v_{(M)}(s, x) \leqslant v_{(E)}(s, x) \leqslant v(s, x)$. При нек-рых достаточно пиироких предположениях известно (см. [3]), что $v_{(E)}=v$ (это так, если, напр., $\sigma, b, c, f, g$ непрерывны по $(\alpha, x)$, непрерывны по $x$ равномерно относительно $\alpha$ при всяком $t$ и абсолютные величины $c, f, g$ не превосходят $K(1+|x|)^{m}$ при всех $\alpha, t, x$, где $K, m$ не зависят от $\alpha, t, x)$. Вопрос о равенстве $v_{(M)}=v$ в достаточно общей ситуации является открытым. Формальное применение идей динамич. п рограмми рования приводит к соотношению, наз. принципом Беллмана:

\[
\begin{aligned}
v(s, x)= & \sup _{\alpha \in \mathfrak{A}} \mathrm{E}_{\mathrm{S}, x}^{\alpha}\left[\int_{0}^{\tau} e^{-\varphi_{t}} f^{\alpha}\left(s+t, x_{t}\right) d t+\right. \\
& \left.+v\left(s+\tau, x_{\tau}\right) e^{-\varphi_{\tau}}\right]
\end{aligned}
\]

где $\tau^{\alpha, s, x}$ - произольным образом оп ределенные марковские моменты, не превосходящие $\tau_{Q}^{\alpha}, s, x$. Если в (12) в качестве $\tau$ взять $t \wedge \tau_{Q}=\min \left(t, \tau_{Q}\right)$, применить к $v\left(s+\tau, x_{\tau}\right) e^{-\varphi_{\tau}}$ форорулу Ито, то после нек-рых нестрогих рассуждений можно прийти к уравнению Беллмана:

\[
\sup _{\alpha \in A}\left(L^{\alpha} v+f^{\alpha}\right)=0
\]

где

\[
\begin{gathered}
L \alpha_{v}=\frac{\partial v}{\partial s}+a^{i j}(a, s, x) \frac{\partial^{2} v}{\partial x^{i} \partial x^{j}}+ \\
+b^{i}(\alpha, s, x) \frac{\partial v}{\partial x^{i}}-c^{\alpha}(s, x) v,
\end{gathered}
\]

по индексам $i, i$ предполагается суммирование от 1 до $d$, матрица

\[
a(\alpha, s, x)=\left(a^{i j}(\alpha, s, x)\right)=\frac{1}{2} \sigma(\alpha, s, x) \sigma^{*}(\alpha, s, x) .
\]

Уравнение Беллмана играет центральную роль в теории у.д.п., так как часто оказывается, что достаточно «хорошее» его решіение, равное на $\partial Q$ функции $g$, является функцией выигрыша, а если $\alpha=\alpha^{0}(s, x)$ прі каждых $(s, x)$ доставляет верхніо грань в (13) и $\alpha_{t}^{0}=\alpha^{0}\left(s_{0}+t, x_{t}\right)$ - марковская стратегия, допустимая в $\left(s_{0}, x_{0}\right)$, то стратегия $\alpha^{0}=\left\{\alpha_{t}^{0}\right\}$ является оптимальной в точке $\left(s_{0}, x_{0}\right)$. Т. о., пногда удается показать, что $v_{M}\left(s_{0}, x_{0}\right)=v\left(s_{0}, x_{0}\right)$.

Строгое обоснование справедливости приведенных выше выводов наталкивается на серьезные трудности, связанные с веливейным характером уравнения (13),


\end{document}