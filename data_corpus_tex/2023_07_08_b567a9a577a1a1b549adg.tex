\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}
\usepackage{mathrsfs}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
Распределение суммы $X_{1}+\ldots+X_{n}$, нормированной математич. ожиданием $n / 2$ и среднеквадратич. отклонением $\sqrt{n / 12}$, с ростом $n$ быстро сближается с нормальным распределением с параметрами 0 и 1 (уже при $n=3$ приближение удовлетворительно для многих практич. целей).

В статистич. приложениях процедура построения случайной величины с заданной функцией распределения $F(x)$ основана на следующем факте. Пусть случайная величина $Y$ распределена равномерно на $[0,1]$ и функция распределения $F(x)$ непрерывна и строго возрастает. Тогда случайная величина $X=F^{-1}(Y)$ имеет функцию распределения $F(x)$ (в общем случае надо заменить в определении $X$ функцию $F^{-1}(y)$ на нек-рый ее аналог, а именно $f(y)=\inf \{x: F(x) \leqslant y \leqslant F(x+0)\})$.

P. p. на отрезке как предельно е ра с п р е д е л е ни е. Ниже приводятся типичные примеры возникновения P. р. на $[0,1]$ в качестве предельного.

\begin{enumerate}
  \item Пусть $X_{1}, X_{2}, \ldots, X_{n}, \ldots-$ независимые случайные величины, имеющие одну и ту же непрерывную функцию распределения. Тогда распределение их суммы $S_{n}$, приведенной по $\bmod 1$, т. е., иными словами, распределение дробной части $\left\{S_{n}\right\}$ суммы $S_{n}$, сходится к равномерному на $[0,1]$ распределению.

  \item Пусть параметры $\alpha$ и $\beta$ имеют абсолютно непрерывное совместное распределение; тогда при $t \rightarrow \infty$ распределение $\{\alpha t+\beta\}$ сходится к равномерному на $[0,1]$.

  \item Р. р. встречается как предельное распределение дробных долей нек-рых функций $g(n)$ натурального аргумента $n$. Напр., при иррациональном $\alpha$ доля тех $m, 1 \leqslant m \leqslant n$, из $n$ для к-рых

\end{enumerate}

\[
0 \leqslant a \leqslant\{n \alpha\} \leqslant b \leqslant 1,
\]

имеет пределом при $n \rightarrow \infty$ величину $b-a$.

P. p. н а п о д м н о ж е с т в а х $\mathbb{R}^{k}$. Пример Р. p. в прямоугольнике встречается уже в Бюффбона задаче (см. также Геометрические вероятности, Стохастическая геометрия). Р. р. на нек-ром ограниченном множестве $D$ в евклидовом пространстве $\mathbb{R}^{k}$ определяется как распределение, имеющее плотность

\[
p\left(x_{1}, \ldots, x_{n}\right)=\left\{\begin{array}{cc}
C \neq 0, & x \in D, \\
0, & x \notin D,
\end{array}\right.
\]

где $C$ обратна $k$-мерному объему (или лебеговой мере) области $D$.

Рассматривают также и Р. р. на поверхностях. Так, «случайное направление» (напр., в $\mathbb{R}^{3}$ ) определяют вектором, идущим из начала координат в случайную точку поверхности единичной сферы, равномерно распределенную в том смысле, что вероятность ее попадания в какую-либо часть поверхности пропорциональна площади этой части.

Роль Р. р. на алгебраич. группах играет нормированная Xаapa мера.

Лит.: [1] Ф е л л е р В., Введение в теорию вероятностей и ее приложения, пер. с англ., 2 изд., т. 2, М., 1967.

РАВНОМЕРНО СХОДИМОСТ А. В. Прохоров. топология пространства $\mathscr{F}(X, Y)$ отображений множества $X$ в равномерное пространство $Y$, порожденная равномерной структурой множества $\mathscr{F}(X, Y)$, базой окружений к-рой являются совокупности всех пар $(f, g) \in \mathscr{F}(X, Y) \times \mathscr{F}(X, Y)$ таких, что $(f(x), g(x)) \in v$ для любого $x \in X$ и $v$ пробегает базу окружений пространства $Y$. Сходимость направления $\left\{f_{\alpha}\right\}_{\alpha \in \mathfrak{A}} \subset \mathcal{F}(X, Y)$ к $f_{0} \in \mathscr{T}(X, Y)$ в такой топологии наз. сходимостью $f_{\alpha}$ к $f_{0}$, равномерной на множестве $X$. Если $Y$ полно, то $\mathscr{J}(X, Y)$ - полное пространство в топологии равномерной сходимости. Если $X$ - топологич. пространство и $\mathscr{C}(X, Y)$ - множество всех непрерывных в топологии пространства $X$ отображений $X$ в $Y$, то $\mathscr{C}(X, Y)$ замкнуто в $\mathscr{T}(X, Y)$ в Р. с. т.; в частности, предел $f_{0}(x)$ равномерно сходящейся последовательности $f_{n}(x)$ непрерывных на $X$ отображений есть отображение, также непрерывное на $X$.

Лит.: [1] Б у р б а к и Н., Общая топология. Использование вещественных чисел в общей топологии. Функциональные пространства. Сводка результатов. Словарь, пер. с франц., М. $1975 ;$ [2] РАВНОСИЛЬНОСТБ, или э к в и в а л е н т н о с т ь, утверждений (формул) $A$ и $B$ - понятие, означающее, что при каждом допустимом наборе значений параметров утверждения $A$ и $B$ оба истинны или оба ложни. Напр., Р. уравнений, неравенств и их систем означает совпадение множеств их решений. Р. формул высказываний исчисления есть совпадение задаваемых ими булевых Функций.

Лuт.: [1] Н о в и к о в П. С., Элементы математической лоРАВНОСИЛЬННЫЕ МЕТОДБІ СУММИРОВАНИЯ методы, суммирующие одни и те же последовательности (быть может, к разным пределам); иначе, Р. м. с.методы суммирования, имеющие одно и то же суммируемости поле. Иногда Р. м. с. наз. методы, к-рые имеют одинаковые поля суммируемости и являются совместными методами суммирования. Примерами равносильных и совместных методов суммирования являются Чезаро метод суммирования $(C, k)$ и Рисса метод сумлирования $(R, n, k)$ (при одном и том же $k \geqslant 0)$, Чезаро метод суммирования $(C, k)$ и суммирования $(H, k)$ (при одном и том же целом $k \geqslant 0)$. Существуют Р. м. с., не являющиеся совместными.

Иногда рассматривают не полные поля суммируемости, а их подмножества, принадлежащие нек-рому множеству $U$. Если для двух методов суммирования әти подмножества совпадают, то говорят, что методы суммирования равносильны на множестве $U$. Методы суммирования действительных последовательностей наз. В П о н е р а в о с и л ь н м и, если равенство их полей суммируемости остается справедливым при включении в них последовательностей, суммируемых к $+\infty$ и $-\infty$. Аналогично определяется равносильность методов суммирования для специальных видов суммируемости (абсолютной, сильной и др.).

Матричные методы суммирования, определенные преобразованиями последовательности в последовательность посредством матриц ||$a_{n k} \|$ и $\left\|b_{n k}\right\|$, наз. а б с о-

\includegraphics[max width=\textwidth, center]{2023_07_08_b567a9a577a1a1b549adg-1}
Э к в и в л е н т н ы и) на множестве $U$ последовательностей $\left\{s_{k}\right\}$, если $\tau_{n}^{(A)}-\tau_{n}^{(B)} \rightarrow 0, n \rightarrow \infty$, для любої $\left\{s_{k}\right\} \subset U$, где

\[
\tau_{n}^{(A)}=\sum_{k=1}^{\infty} a_{n k} s_{k}, \quad \tau_{n}^{(B)}=\sum_{k=1}^{\infty} b_{n k} s_{k},
\]

а ряды в выражениях для $\tau_{n}^{(A)}$ и $\tau_{n}^{(B)} \operatorname{cходятся~для~всех~} \boldsymbol{n}$.

Jum.: [1] K у к Р., Бесконечные матрицы и пространства последовательностей, пер. с англ., М., $1960 ;$ [2] Х а р д и Г., в сб.: Итоги науки и техники. Математический анализ, т. 12 в сб.: Итоги науки и техники. Математический анализ, т.
М., 1974 , с. $5-70$.
И. И. Волков.

РАВНОСТЕПЕННАЯ НЕПРЕРЫІВНОСТЬ М Ож е с т в а ф у н к ц й - понятие, тесно связанное с понятием компактности множества непрерывных функций. Пусть $X, Y$ - компактные метрич. пространства и $C(X, Y)$ - множество непрерывных отображений $X$ в $Y$. Множество $D \subset C(X, Y)$ наз. р а в н о с т е п е н н о н е п р е р ы в н ы м, если для любого $\varepsilon>0$ существует такое $\delta>0$, что из $\rho_{X}\left(x_{1}, x_{2}\right) \leqslant \delta$ вытекает $\rho_{Y}\left(f\left(x_{1}\right), f\left(x_{2}\right)\right) \leqslant$ $\leqslant \varepsilon$ для всех $x_{1}, x_{2} \in X, f \in D$. Р.н. $D$ эквивалентна относительной компактности $D$ в $C(X, Y)$, наделенном метрикой

\[
\rho(f, g)=\max _{x \in X} \rho_{Y}(f(x), g(x)),
\]


\end{document}