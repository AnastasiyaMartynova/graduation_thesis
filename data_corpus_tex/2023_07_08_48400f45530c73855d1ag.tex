\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }
\usepackage{bbold}

\begin{document}
димости $U(\zeta, R)$ непременно имеется хотя бы одна

\includegraphics[max width=\textwidth, center]{2023_07_08_48400f45530c73855d1ag-1(1)}
то есть О. т. аналитич. функции $f(z)$ при продолжении вдоль путей $z=\varphi(t), 0 \leqslant t \leqslant 1$, класса $\{L\}$ таких, что $z=\varphi(t) \in U(\zeta, R)$ при $0 \leqslant t<1, a=\varphi(1)$. Иначе говоря, O. т. элемента $\left(U(\zeta, R), f_{\zeta}\right)$ - это такая точка $a \in \Gamma$, что непосредственное аналитич. продолжение элемента $\left(U(\zeta, R), f_{\zeta}\right)$ из круга $U(\zeta, R)$ в любую окрестность $V(a)$ невозможно. В этой ситуации и вообще во всех случаях, когда отсутствие явного описания класса путей $\{L\}$ не может повести к недоразумениям, ограничиваются обычно только указанием $z$-координаты О. т. a. Изучение расположения О. т. аналитич. функции в зависимости от свойств последовательности коәффициентов $\left\{c_{k}\right\}_{k=0}^{\infty}$ исходного элемента $(U(\xi, R)$, $\left.f_{\zeta}\right)$ является одним из важных направлений исследований в төории функций (см. Адамара теорема мультипликационная, Звезда элемента функции, а также [1], [3], [5]). Известно, напр., что О. т. ряда

\[
f_{0}(z)=\sum_{k=0}^{\infty} b^{k} z^{d^{k}}
\]

где $b \in \overline{\mathbb{C}},|b|<1, d \geqslant 2-$ натуральное число, заполняют всю границу $\Gamma=\{z \in \mathbb{C}:|z|=1\}$ его круга сходимости $U(0,1)$, хотя сумма этого ряда непрерывна всюду в замкнутом круге $\bar{U}(0,1)=\{z \in \overline{\mathbb{C}}:|z| \leqslant 1\}$. Здесь окружность $\Gamma$ есть естественная граница аналитич. функции $f_{0}(z)$, аналитич. продолжение $f_{0}(z)$ за пределы круга $U(0,1)$ невозможно.

Пусть в достаточно малой окрестности $V(a)=\{z \in \overline{\mathbb{C}}:$ $|z-a|<R\}$ точки $a \neq \infty$ (или $V(\infty)=\{z \in \overline{\mathbb{C}}:|z|>R\}$ ) аналитич. продолжение элементов, получаемых вдоль путей определенного класса $\{L\}$, возможно во все точки, отличные от $a$, т. е. по всем путям, расположенным в проколотой окрестности $V^{\prime}(a)=$ $=\{z \in \overline{\mathbb{C}}: 0<|z-a|<R\} \quad$ (соответственно $V^{\prime}(\infty)=$ $=\{z \in \mathbb{C}: R<|z|<\infty\})$; тогда O. т. $(a,\{L\})$ наз. изолированной особой точкой. Если при этом аналитич. продолжение элементов, получаемых вдоль путей класса $\{L\}$, по всевозможным замкнутым путям, расположенным в $V^{\prime}(a)$, не изменяет этих элементов, то изолированная 0 . т. $(a,\{L\})$ наз. о с о б о й т о ч-

\includegraphics[max width=\textwidth, center]{2023_07_08_48400f45530c73855d1ag-1}
О. т. может быть полюсом или существенно особой точкой: если существует бесконечный предел $\lim f(z)=$ $=\infty$ при стремлении $z \rightarrow a$ вдоль путей класса $\{L\}$, то О. т. однозначного характера $(a,\{L\})$ наз. полюсом; если не существует никакого конечного или бесконечного предела $\lim f(z)$ при стремлении $z \rightarrow a$ вдоль путей класса $\{L\}$, то $(a,\{L\})$ - существенно особая точка; случай конечного предела соответствует регулярной паре $(a,\{L\})$. Если же аналитич. продолжение әлементов, получаемых вдоль путей класса $\{L\}$, по замкнутым путям, окружающим в $V^{\prime}(a)$ точку $a$, изменяет эти әлементы, то изолированная О. т. $(a,\{L\})$ наз. өетөления точкой, или о с о бо й точ к о й м ног о в н а ч о г о х а p а к т е а. Класс точек ветвления, в свою очередь, подразделяется на алгебраические точки ветвления и трансчендентные точки ветвления (включая погарибмические точки ветвления). Если после нек-рого конечного числа $m \geqslant 2$ однократных обходов точки $a$ в одном и том же направлении в $V^{\prime}(a)$ әлементы, получаемые вдоль путей класса $\{L\}$, принимают исходный вид, то $(\{a,\{L\})$ есть алгебраич. точка ветвления и число $m-1$ наз. ее п ор я д к о м. В противном случае, когда обходы точки $a$ дают все вовые и новые әлементы, $(a,\{L\})$ есть трансдендентная точка ветвления. Напр., для функции

\[
f(z)=\frac{1}{(1+\sqrt{z})(1+\sqrt[6]{z})}
\]

точки $a=0, \infty$ (для всех путей) являются алгебрапч. точками ветвления порядка 5. Как однозначную функцию точки $f(z)$ можно представить только на соответствующей римановой поверхности $S$, состоящей из 6 листов над $\overline{\mathbb{C}}$, определенным образом соединенных над точками $0, \infty$. Кроме того, над точкой $a=1$ расположены три правильные ветви $f(z)$, однозначные на трех соответствующх листах $S$; на одном листе $S$ расположен полюс второго порядка и на двух листах $S$ - полюсы первого порядка. Вообще, привлечение понятия римановой поверхности оказывается весьма удобным и плодотворным при изучении характера O. T.

Если радиус сходимости исходного ряда (1) $R=\infty$, то он представляет целую бункцию $f(z)$, голоморфную во всей конечной плоскости $\mathbb{C}$. Такая функция в случае $f(z) \neq$ const имеет единственную изолированную O. т. $a=\infty$ однозначного характера; если при этом $a=\infty$ - полюс, то $f(z)$ есть, це ла я р а ц и о на льная фу укция, или мно гочлен; если же $a=\infty$ - существенно особая точка, то $f(z)$ есть ц елая т р ан с ценд ен тная фу унция.

Мероморфная функция $f(z)$ в конечной плоскости $\mathbb{C}$ получается, когда аналитич. продолжение ряда (1) приводит к однозначной аналитич. функции $f(z)$ в $\mathbb{C}$, имеюцей в $\mathbb{C}$ в качестве 0 . т. только полюсы. Если при этом и $a=\infty$ есть полюс или регулярная точка, то общее число всех полюсов $f(z)$ в расширенной плоскости $\overline{\mathbb{C}}$ конечно и $f(z)$ есть р а ц и о на л ь на я ф у к к и я. Для трансцендентной мероморфной функции $f(z)$ в $\mathbb{C}$ бесконечно удаленная точка $a=\infty$ может оказаться предельной точкой полюсов - это простей ший пример неизолированной $\mathrm{O}$. Т. однозначной аналитич. функции. Мероморфоная функция в произвольной области $D \subset \overline{\mathbb{C}}$ определяется аналогично.

Вообще говоря, проекции неизолированных $\mathrm{O}$ т. могут образовывать различные множества точек расширенной комплексной плоскости $\overline{\mathbb{C}}$. В частности, какова бы ни была область $D \subset \overline{\mathbb{C}}$, существует аналитич. Функция $f_{D}(z)$ в $D$, для к-рой $D$ является ее естественной областью существования, а граница $\Gamma=$ $=\partial D$ - естественной границей, так что аналитич. продолжение функции $f_{D}(z)$ за пределы области $D$ невозможно. При этом естественная граніца $\Gamma$ состоит из достижимых и недостижимых точек (см. Граничные алементы). Если точка $a \in \Gamma$ достижима вдоль путей класса $\{L\}$ (таких классов может быть и несколько), расположенных целиком, кроме конечной точки $a$, в области $D$, то над ней необходимо расположены только О. т. функции $f_{D}(z)$, т. к. в противном случае было бы возможно аналитич. продолжение $f_{D}(z)$ за пределы области $D$ через нек-рую часть $\Gamma$ в окрестности точки $a$; достижимые точки образуют плотное множество на $\Gamma$.

В качестве определяющего элемента аналитич. функции $f(z)$ многих комплексных переменных $z=$ $=\left(z_{1}, \ldots, z_{n}\right), n>1$, можно принять, начр., в $\mathrm{e}$ ӥ е $\mathrm{p}$ ш т р а с с о в э л е м е н т $\left(U^{n}(\zeta, R), f_{\zeta}\right)$ в виде кратного степенного ряда

$f_{\zeta}=f_{\zeta}(z)=\sum_{k_{1}, \ldots, k_{n}=0}^{\infty} c_{k_{1} \ldots k_{n}}\left(z_{1}-\zeta_{1}\right)^{k_{1}} \ldots\left(z_{n}-\zeta_{n}\right)^{k_{n}}$

I Іоликруга сходимості этого ряда

\[
U^{n}(\zeta, R)=\left\{z \in \mathbb{C}^{n}:\left|z_{v}-\zeta_{v}\right|<R_{v}, v=1, \ldots, n\right\},
\]


\end{document}