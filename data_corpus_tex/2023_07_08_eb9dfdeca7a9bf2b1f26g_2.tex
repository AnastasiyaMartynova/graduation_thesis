\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}

\begin{document}
Кроме того, $N(12) \geqslant 5, N(33) \geqslant 3, N(35) \geqslant 4, N(40) \geqslant 4$, $N(45) \geqslant 4$ и доказано, что $N(n) \rightarrow \infty$ при $n \rightarrow \infty$; напр., $N(n) \geqslant n^{1 / 17}-2$ при достаточно большом $n$ (cM. [2]).

Множество из $n-1$ попарно 0 . Л. к. порядка $n$ наз. П о л н Ы м. При $n>4$ множество из $n-3$ попарно О. л. к. всегда может быть дополнено до полного. В настоящее время (1983) полные множества известны только в случае $n=p^{k}$, где $k$ - натуральное, $p$ - простое qисла (то өсть $\left.N\left(p^{k}\right)=p^{k}-1\right)$. Если же $n=1(\bmod 4)$ или $n \equiv 2(\bmod 4)$ и свободная от квадрата часть числа $n$ содержит хотя бы один простой множитель $p \equiv 3(\bmod 4)$, то не существует полного множества попарно 0 . л. к. порядка $n$. Напр., не существует полных множеств при $n=2 p, \quad p \equiv 3(\bmod 4)$.

Полные множества попарно О. л. к. находят приложение в статистике при построении симметрических уравновешенных неполных блок-схем с параметрами $v=n^{2}+n+1, k=n+1, \lambda=1$. Полные множества могут интерпретироваться и как конечные проективные плоскости (см. [2]).

Предложено много методов построения О. л. к. (см. [2]). Все они созданы с целью получения как можно большего множества попарно О. л. к. порядка п. Каждый из методов может быть отнесен к одной из следующих двух групп. К первой группе принадлежат методы, характерной особенностью к-рых является то, что они дают способ построения «основного» латинского квадрата и указывают, как переставить в нем строки и столбды, чтобы получить ортогональный соквадрат. Ко второй группе относятся методы, к-рые используют известные приемы построения О. л. к. меньшего порядка для построения О. л. к. заданного порядка.

Если $A=\left\|a_{i j}\right\|$ - латинский квадрат порядка $n$, построенный на множестве $S$, то упорядоченный набор перестановок $\sigma_{i}, i \in S$, определяемых равенствами $\sigma_{i}(j)=$ $=a_{i j}$, однозначно определяет латинский квадрат $A$. He каждый упорядоченный набор перестановок соответствует какому-либо латинскому квадрату. Если $\boldsymbol{A}=$ $=\left[\sigma_{1}, \ldots, \sigma_{n}\right]$ и $B=\left[\tau_{1}, \ldots, \tau_{n}\right]-$ два латинских квадрата, заданных укаванным способом перестановками $\sigma_{i}$ и $\tau_{i}$ множества $S$, то $A \perp B$ тогда и только тогда, когда $\left[\sigma_{1}^{-1} \tau_{1}, \ldots, \sigma_{n}^{-1} \tau_{n}\right]$ - латинский квадрат. Если определить произведения $\alpha A=\left[\alpha \sigma_{1}, \ldots, \alpha \sigma_{n}\right], A \beta=\left[\sigma_{1} \beta, \ldots\right.$, $\left.\ldots, \sigma_{n} \beta\right]$, где $\alpha$ и $\beta-$ перестановки $S$, то, напр., $A \perp \alpha A$ тогда и только тогда, когда $\left[\sigma_{1}^{-1} \alpha \sigma_{1}, \ldots, \sigma_{n}^{-1} \alpha \sigma_{n}\right]-$ латинский квадрат.

Методы первой группы обычно используются в случае, когда $A$ - таблица умножения конечной группы $G$, то есть $a_{i j}=g_{i} g_{j}, g_{i}, g_{j} \in G, i, j \in S ;$ отличие одного метода от другого заключается в выборе группы $G$, в выборе взаимно однозначных отображений $\alpha, \beta$ группы $G$ на себя и в пспользовании произведений $\alpha A, A \beta$, $\alpha^{-1} A \alpha$ и т. д.

Еслік $G$ - ацдитивная групша, то условие $A \perp \alpha A$ сводится к тому, что $\alpha-0$ р т о м ор фи з м $G$, т. е. такое взаимно однозначное отображение $G$ на себя, что если для $g_{1}, g_{2} \in G$ выполняется равенство $\alpha\left(g_{1}\right)-g_{1}=$ $=\alpha\left(g_{2}\right)-g_{2}$, то $g_{1}=g_{2}$. Напр., пять попарно О. л. к. порядка 12 были найдены после определения четырех нетривиальных ортоморфизмов абелевой группы, являюпейся прямым произведением циклич. групп 6-го и 2-го порядков (см. [2], [6]).

Если $G-$ аддитивная группа конетного поля $G F\left(p^{r}\right)=\left\{a_{\mathrm{j}}=0, a_{1}=1, a_{2}, \ldots, a_{n-1}\right\}, n=p^{r}$, то все построения значительно упрощаются і получается следующее полное множество попарно О. л. к.:

\[
\begin{gathered}
A_{k}=\left\|a_{i j}^{k}\right\|, a_{i j}^{k}=a_{i} a_{k}+a_{j} ; i, j \in\{0,1, \ldots, n-1\}, \\
k \in\{1,2, \ldots, n-1\} .
\end{gathered}
\]

Следует отметить, что для $n$, удовлетворяющих условиям $n \not 2(\bmod 4), n \neq 3(\bmod 9)$ ІІ $n \neq 6(\bmod 9)$, оказалось возможным всегда построить такої латинский квадрат $A$ порядка $n$, что $A \perp A^{\top}$.

Использование прямого произведения латинских квадратов составляет основу следующего метода, относящегося ко второй групше. Пусть $A_{1}$ и $B_{1}-$ О. л. к. порядка $n$, построенные на множестве $X$, а $A_{2}$ и $B_{2}-$ O. л. к. порядка $m$, построенные на множестве $Y$, тогда прямые произведевия матриц $A_{1} \times A_{2}$ и $B_{1} \times B_{2}$ будут О. л. к. порядка $m n$, построенные на множестве $X \times Y$. Если $n=p_{1}^{k_{1}} p_{2}^{k_{2}} \ldots p_{r}^{k_{r}}$, то указанныӥ метод приводит к оценке $N(n) \geqslant \min ^{r}\left(p_{i}^{k_{i}}-1\right)$.

В основе многих других методов второй группы лежит следующее построение. Пусть $A_{1}, B_{1}, C_{1}-$ попарно О. л. к. порядка $m \geqslant 2 n$, построенные на $S_{1}=\{1,2, \ldots$, $\ldots, m\}, A_{2}, B_{2}-$ О. л. к. порядка $n$, построенные на $S_{2}=\{m+1, \ldots, m+n\}$. Чтобы получіть теперь два латинских квадрата $A$ и $B$ порядка $m+n$, построенные на $S=S_{1} \cup S_{2}$, добавляют к $A$ строки і столбцы с номерами $m+1, \ldots, m+n$ с незаполненными клетками, в результате чего получают частичный латинский квадрат порядка $m+n$, содержащий $A_{1}$ в верхнем левом углу. Клетки $A_{1}$ и $B_{1}$, имеюгце те же номера, что и клетки $C_{1}$, содержащие әлемент $i$, составляют общую для $A_{1}$ и $B_{1}$ $i$-ю трансверсаль, $i=1,2, \ldots$, . Элементы $i$-й трансверсали в $A_{1}$ при $i=1,2, \ldots, n$ помещают в $(m+i)$-й столбец (и в $(m+i)$-ю строку) в том же порядке, в каком они располагались в строках (соответственно столбцах) $A_{1}$, а на их место ставится число $m+i$. Остается в правый нижний угол частичного квадрата поставить $A_{2}$, чтобы завершить построение $A$.

Построение $B$ проводится аналогичным образом из $B_{1}$ и $B_{2}$, но только с использованием трансверсалей с номерами $n+1, n+2, \ldots, 2 n$. Квадраты $A$ и $B$ будут латинскими, но не обязательно ортогональными. Всегда мокно получить пару О. Л. к. порядка $m+n$ при $m=$ $=p^{k} \neq 13$, где $p$ - нечетное, и $n=(m-1) / 2$; показано, как, используя приведенное выше построение, получить пару О. л. к. порядка $n$ при $n \equiv 2(\bmod 4), n>6$ (см. [2]).

Приложения О. л. к. в статистике, теории информации и в теории планирования эксперимента требуют построения О. л. к. специального вида и перенесения понятия ортогональности на другие объекты. Так, обобщением О.л. к. являются ортогональные таблицы. Два частичных латинских квадрата одного порядка наз. о р т о го н а л в ш м и, если при наложении их друг на друга получаемые в клетках упорядоченные пары будут все различны. Говорят, что частичный латинский квадрат $A$ вложен в латинский квадрат $B$, если $A$ совпадает с нек-ройі подматрицей $B$ (за исключением пустых клеток $A$ ). Каждый квадрат из множества попарно ортогональных частичных латинских квадратов может быть вложен в латинский квадрат таким образом, что полученные латинские квадраты будут О. Л. К. (см. [6]).

Лuт.: [1] С а ч к о в В. Н., Комбинаторные методы дискретной математики, М., 1977; [2] D é n e s J., K e e d w e 11 A. D., Latin Squares and their applications, Bdpst, 1974; [3] Х о л л M.. Комбинаторика, пер. с англ., м., 1970; [4] Р а й з е р Г.- Д ж., Комбинаторная математика, пер. c англ., M., 1966; [5] $\mathbf{H}$ e p. 85-113; '[6] L i n d n e r C h., "Proc. Amer. Math.' Soc.", 1976 $\begin{array}{ll}\text { v. } 59 \text {, № 1, p. } 184-86 \text {. } & \text { B. Михеeв. }\end{array}$ ОРТОГОНАЛЬНЫЕ МНОГОЧЛЕНЫ - система Многочленов $\left\{P_{n}(x)\right\}$, удовлетворяюгих условию ортогональности

\[
\int_{a}^{b} P_{n}(x) P_{m}(x) h(x) d x=0, n \neq m,
\]

причем степень каждого многочлена $P_{n}(x)$ равна его Іиндексу $n$, а весовая функция (вес) $h(x) \geqslant 0$ на интервале $(a, b)$ или (в случае конечности $a$ и $b$ ) на отрез-


\end{document}