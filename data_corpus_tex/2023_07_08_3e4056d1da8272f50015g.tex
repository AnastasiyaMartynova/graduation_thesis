\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}

\begin{document}
тельно в замкнутой области $\bar{D}=D \cup C$ и удовлетворяющую на границе $C$ условиям:

\[
\left.\begin{array}{l}
\left.u\right|_{C}=f_{0}(y), \\
\left.\frac{\partial u}{\partial n}\right|_{C}=f_{1}(y), \ldots,\left.\frac{\partial^{m-1} u}{\partial n^{m-1}}\right|_{C}=f_{m-1}(y), \quad y \in C,
\end{array}\right\}
\]

где $\partial u / \partial n-$ производная по нормали к $C$, а $f_{0}(y)$, ..., $f_{m-1}(y)$ - заданные достаточно гладкие функции на достаточно гладкой границе $C$.

Лит.: [1] В е ку а И. Н., Новые методы решения эллиптических уравнений, М.-Л., 1948. ПОЛИГАРМОНИЧЕСКОЕ УРАВНЕНИЕ - см. Полигармоническая функиия.

ПОЛИНОМИАЛЬНЫЙ ИНТЕГРАЛ ДВИЖЕНИЯ см. Обратной задачи рассеяния метод.

ПОЛНАЯ АНАЛИТИЧЕСКАЯ ФУНКЦИЯ - совокУПНостЬ всех элементов аналитической функции, получающихся при всевозможных аналитических продолжениях исходной аналитической функции $f=f(z)$ комплексного переменного $z$, заданной первоначально в некоторой области $D$ расширенной комплексной плоскости $\overline{\mathbb{C}}$.

Пара $(D, f)$, состоящая из области $D \subset \overline{\mathbb{C}}$ и заданной в $D$ однозначной аналитической, или голоморфной, функции $f$, называется элементом аналитической функции, аналитическим элементом, или, короче, просто элементом. Всегда возможно, в частности, при задании аналитич. функции пользоваться круговым элементом $\left(U(a, R), f_{a}\right)$, состоящим при $a \neq \infty$ из степенного ряда

\[
f_{a}=f_{a}(z)=\sum_{k=0}^{\infty} c_{k}(z-a)^{k}
\]

и круга сходимости $U(a, R)=\{z \in \overline{\mathbb{C}}:|z-a|<R\}$ этого ряда с центром $a$ и радиусом сходимости $R>0$. В случчае $a=\infty$ круговой элемент $\left(U(\infty, R), f_{\infty}\right)$ состоит из ряда

\[
f_{\infty}=f_{\infty}(z)=\sum_{k=0}^{\infty} c_{k} z^{-k}
\]

и области сходимости этого ряда $U(\infty, R)=\{z \in \overline{\mathbb{C}}:|z|>R\}$, $R \geqslant 0$.

Пусть $E_{f}-$ множество всех тех точек $\zeta \in \overline{\mathbb{C}}$, в к-рые исходный элемент $\left(U(a, R), f_{a}\right)$ аналитически продолжается хотя бы по одному пути, связывающему в $\overline{\mathbb{C}}$ точки $a$ и $\zeta$. Следует иметь в виду возможность такой ситуации, когда в точку $\zeta \in E_{f}$ аналитич. продолжение возможно вдоль нек-рого класса путей $L_{1}$ и невозможно вдоль другого класса путей $L_{2}$. Множество $E_{f}$ есть область плоскости $\overline{\mathbb{C}}$. Полной ан а л и т чесской функ ц и ей (в смысле Вейерштрасса) $f_{W}$, порожденной элементом $\left(U(a, R), f_{a}\right)$, называется совокупность всех круговых элементов $\left(U(\zeta, R), f_{\zeta}\right), \zeta \in E_{f}$, получаемых при этом аналитич. продолжении вдоль всевозможных путей $L \subset \overline{\mathbb{C}}$. Область $E_{f}$ называется (в е й е рштрассовой) областью существования П.а.ф. $f_{W}$. Применяя элементы общего вида $(D, f)$, вместо круговых на самом деле получают ту же самую П.а. ф. $f_{W}$. Элементы $(D, f)$ П.а. ф. $f_{W}$ часто называют ветвями аналитической функции $f_{W}$. Любой элемент $(D, f)$ П. а. ф. $f_{W}$, будучи взят за исходный при аналитич. продолжении, приводит к той же самой П. а. ф. $f_{W}$. Каждый элемент $(U(\zeta, R)$, $\left.f_{\zeta}\right)$ П.а. ф. $f_{W}$ может быть получен из любого другого ее элемента $\left(U(a, R), f_{a}\right)$ посредством аналитич. продолжения вдоль нек-рого пути, связывающего в $\overline{\mathbb{C}}$ точки $a$ и $\zeta$.

Может оказаться, что исходный элемент $(D, f)$ не допускает аналитич. продолжения ни в одну точку $\zeta \notin D$. В этом случае $D=E_{f}$ является естественной областью су шествования, или областью голоморфности, функции $f$, а ее граница $\Gamma=\partial D-$ естественной гран и ц е й функции $f$. Напр., Для кругового элемента

\[
\left(U(0,1), f_{0}(z)=\sum_{k=0}^{\infty} z^{k !}\right)
\]

естественной границей является окружность

\[
\Gamma=\{z \in \overline{\mathbb{C}}:|z|=1\}
\]

его круга сходимости $U(0,1)$, так как этот элемент нельзя продолжить аналитически ни в одну точку $\zeta$ такую, что $|\zeta| \geqslant 1$. Какова бы ни была область $D \subset \overline{\mathbb{C}}$, можно построить аналитич. функцию $f_{D}(z)$, лля к-рой $D$ есть естественная область существования $f_{D}(z)$, а ее граница $\Gamma=\partial D-$ естественная граница $f_{D}(z)$.

Е. Д. Соломенцев.

ПОЛНАЯ ИНТЕГРИРУЕМОСТЬ - см. СИстема в Инволюции.

ПОЛнАЯ КРИВИЗНА - величина, равная произведению главных кривизн поверхности.

ПОЛНАЯ СИСТЕМА пе рв ы х ин те гралов-см. Первый интеграл.

ПОЛНАЯ СИСТЕМА ФУНКЦИЙ - см. Ортогональная система функций.

ПОЛНАЯ ФУНКЦИЯ ГРИНА - см. Грина функция в квантовой теории поля.

пОЛнЫЙ ИНTEГPAЛ - см. Дифференциальное уравнение с частными производными второго порядка.

ПОЛНЫЙ РЕЗОНАНС - сМ. МноговолновЫе процессы.

ПОЛНЫЙ ЭЛЛИПТИЧЕСКИЙ ИНТЕГРАЛ - см. Эллиптический интеграл.

ПОЛОЖИТЕЛЬНАЯ ОПРЕДЕЛЕННОСТЬ э Н Р Г И И гр а в и та ц и о н н о го п о л я - свойство положительности полной энергии изолированной гравитирующей системы с неотрицательной локальной плотностью материи. Наиболее последовательное определение энергии гравитационного поля дается в рамках гамильтонова формализма общей теории относительности; при этом полная энергия может быть выражена через асимптотику гравитационного поля.

Пусть $N-4$-мерное многообразие с метрикой $g_{\alpha \beta}$ сигнатуры,$-+++ R_{\alpha \beta}-$ кривизна Риччи, $R-$ скалярная кривизна, $T_{\alpha \beta}-$ симметрич. тензор энергии-импульса материи, так что выполнены ура внен и я Эйнштейна

\[
R_{\alpha \beta}-\frac{1}{2} g_{\alpha \beta} R=T_{\alpha \beta} \text {. }
\]

Предполагается, что задана полная пространственно-подобная трехмерная гиперповерхность $M \subset N$ (поверхность начальных данных) такая, что дополнение $\widetilde{M}=M \backslash K$ к нек-рому компактному подмножеству $K$ диффеоморфно дополнению шара в $\mathbb{R}^{3}$, и в индуцированных декартовых координатах метрика в $\tilde{M}$ имеет вид:

\[
g_{i j}=\delta_{i j}+O\left(r^{-1}\right), \partial_{k} g_{i j}=O\left(r^{-2}\right), \partial_{k} \partial_{l} g_{i j}=O\left(r^{-3}\right)
\]

кроме того, вторая фундаментальная форма $\pi_{i j}$ поверхности $\tilde{M} \subset N$ удовлетворяет условиям

\[
\pi_{i j}=O\left(r^{-2}\right), \partial_{k} \pi_{i j}=O\left(r^{-3}\right) .
\]

Эти асимптотич. условия отражают изолированность гравитируюшей системы; при этом говорят, что $N$ асимптотически плоско вдоль $M$.

Полная энергия $E$ и полный импульс $P_{k}$ определяются как пределы интегралов по сферам $S_{r}$ радиуса $r$ в $\widetilde{M}$ :

\[
\begin{aligned}
E & =\lim _{r \rightarrow \infty} \int_{S_{r}}\left(\partial_{j} g_{i j}-\partial_{i} g_{i j}\right) d s_{i}, \\
P_{k} & =\lim _{r \rightarrow \infty} \int_{S_{r}} 2\left(\pi_{i k}-\delta_{i k} \pi_{j j}\right) d s_{i} .
\end{aligned}
\]

Далее предполагается, что $T_{\alpha \beta}$ удовлетворяет локальному условию положительности энергии $T_{00} \geqslant\left(T_{0 i} T_{0}^{i}\right)^{1 / 2}$ в любой локальной лоренцевой системе координат.


\end{document}