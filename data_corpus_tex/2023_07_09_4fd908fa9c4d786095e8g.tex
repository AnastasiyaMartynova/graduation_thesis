\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}

\begin{document}
Іодставляя (7) при $N=k-1, k, k+1$ в (6), определяют параметры $\alpha_{k+1}, \beta_{k+1}$ метода (3):

\[
\alpha_{k+1}=4 \delta_{k+1} /(M-m), \beta_{k+1}=-\delta_{k} \delta_{k+1},
\]

где

Таким образом, вычисляя $\alpha_{k+1}, \beta_{k+1}$ по формулам (9), (10), получают Ч. и. м. (3), к-рый при любом $N \supseteqq 1$ оптимально уменьшает $\left\|\varepsilon^{N}\right\|$.

В оптимальном методе (2) для заданного $N$ параметры $\alpha_{k+1}$ выбирают в соответствии с перестановкой $\chi_{N}$ по формуле (5) так, qтобы было (7), т. е.

\[
\begin{aligned}
& \gamma_{j}=2\left(M+m-(M-m) \cos \pi \psi_{j}\right)^{-1}, \\
& \psi_{j}=\left(2_{j}-1\right) /(2 N), j=1,2, \ldots, N .
\end{aligned}
\]

Тогда после $N$ итераций для $\left\|\varepsilon^{N}\right\|$ будет справедливо неравенство (8).

Важной проблемой при малых $m / M$ является вопрос об устойч ивости метода (2), (5), (11). Неосмотрительный выбор $x_{N}$ может привести к катастрофич. возрастанию $\left\|u^{k}\right\|$ для нек-рых $1 \leqslant k \leqslant N$, к потере значащих цифр, к возрастанию ошибок округления, допущенных на промежуточных итерациях. Известны алгоритмы, перемешивающие параметры (11) и гаравтирующие устойчивый счет: для $N=2^{p}$ см. ст. Итерационный алгоритм, а для $N=3^{p}$ один из алгоритмов построения $x_{N}$ следующий: пусть $x_{1}=(1), \quad$ а $x_{3 r-1}=$ $=\left(j_{1}, j_{2}, \ldots, j_{3} r-1\right)$-построена, тогда образуют по правилу

\[
\begin{gathered}
x_{3 r}=\left(j_{1}, 2 \cdot 3^{r-1}+j_{1}, 2 \cdot 3^{r-1}+1-j_{1}, \ldots, 2 \cdot 3^{r-1}+\right. \\
\left.+1-i_{3} r-1\right) \\
r=1,2 \ldots, p .
\end{gathered}
\]

Существует класс методов (2) - устойчивые бесконечно продолжаемые оптимальные Ч. п. м. - позволяющих продолжить метод (2), (5), (11) после $N$ итераций так, чтобы он был устойчив и вновь становился оптимальным для нек-рой последовательности $N_{i} \rightarrow \infty$. Для случая $N_{i}=3 i N$ нз представления

\[
T_{3 \boldsymbol{N}}(x)=T_{N}(x)\left(2 T_{N}(x)+\sqrt{\overline{3}}\right)\left(2 T_{N}(x)-\sqrt{\overline{3}}\right)
\]

видно, что треть параметров $P_{3 N}(t)$ совпадает с (11). Если после $N$ итераций продолжить итерацип (2), (5), (11) далее, взяв в (11) за $\psi_{j} 2 N$ значений:

\[
\tilde{\psi}_{j}=(2 j-1) /(6 N), 2 j \neq 1(\bmod 3),
\]

то снова получается после $3 N$ итераций Ч. и. м. Для обеспечения устойчивости множество (14) разбивают на два: к $i$-му, $i=1,2$, относят те $\tilde{\psi}_{j}$, для к-рых $\pi \cos \tilde{\psi}_{j}$ является корнем $i$-й скобки в (13); внутри каждого из подмножеств $\tilde{\psi}_{j}$ перемешивают с помощью перестановки $x_{N}$. При $N<k<2 N$ употребляют в (5), (11) элементы первого множества, при $2 N<k \leqslant 3 N-$ второго, тем самым определяется перестановка $x_{3 N}$. Продолжив аналогичным образом процесс образовавия параметров, получают бесконечную равномерно распределенную ва $[0,1]$ последовательность $\left\{\omega_{j}\right\}_{1}^{\infty}$, наз. $T$-последовательностью, для к-рой метод [2] становится оптимальным при $N_{i}=3^{i} N$ и

\[
\begin{gathered}
\alpha_{k+1}=2\left(M+m-(M-m) \cos \pi \omega_{k+1}\right)^{-1}, \\
k=0,1, \ldots .
\end{gathered}
\]

Теория Ч. и. м. (2), (3) может быть распространена на век-рый класс весамосопряженных операторов, когда $\operatorname{Sp} A$ лежит на нескольких отрезках или внутри нескольких специальной формы областей (в частности, эллипса), или, когда известна информация о распреде- лении начальної опибки, на случай комбинированных с методом сопряженных градиентов Ч. и. м., на задачи частичной проблемы собственных знаqениї.

Одним из әффективных приемов ускорения сходимости итераций (2), (3) является предварительное преобразование уравнения (1) к әквивалентному уравнению вида

\[
B A u=B f,
\]

и применение уже к этому уравнению Ч. и. м. Оператор $B$ определяют, руководствуясь двумя факторами: 1) ттобы алгоритм вычисления величин вида $B v$ был нетрудоемким, 2) чтобы $\operatorname{Sp}(B A)$ принадлежал множеству, для к-рого обеспечена быстрая сходимость Ч. и. м.

Лит.: 【1Ј М а р ч у к Г. И., Ј е 6 е д е в В. И., Численные методы в теории переноса нейтронов, 2 изд., М., 1981; [2]
Б а х в а л о в Н. С., Численные методы, 2 изд., М., 1975; Б a х в а л о в Н. С., Численные методы, 2 изд., М., 1975; 2 изд., М., 1980: [4] С' а м а р с к и й А. А., Теория разностных схем, М., 1977; [5] J е б е д е в В. И., Ф и н ог е н о в С. А., «Ж. вычисл. матем. и матем. физ.», 1971, т. 11, № 2, с. 425-38; Ј е б е д е в В. И., там же, 1969, т. 9, № 6, с. 1247-52; е г о ж е, в кн.: Матем. анализ и смежные вопросы математики, $\begin{array}{ll}\text { е г о же, в кн.: Матем. анализ и смежные вопросы математики, } \\ \text { Новосиб., } 1978, \text { с. } 89-108 . & \text { В. И. Лебедев. }\end{array}$

ЧЕБЫІІІЕСКИЙ РАДИУС О $\mathrm{r} \mathrm{p}$ а н и ч е н м н о же с т в а $M$ из метрич. пространства $(X, \rho)-$ точная нижняя грань радиусов всех шаров, содержащих $M$ (см. Чебышевский центр). ЧЕБЫIIЕВСКИЙ ЦЕНТР О г $p$ а н и ч е н н 0 г 0 м в о ж е с т в а $M$ из метрич. пространства $X=(X, \rho)$ - элемент $x_{0} \in X$, для к-рого

\[
\sup _{y \in M} \rho\left(x_{0}, y\right)=\inf _{x \in X} \sup _{y \in M} \rho(x, y) .
\]

Величина (*) есть чебышевский радиус множества $M$. Если линейное нормированное пространство является сопряжевным к нек-рому линейному нормированному пространству, то любое ограниченное множество $M \subset X$ имеет хотя бы один Ч. ц. Существует банахово пространство и трехточечное множество в нем, не имеющее Ч. ц. Для того чтобы каждое ограниченное множество банахова пространства $X$ имело не более одного Ч. ц., необходимо и достаточно, чтобы $X$ было равномерно выпуклым по каждому направлению, т. е. чтобы для любого $z \in X$ и любого $\varepsilon>0$ существовало такое число $\delta=\delta(z, \varepsilon)>0$, что если $\left\|x_{1}\right\|=\left\|x_{2}\right\|=1, \quad x_{1}-x_{2}=\lambda z \quad$ и $\left\|x_{1}+x_{2}\right\| \geqslant 1-\delta$, то $|\lambda| \leqslant \varepsilon$. Ч. ц. каждого ограниченного множества $M$ из линейного нормированного простра́нства $X$ размерности большей двух принадлежит выпуклой оболочке этого множества тогда и только тогда, когда $X$ гильбертово. Ч. ■.- частный случай более общего понятия наилучшей $N$-сети.

Лит.: [1] Итоги науки. Математический анализ. 1967, М., 1969 , с. $75-132$.

ЧЕБЫШЕВСКОЕ МНОЖЕСТВО - такое множество $M$ в метрич. пространстве $(X, \rho)$, что для любого $x \in X$ в $M$ существует единственный наилучшего приближения элемент, т. е. элемент $y \in M$, для к-рого $\rho(x, y)=\rho(x$, $M)$. Существование и единственность элемента наилучшего приближения являются простейшими, естественными требованиями, весьма удобными как с теоретической, так и с вычислительной точек зрения. Это и определяет роль Ч. м. в теории приближений и теории банаховых пространств. Логически понятие Ч. м. является развитием понятия Чебышева системы.

Конечномерное векторное подпространство $L \subset C(Q)$ с базисом $\varphi_{1}, \ldots, \varphi_{n}$ тогда и только тогда является Ч. М. (че б ыше в ск и м п̈ од п о ос т ра н с т в о м), когда функции $\varphi_{1}, \ldots, \varphi_{n}$ образуют систему Чебышева (т. е. удовлетворяют Хаара условию). В евклидовом пространстве Ч. м. являются прямые, плоскости, выпуклые фигуры и тела. Нетривиальные примеры Ч. м. рассматривал впервые П. Л. Чебышев [1]. Это - подпространство алгебраич. многочленов степени $\leqslant n$ и


\end{document}