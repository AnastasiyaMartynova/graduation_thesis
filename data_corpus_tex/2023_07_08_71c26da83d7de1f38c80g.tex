\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{CJKutf8}
\usepackage{bbold}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\DeclareUnicodeCharacter{0131}{$\imath$}

\begin{document}
Если же непрерывность приближаемой функции не гарантирована или по условию задачи важна близость между $f(t)$ и $\varphi(t)$ лишь в среднем на $[a, b]$, можно использовать интегральные метрики пространств $L_{p}[a$, b], полагая

\[
\mu(f, \varphi)=\int_{a}^{b} q(t)|f(t)-\varphi(t)| p d t, p>0,
\]

где $q(t)$ - нек-рая весовая функция. Здесь наиболее употребительннм и удобным с практич. точки зрения является случай $p=2$ (см. Среднеквадратическое приближение функций).

П. Ф. м. может учитывать значения функций $f(t)$ и $\varphi(t)$ лишь в отдельных точках $t_{k}, k=1, \ldots, n$, іромежутка $[a, b]$, напр

\[
\begin{gathered}
\mu(f, \varphi)=\max _{1 \leqslant k \leqslant n}\left|f\left(t_{k}\right)-\varphi\left(t_{k}\right)\right|, \\
\mu(f, \varphi)=\sum_{k=1}^{n} q_{k}\left|f\left(t_{k}\right)-\varphi\left(t_{k}\right)\right|^{p},
\end{gathered}
\]

где $q_{k}$ - нек-рые положительные коэффициенты.

Аналогично определяется мера приближения функций двух и большего числа переменных.

Мера при ближения фу нкции $f(t)$ с е м е й с т в о ф ун к ци й $F$ обычно определяется как наилучшее приближение:

\[
E(f, F)=\mu(f, F)=\inf _{\varphi \in F} \mu(f, \varphi) .
\]

Под мерой приближения класса \begin{CJK}{UTF8}{mj}円\end{CJK} функциї $f(t)$ Функциями $\varphi(t)$ из фикскрованного множества $F$ понимают величину

\[
E(\mathfrak{M}, F)=\mu(\mathfrak{M}\}, F)=\sup _{f \in \mathbb{N} \mathfrak{N}} \inf _{\varphi \in F} \mu(f, \varphi),
\]

к-рая характеризует максимальное отклонение функций множества $\mathfrak{M}$ от ближайших к ним функций из $F$.

В общем случае, когда рассматривается приближение в произвольном метрич. пространстве $X$, мера приближения $\mu(x, u)$ элемента $x$ әлементом $u$ (множеством $F)$ есть расстояние $\rho(x, u)(\rho(x, F))$ между $x$ и $u(x$ и $F)$ в смысле метрики пространства $X$.

Лum.: [1] $\Gamma$ о н ч а p о в В JI., Теория интерполирования и приближения функций, 2 изд, M, $1954 ;[2]$ Н ико л гтеоремы вложения, 2 изд., $\mathrm{M}, 1977$

IРИВАЛОВА ОІЕРАТОРЫ, П р и в а л о в а, - операторы, позволяюшие выразить условие гармоничности функции без использования частных производных. Пусть $u(x)$ - локально интегрируемая функция в конечной области $D$ евклидова ıгространства $\mathbb{R} n, n \geqslant 2 ; \omega(h)$ - объем шара $B(x ; h)$ радиуса $h$ с центром $x \in D$, расположенного в $D$;

\[
\Delta_{h} u(x)=\frac{1}{\omega(h)} \int_{B(x, h)} u(y) d y-u(x) .
\]

\includegraphics[max width=\textwidth, center]{2023_07_08_71c26da83d7de1f38c80g-1(1)}
в а л о в а $\bar{\Delta}^{*} u(x)$ и $\Delta^{*} u(x)$ соответственно определяются формулами

\[
\begin{aligned}
& \bar{\Delta}^{*} u^{\prime}(x)=\varlimsup_{h \rightarrow 0}\left(\Delta_{h} u(x): \frac{h^{2}}{2(n+2)}\right), \\
& \Delta^{*} u(x)=\prod_{h \rightarrow 0}\left(\Delta_{h} u(x) \cdot \frac{h^{2}}{2(n+2)}\right) .
\end{aligned}
\]

Если верхний и нижний П. о. совпадают, то о п е р ат о р П р и в а л о в а $\Delta^{*} u(x)$ определяется формулой $\Delta^{*} u(x)=\bar{\Delta}^{*} u(x)=\Delta^{*} u(x)=\lim _{h \rightarrow 0}\left(\Delta_{h} u(x): \frac{h^{2}}{2(n+2)}\right)$.

Если функция $u(x)$ имеет непрерывные частные производные до 2-го порядка включительно в точке $x \in D$, то в этой точке существует П. о. $\Delta^{*} u(x)$, п ов равен значению оператора Лапласа: $\Delta^{*} u(x)=\Delta u(x)$. Справедлива т е о р е м а П р и в а л о в а: есліг непрерывная в области $D$ функция $u(x)$ удовлетворяет всюду в $D$ условию

\[
\underline{\Delta}^{*} u(x) \leqslant 0 \leqslant \bar{\Delta}^{*} u(x),
\]

то $u(x)$ - гармонич. функция в $D$. Отсюда вытекает, что непрерывная функция $u(x)$ в $D$ является гармонической тогда и только тогда, когда во всякой точке $x \in D$, начиная с достаточно малого $h, \Delta_{h} u(x)=0$ или, пначе,

\[
u(x)=\frac{1}{\omega(h)} \int_{B(x ; h)} u(y) d y .
\]

Среднее значение по объему шара здесь можно заменить средним значением по площади сферы.

Jum.: [1] П р и в а л о в И. И., «Матем. сб.", 1925, т. 32, c. $464-71$, [2] е г о Ж е, Субгармонические функции, М.-
Ј, 1937, [3] Б р л о М., Основы классической теории потенциала, пер. с франц., М., $1964 . \quad E$. Д. Соломенчев.

ПРИВАЛОВА ТЕОРЕМА - 1) П. т. о с о п р яже н ны ф ф у н ция х: пусть

\[
f(t)=\frac{a_{0}}{2}+\sum_{k=1}^{\infty}\left(a_{k} \cos k t+b_{k} \sin k t\right)
\]

\begin{itemize}
  \item периодическая непрерывная функция с периодом $2 \pi$ и
\end{itemize}

\[
\tilde{f}(t)=\frac{\tilde{a_{0}}}{2}+\sum_{k=1}^{\infty}\left(b_{k} \cos k t-a_{k} \sin k i t\right)
\]

\begin{itemize}
  \item тригонометрически сопряженная функция с $f(t)$; тогда если $f(t)$ удовлетворяет условию Липшица с показателем $\alpha, f \in \operatorname{Lip} \alpha, 0<\alpha \leqslant 1$, то $\tilde{f} \in \operatorname{Lip} \alpha$ при $0<\alpha<1$ и $\tilde{f}$ имеет модуль непрерывности, не больпий $M \delta \ln (1 / \delta)$ при $\alpha=1$. Эта теорема, доказанная И. И. Приваловым [1], имеет важные применения в теории тригонометрич. рядов. Она переносится и на условия Липшица в нек-рых других метриках (см., напр., [5]).
\end{itemize}

\begin{enumerate}
  \setcounter{enumi}{1}
  \item П. т. е динственности аналитиче-
\end{enumerate}

\includegraphics[max width=\textwidth, center]{2023_07_08_71c26da83d7de1f38c80g-1}
функция $f(z)$ в области $D$ плоскости комплексного переменного $z$, ограниченной спрямляемой жордавовой кривой $\Gamma$, на нек-ром множестве $E \subset \Gamma$ положительной меры Лебега на $\Gamma$ имеет нулевые угловые граничные значения, то $f(z) \equiv 0$ в $D$. Эта теорема доказана И. И. Приваловым [2]; ее обобщением является Лузина - Привалова теорема; см. также Единстөенности свойство аналитических функций.

\begin{enumerate}
  \setcounter{enumi}{2}
  \item П. т. о сингуля яном инте г р а ле Коши, ос но вная ле мма П рива ло о а,один из основных результатов теории интеграла типа Коши - Стилтьеса (см. Кошu интеграл). Пусть $\Gamma: \zeta=$ $=\zeta(s), 0 \leqslant s \leqslant l,-$ спрямляемая (замкнутая) жорданова кривая на плоскости комплексного переменного $z, l-$ длина кривой $\Gamma, s$ - длина дуги на $\Gamma$, отсчитываемая от нек-рой фиксированной точки; $\varphi=\varphi(s)$ - угол между положительным направлением оси абсцисс и касательной к $\Gamma, \psi(s)$ - комплексная функция ограниченной вариации на $\Gamma$. Пусть точка $\zeta_{0} \in \Gamma$ определяется значением $s_{0}$ длины дуги, $\zeta_{0}=\zeta\left(s_{0}\right), 0 \leqslant s_{0} \leqslant l$, и $\Gamma_{\delta}-$ часть линии $\Gamma$, оставшаяся после удаления из $\Gamma$ меньшей дуги, концами к-рой являются точки $\zeta\left(s_{0}-\delta\right)$ и $\zeta\left(s_{0}+\delta\right)$. Конечный предел при $\delta \rightarrow 0$
\end{enumerate}

\[
\lim _{\delta \rightarrow 0} \frac{1}{2 \pi i} \int_{\Gamma_{\delta}} \frac{e^{i \varphi(s)} d \psi(s)}{\zeta-\zeta_{0}}=\frac{1}{2 \pi i} \int_{\Gamma} \frac{e^{i \varphi(s) d \psi(s)}}{\zeta-\zeta_{0}},
\]

если он существует, наз. с и н г у л я р ны м и т тег ралом Коши-Стилтьеса. Пусть $D+$ и $D$ - - соответственно конечная и бесконечная области, ограничиваемые кривой $Г$. Ф о р м у л и р о в к а П. т.: если для почти всех по мере Јебега на $\Gamma$ точек $\Gamma$ существует сингулярный интеграл (1), то почти всюду


\end{document}