\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }
\usepackage{bbold}

\begin{document}
что составляет содержание т е о р е м ы А р ц е л а А с к о ли. Іонятіе Р. н. переносится на толологич. пространства.

Лum.: [1] К о лм о г о р о в А. Н., Ф о м и н С. В., Элементы теории функций и функционального анализа, 5 изд., М., $1981 ;[2]$ Э д в а р д с Р., Функциональный анализ, пер. с англ.,
М., 1969. МАВНОСХОДЯ ЦИЕСЯ РЯДЬІ - такие сходящиеся или расходящиеся числовые ряды $\sum_{n=1}^{\infty} a_{n}$ и $\sum_{n=1}^{\infty} b_{n}$, разность к-рых является сходящимся рядом с суммой, равной нулю: $\sum_{n=1}^{\infty}\left(a_{n}-b_{n}\right)=0$. Если же их разность является лишь сходящимся рядом, то исходные ряды

\includegraphics[max width=\textwidth, center]{2023_07_08_a0b2d380b4a5d9bd0f8fg-1}
с м Ы с л e.

Если $a_{n}=a_{n}(x)$ и $b_{n}=b_{n}(x)$ - функции, напр. $a_{n}$ : $X \rightarrow \mathbb{R}, b_{n}: X \rightarrow \mathbb{R}$, где $X$ - Іроизвольное множество, a $\mathbb{R}$ - множество действительных чисел, то ряды $\sum_{n=1}^{\infty} a_{n}(x)$ и $\sum_{n=1}^{\infty} b_{n}(x) \quad$ наз. р а в н о м е р н о

\includegraphics[max width=\textwidth, center]{2023_07_08_a0b2d380b4a5d9bd0f8fg-1(2)}
носходящи ис я в широком смысле) на множестве $X$, если их разность есть ряд, к-рый равномерно сходится на $X$ и его сумма равна нулю (соответственно просто равномерно сходится на $X)$.

II $р$ и м е р. Если две интегрируемые на отрезке $[-\pi, \pi]$ функции равны на интервале $I \subset[-\pi, \pi]$, то их ряды Фурье - равномерно равносходящиеся на каждом интервале $I^{*}$, внутреннем к интервалу $I, \mathrm{a}$ сопряженные ряды Фурье - равномерно равносходящиеся на $I^{*}$ в широком смысле.

РАДЕМАХЕРА СИСТЕМА - ортонормированная на отрезке $[0,1]$ система $\left\{r_{k}(x)\right\}$. Введена X. Радемахером [1]. Функции $r_{k}(x)$ можно определить равенствами

\[
r_{k}(x)=\operatorname{sign} \sin 2^{k} \pi x, \quad x \in[0,1], k=1,2, \ldots
\]

Другое определение функций Радемахера $r_{k}(x)$ получается путем рассмотрения двоичных разложений чисел отрезка [0,1]: если в двоичном разлоякении числа $x$ на $k$-м месте стоит цифра 0 , то полагают $r_{k}(x)=1$, если же на $k$-м месте стоит 1 , 'го $r_{k}(x)=-1$; в случае же, когда $x=0$ или число $x$ допускает два разложения, полагают $r_{k}(x)=0$. Согласно этому огределению отрезок $[0,1]$ распадается на $2^{k}$ равных подинтервала, в каждом из которых функция $r_{k}(x)$ принимает попеременно значения +1 и -1 , а на концах подинтервалов $r_{k}(x)=0$.

Система $\left\{r_{k}(x)\right\}$ представляет типичный пример стохастически независимых функций и имеет применения как в теории вероятностей, так и в теории ортогональных рядов.

Одно из важных свойств Р. с. устанавливается т е ор е м о й $\mathbf{P}$ а д е м а х е р а: если $\sum c_{k}^{2}<+\infty$, то ряд $\sum c_{k} r_{k}(x)$ сходится почти всюду на $[0,1]$, и т е о р е м о й $\mathrm{X}$ ивчин а - К о лм о го ро в а: если $\sum c_{k}^{2}=+\infty$, то ряд $\sum c_{k} r_{k}(x)$ расходится почти всіоду на $[0,1]$.

Так как функции Радемахера в двоитно иррациональных точках интервала [0,1] принимают лишь значения \textbackslash pm 1 , то рассмотрение ряда $\sum c_{n} r_{n}(x)$ означает, что у членов ряда $\sum c_{n}$ выбирается распределение знаков \textbackslash pm 1 , зависящее от точки $x$. Если $x=0, \alpha_{1} \alpha_{2} \ldots$ $\alpha_{n} \ldots$. представление числа $x \in[0,1]$ в виде бесконечной двоичной дроби, то при $\alpha_{n}=0$ перед $c_{n}$ ставится знак + и пири $\alpha_{n}=1$ ставится знак - .

Вышеприведенные теоремы в терминах теории вероятностей означают, что если $\sum c_{n}^{2}<+\infty$, то ряд $\sum \pm c_{n}$ сходится для почти всех распределений знаков (сходится с вероятностыо 1), и если $\sum c_{n}^{2}=+\infty$, то ряд $\sum \pm c_{n}$ расходится для почти всех распределений знаков (расходится с вероятностью 1).

Наоборот, ряд теорем теории вероятностей можно сформулировать в терминах функций Радемахера. Напр., теорема Кантелли о том, что при игре «в герб и решетку» со ставкой 1 средний выигрыш с вероятностью 1 стремится к нулю, означает, что почти всюду на $[0,1]$ выполняется равенство

\[
\lim _{n \rightarrow \infty} \frac{1}{n} \sum_{k=1}^{n} r_{k}(x)=0 .
\]

Jum.: [1] R a d e m a c h e r H., "Math. Ann.", 1922, Bd 87,

\includegraphics[max width=\textwidth, center]{2023_07_08_a0b2d380b4a5d9bd0f8fg-1(1)}
гоналыных рядов, пер. с нем., М., 1958; [3] А ле к с и ч Г., $1963 ;$ [4] К а ц М., Статистическая независимость в теории вероятностей, анализе и теории чисел, пер. с англ., М., 1963.

РАДИАЛЬНОЕ ГРАНИЧНОЕ ЗНАЧЕНИЕ - ЗНачение функции $f(z)$, определенной в единичном круге $D=\{z \in \mathbb{C}:|z|<1\}$ в граничной точке $\zeta=e^{i \theta}$, равное пределу

\[
\lim _{r \rightarrow 1-0} f\left(r e^{i \theta}\right)=f^{*}\left(e^{i \theta}\right)
\]

функции $f(z)$ по множеству точек радиуса $H=\left\{z=r e^{i \theta}\right.$ : $0<r<1\}$, проведенного в точку $\zeta$. Термин «Р. г. з.» иногда употребляется в обобщенном смысле для фұункций $f(z)$, заданных в произвольных областях (включая многомерные) $D$, причем в качестве $H$ берется множество точек нормали (или ее аналога) к границе $D$, проведенной в граничной точке. Напр., в случае бикруга

\[
D=\left\{\left(z_{1}, z_{2}\right) \in \mathbb{C}^{2}:\left|z_{1}\right|<1,\left|z_{2}\right|<1\right\}
\]

под Р. г. з. в точке $\zeta=\left(e^{i \theta_{1}}, e^{i \theta_{2}}\right)$ понимается предел

\[
\lim _{r \rightarrow 1-0} f\left(r e^{i \theta_{1}}, r e^{i \theta_{2}}\right)=f^{*}(\zeta)
\]

Лum.: [1] М а р к у ш е в и ч А. И., Теория аналитических Функций, 2 изл., т. $1-2$, М.. 1967-68; [2] П р и в а л о в И. И., Граничные свойства аналитических функций, 2 изд., М.- Ј., Ј.,

РАДИАН - угол, соответствующий дуге, длина к-рой равна ее радиусу; солержит приблизительно $57^{\circ} 17^{\prime} 44^{\prime \prime}, 80625$. Р. принимается за единицу измерения углов при т. н. круговом, или радианном, измерении углов. Если круговая мера угла равна $a$ Р., то угол содержит $180^{\circ}$ a/л грапусов; обратно, угол в $n^{\circ}$ имеет круговую меру $\pi n^{\circ} / 180^{\circ} \mathrm{P}$ БCЭ-3.

РАДИКАЛ - 1) Р. - математический знак $V^{-}$(измененное латинское $r$ ), к-рым обозначают извлеqение корня, т. е. решение двучленного алгебраич. уравнения вида $x^{n}-a=0$. Под символом $\sqrt[n]{a}$ подразумевается один из корней этого уравнения.

Проблема решения алгебраич. уравнений над полем $\mathbb{C}$ комплексных чисел в Р.: выразить корни алгебраич. уравнения (с комшлексными коэффиццентами) через его коэффициенты с помоцью конечного числа действий сложения, вычитания, умножения, деления, возвышения в степень и извлечения корня. Уравнения выше 4-ї степени, вообще говоря, нельзя решить в Р. (см. lалуа теория).

Знак Р. исіользуется также для обозначения радикала идеала.

\begin{enumerate}
  \setcounter{enumi}{1}
  \item Р. в нек-ром классе алгебраических систем - понятие, связанное $\mathrm{c}$ понятием радикального свойства. Первые примеры Р. возникли в теории ассоциативных колец (см. подробнее Радикаль колец и алгебр), построение общей теории P. было начато С. Амицуром (S. Amitsur) и А. Г. Курошем. Теория Р. может быть развита в любой категории алгебраич. систем, обладающей нек-рыми необходимыми свойствами (напр., в категории мультиоператорных групп). Многие вопросы
\end{enumerate}

\end{document}