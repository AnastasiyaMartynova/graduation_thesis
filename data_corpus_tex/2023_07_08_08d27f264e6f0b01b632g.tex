\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}

\begin{document}
пространство полно тогда и только тогда, когда оно геодезически полно. В полном римановом пространстве любые две точки можно соединить кратчайшей (не обязательно единственной). На любом дифференцируемом многообразии можно ввести структуру полного риманова пространства.

Р и а а о в о прос т р анс тво ка к мно гооб разие со свя производная $\nabla$ наз. с и м м е т р и ч е с к о й и с о вм е с т н о й с ме т р и к о й $g$ пространства $M^{n}$, если выполняются условия симметричности

\[
\nabla_{X} Y-\nabla_{Y} X=[X, Y]
\]

\section{и совместности}
\[
Z\langle X, Y\rangle=\left\langle\nabla_{Z} X, Y\right\rangle+\left\langle X, \nabla_{Z} Y\right\rangle,
\]

где $X, Y, Z$ - дифференцируемые векторшые поля, а $[X, Y]$ - их коммутатор. Этими уеловиями производная $\nabla$ определяется однозначно через поле метрич. тензора g. В локальных координатах $\left\{x^{i}\right\}$ компоненты связности $\nabla$ имеют вид $\Gamma_{k j, i}=\left\langle\nabla_{k} \partial_{j}, \partial_{i}\right\rangle$ и совпадают с символом Кристоффеля 1-го рода, а

\[
\nabla X Y=\sum_{i=1}^{n}\left(\sum_{k=1}^{n} \frac{\partial Y^{i}}{\partial x^{k}} X^{k}+\sum_{j, k=1}^{n} \Gamma_{j k}^{i} X^{j} Y^{k}\right) \partial_{i}
\]

Аналогичной формулой определяется ковариантная производная любого тензора.

Векторное поле $Y(t)$ вдоль кривой $c(t)$ наз. п а р а лл е ль н ы м, если $\nabla_{c} \boldsymbol{Y}=0$. Аналитически параллельное поле $Y(t)$ определяется решением системы

\[
\frac{d Y^{i}}{d t}+\sum_{j, k=1}^{n} \Gamma_{j k}^{i} Y^{j} \frac{d x^{k}}{d t}, i=1, \ldots, n,
\]

где $x^{i}=x^{i}(t)$ - уравнения кривой $c(t)$. Решения әтой системы при различных начальных условиях дают отображение $\left(T M^{n}\right)_{c(0)}$ в $\left(T M^{n}\right)_{c(1)} ;$ оно оказывается изометричным и наз, п а р а л л ө л ьны м п е р е н ес е н и е м Л е ви-Ч и вит а. Результат перенесения зависит, вообще говоря, не только от конөчных точек $c(0)$ и $c(1)$, но и от самого пути $c(t)$. Кривая, для к-рой $\nabla c=0$, является геодезической, и это свойство геодезической можно взять за еө определение.

Подмногооб ра а ия риманова ї рос тр а н с т в а. Если $M^{k}, k \leqslant n,-$ дифференцируемое подмногообразие риманова пространства $M^{n}$, то в каждом касательном пространстве к $M^{k}\left(\left(T M^{k}\right)_{p} \subsetneq\left(T M^{n}\right)_{p}\right)$ индуцируется скалярное произведение и тем самым на $M^{k}$ возникает структура риманова пространства с метрич. тензором $a$, компоненты к-рого вычисляются по формулам

\[
a_{\alpha \beta}=\sum_{i, j=1}^{n} g_{i j} \frac{\partial x^{i}}{\partial u^{\alpha}} \frac{\partial x^{j}}{\partial u^{\beta}}, \quad \alpha, \beta=1, \ldots, k,
\]

где $x^{i}=x^{i}\left(u^{1}, \ldots, u^{k}\right)$ - уравнения $M^{k}$ в локальных координатах. Внешняя гөометрия $M^{k}, k \geqslant 2$, описывается вторыми квадратичными формами $B\left(v_{p}\right)$, к-рые определяются для каждой единичной нормали $v_{p}$ к $M^{k}$ формулой

\[
B\left(v_{p}\right)(X, Y)=-\left(\nabla X^{v}, Y\right),
\]

где $X$ и $Y$ - векторные поля на $M^{k}$, а $v$ - произвольное поле единичных нормалей, содержащее $v_{p}$. Для любой формы $B\left(v_{p}\right)$ определяются нормальные кривизны, главные направления и кривизны, средняя и полная кривизны и т. д. и выводятся уравнения Гаусса - Кoдацци - Риччи, связывающие коэффициенты первой и вторых квадратичных форм. Свойствами вторых форм характеризуются важные классы подмногообразий, напр. минимальные, вполне геодезические, выпуклые и т. п. Для $M^{1} \equiv c(t)$ (гладкая кривая) строится төория, аналогичная теории кривых в $E^{n}$, определяются первая, вторая и т. Д. кривизны и выводятся уравнения, аналогичные формулам Френе. Первая кривизна кривой $k_{1}$ обычно наз. $\mathrm{r}$ е о д е 3 и ч е с к о й к р и в и зн о й и вычисляется по формуле

\[
k_{1}=\left|\nabla_{\dot{c}} \dot{c}\right|
\]

если $t$ - длина дуги; в локальных координатах

\[
k_{1}=\sqrt{\sum_{i, j=1}^{n} g_{i j} \mu^{i} \mu^{j}}
\]

где

\[
\mu^{i}=\frac{d^{2} x^{i}}{d t^{2}}+\sum_{j, k=1}^{n} \Gamma_{j k}^{i} \frac{d x^{j}}{d t} \frac{d x^{k}}{d t}
\]

а $x^{i}=x^{i}(t)$ - уравнения $c(t)$.

Большой круг задач Р. г. связан с изометрическими погружениями одного риманова пространства в другое и изучением свойств этих погружений. Эти задачи трудны и исследованы мало (более подробно - в двумерном случае).

Экс поненци а льное отоб р а жение $\exp _{q}:\left(T M^{n}\right)_{q} \rightarrow M^{n}$ определяется условием: $\exp _{q} X=$ $=r$, где $r$ - конец дуги геодезической с началом в $q, \mathrm{c}$ направлением $X \in\left(T M^{n}\right)_{q}$ и длины $|X|$. Если в окрестности точки $p$ ввести систему координат, сопоставляя точке $p$ декартовы координаты точки $\exp _{q}^{-1} p \in\left(T M^{n}\right)_{q}$, то окажется

\[
\begin{aligned}
& \left.\Gamma_{j n}^{i}\right|_{x=q}=\left.\Gamma_{j k,} i\right|_{x=q}=0, \text { то есть } \\
& \left.\frac{\partial g_{j k}}{\partial x^{i}}\right|_{x=q}=0, \quad i, j, k=1, \ldots, n
\end{aligned}
\]

это - т. н. (римановы) но р м а л ь ны е ко о р дин а т ы.

К р и в и з н а. Если в окрестности точки $q$ ввести нормальные координаты, то компоненты метрич. тензора записываются в виде

$g_{i j}=\delta_{i j}-\frac{1}{3} \sum_{k, l=1}^{n} R_{i k, j l} x^{k} x^{l}+\sum_{k, l=1}^{n} \varepsilon_{i k, j l} x^{k} x^{l}$,

где $\varepsilon_{i k, j l} \rightarrow 0$ при $x^{i} \rightarrow 0, i, j, k, l=1, \ldots, n$. Отсюда выводится важное свойство римановой метрики: для любой точки $g \in M^{n}$ экспоненциальное отображение $\varphi \equiv$ $\equiv \exp _{q}:\left(T M^{n}\right)_{q} \rightarrow M^{n}$ обладает свойством

\[
\begin{aligned}
\rho_{\left(T M^{n}\right)_{q}}(X, Y) & =|X-Y|=\rho_{M^{n}}(\varphi(X), \varphi(Y)) \times \\
& \times(1+\varepsilon(X, Y)),
\end{aligned}
\]

где $\varepsilon(X, Y) \rightarrow 0$ при $|X|,|Y| \rightarrow 0$. Добиться более высокого по порядку малости совпадения метрик $M^{n}$ и $\left(T M^{n}\right)_{q}$ за счет удачного выбора отображения $\varphi$ в общем случае невозможно. Поэтому коэффициенты $R_{i k, j l}$ характеризуют отличие метрики $M^{n}$ от евклидовой метрики $\left(T M^{n}\right)_{q}$. Эти коэффициенты являются компонентами т. н. тен 3 ора к р и в и 3 ны, или тен зо р а Р и ма на- K р и с тоффел я (в точке $q$ ). В локальных координатах $\left\{x^{i}\right\}$ они выражаются через коэффициенты метрич. тензора и их первые и вторые производные по формуле

$R_{i k, j l}=\frac{1}{2}\left(\frac{\partial^{2} g_{i l}}{\partial x^{k} \partial x^{j}}+\frac{\partial^{2} g_{k j}}{\partial x^{i} \partial x^{l}}-\frac{\partial^{2} g_{i j}}{\partial x^{k} \partial x^{l}}-\frac{\partial^{2} g_{k l}}{\partial x^{i} \partial x^{j}}\right)+$ $+\sum_{a, b=1}^{n} g^{a b}\left(\Gamma_{k j, a} \Gamma_{i l, b}-\Gamma_{k l, a}, \Gamma_{i j, b}\right)$, $i, j, k, l, a, b=1, \ldots, n$.

С тензором кривизны связан целый ряд других понятий, также (с разњых сторон) характеризующих меру отли-


\end{document}