\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{mathrsfs}
\usepackage{bbold}

\begin{document}
\includegraphics[max width=\textwidth, center]{2023_07_08_91b9dede95c244c8aac8g-1}
нерегулярна тогда и только тогда, когда

\[
\sum_{i=0}^{\infty} \lambda_{i}^{-1}<\infty
\]

Пусть

\[
\begin{gathered}
\tau_{0 n}=\inf \{t>0: \xi(t)=n(\xi(0)=0)\}, \\
\tau=\lim _{n \rightarrow \infty} \tau_{0 n},
\end{gathered}
\]

тогда

\[
\mathbf{E} \tau=\sum_{i=1}^{\infty} \lambda_{i}^{-1}
\]

и при $Е \tau<\infty$ имеет место $P\{\tau<\infty\}=1$, т. е. траектория цепи $\xi(t)$ «с вероятностью 1 за конечное время уходит в бесконечность" (см. также Ветвящихся процессов регулярность).

Лuт.: [1] Ч ж у н К а й - л а й, Однородные цепи Маркова, пер. c англ., M. ${ }^{1964}$.

IЕРЕХОДНЫХ ВЕРОЯТНОСТЕЙ МАТРИЦА переходные вероятности за время $t$ однородной $M a p-$ кова цепи $\xi(t)$ с не более чем очетным множеством состояний $S$ :

\[
p_{i j}(t)=\mathbf{P}\{\xi(t)=j \mid \xi(0)=i\}, i, j \in S .
\]

П. в. м. $\| p_{i j}(t)$ ! цепей Маркова с дискретным временем и регулярных цепей Маркова (см. Переходные вероятности) с непрерывным временем при любых $t>0, i, j \in S$ удовлетворяют условиям

\[
p_{i j}(t) \geqslant 0, \sum_{j \in S} p_{i j}(t)=1,
\]

т. е. являются стохастическими матрицами, для нерегулярных цепей

\[
p_{i j}(t) \geqslant 0, \quad \sum_{j \in S} p_{i j}(t) \leqslant 1,
\]

такие матрицы наз. п о л у с т о х а с т и ч е с к и м и.

В силу основного свойства однородной цепи Маркова:

\[
p_{i j}(s+t)=\sum_{k \in S} p_{i k}(s) p_{k j}(t)
\]

семейство матриц $\left\{P_{t}, t>0\right\}$ образует полугруппу по умножению; в случае, когда время дискретно, эта полугруппа однозначно определяется матрицей $P_{1}$.

ПЕРЕХОДНЫХ ОПЕРАТОРОВ ПОЛУГРУПІІА полугруппа операторов, порождаемых переходной функцией марковского процесса. По переходной функции $P(t, x, A)$ однородного марковского процесса $X=\left(x_{t}\right.$, $\left.\xi, \mathcal{F}_{t}, \mathrm{P}_{x}\right)$ в фазовом пространстве $(E, \mathscr{B})$ можно построить нек-рые полугруппы линейных операторов $P t$, действующих в том или ином банаховом пространстве $\mathbb{B}$ (см., напр., [1]). Чаще всего в роли $\mathbb{B}$ берут пространство $\mathbb{B}(E)$ ограниченных действительных функций $f$ на $E$ с равномерной нормой (а для феллеровского процесса $X-$ пространство $\mathbb{C}(E)$ непрерывных фуункций с той же нормой) или пространство $\mathrm{V}(E)$ конечных счетно аддитивных функций $\varphi$ на $\mathscr{B}$ с полной вариацией в качестве нормы. В первых двух случаях полагают

\[
P^{t} f(x)=\int_{E} f(y) P(t, x, d y)
\]

в третьем

\[
{ }^{P t} \varphi(A)=\int_{E} P(t, y, A) \varphi(d y)
\]

(здесь $f$ и $\varphi$ принадлежат соответствующим пространствам, $x \in E, A \in \mathscr{B})$. Во всех этих случаях выполнено полугрупповое свойство: $P^{t} P^{s}=P^{t+s}, s, t \geqslant 0$, и любая из трех полугрупп $\left\{p^{t}\right\}$ наз. полу г р у п П о й пе реходных о пе раторов. В дальнейшем речь идет только о первом случае. Ин фини те 3 им а львы й опе ра тор $A$ полугруппы $\left\{p^{t}\right\}$ (он же- и нфи н и те з им а л ьн ы й о п е p a т о p п р ц ц с c a) определяется обычным образом:

\[
A f=\lim _{t \downarrow 0} t^{-1}\left(p^{t} f-f\right)
\]

для всех тех $f \in \mathbb{B}(E)$, для к-рых указавный предел существует как предөл в $\mathbb{B}(E)$. Предполагая, что $\underset{P}{P}(t$, $\boldsymbol{x}, \boldsymbol{A})$ при $A \in \mathscr{B}$ является измеримой функцией пары переменных $(t, x)$, вводят резольвенту $R \alpha$ процесса $X, \alpha>0$

\[
R \alpha f=\int_{0}^{\infty} e^{-\alpha t} P^{t} f d t, f \in \mathbb{B}(E) .
\]

Если $\left\|P_{f}-f\right\| \rightarrow 0$ при $t \downarrow 0$, то $A g=\alpha g-f$, где $g=R \alpha f$. При определенвых предположениях интеграл (*) существует и при $\alpha=0$, причем $g=R^{0} f$ удовлетворяет "уравнению Пуассона"

\[
A g=-f
\]

(по этой причине, в частности, $R^{0} f$ наз. п о т е в ц и ал о м функции $f$ ).

Знание инфинитезимального оператора позволяет найти важные характеристики исходного процесса; более того, вопросы классификации марковских процессов сводятся к описанию соответствующих им инфинитезимальных операторов (см. [2], [3]). Немаловажно и то обстоятельство, что инфинитезимальный оператор входит в уравнения, позволяющие находить средние значения различных функционалов от процесса. Так, при нек-рых предположениях функция $v(t, x)=\mathrm{E}_{x}\left[\exp \left\{\int_{0}^{t \wedge \zeta} c\left(x_{s}\right) d s\right\} f\left(x_{t \wedge \zeta}\right)\right], t \geqslant 0, x \in E$, является единственным не слишком быстро растущим по $t$ решением задачи $v_{t}^{\prime}=A v+c v, v(0, x)=f(x)$, где $\mathrm{E}_{x}$ математич. ожидание, отвечающее $\mathrm{P}_{x}$, a $t \wedge \zeta=\min (t, \zeta)$.

Orератор $A$ родственен характеристическому оператору $\mathfrak{A}$ (cм. [2]). Пусть $X$ - непрерывный справа марковский процесс в топологич. пространстве $E$. Для борелевской функции $f$ полагают

\[
\mathfrak{A} f(x)=\lim _{U_{\downarrow}}\left[\frac{\mathrm{E}_{x} f\left(x_{\tau}\right)-f(x)}{\mathrm{E}_{x} \tau}\right],
\]

если предел существует для всех $x \in E$, где $U$ пробегает систему окрестностей точки $x$, стягивающихся к $x$, и где $\tau$ - момент первого выхода $X$ из $U$ (при $\mathrm{E}_{x} \tau=\infty$ дробь, стоящую под знаком предела, приравнивают нулю). Во многих случаях вычисление Af сводится к вычислению $\mathfrak{A} f$.

Jum.: [1] F e 11 e r W., "Ann. Math.", 1952, v. 55, p. 468519 ; [2] Д ы н к и н Е. Б., Основания теории марковских процессов, М., 1959; [3] $\Gamma$ их м а н И. И., С к о р о х о Д А. В.,
Теория случаtных процессов, т. 2, М., 1973. ПЕРЕЧИСЛЕНИЯ ОПЕРАТОР - отображение мНожества всех множеств натуральных чисел в себя (т. е. отображение $2^{\mathbb{N}}$ в $2^{\mathbb{N}}$, где $\mathbb{N}$ - множество нату ральных чисел), определяемое следующим образом. Пусть $W_{z}$ - рекурсивно перечислимое множество с гёделевым вомером $\boldsymbol{z}, D_{u}$ - конечное множество натуральных чисел с канонич. индексом $u$ (то есть $D_{u}=\left\{x_{1}, x_{2}, \ldots\right.$,

\includegraphics[max width=\textwidth, center]{2023_07_08_91b9dede95c244c8aac8g-1(1)}
$\langle x, u\rangle-$ номер упорядоченной пары, состоящей из чисел $x$ и $u$, при нек-ром фиксированном взаимно однозначном рекурсивном кодировании пар. С каждым рекурсивно перечислимым множеством $W_{z}$ связана процедура, преобразующая любое множество натуральных чисел $B$ в нек-рое множество натуральных чисел $A$. А именно, если число $\langle x, u\rangle$ принадлежит множеству $W_{z}$ и конечное множество $D_{u}$ содержится


\end{document}