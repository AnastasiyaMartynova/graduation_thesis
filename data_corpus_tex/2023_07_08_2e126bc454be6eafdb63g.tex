\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
перпендикулярное $M O$, то есть направленное по касательной к поверхности сферы радиуса $l$. При этом безразлично, находится ли груз в положении $M$ в покое или движется и проходит через положение $M$ в какой-то момент времени $t$. В последнем случае груз, продолжая движение, совершит из положения $M$ за промежуток времени $d t$ действительное

\includegraphics[max width=\textwidth, center]{2023_07_08_2e126bc454be6eafdb63g-1}
элементарное перемещение $d s$, к-poe совпадает с одним из В. П. Этот результат имеет место всегда, когда связь стационарна (не изменяется со временем). Если же шарнир укреплен на ползуне, к-рый будет перемещаться, напр. вертикально вниз, то получится случай нестационарной связи (связи, изменяющейся со временем). Когда при этом груз в какой-то момент времени $t$ придет в положение $M$, то его B. п. из данного положения в этот момент времени будет по-прежнему любое бесконечно малое перемещение $\delta s$, перпендикулярное $M O$. Однако действительное перемещение, к-рое груз совершит за промежуток времени $d t$, продолжая свое движение из положения $M$ вместе со стержнем, не будет, очевидно, совпадать ни с одним из В. П. груза в положении $M$.

Если стержень $O M$ заменить нерастяжимой нитью, то связь станет неудерживающей. В этом случае В. П. груза из положения $M$ будут не только все перемещения, перпендикулярные нити, но и перемещения, направленные во внутрь сферы радиуса $l$ с центром в точке $O$. Если положение механич. системы однозначно определяется $n$ независимыми между собой параметрами $q_{1}, q_{2}, \ldots, q_{n}$, то В. П. каждой точки системы, положение к-рой определяется ее радиус-вектором $\boldsymbol{r}_{k}=\boldsymbol{r}_{k}\left(q_{1}, q_{2}, \ldots, q_{n}\right)$, будет:

\[
\delta s_{k} \equiv \delta \boldsymbol{r}_{k}=\sum_{i=1}^{n} \frac{\partial r_{k}}{\partial q_{i}} \delta q_{i}
\]

В случае нестационарных связей равенства, выражающие зависимость $\boldsymbol{r}_{k}$ от $q_{i}$, будут содержать время $t$ и $\boldsymbol{r}_{k}=\boldsymbol{r}_{k}\left(t, q_{1}\right.$, $\left.q_{2}, \ldots, q_{n}\right)$. Однако формула $\left(^{*}\right)$ при этом сохраняется, а время $t$ считается равным постоянной величине $t_{1}$, где $t_{1}-$ значение момента времени, в к-рый вычисляется В. п.

Понятие о В. п. используется в механике для определения условий равновесия и составления уравнений движения механич. систем (см. Возможных перемещений принцип).

C. M. Tаре.

ВОЗМОЖНЫХ ПЕРЕМЕЩЕНИЙ ПРИНЩИП, П р и ци П виртуальных пе ремеше ни й,- один из вариационных принципов классической механики: механическая система находится в равновесии в некотором положении тогда и только тогда, когда сумма элементарных (бесконечно малых) работ активных сил $F_{i}\left(t, r_{j}, \dot{r}_{j}\right) \in C^{1}$ на всяком возможном перемещении $\delta r_{i}$, выводящем систему из рассматриваемого положения $r_{i}$, равна нулю

\[
\sum_{i} F_{i} \delta r_{i}=0
\]

в любой момент времени $t$.

Уравнение $\left({ }^{*}\right)$ является обшим уравнением статики, сводящим любую задачу статики к математич. задаче исследования этого уравнения. Для частного случая потенциальных сил

\[
F_{i}=\operatorname{grad} r_{i} U\left(t, r_{1}, \ldots, r_{N}\right) ; U\left(t, r_{i}\right) \in C^{2}
\]

где $N$ - число точек системы, равенство $\left({ }^{*}\right)$ принимает вид $\delta U=0$

при любом $t$, то есть механич. система, подверженная действию потенциальных сил, находится в равновесии тогда и только тогда, когда силовая функция имеет стационарное значение. В. В. Румянцев.

ВОЗМУЩЕНИЙ ТЕОРИЯ - теория, представляющая решение физической задачи в виде ряда по степеням некото-

94 возмохных рого малого параметра $\lambda$ или нескольких малых параметров. Предполагается, что при $\lambda=0$ задача допускает точное решение $X^{(0)}$ (невозмущенное решение). Точное решение $X \equiv X(\lambda)$ удовлетворяет условию $X \rightarrow X^{(0)}$ при $\lambda \rightarrow 0$.

Если ряд В. т.

\[
X=\sum_{k \geqslant 0} \lambda^{k} X^{(k)}
\]

сходится при $\lambda<\lambda_{\text {max }}$, то для таких $\lambda$ ряд (1) - точное решение задачи. Если же (1) является асимптотич. рядом, то есть

\[
\left(X-X_{m}\right) \sim \lambda^{m+1} ; X_{m} \equiv \sum_{k=0}^{m} \lambda^{k} X^{(k)},
\]

то точное решение может содержать члены, к-рые в принципе невозможно описать по В. т. Функция $\exp (-C / \lambda)$, где $C$ - константа,- простейший пример такого рода членов. На практике обычно ограничиваются несколькими первыми приближениями, так как вычислительные трудности резко возрастают в более высоких порядках В. т. Поэтому при решении болышинства задач важно не поведение ряда (1) в целом, а то, насколько хорошо несколько первых членов ряда аппроксимируют точное решение. Вместе с тем в ряде областей, напр. в квантовой электродинамике, точность расчетов по В.т. очень высока (нек-рые эффекты рассчитаны с точностью до $10^{-9}$ ).

В. т. возникла в небесной механике в задаче трех тел. Задача двух тел (задача Кеплера) решается точно, а для решения задачи трех тел необходимо привлекать В. т. Если рассматривается, напр., движение Земли, то невозмущенная задача $(\lambda=0)$ сводится к изучению движения Земли только под влиянием Солнца. Влияние других планет рассматривается как возмущение, а роль малых параметров играют отношения масс планет к массе Солнца. При этом первоначальная орбита Земли (эллипс Кеплера) «возмущается», то есть константы, описывающие эллипс Кеплера невозмущенной задачи, становятся функциями времени, поведение к-рых определяется влиянием планет на движение Земли.

Схема построения решения по В. т. в общем виде может быть описана следуюшим образом. Имеется нек-рое уравнение, содержащее малый параметр $\lambda$, в связи с чем его решение ищется в виде (1). При подстановке (1) в исходное уравнение оно превращается в равенство вида

\[
\sum_{k \geqslant 0} \lambda^{k} A_{k}\left(X^{(0)}, X^{(1)}, \ldots, X^{(k)}\right)=0 .
\]

Из справедливости равенства (3) при всех достаточно малых $\lambda$ следуют равенства $A_{k}\left(X^{(0)}, X^{(1)}, \ldots, X^{(k)}\right)=0$. Получающаяся при $k=1,2, \ldots$ цепочка уравнений позволяет определять последовательно $X^{(1)}, X^{(2)}$ и т. д.

Одна из проблем В.т. связана с тем, что поведение отдельных членов ряда (1) может быть качественно отличным от поведения суммы. Это приводит к тому, что фактич. область применения В. т. оказывается существенно меньше области сходимости ряда (1). Следующий простой пример демонстрирует характер возникающих трудностей. Для точного решения $X=\sin (\omega+\lambda) t, \omega-$ константа, $t$ - время, ряд (1) принимает вид:

\[
X=\sin \omega t+\lambda t \cos \omega t-\frac{\lambda^{2} t^{2}}{2 !} \sin \omega t+\ldots
\]

Равенство (4) показывает, что в то время как точное решение - периодич. функция $t$, члены ряда (4) неограниченно возрастают при $t \rightarrow \infty$. Фактически параметром разложения в данном случае является не $\lambda$, а $\lambda t$ и, поскольку в реальных случаях приходится ограничиваться неболышим числом членов разложения, В. т. имеет смысл, только если $\lambda t \ll 1$. Такого рода проблемы возникают как в небесной механике (проблема вековых членов при описании движения планет), так и в теории нелинейных колебаний. Для устранения таких растущих со временем членов в механике и теории колебаний были разработаны усовершенствованные варианты В. т. Основная идея состоит в том, что разложение, аналогичное (1), ищется в виде разложения по функциям


\end{document}