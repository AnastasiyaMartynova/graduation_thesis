\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{mathrsfs}

\begin{document}
РЕЛЯТИВНАЯ ГЕОМЕТРИЯ - геометрия конфигурации, состоящей из двух поверхностей $S_{0}: \boldsymbol{n}=\boldsymbol{n}\left(u^{\mathbf{1}}\right.$, $\left.u^{2}\right)$ и $S: \boldsymbol{r}=\boldsymbol{r}\left(u^{1}, u^{2}\right)$, находящихся в Петерсона соответствии. Аналогия между этим соответствием и сфереически.м отображением позволила ввести понятия релятивнойі площади, полної и средней кривизны и т. д., в частности релятивно минимальной поверхности (cM. [1]).

Рассмотрение деривационных уравнений для репера $\frac{\partial \boldsymbol{r}}{\partial u^{1}}, \frac{\partial \boldsymbol{r}}{\partial u^{2}}, \boldsymbol{n}$ привело к понятию внутренней Р. г. поверхности $S$ (см. [2]). Это есть геометрия аффинной связности (точнее эквиаффинной) без кручения. Было введено понятие геометрии 2-го рода, аналогичной геометрии сферич. отображения (см. [3]).

Р. г. позволяет включить в общую схему, кроме геометрии поверхностей евклидова и псевдоевклидова пространств, также и геометрию аффинной дифференциальной геометрии. Вектор $\boldsymbol{n}$ аффинной нормали характеризуется тем, что асимптотич. сеть поверхности $S$ - чебышевская (см. [3]).

Дальнейшим обобщением P.r. является теория но [4]). С каждой точкой поверхности $S$ проективного пространства связываются две прямые: нормаль 1-го рода, проходящая через точку поверхности $A$, но не имеющая с касательной плоскостью $\alpha$ других общих точек и нормаль 2-го рода, принадлежацая $\alpha$, но не проходящая через $A$. На поверхности $S$ определяются при әтом две внутренние геометрии, сопряженные относительно асимптотич. сети. Построения P. г. допускают многомерное обобщение (см. [4]).

Лum.: [1] M ü l l e r E., "Monatsh. Math. und Physik», 1921, Bd 31, S. 3-19; [2] N o r de n A. P. (H o p д е н A. П.), "C. r. Acad. sci.", 1931, t. 192, p. $135-37 ;$ [3] е г о ж е, «Изв. ВУЗов. Математика", $1958, \mathcal{N} 44$, с. $172-83 ;[4]$ е г о ж е, Простран-

РЕНЫИ КРИТЕРЙ̆ - статистический $\begin{aligned} & \text { критерий, }\end{aligned}$ применяемый для проверки простой непараметрич. гипотезы $H_{0}$, согласно к-рой независимые одинаково распределенные случайные величины $X_{1}, \ldots, X_{n}$ имеют заданную непрерывную функцию распределения $F(x)$, против альтернатив следующего вида:

\[
\begin{gathered}
H_{1}^{+}: \sup _{|x|<\infty} \Psi[F(x)]\left(E F_{n}(x)-F(x)\right)>0, \\
H_{1}^{-}: \inf _{|x|<\infty} \Psi[F(x)]\left(E F_{n}-(x) F(x)\right)<0, \\
H_{1}: \sup _{|x|<\infty} \psi[F(x)]\left|E F_{n}(x)-F(x)\right|>0,
\end{gathered}
\]

где $F_{n}(x)$ - функция эмпирич. распределения, построенная по выборке $X_{1}, \ldots, X_{n}, \psi(F), \psi \geqslant 0,-$ весовая функция. В случае, если

\[
\psi[F(x)]=\left\{\begin{array}{cl}
1 / F(x) & \text { при } F(x) \geqslant a, \\
0 & \text { при } F(x)<a,
\end{array}\right.
\]

где $a$ - любое фиксированное число из отрезка $[0,1]$, то Р.к., предназначенныӥ для проверки $H_{0}$ против указанных альтернатив $H_{1}^{+}, H_{1}^{-}, H_{1}$, основан на соответствующих им с т а т и т ик а х Р е ь и:

\[
\begin{aligned}
& R_{n}^{+}(a, 1)=\sup _{F(x) \geqslant a} \frac{F_{n}(x)-F(x)}{F(x)}=\max _{F\left(X_{(m)}\right) \geqslant a} \frac{\frac{m}{n}-F\left(X_{(m)}\right)}{F\left(X_{(m)}\right)}, \\
& R_{n}^{-}(a, 1)=-\inf _{F(x) \geqslant a} \frac{F_{n}(x)-F(x)}{F(x)}= \\
& =\max _{F\left(X_{(m)}\right) \geqslant a} \frac{F\left(X_{(m)}\right)-\frac{m-1}{n}}{F\left(X_{(m)}\right)}, \\
& R_{n}(a, 1)=\sup _{F(x) \geqslant a} \frac{\left|F_{n}(x)-F(x)\right|}{F(x)}= \\
& =\max \left\{R_{n}^{+}(a, 1), R_{n}^{-}(a, 1)\right\}
\end{aligned}
\]

где $X_{(1)}, X_{(2)}, \ldots, X_{(n)}$ - члены вариационного ряда

\[
X_{(1)} \leqslant X_{(2)} \leqslant \cdots \leqslant X_{(n)}
\]

построенного по наблюдениям $X_{1}, \ldots, X_{(n)}$.

Статистики $R_{n}^{+}(a, 1)$ и $R_{n}^{-}(a, 1)$ подчиняются одному и тому же вероятностному закону, и если $0<a \leqslant 1$, то\[
\lim _{n \rightarrow \infty} \mathrm{P}\left\{\sqrt{\frac{n a}{1-a}} R_{n}^{+}(a, 1)<x\right\}=2 \Phi(x)-1, x>0 \text {, }
\]

\[
\lim _{n \rightarrow \infty} \mathrm{P}\left\{\sqrt{\frac{n a}{1-a}} R_{n}(a, 1)<x\right\}=L(x), x>0,
\]

где $\Phi(x)$ - функция распределения стандартного нормального закона, $L(x)-\varnothing$ у н к ц и я $\mathrm{p} \mathbf{a}$ с п р е д ел е н и я $\mathrm{P}$ е вь и, определяемая формулой:

\[
L(x)=\frac{4}{\pi} \sum_{k=0}^{\infty} \frac{(-1)^{k}}{2 k+1} \exp \left\{-\frac{(2 k+1)^{2} \pi^{2}}{8 x^{2}}\right\} .
\]

В случае, если $a=0$, то

\[
\mathrm{P}\left\{R_{n}^{+}(0,1) \geqslant x\right\}=1-\frac{x}{1+x}, x>0 .
\]

Из (1) и (2) следует, что при больших значения $n$ для вычисления $Q$-процентных критич. значений $(0 \%<Q<$ $<50 \%)$ для статистик $R_{n}^{+}(a, 1)$ и $R_{n}(a, 1)$ можно воспользоваться следующими приближенными значениями:

\[
\sqrt{\frac{1-a}{n a}} \Phi^{-1}(1-0,005 Q) \text { и } \sqrt{\frac{1-a}{n a}} L^{-1}(1-0,01 Q)
\]

соответственно, где $\Phi^{-1}(x)$ и $L^{-1}(x)$ функции, обратные $\Phi(x)$ и $L(x)$ соответственно, при этом имеют в виду, что если $0 \%<Q<10 \%$, то $\Phi^{-1}(1-0,005 Q) \approx L^{-1}(1-0,02 Q)$.

Кроме того, если $x>2,99$, то при вычислении значений функции распределения Реньи $L(x)$ рекомендуется пользоваться приближенным равенством

\[
L(x) \approx 4 \Phi(x)-3,
\]

погрешность к-рого не превосходит $5 \cdot 10^{-7}$.

Кроме рассмотренных Р. к., существуют аналогичные критерии, отвечающие весовой функции

\[
\psi[F(x)]=\left\{\begin{array}{cl}
\frac{1}{1-F(x)}, & \text { если } F(x) \leqslant a, \\
0, & \text { если } F(x)>a,
\end{array}\right.
\]

где $a$ - любое фиксированное число из отрезка $[0,1]$. Jum.: [1] R é n y i A., "Acta math. Acad. scient. hung.", говых критериев, пер. с англ., М., 1971; [3] Б о л в ш е в Ј. Н., С м и р н о в Н. В., Таблицы математической статистики, 2
изд., М., 1968.

РЕIЕР - совокупность линейно независимых векторов, взятых в определенном порядке и отложенных от общего начала. Для векторов в пространстве Р. может служить любая тройка непараллельных одной плоскости векторов. Если векторы, составляющие Р., попарно ортогональны, то Р. наз. о р т о г о н а л ь н ы м; если при этом векторы имеют длину, равную единице, то Р. наз. о р т о но р ми р о в а н н ы м. Б̈Ә-з.

РЕПЛИКА а л г е бр а ич е с ко й си с т е м ы А в ваданном классе $\mathscr{\Re}$ алгебраических систем той же сигнатуры - алгебраическая система $K_{0}$ из $\Re$, обладающая следующими свойствами: 1) существует сюръективный гомоморфизм $\varphi_{0}$ системы $A$ на $\left.K_{0} ; 2\right)$ если $K \in \mathscr{\Re}$ и $\varphi$ - гомоморфизм системы $A$ в $K$, то $\varphi=\varphi_{0} \psi$ для нек-рого сюръективного гомоморфиизма $\psi$ системы $K_{0}$ на $K$. Р. системы $A$ в классе $\mathfrak{A}$ (если она существует) определяется однозначно с точностью до изоморфизма. Класс держит Р. любой алгебраич. системы той же сигнатуры. Класс алгебраич. систем фиксированной сигнатуры реплично полон тогда и только тогда, когда он содержит одноәлементную систему и замкнут относитөльно под-


\end{document}