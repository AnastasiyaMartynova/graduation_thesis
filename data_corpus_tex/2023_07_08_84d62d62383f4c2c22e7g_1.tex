\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
Если $X$ и $Y$ - топологические векторные пространства, то О. в примерах 1) и 2) непрерывны; если в примере 3) пространство $X$ есть $L^{2}\left(M, \Sigma_{M}, \mu\right)$, где $\mu-$ мера на $X$, то $О$. умножения на ограниченную измеримую функцию замкнут и имеет плотную область определения; если в примере 5) пространство $X=Y$ есть гильбертово пространство $L^{2}\left(M, \Sigma_{M}, \mu\right)$ и $K(x, y, z)=$ $=K(x, y) z$, где $K(x, y) \quad$ принадлежит $L^{2}(M \times M$, $\left.\Sigma_{M} \times \Sigma_{M}, \mu \times \mu\right)$, то $A$ компактен; если в примере 8) пространства $X$ и $Y$ рассматриваются как гильбертовы пространства, то $A$ непрерывен.

Если $A-\mathrm{O}$. из $X$ в $Y$ такой, что $A x \neq A y$ при $x \neq y$, $x, y \in D(A)$, то можно определить обратный оператор $A^{-1}$ к $A$; вопрос о существовании обратного О. и его свойствах связан с теоремой существования и единственности решения уравнения $A x=f$; если $A^{-1}$ существует, то $x=A^{-1} f$ при $f \in R(A)$.

Для О. в векторных пространствах можно определить сумму, произведение на число и произведение $\mathrm{O}$. Если $A, B$ - огераторы из $X$ в $Y$ с областями определения $D(A)$ и $D(B)$ соответственно, то с у м м о ї о п е р а то р о в $A$ и $B$ наз. О., обозначаемый $A+B$, с областью определения

\[
D(A+B)=D(A) \cap D(B),
\]

действующий по правилу

\[
(A+B) x=A x+B x
\]

при $x \in D(A+B)$. П р оиз ве д ени е м о пе р ат о р а $A$ н а ч и с л о $\lambda$ наз. О., обозначаемый $\lambda A$, с областью определения

\[
D(\lambda A)=D(A),
\]

действующей по правилу

\[
(\lambda A) x=\lambda(A x)
\]

при $x \in D(\lambda A)$. П р о и в в дение о пе р а тор о в определяется как композиция отображеній: если $A$ оператор из $X$ в $Y, B$ - оператор из $Y$ в $Z$. то произведением $B$ и $A$ наз. оператор $B A$ с областью определения

\[
D(B A)=\{x \in X: x \in D(A) \text { и } A x \in D(B)\},
\]

действующий по правилу

\[
(B A) x=B(A x)
\]

при и $x \in D(B A)$.

Если $P$ - всюду онределенный О. в $X$ такой, что $P P=P$, то $P$ наз. п р о е к т о р ом в $X$; если $I-$ всюду огределенный $\mathrm{O}$. в $X$ такой, что $I I=\mathrm{id}_{X}$, то $I$ наз. и н в о л ю ц и й в $X$.

Теория О. составляет важнейпую часть линейного и нелинейного функционального анализа, являясь, в частности, основным аппаратом теории динамич. систем, представлений групп и алгебр и важнейшим математич. инструментом математич. физики и квантовой механики

Лum.: [1] Л ю с т е р н и к Л. А., С о б о л е в В. И., Элементы функционального анализа, 2 изд., М., 1965; [2] К о л м оГо р о в А. Н., Фо м и н С. В., Элементы теории функций и
функционального анализа, 5 изд., М., $1981 ;$ [3] К а н то р оФункционального анализа, 5 изд., М., 1981; [3] К а н т о р оМ., 1977; [4] Д а н ф о р д Н., Ш в а p ц Дж. Т., Линейные операторы, пер. с англ., ч. 1 -3, М.,,1962-74;[5] Э д в а р д с Р. -Э., Функциональный анализ, пер. с англ., М., 1969; [6] И о с и д а К., Функциональный анализ, пер. с англ., М., 1967.

ОІІЕРАТРНАЯ ГРУПI М. А. Наймарк, А. И. Штерн. Олчет група операторов - 1) О. г.- однопараметрическая группа операторов в банаховом пространстве $E$, т. е. семейство линейных ограниченных операторов $U_{t},-\infty<t<\infty$, такое, что $U_{0}=I, U_{s+t}=U_{s} \cdot U_{t}$ и $U_{t}$ непрерывно зависит от $t$ (в равномерной, сильної или слабой топологии). Если $E$ - гильбертово пространство и $\left\|U_{t}\right\|$ равномерно ограничены, то грушиа $\left\{U_{t}\right\}$ подобна группе унитарных операторов (т е о p eм a H а д я).

Jum.: [1] S z.- N a g y B.. "Acta Univ, szeged. Sec. scient. Math.», 1947, t. 11, p. 152-57; [2] Х и л ле Э., Ф и л ли п с Р., Фуннцональный анализ и полугруппы, пер.' с англ., 2 изд., 2) 0. г., г р упп а с о бл а с т ью о пе р а тор о в $\Sigma$, - такая группа $G$, что всякому элементу $a \in G$ и всякому $\sigma \in \Sigma$, где $\Sigma$ - нек-рое множество символов, поставлен в соответствие элемент $a \sigma \in G$, причем $(a b) \sigma=a \sigma \cdot b \sigma$ при любых $a, b \in G$. Пусть $G$ и $G^{\prime}-$ группы с одной и той же областью операторов $\Sigma$; изоморфное (гомоморфное) отображение $\varphi$ группы $G$ на $G^{\prime}$ наз. о п ера тор ным изомор фи змом (гомом о рф и з м о м $)$, если $(a \sigma) \varphi=(a \varphi) \sigma$ для любых $a \in G, \sigma \in \Sigma$. Подгруппа. (нормальный делитель) $H$ группы $G$ с областью операторов $\Sigma$ наз. д о П у с т и м о й п о дгр уп пой ( Допустимым но р ма лыным д е ли т е лем), если $H \sigma \subseteq H$ при всяком $\sigma \in \Sigma$. Пересечение всех допустимых подгрупп, содержащих данное подмножество $M$ группы $G$, наз. д о п у с т имо й под г ру п по й, поро жд нно й множ е с т в о $M$. Группа, не имеющая допустимых нормальных делителей, кроме себя и единичной подгруппы, наз. п р о с т о й (относительно заданной области операторов). Всякая факторгруппа О. г. по допустимому нормальному делителю является группой с той же областью операторов.

Группа $G$ наз. г р у п п о й с п о л у г p у п по й о п е р а тор о в $\Sigma$, если $G$ - группа с областью операторов $\Sigma, \Sigma$ - полугруппа и $a(\sigma \tau)=(a \sigma) \tau$ для любых $a \in G, \sigma, \tau \in \Sigma$. Если $\Sigma-$ полугруппа с единицей $\varepsilon$, то считается, что $a \varepsilon=a$ при всяком $a \in G$. Всякая группа с произвольной областью операторов $\Sigma_{0}$ есть группа с полугруппой операторов $\Sigma$, где $\Sigma$ - свободная полугруппа, порожденная множеством $\Sigma_{0}$. Группа $F$ с полугруппой операторов $\Sigma$, обладающей единицей, наз. $\Sigma$ - с воб о д н о й, если она порождается такой системой элементов $X$, что әлементы $x \alpha$, где $x \in X, \alpha \in \Sigma$, составляют для $F$ (как группы без операторов) систему свободных образующих. Пусть $F$ есть Г-свободная группа ( пы $\Gamma, f \in F$ и $A_{f, \Delta}$ - допустимая подгруппа группы $F$, порожденная всеми элементами вида $f^{-1}(f \alpha)$, где $\alpha \in \Delta$. Тогда всякая допустимая подгруппа группы $F$ является операторным свободным произведением групп типа

\includegraphics[max width=\textwidth, center]{2023_07_08_84d62d62383f4c2c22e7g-1}
$\Sigma$ - свободная полугруппа операторов, то при $a \neq 1$ допустимая подгруппа $\Sigma$-свободной группы $F$, порожденная элементом $a$, сама является $\Sigma$-свободной группой со свободным образующим $a$ (см. также [3]).

Абелева группа с ассоциативным кольцом операторов $K$ - это модуль над $K$.

[2] Лит.: [1] К у р о т А. Г., Теория групп, 3 изд., М., 1967;

\includegraphics[max width=\textwidth, center]{2023_07_08_84d62d62383f4c2c22e7g-1(1)}
ОІЕРАТОРНАЯ ТОПОЛОГИЯ - топология в пространстве $L(E, F)$ непрерывных линейных отображений одного топологического векторного пространства $E$ в другое топологич. пространство $F$, превращающая пространство $L(E, F)$ в топологическое векторное пространство. Пусть $F$ - локально выпуклое простран-

\includegraphics[max width=\textwidth, center]{2023_07_08_84d62d62383f4c2c22e7g-1(2)}
пространства $E$, что линейная оболочка объединения множеств этого семейства плотна в $E$; пусть $\mathfrak{B}-$ базис окрестностей нуля в $F$. Семейство

\[
M(S, V)=\{f: f \in L(E, F), f(S) \subset V\},
\]

где $S$ пробегает $\Im$, а $V$ пробегает $\mathfrak{B}$, является базисом окрестностеї нуля для единственноӥ инвариантной относительно сдвигов топологии, к-рая является О. т. и превращает пространство $L(E, F)$ в локально выпук-


\end{document}