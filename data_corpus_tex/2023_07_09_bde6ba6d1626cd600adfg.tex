\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}

\begin{document}
к-рых делятся на $m$. Разрешимость такого сравнения для составного модуля $m=p_{1}^{\alpha_{1}} \ldots p_{s}^{\alpha_{s}}$, где $p_{1}, \ldots, p_{s}$ различные иростые числа, равносильна разрешимості сравнений

\[
f\left(x_{1}, \ldots, x_{n}\right) \equiv 0\left(\bmod p_{i}^{\alpha_{i}}\right)
\]

для всех $i=1, \ldots, s$. При этом число $N$ решений сравнения (1) равно прогзведению $N_{1} . . N_{s}$, где $N_{i}$ писло решений сравнения (2). Таким образом, при изучении сравнений вида (1) достаточно ограничиться модулями, являющимися степенями простых чисел.

Для разрешимости сравнения

\[
f\left(x_{1}, \ldots, x_{n}\right) \equiv 0\left(\bmod p^{\alpha}\right), \alpha>1,
\]

необходимо, чтобы было разрешимо сравнение

\[
f\left(x_{1}, \ldots, x_{n}\right) \equiv 0(\bmod p)
\]

по простому модулю $p$. В невырожденных случаях разрешииость сравнения (4) является также и достаточным условием для разрешимости сравнения (3). Точнее, справедливо следующее утверждение: каждое решение $x_{i} \equiv x_{i}^{(1)}(\bmod p)$ сравнения (4) такое, что $\frac{\partial f}{\partial x_{i}}\left(x_{1}^{(1)}, \ldots, x_{n}^{(1)}\right) \neq 0(\bmod p), \quad$ хотя бы для одного $i=1,2, \ldots, n$ порождает $p^{(\alpha-1)(n-1)}$ решений $x_{i} \equiv$ $\equiv x_{i}^{(\alpha)}\left(\bmod p^{\alpha}\right)$ сравнения (3), шричем $x_{i}^{(\alpha)} \equiv x_{i}^{(1)}(\bmod p)$ при $i=1,2, \ldots, n$.

Итак, в невырожденном случае вопрос о числе решений сравнения (1) по составному модулю $m$ сводится к вопросу о числе решений сравнений вида (4) по простым модулям $p$, делящим $m$. Если $f\left(x_{1}, \ldots, x_{n}\right)-$ абсолютно неприводимый многочлен с целыми рациональными коэффициентами, то для числа $N_{p}$ репений сравнения (4) имеет место оценка

\[
\left|N_{p}-p^{n-1}\right| \leqslant C(f) p^{n-1-1 / 2},
\]

где константа $C(f)$ зависит только от многочлена $f$ и не зависит от $p$. Из этоӥ оценки, в частности, следует, что сравнение (4) разрешимо для всех простых $p$, больших нек-рой әффективно вычислимой константы $C_{0}(f)$, зависящей от данного многочлена $f\left(x_{1}, \ldots, x_{n}\right)$ (см. также Сравнение по простому модулю). Более сильный результат в этом вопросе получен П. Делинем [3].

Лит.: [1] Б о ре вич З. И., III а ф а р е в ич П. Р., Теория чисел, 2 изд., М., 1972; [2] X a c c e Г., Лекции по теоpии чисел, пеp. c нем., M., 1953; [3] D e l i g n e P., "Publ.
Math. IHES», 1973 , v. 43, p. 273-307. C. A. Степанов.

СРАВНЕНИЕ ПО ДВОЙНОМУ МОДУЛЮ $(p, f(x))-$ соотношение между целочисленными многочленами $\boldsymbol{a}(x)$ и $b(x)$ вида

\[
a(x)-b(x)=f(x) g(x)+p h(x),
\]

где $p$ - простое число, а $f(x)=x^{n}+\alpha_{1} x^{n-1}+\ldots+$ $+\alpha_{n}, g(x)$ и $h(x)$ - многочлены с целыми рациональными коэффициентами. Иными словами, многочлены $a(x)$ и $b(x)$ с целыми рациональными коэффициентами наз. с р а в н и мы м и п о д в о й ном у мод у лю $(p, f(x))$, если их разность $a(x)-b(x)$ делится на $f(x)$ по модулю $p$. Для обозначения сравнимости $a(x)$ и $b(x)$ по двойному модулю $(p, f(x))$ используется символ

\[
a(x) \equiv b(x)(\bmod p, f(x)),
\]

введенный, как и само понятие «С. по д. м.», Р. Дедекиндом (R. Dedekind).

Сравнимость по двойному модулю является отношением әквивалентности на множестве всех целочисленных многочленов и, следовательно, разбивает это множество на непересекающиеся классы - классы вычетов по двойному модулю $(p, f(x))$. Поскольку каждый многочлен $a(x)$ сравним по двойному модулю $(p, f(x))$ с одним и только одним многочленом вида

\[
\beta_{1} x^{n-1}+\beta_{2} x^{n-2}+\ldots-\beta_{n},
\]

где $\beta_{1}, \ldots, \beta_{n}$ пробегают независимо друг от друга полную систему вычетов по модулю $p$, то имеется в точности $p^{n}$ классов вычетов по двойному модулю.

С. по Д. М. можно складывать, вычитать и перемножать, подобно обычным сравнениям. Эти операции индуцируют аналогичные операции на классах вычетов по двойному модулю, превращая множество классов вычетов в коммутативное кольцо.

Кольцо классов вычетов по двойному модулю ( $p$, $f(x))$ является факторкольцом кольца многочленов $K_{p}[x]$ с коэффициентами из простого конечного поля $K_{p}$ по идеалу $(\bar{f}(x))$, порожденному многочленом $\bar{f}(x)$, полученному из $f(x)$ редукцией по модулю $p$. В частности, если $f(x)$ неприводим по модулю $p$, то $(\bar{f}(x))$ максимальный идеал в $K_{p}[x]$ и $K_{p}[x] /(\bar{f}(x))$ - поле, состоящее из $p^{n}$ әлементов (расгииение степени $n$ простого поля $K_{p}$ ).

Если $f(x)$ неприводим по модулю $p$, то для сравнений по двойному модулю имеет место аналог Ферма малой теоремы:

\[
a(x)^{p^{i}} \equiv a(x)(\bmod p, f(x)),
\]

а также Лагранжа теоремы: сравнение

\[
z^{m}+a_{1}(x) z^{m-1}+\ldots+a_{m}(x)=0(\bmod p, f(x))
\]

коәфффициенты к-рого целочисленные многочлены, имеет не более $m$ несравнимых по модулю $(p, f(x))$ решений. Из этих теорем можно получить, что

\[
x p^{n}-x \equiv \Pi_{d / n} \Phi_{d}(x)(\bmod p),
\]

где $\Phi_{d}(x)$ - произведение всевозможных различных нормированных (т. е. со старшим коэфффициентом 1) неприводимых по модулю $p$ многочленов степени $d$. Если через $(n)$ обознатить число разіичных нормированных неприводимых по модулю $p$ многочленов стелени $n$, то

\[
(n)=\frac{1}{n} \sum_{d / n} \mu(d) p^{\frac{n}{d}},
\]

$\mu(d)$ - функция Мёбиуса и, в частности, $(n)>0$ для любого натурального $n$. Следовательно, для любого целого $n>0$ существует конечное шоле $K_{q}$, состоящее из $p^{n}$ элементов и являющееся распирением степени $n$ Іоля вычетов $K_{p}$ по простому модулю $p$.

М Лит.: [1] В е н ко в Б. А., Элементарная теория чисел,

СРАВНЕНИЕ ПО ПРОСТОМУ МОДУЈЮ - сравнение, в к-ром модуль является простым числом. Отличительной чертой теории С. по п. м. является то, что классы вычетов по модулю $p$ образуют конечное поле из $p$ әлементов. Поэтому С. по п. м. можно трактовать как уравнения над іростыми конечными полями и применять для их изучения, наряду с методами теории чисел, алгебро-геометрические методы.

Одним из основных вопросов теории сравнений от одного переменного $x$, имеюџцм важное значение в теории алгебраич. чисел, теории кодирования и других разделах математики, является вопрос об изучении законов разложения

\[
f(x) \equiv f_{1}(x) \ldots f_{r}(x)(\bmod p)
\]

по простому модулю $p$ произвольных целочисленных многочленов $f(x)$ на неприводимые сомножители.

Основным вопросом теории С. по п. м. $p$ от $n \geqslant 2$ переменных является вопрос о числе репений алгебраических сравнений

\[
f\left(x_{1}, \ldots, x_{n}\right) \equiv 0(\bmod p),
\]


\end{document}