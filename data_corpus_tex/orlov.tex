%\input{../preamblw}
%
%
%\begin{document}

\renewcommand{\firstpage}{195}
\renewcommand{\lastpage}{199}


\udk{519.112.6:004.031.43}
\msc{68R10}

\title{Алгоритм анализа реш\"eточной модели структуры информационно"=измерительной системы}% Полный заголовок
{Алгоритм анализа решёточной модели структуры информационно"=измерительной системы}

\author{С.\,П.~Орлов}
{О\,р\,л\,о\,в~С.\,П.}

\institute{\samgtu.}

\email{E-mail}{orlov\_sp@mystep.ru}

\otherinf{{Орлов Сергей Павлович} \regalia{д.т.н., проф.}
\position{зав. кафедрой}
\dept{каф. вычислительной техники}}

\keywords{информационно"=измерительная система, решёточная модель, дистрибутивные решётки, структурный анализ}

\workabstract{Рассмотрена решёточная модель структур развивающихся информационно"=измерительных систем. 
Показано, что такая модель позволяет исследовать процесс трансформации структур
информационно"=измерительной системы в период жизненного цикла.
Для подтверждения свойства дистрибутивности исходной решёточной модели предложен алгоритм поиска в ней подрешёток <<пентагон>> и~<<диамант>>.}

\engtitle{Algorithm for lattice model analyzing of~information-measuring system structure}

\engauthor{S.\,P.~Orlov}{O\,r\,l\,o\,v~S.\,P.}


\enginstitute{\engsamgtu.}

\otherenginf{\fullauthor{Sergey P. Orlov} \regalia{Dr.\,Sci.
(Techn.)} \position{Head of Dept.} \dept{Dept. of Computational Technique}}

\engabstract{A lattice model of the structures of developing information-measuring systems is considered. It is shown that this model allows studying the transformation of structures of information-measuring system during the life cycle. To confirm the distributive property of the lattice model the original algorithm of search in it for the sublattices of pentagon and diamond types is proposed.
}

\engkeywords{information-measuring system, lattice model, distributive lattices, structure analysis}

\date{27/XII/2010}
\revisiondate{15/VIII/2011}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


\maketitle

\small

\Section[n]{Введение} Информационно"=измерительные системы (ИИС), предназначенные для исследования сложных технических объектов,
изменяют свою структуру, адаптируясь к~задачам измерений в~течение жизненного цикла \cite{orlov:bib1}. 
Переменная структура измерительных каналов и блоков обработки данных ИИС может быть представлена в~виде графовой модели. 
Она должна отражать сложность системы, взаимосвязь подсистем и~изменение их во времени. 
В~работе \cite{orlov:bib2} предложена модель развивающейся ИИС на основе решёток.

Система называется решёточной, если её модель $\Psi _L $ есть тройка \cite{orlov:bib2}:
\[
\Psi_L =\langle \Psi _a,\,\Psi _b ,\,P_0 (\Psi_a,\,\Psi _b )
\rangle =\langle M_a ,\,S_{a1},\,\ldots,\,S_{an},\,M_b
,\,S_{b1},\,S_{b2},\,P_0 ( \Psi_a,\,\Psi _b)\rangle,
\]
где $\Psi _a $ "--- модель, описывающая поведение системы; 
$\Psi _b $ "--- модель структуры системы; 
$P_0(\Psi_a,\,\Psi _b)$ "--- предикат функциональной целостности,  он равен единице, если существует взаимно однозначное преобразование моделей, и равен нулю в противном случае; 
$M_a$ "--- множество"=носитель модели $\Psi_a$; 
$S_{a1}$, $\ldots$, $S_{an}$ "--- произвольные операции;
$M_b$ "--- множество"=носитель модели $\Psi _b $; 
$S_{b1}$, $S_{b2} $ "--- бинарные операции, для которых выполняются законы идемпотентности, ассоциативности, коммутативности и поглощения.

В большинстве решёточных моделей операции $S_{b1}$, $S_{b2}$ "--- операции объединения и пересечения.

Таким образом, модель поведения $\Psi _a $ определяет модель переменной структуры $\Psi _b $ в зависимости от множества задач, выполняемых в~период времени.

Часто необходимо определить, является ли дистрибутивной решётка структуры $\Psi _b $, полученная в результате анализа системы. Если решёточная модель структуры $\Psi _b $ дистрибутивна, то можно применить известные методики теории решёток для исследования её свойств.

Проверка дистрибутивности произвольной решётки может базироваться на теореме об изоморфности дистрибутивной решётки некоторой
подрешётке булевой алгебры \cite{orlov:bib3}. 
Но в~этом случае необходим алгоритм с экспоненциальной временной сложностью для перебора всех подрешёток исходной решётки.

В~настоящей статье предложен алгоритм определения дистрибутивности решёточных моделей структур ИИС, использующий сокращённый перебор подрешёток.



\Section[n]{Алгоритм анализа структурной решёточной модели}
Исследование сложной информационно"=измерительной системы часто приводит к~построению модели $\Psi _b$ на основе графа $F=
(V,\,E)$, обладающего следующими свойствами.
\begin{enumerate}

\item[1.] На множестве вершин $V$ графа определён частичный порядок ``$\le $''. 
Тогда граф $F$ "--- орграф, описывающий частично упорядоченное множество, 
а множество рёбер $E $ задаёт отношение частичного порядка.

\item[2.] Если в $F$ любые два элемента $a$, $b\in V$ имеют нижнюю грань
$a\wedge b$ и верхнюю грань $a\vee b$, то $F$ "--- решётка. 
Здесь ``$\wedge $'' и ``$\vee $'' "--- операции пересечения и объединения.
Интерпретация технической сущности операций ``$\wedge $'' и ``$\vee $''
зависит от свойств изучаемой ИИС.
\end{enumerate}

\begin{wrapfigure}{O}{.5\textwidth}
\centering  \small
\includegraphics[width=5.5cm]{./00PIC/orlov1.eps}

{\sl а \hspace{30mm} б}
\caption{Подрешётки $N_5$ и $M_3$}
\end{wrapfigure}


Для решения задачи определения дистрибутивности решётки воспользуемся известным утверждением \cite{orlov:bib4}: решётка
дистрибутивна тогда и только тогда, когда она не содержит подрешётку, изоморфную пентагону $N_5$ (рис.~1,~{\sl а}) или
диаманту $M_3$ (рис.~1, {\sl б}). Это позволяет значительно
снизить сложность алгоритма.

Алгоритм может преследовать две цели: 
а)~обнаружение пентагона или диаманта и~вывод о недистрибутивности исходной решётки; 
б)~обнаружение всех подрешёток, изоморфных пентагону или диаманту, с целью дальнейшего
преобразования модели системы к дистрибутивной решётке. 
В статье описан алгоритм, решающий вторую задачу.

Будем задавать решётку $F$ матрицей смежности $A(F)$. 
В~любом частично упорядоченном множестве существует нумерация, соответствующая отношению доминирования. 
Для такой нумерации матрица смежности имеет треугольный вид. 
Пусть для решётки $F$ заданы множество вершин $V= \{ v_1,\,v_2,\,\ldots,\,v_K\}$,
множество рёбер $E=\{e_1,\,e_2,\,\ldots,\,e_D\}$ и матрица смежности имеет размерность $K \times K$.

В силу того, что $F$ "--- решётка и ее нумерация соответствует
частичному порядку, вершина $v_1$ есть нулевой элемент \textbf{0},
а вершина $v_K$ "--- единичный элемент \textbf{1} решётки~$F$.
Тогда первый столбец и последняя строка матрицы смежности $A( F)$
"--- нулевые.

Алгоритм выполняется в два этапа. На первом этапе цель алгоритма
заключается в поиске в $ F$ такой пары путей длиной 2 и 3, которые
образуют подрешётку, изоморфную пентагону $N_5$.

Пусть дана вершина $v_i \in  F$, которая является нулевым
элементом \textbf{0} некоторого подмножества подрешёток в $F$.
Найдём множества $S_i^1$, $S_i^2 $ и $S_i^3 $ вершин, достижимых
из $v_{i}$ соответственно за 1, 2 и 3 шага. 
Затем найдём множество $Z_i $ вершин $v_j$, которые достижимы из $v_i$ одновременно за 2
шага и за 3 шага, и недостижимы за один шаг:
\begin{equation}
\label{orlov:eq1}
Z_i =(S_i^2 \cap S_i^3)\backslash S_i^1.
\end{equation}

Такие вершины являются единичными элементами подрешёток $L_n^{ij}$, $n=1,\, 2, \, \ldots,$ $H_{ij}$, образованных всеми парами путей длиной 2 и 3, проходящих из $v_i$ в $v_j$. 
Условие~\eqref{orlov:eq1} исключает подрешётки, в~которых есть транзитивное замыкание ребром вершин $v_i$ и $v_j$.

Пентагон $N_5$ также образован двумя путями длиной 2 и 3 (рис. 1, {\sl а}).
Следовательно, необходимо проверить, есть ли среди подрешёток $L_n^{ij} $
такие, которые изоморфны пентагону $ N_5$.

Далее сформируем два множества:
\begin{itemize}


\item $P_{ij}^2 =\{ p_{ij, k}^2 \}$, $k=1, 2, \ldots, K_{ij}^2$, $K_{ij}^2 =| P_{ij}^2  |$ "--- множество
всех путей длиной 2 из $v_i$ в $v_j$;

\item $Z_{ij,k}^2 $ "--- множество промежуточных вершин, принадлежащих пути $p_{ij,k}^2 $ между $v_i$ и $v_j$.


\end{itemize}

Под промежуточными вершинами в пути из $v_i$ в $v_j$ понимаются все
вершины пути за исключением начальной $v_i$ и конечной $v_j$ вершин.

Аналогично, $P_{ij}^3 =\{ p_{ij,m}^3 \}$,  $m=1, 2, \ldots, M_{ij}^3$, $M_{ij}^3 =|P_{ij}^3 |$ "--- множество всех путей длиной~3 из $v_i$ в $v_j$, и $Z_{ij,m}^3 $ "--- множество промежуточных вершин, принадлежащих пути $p_{ij,m}^3 $ между вершинами $v_i$ и~$v_j$.

\begin{wrapfigure}[22]{O}{.5\textwidth}
\centering  \small 
\includegraphics[width=4.5cm]{./00PIC/orlov2.eps}
\caption{Дистрибутивные подрешётки}

\bigskip

\includegraphics[width=5.5cm]{./00PIC/orlov3.eps}
\caption{Пример решётки, содержащей подрешётку $M_3$}

\end{wrapfigure}

Для подрешётки, образованной двумя путями $p_{ij,k}^2 $ и $p_{ij,m}^3 $ и
изоморфной пентагону, должны выполняться условия:
\begin{itemize}
\item[а)] $Z_{ij,k}^2 \cap Z_{ij,m}^3 =\emptyset;$

\item[б)] не существует ни одного другого пути $p_{ij,l}^2 \in P_{ij}^2 $ такого,
что
\begin{equation}
\label{orlov:eq3}
Z_{ij,l}^2 \cap Z_{ij,m}^3 \ne \emptyset,
\end{equation}
где $l\ne k$;

\item[в)] не существует ни одного другого пути $p_{ij,d}^3 \in P_{ij}^3 $ такого,
что
\begin{equation}
\label{orlov:eq4}
Z_{ij,r}^3 \cap Z_{ij,k}^2 \ne \emptyset,
\end{equation}
где $d\ne m$.
\end{itemize}



Условия \eqref{orlov:eq3} и \eqref{orlov:eq4} исключают из рассмотрения дистрибутивные подрешётки, вид
которых показан на рис.~2.




Подрешётка "--- пентагон $N_{ij,km}$, образованная путями $p_{ij,k}^2$ и
$p_{ij,m}^3$, однозначно задаётся кортежем из пяти вершин:
$
N_{ij,km} \{ v_i ,\,v_a ,\,v_b ,\,v_c ,\,v_j \},
$
где  $v_a \in Z_{ij,k}^2$, $v_b$, $v_c \in Z_{ij,m}^3$, $v_b
\le v_c$.
Этот кортеж заносится в выходной список пентагонов, формируемый алгоритмом.


Аналогичный подход используется для поиска подрешёток, изоморфных диаманту.
На рис.~3 представлен пример решётки $L_{ij}$, образованной путями длиной
2 из вершины $v_i$ в вершину $v_j$.
Промежуточные вершины на путях между вершинами $v_i$ и $v_j$ в исходной
решётке могут иметь смежные ребра (пунктирные линии между $v_1$ и $v_2$ и
между $v_1$ и $v_3$ на рис.~3). В этом случае пары путей, связанные
через такие промежуточные вершины, не могут образовывать подрешётки,
изоморфные диаманту.
Матрица смежности $A_M$ для решётки $L_{ij}$ задаётся табл.~1.

\begin{table}[h]
\centering 

\begin{minipage}{85mm}
\small \centering 
\caption{} \vspace{-3mm}
\begin{tabular}{c|c|c|c|c|c|c|c} \hline
\footnotesize Вершины графа& $v_i $&$v_1 $&$v_2 $&$v_3 $&$v_4 $&$v_5 $&$v_j $ \\ \hline
\rule{0mm}{12pt}%
$v_i $&0&1&1&1&1&1&0 \\
$v_1 $&&0&1&1&0&0&1 \\ 
$v_2 $&&&0&1&0&0&1 \\ 
$v_3 $&&&&0&0&0&1 \\ 
$v_4 $&&&&&0&0&1 \\ 
$v_5 $&&&&&&0&1 \\ 
$v_j $&&&&&&&0 \\ \hline
\end{tabular}
\end{minipage}
\end{table}

\begin{wraptable}{O}{.52\textwidth}
\centering \small
\caption{}\vspace{-3mm}
\begin{tabular}{c|c|c|c|c|c}
\hline
\footnotesize Вершины графа &$v_1 $&$v_2 $&$v_3 $&$v_4 $&$v_5 $ \\ \hline
\rule{0mm}{12pt}%
$v_1 $&0&1&1&0&0 \\ 
$v_2 $&&0&1&0&0 \\ 
$v_3 $&&&0&0&0 \\ 
$v_4 $&&&&0&0 \\ 
$v_5 $&&&&&0 \\ \hline
\end{tabular}
\end{wraptable}

В подрешётке, изоморфной диаманту, промежуточные вершины не должны иметь
смежных ребер. На рис.~3 это вершины $v_3 $, $v_4 $ и $v_5 $.

Пусть $L_M$ "--- решётка, образованная всеми путями длиной 2 из $v_i$ в
$v_j$.
В матрице смежности $A(L_M)$ исключим строки и столбцы, соответствующие
вершинам $v_i$ и $v_j$. Полученная редуцированная матрица
$A^R(L_M)$ "--- треугольная матрица смежности только для промежуточных
вершин. Для решётки, приведенной на рис.~3, редуцированная матрица задаётся
табл.~2.



\begin{newthm}{Утверждение} Подрешётка $L_{M,l}$ решётки $L_M$,
образованная тремя путями из вершины $v_i$ в вершину $v_j$ через
некоторые вершины $v_a$, $v_b$ и $v_c$, изоморфна подрешётке --- диаманту
тогда и только тогда, когда вершины $v_a$, $v_b$ и $v_c$ не смежные
между собой.
\end{newthm}

Отсюда следует, что в матрице смежности $ A^R(L_M)$ в строке,
соответствующей одной из вершин $v_a$, $v_b$ и $v_c$, должны стоять
нули на пересечениях со столбцами, соответствующими двум другим вершинам.

Таким образом, при поиске диамантов будет проводиться выявление пар нулей в
редуцированных матрицах смежности рассматриваемых подрешёток.

\begin{itemize}

\item[1.] Пусть дана вершина $v_i \in  F$, которая является нулевым элементом \textbf{0} некоторого подмножества подрешёток в $F$. Найдём
множества $S_i^1$ и $S_i^2$ вершин, достижимых из $v_{i}$ соответственно
за 1 и 2 шага. Затем найдём множество $Z_i$ вершин $v_j$, которые
достижимы из $v_i$ за 2 шага и не достижимы за один шаг:
\begin{equation}
\label{orlov:eq5}
Z_i =S_i^2 \backslash S_i^1.
\end{equation}

Такие вершины являются единичными элементами подрешёток $L_r^{ij}$,
$r=1, 2, \ldots,$ $ R_{ij}$, образованных всеми путями длиной 2, проходящих из $v_i$ в $v_j$. 
Условие \eqref{orlov:eq5} исключает подрешётки, в которых есть транзитивное
замыкание ребром вершин $v_i$ и $v_j$.

\item[2.] Обозначим через $T_{ij}^2 =\{ t_{ij,q}^2 \}$, $q=1, 2, \ldots,$ $ Q_{ij}^2$; $Q_{ij}^2 =|T_{ij}^2|$ "--- множество всех путей длиной 2 из $v_i$ в $v_j$ и $Z_{ij,q}^2$ "--- множество промежуточных вершин, принадлежащих пути $t_{ij,q}^2 $ между
$v_i$ и $v_j$. Множество путей $T_{ij}^2 $ образует решётку $L_r^{ij}$,
способную включать в себя подрешётку"=диамант.

\item[3.] Построим редуцированную матрицу смежности $A^R(L_r^{ij})$ решётки $L_r^{ij}$. 
С~помощью этой матрицы можно найти все тройки путей $\bigl\{ t_{ij,\gamma }^2,\,t_{ij,\eta }^2 ,\,t_{ij,\mu }^2 \bigr\}$, $\gamma \hm \ne
\eta \ne \mu $, которые образуют подрешётки, изоморфные диаманту. Для этого
в~каждой строке матрицы $A^R(L_r^{ij}) $ находятся пары нулей, которым
соответствуют несмежные промежуточные вершины искомой подрешётки"=диаманта.
При этом из рассмотрения исключаются нулевые элементы диагонали матрицы
$A^R\left(L_r^{ij} \right)$.

\end{itemize}

Таким образом, формируется выходной список диамантов.

В результате работы описанного алгоритма можно определить, является ли
исследуемая исходная решётка дистрибутивной или модулярной:
\begin{enumerate}
\item если выходные списки подрешёток пентагонов и диамантов пусты, то решётка
дистрибутивна;

\item если список пентагонов пуст и есть хотя бы один диамант в списке подрешёток"=диамантов, то решётка модулярна.

\end{enumerate}


\Section[n]{Заключение} В развивающихся ИИС с переменной структурой решёточная
модель характеризует внутренние свойства системы в процессе трансформации.
Дистрибутивность такой модели свидетельствует об эффективных путях
преобразования структур. Это связано с тем, что в моделях на дистрибутивных
решётках наиболее рационально, а во многих случаях и оптимально,
осуществляется кодирование элементов системы, что исключает избыточность
структуры ИИС при её трансформации.

В случае неэффективных путей развития структуры решёточная модель, как
правило, недистрибутивна. Обнаружение этого факта с помощью предлагаемого
алгоритма позволяет также определить причину и локальное <<место>> в
структуре, нуждающееся в исправлении. Для этого используется множество
найденных подрешёток, изоморфных $N_5$ и $M_3$. Такие подрешётки могут
быть преобразованы в дистрибутивные подрешётки путём расщепления вершин и
добавления рёбер, что приводит к дистрибутивности исследуемой решёточной
модели.

\begin{thebibliography}{9}

\RBibitem{orlov:bib1}
\by Орлов~С.\,П.
\paper Моделирование структур сложных информационно"=измерительных систем
\jour Вестн. Сам. гос. техн. ун-та. Сер. Техн. науки
\yr 2005
\issue 33
\pages 251--254
\misctransl
\by Orlov~S.\,P.
\paper Modeling of structures of complex information-measuring systems
\jour Vestn. Sam. Gos. Tehn. Un-ta. Ser. Tehn. Nauki
\yr 2005
\issue 33
\pages 251--254



\RBibitem{orlov:bib2}
\by Орлов~С.\,П.
\thesis Методы и модели при структурном синтезе гибких информационно"=измерительных систем
\thesisinfo Автореф. дис-ции {\ldots} д.т.н.
\publ СамГТУ
\publaddr Самара
\yr 1990
\totalpages 35
\misctransl
\by Orlov~S.\,P.
\thesis Methods and models for the structural synthesis of flexible information-measuring systems
\thesisinfo Dr. Sci. Thesis (Techn.)
\publ SamGTU
\publaddr Samara
\yr 1990
\totalpages 35

\Bibitem{orlov:bib3}
\by Aigner~M.
\book Combinatorial Theory
\publaddr New York
\publ Springer Verlag
\yr 1979
\totalpages 483
\rtransl русск. пер.: {} 
\by Айг\-нер~М.
\book Комбинаторная теория
\publaddr М.
\publ Мир
\yr 1982
\totalpages 558

\RBibitem{orlov:bib4}
\by Биркгоф~Г.
\book Теория решёток
\publaddr М.
\publ Наука
\yr 1984
\totalpages 568
\misctransl
\by Birkgof~G.
\book Lattice Theory
\publ Nauka
\publaddr Moscow 
\yr 1984
\totalpages 568


\end{thebibliography}

\noindent
\hskip6.5cm \thedate \par\noindent
\hskip6.5cm\therevdate




\vspace{-5mm}

\makeengtitle



 \newpage


%\end{document}
