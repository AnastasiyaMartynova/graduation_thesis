\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
$k \in\{0, \ldots, n\})$ относительно отображения $f: V^{n} \longrightarrow V^{n}$, еспи найдется вложенный (обычно гладко вложенный) в $V^{n} k$-мерный диск $D^{k}$, содержащий точку $x_{0}$ и обладающий свойством: для всякого $\varepsilon>0$ найдется $\delta>0$ такое, что для всякого $x \in D^{k}$, удовлетворяющего неравенству $d\left(x, x_{0}\right)<\delta$, пмеет место неравенство $d\left(f^{t} x, f^{t} x_{0}\right)<\varepsilon$ для всякого $t \in \mathbb{N}$. Если диск $D^{k}$, имеющий указанные свойства, может быть выбран так, что

\[
d\left(f^{t} x, \quad f^{t} x_{0}\right) \longrightarrow 0 \quad \text { при } t \longrightarrow+\infty
\]

(соответственно,

\[
\left.\varlimsup_{t \rightarrow+\infty} \frac{1}{t} \ln d\left(f^{t} x, f^{t} x_{0}\right)<0\right)
\]

для всякого $x \in D^{k}$, то точка $x_{0}$ наз. а $\boldsymbol{c}$ घ пп то т ически (соответственно, әкспоненци а льно) условно устой чи во й (с индексом $k$ ) относительно отображения $f$.

Пусть $V^{n}$-компактное дифференцируемое многообразие. Төчка $x_{0} \in V^{n}$ наз. ус ло вн о ус то й ч и в о й

\includegraphics[max width=\textwidth, center]{2023_07_09_d98248e95e97ea016929g-1}
ловно устой ч и вой) с индексом $k$ относительно отображения $f: V^{n} \longrightarrow V^{n}$, если она становится таковой в результате наделения $V^{n}$ нек-рой римановой метрпкой. От выбора римановой метрики на $V^{n}$ это свойство точки $x_{0}$ не зависит.

\begin{enumerate}
  \setcounter{enumi}{2}
  \item Пусть дано дифференциальное уравнение (2) на римановом (или на финслеровом) $n$-мерном многообразии $V^{n}$, расстояние в к-ром обозначается $d(\cdot, \cdot)$. Решение $x_{0}(\cdot): t_{0}+\mathbb{R}+\longrightarrow V^{n}$ этого уравнения наз. у с ло вн о устой ч ивым (с индексом $k$ ), если найдется $k$-мерный диск $D^{k}$, вложенный в $V^{n}$ (рассматриваемый как многообразие нек-рого класса $C^{(n}$, обычно $m \geqslant 1$ ), содержащий точку $x_{0}\left(t_{0}\right)$ и обладающий свойством: для всякого $\varepsilon>0$ найдется $\delta>0$ такое, что для всякого $\quad x \in D^{k}$, удовлетворяющего неравенству $d\left(x, x_{0}\left(t_{0}\right)\right)<\delta$, решение $x(\cdot)$ того же уравнения, удовлетворяющее начальному условию $x\left(t_{0}\right)=x$, единственно, определено на $t_{0}+\mathbb{R}+$ и при всяком $t \in t_{0}+\mathbb{R}+$ удовлетворяет неравенству $d\left(x(t), x_{0}(t)\right)<\varepsilon$. Если диск $D^{k}$, имеющий указанные свойства, может быть выбран так, что
\end{enumerate}

\[
d\left(x(t), \quad x_{0}(t)\right) \longrightarrow 0 \text { при } t \longrightarrow+\infty
\]

(соответственно,

\[
\left.\varlimsup_{t \rightarrow+\infty} \frac{1}{t} \ln d\left(x(t), \quad x_{0}(t)\right)<0\right)
\]

для всякого решения $x(\cdot)$ этого же уравнения, начинающегося в этом диске (т. е. такого, что $\left.x\left(t_{0}\right) \in D^{k}\right)$, то решение $x_{0}(\cdot)$ наз. а с и мп т о т и ч е с к и (соответственно, эк с поненциа льно) условно устойч и в ы м (с индексом $k$ ).

\begin{enumerate}
  \setcounter{enumi}{3}
  \item Пусть $V^{n}$ есть $n$-мерное многообразие класса $C^{m}$, $U$-открытое множество в нем. Пусть точка $x_{0} \in U$ неподвижна при отображениях $f_{t}: U \longrightarrow V^{n}$ класга $C^{m}\left(t \in G^{+}\right.$, где $G$ есть $\mathbb{R}$ или $\left.\mathbb{Z}\right)$. Неподвижная точка $x_{0}$ наз. условно устой ч и в о й (с индексом $k$ ) относительно семейства отображений $\left\{f_{t}\right\}_{t \in G+}$, если найдется $k$-мерный диск $D^{k}$, гладко вложенный (вложение класса $C^{m}$ ) в $V^{n}$ и такой, что для всякой окрестности $V \subset V^{n}$ точки $x_{0}$ найдется окрестность $W$ той же точки такая, что $f_{t}\left(D^{k} \cap W\right) \subset V$ при всяком $t \in G^{+}$. Если диск $D^{k}$, имеющий указанные свойства, может быть выбран так, что $\lim _{t \rightarrow+\infty} f_{t} x=x_{0}$ для всякого $x \in D^{k}$, то неподвижная точка $x_{0}$ наз. $\mathbf{a}$ с имптот и ч е с ки условно у с то й ч І в ой (с пндексом $k$ ) относительно семейства отображений $\left\{f_{t}\right\}_{t \in G+}$

  \item Условная (условная асимптотическая, условная экспоненциальная) устойчивость (с индексом $k$ ) реше- ния $y_{0}(\cdot)$ уравнения произвольного порядка $y^{(m)}=$ $=g\left(y, \dot{y}, \ldots, y^{(m-1)}, 1\right)$ определяется как условная (условная асимптотическая, әкспоненшиальная) устойчивость (с индексом $k$ ) решения $x_{0}(\cdot)=\left(y_{0}(\cdot), y_{0}(\cdot), \ldots\right.$ $\left.\ldots, y_{0}^{(m-1)}(\cdot)\right)$ соответствующего уравнения 1 -го порядка (2), где

\end{enumerate}

\[
\begin{gathered}
x=\left(x_{1}, \ldots, x_{m}\right), \\
f(x, t)=\left(x_{2}, \ldots, x_{m}, g\left(x_{1}, \ldots, x_{m}, t\right)\right) .
\end{gathered}
\]

Иногда (см., напр., [3]) в определении У. у. требуют, чтобы индекс $k$ был отличен от нуля: У. у. с индексом нуль всегда имеет место. У. у. (условная асимптотическая, условная экспоненциальная устойqивость) с индексом $n$ (размерность фазового пространства) есть то же, что устойчивость по Ляпунову (соответственно, асимптотическая, экспоненциальная).

Исследование положения равновесия на У. У. Пусть в окрестности точки $x_{0} \in \mathbb{R}^{n}$ задано автономное дифференциальное уравнение

\[
\dot{x}=f(x),
\]

правая часть к-рого непрерывно дифференци руема п в точке $x_{0}$ обращается в нуль. Еслиг в открытой левой комплексной полуплоскости лежат $k$ собственных значений пропзводной $d f_{x_{0}}$, то неподвіжная точка уравнения (3) экспоненциалььно условно устойчива с индексом $k$ (теорема Ляпунов а об условной у с т о й ч І в о сти). Напр., верхнее положение равновесия $y=\pi, \dot{y}=0$ уравнения колебаний маятника $\ddot{y}+\omega^{2} \sin y=0$ экспоненциально условно устойчиво с пидексом 1, так как один из корней характеристического уравнення $\lambda^{2}-\omega^{2}=0$ уравнения в вариациях $\dot{y}-\omega^{2} y=0$ отрицателен.

Неподвижная точка $x_{0}$ дифференіпруемого отображенія $f: \mathbb{R}^{n} \longrightarrow \mathbb{R}^{n}$ экспоненциально условно устойчива с индексом $k$ относительно $f$, если $k$ собственных значений производной $d f_{x_{0}}$ лежат в открытом единичном круге. Периодич. точка $x_{0}$ дифференцируемого отображения $f: \mathbb{R}^{n} \longrightarrow \mathbb{R}^{n}$, имеющая период $m$, уеловно (асимптотически условно, экспоненциально условно) устойчива c индексом $k$ относительно $f$ тогда и тольєо тогда, когда она обладает этим свойством относительно отображения $f^{m}$.

Периодич. решение автономного дифферендиального уравнения (3) с гладкой правой частью $f(x)$, имеющее период $T$, (асимптотически, экспоненциально) условно устойчиво с индексом $k$ в том и только в том случае, если его значение в точке $t=0$ (соответственно, асимптотически, экспоненциально) условно устойчиво с индексом $k$ относительно отображения $X(T, 0)$, где $X(\theta, \tau)-$ птератор Коши уравнения (3).

Пример Перрона (см. Ј'стойчивость по Ляпунову) показывает, что изз отрицательности $k$ показателей Ляпунова уравнения в вариациях вдоль решения уравнения (3) не следует $\mathbf{y}$. у. с индексом $k$ этого решения. Однако имеют место следующие теоремы, показывающие, что ситуация, описываемая примером Перрона, нетипична.

\begin{enumerate}
  \item Пусть $S$ - множество всех диффеоморфизмов $f$ евклидова пространства $E^{n}$, имеющих равномерно непрерывную производную, удовлетворяющую неравенству
\end{enumerate}

\[
\sup _{x \in E^{n}} \max \left\{\left\|d f_{x}\right\|, \quad\left\|\left(d f_{x}\right)^{-1}\right\|\right\}<+\infty .
\]

Для всякого диффеоморфизма $j \in S$ через $S_{j}$ обозначается множество диффеоморфизмов $f \in S$, удовлетворяющих неравенству

\[
\sup _{x \in E^{n}}|f x-j x|<+\infty
\]


\end{document}