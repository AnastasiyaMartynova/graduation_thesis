\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}

\begin{document}
с $x=[n q(1-q)]^{-1 / 2}(m-n q)$ равномерно лля всех $x$ из ограниченного интервала (ранее в частном случае это было сделано А. Муавром (A. de Moivre, 1730). Это утверждение является простейшим среди п ре де л ьных те о ре м теории вероятностей, к-рые обуславливают фундаментальную роль Г. р. в приложениях. Общий смысл предельных теорем состоит в том, что при широких предположениях суммы случайных величин асимптотически с ростом числа слагаемых ведут себя согласно Г.р.

Г. р. является единственным типом устойчивого распределения с конечным вторым моментом. Оно может появляться как точное решение нек-рых задач (в рамках принятой математич. модели) (см. Винеровский процесс, ОрнштейнаУленбека процесс).

Г. p. часто встречается в физич. приложениях: Г. р. описывает малые флуктуации термодинамич. величин вблизи положения равновесия, распределение молекул по скоростям (см. Максвелла распределение), распределение ошибок наблюдения и др.

Многомер ное Г. р. случайного вектора $\xi=\left(\xi_{1}, \xi_{2}, \ldots, \xi_{n}\right)$ в $\mathbb{R}^{n}$ определяется таким образом, что при любом $a \in \mathbb{R}^{n}$ скалярное произведение $(a, \xi)$ имеет $\Gamma$. р. или неслучайно. Характеристич. функция многомерного Г. р. при $t \in \mathbb{R}^{n}$ имеет вид:

\[
\varphi_{\xi}(t)=\exp \left\{i \mathrm{E}(t, \xi)-\frac{1}{2} \mathrm{E}(t, \xi-\mathrm{E} \xi)^{2}\right\} .
\]

В случае, если квадратичная форма

\[
\mathrm{E}(t, \xi-\mathrm{E} \xi)^{2}=\sum_{k, l=1}^{n} \sigma_{k l} t_{k} t_{l},
\]

где $\left\|\sigma_{k l}\right\|$ - ковариационная матрица вектора $\xi$, строго положительна, то соответствующая плотность Г. р. имеет вид

\[
p(X)=\left[(2 \pi)^{n} \operatorname{det}\|\sigma\|\right]^{-1 / 2} \exp \left\{-\frac{1}{2}\left(X-E \xi, \sigma^{-1}(X-E \xi)\right)\right\},
\]

где $X=\left(x_{1}, x_{2}, \ldots, x_{n}\right) \in \mathbb{R}^{n}$.

Лит.: [1] Ширяев А. Н., Вероятность, М., 1980; [2] Боровков А. А., Теория вероятностей, 2 изд., М., 1986; [3] Фелле р В., Введение в теорию вероятностей и ее приложения, пер. с англ., т. 1-2, 2 изд., М., 1984.

ГАУССА РЯД - см. Гипергеометрическая функция.

ГАУССА TEOPEMA - см. Грина формулы.

ГАУССА УРАВНЕНИЕ - см. Гипергеометрическая функиия. ГАУССА ФОРМУЛА - см. Грина формулы.

ГАУССА - БОННЕ ФОРМУЛА - см. Эйлерова характеристика.

ГАУССА - ЛАПЛАСА РАСПРЕДЕЛЕНИЕ - см. Гаусcа распределение.

ГАУCCA - MAНИНА СвЯЗНОСТЬ - операция переноса классов (ко)гомологий. Пусть с каждым расслоением связано расслоение (ко)гомологий, слои которого - (ко)гомологии соответствующих слоев исходного расслоения. Перенос слоев над кривыми в базе индуцирует перенос классов (ко)гомологий; операция переноса этих классов и называется связностью Гаусса-Манина. С. В. Чмутов. ГАУССА - ОСТРОГРАДСКОГО ФОРМУЛА - сМ. Осmроградского формула.

ГАУССОВ ФУНКЦИОНАЛЬНЫЙ ИНТЕГРАЛ - ФУНКЦИональный интеграл вида

\[
I=\int \exp \left(S_{2}+S_{1}\right) D \mu,
\]

где $S_{2}$ - квадратичная, а $S_{1}$ - линейная формы относительно переменных интегрирования. Г. Ф. и. представляет собой важный с точки зрения приложений класс функциональных интегралов, значение к-рых находится в замкнутом виде. В квантовой теории поля Г.Ф. и. используется для описания невзаимодействующих систем во внешнем поле.

Приме ры вычисления Г. ф. и.:

\begin{enumerate}
  \item Пусть
\end{enumerate}

\[
\begin{gathered}
S_{2}=-\frac{1}{2} \int a(x) K(x, y) a(y) d x d y, \\
S_{1}=\int \eta(x) a(x) d x=\eta a, \\
D_{\mu}=\prod_{x} d a(x) ;
\end{gathered}
\]

тогда

\[
I=\operatorname{det}^{-x / 2}(K) \exp \left\{\frac{1}{2} \eta K^{-1} \eta\right\}, \quad,
\]

где $x=1$ в случае, когда интегрирование ведется по вещественному гильбертову пространству $L, a(x)$ и $\eta(x) \in L$, а $K-$ положительно определенный симметричный обратимый на $L$ интегральный оператор с ядром $K(x, y) ; x=-1$ в случае, когда интегрирование ведется по грассмановой алгебре $\Lambda$, $a(x)$ и $\eta(x) \in \Lambda$, а $K$ - обратимый на $\Lambda$ кососимметрич. интегральный оператор с ядром $K(x, y)$.

\begin{enumerate}
  \setcounter{enumi}{1}
  \item Пусть
\end{enumerate}

\[
\begin{gathered}
S_{2}=-\int a^{+}(x) K(x, y) a(x) d x d y=a^{+} K a \\
S_{1}=\int\left[a^{+}(x) \eta(x)+\eta^{+}(x) a(x)\right] d x=\eta^{+} a+a^{+} \eta, \\
D \mu=\prod_{x} d a^{+}(x) d a(x)
\end{gathered}
\]

тогда

\[
I=\operatorname{det}^{-x}(K) \exp \left\{\eta^{+} K^{-1} \eta\right\}
\]

где $x=1$ в случае, когда интегрирование ведется по гильбертову пространству $L$ с инволюцией, $a(x), a^{+}(x), \eta^{+}(x)$, $\eta(x) \in L$, а $K$ обратим на $L$ и $\operatorname{Re} a^{+} K a>0 ; x=-1$ в случае, когда интегрирование ведется по грассмановой алгебре $\Lambda$ с инволюцией, $a(x), a^{+}(a), \eta^{+}(x)$ и $\eta(x) \in \Lambda$, а $K$ обратим на $\Lambda$.

Строго говоря, для существования рассмотренных Г. Ф. и. необходимо требовать существования входящих в формулы примеров определителей, что выполняется тогда и только тогда, когда оператор $K-E$ является ядерным. Последнее обычно не выполняется для функциональных интегралов, используемых при описании квантовополевых систем. В этом случае необходимо регуляризовать определитель в соответствии с конечномерными аппроксимациями, пределом к-рых является исследуемый функциональный интеграл.

Лит.: [1] Бе р е 3 и н Ф.А., Метод вторичного квантования, 2 изд., М., 1986; [2] В а с и л ь е в А. Н., Функциональные методы в квантовой теории поля и статистике, Л., 1976; [3] Далецк й й Ю. Л., Фом и н С. В., Меры и дифференциальные уравнения в бесконечномерных пространствах, М., $1983 . \quad$ С. А. Федотов.

ГАУССОВА КРИВИЗНА - величина, характеризующая отклонение поверхности от плоскости; она выражается только через коэффициенты первой квадратичной формы:

\[
\begin{gathered}
k=\frac{1}{\left(E G-F^{2}\right)^{2}}\left|\begin{array}{lll}
E & E_{u} & E_{v} \\
F & F_{u} & F_{v} \\
G & G_{u} & G_{v}
\end{array}\right|- \\
-\frac{1}{2 \sqrt{E G-F^{2}}}\left\{\left(\frac{E_{v}-F_{u}}{\sqrt{E G-F^{2}}}\right)_{v}-\left(\frac{F_{v}-G_{u}}{\sqrt{E G-F^{2}}}\right)_{u}\right\} .
\end{gathered}
\]

М. И. Войцеховский.

ГАУССОВО СЛУЧАЙНОЕ ПОЛЕ - случайная функция $\left\{\xi_{t}, t \in T\right\}$, определенная на $v$-мерной решетке $T=\mathbb{Z}^{V}$ или в $v$-мерном пространстве $T=\mathbb{R}^{v}$, принимающая вещественные значения и такая, что все ее конечномерные распределения гауссовы. Подробнее:

\begin{enumerate}
  \item $T=\mathbb{Z}^{\vee}$. В этом случае Г. с. п. $\left\{\xi_{t}, t \in \mathbb{Z}^{\vee}\right\}$ задается средним $\left\langle\xi_{t}\right\rangle=a_{t}$ и матрицей ковариаций $\left\langle\xi_{t_{1}}, \xi_{t_{2}}\right\rangle-\left\langle\xi_{t_{1}}\right\rangle\left\langle\xi_{t_{2}}\right\rangle=b_{t_{1} t_{2}}$, где $\langle\cdot, \cdot\rangle$ означает среднее по распределению поля. При этом для любого конечного подмножества $\Lambda=\left\{t_{1}, \ldots, t_{n}\right\} \subset \mathbb{Z}^{V}$ плотность совместного распределения случайных величин $\left\{\xi_{t_{i}}=\xi_{i}, i=1, \ldots, n\right\}$ имеет вид:
\end{enumerate}

$p\left(x_{1}, \ldots, x_{n}\right)=\frac{\left(\operatorname{det} A_{\Lambda}\right)^{1 / 2}}{(2 \pi)^{n / 2}} \exp \left\{-\frac{1}{2} \sum_{i, j} a_{i j}^{(\Lambda)}\left(x_{i}-a_{i}\right)\left(x_{j}-a_{j}\right)\right\}$

где

\[
\begin{gathered}
A_{\Lambda}=\left\{a_{i j}^{(\Lambda)}\right\}=\left(B_{\Lambda}\right)^{-1}, \\
B_{\Lambda}=\left\{b_{i j}\right\}, i, j=1, \ldots, n, \\
b_{i j}=b_{t_{i} t_{j}}, a_{i}=a_{t_{j}}
\end{gathered}
\]


\end{document}