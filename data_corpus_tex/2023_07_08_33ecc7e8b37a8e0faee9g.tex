\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{mathrsfs}

\begin{document}
Пусть дан набор гильбертовых пространств $H_{x}$, где $x$ пробегает нек-рое множество $X$, на к-ром введена мера $d_{\mu}$, и все $\boldsymbol{H}_{x}$ можно реализовать как подпространства в одном и том же гильбертовом пространстве $H$, причем для нек-рого (а значит и для любого) базиса $e_{k}$ в $H$ измеримы числовые функции

\[
\alpha(x)=\left\langle e_{k}\left|P_{x}\right| e_{l}\right\rangle,
\]

где через $\langle\cdot \mid \cdot\rangle$ обозначено скалярное произведение в $H$, а через $P_{x}$ - ортогональный проектор на $H_{x}$. (Случай, когда все $H_{x}$ совпадают с $H$, вполне содержателен.) Можно считать (это не приведет к потере общности), что подпространство $H_{x}$ натянуто на первые $n(x)$ базисньх векторов в $H$, где $n(x)$ - измеримая функшия на $X$, принимающая значения в множестве $1,2, \ldots, \infty$. Рассмотрим вектор-функцию $f(x)$ на $X$ такую, что для любого $x$ выполнено $f(x) \in H_{x}$. Назовем такую функцию и з м е р и м о й, если для любого $e_{l}$ числовая функция $\left\langle f(x), e_{l}\right\rangle$ измерима. Элементами пространства

\[
\mathscr{H}=\int_{X} H_{x} d \mu
\]

(прямого интеграла гильбертовых пространств $H_{x}$ ) являются измеримые вектор-функции $f(x)$ на $X$ такие, что:

\begin{enumerate}
  \item $f(x) \in H_{x}$

  \item $\int_{X}\langle f(x) \mid f(x)\rangle d \mu(x)<\infty$.

\end{enumerate}

Скалярное произведение в $\mathscr{H}$ вводится по формуле

\[
(f, g)=\int_{X}\langle f(x) \mid g(x)\rangle d \mu(x) .
\]

Пусть теперь в каждом пространстве $H_{x}$ задано унитарное представление $T^{x}$ группы $G$, причем все числовые функции

\[
\beta(x)=\left\langle e_{k}\left|T_{g}^{x}\right| e_{l}\right\rangle
\]

измеримы. Определим представление

\[
\tilde{T}=\int T_{x} d \mu(x)
\]

группы $G$ в пространстве $\mathscr{H}$ по формуле:

\[
\left[\widetilde{T}_{g} f\right](x)=T_{g}^{x} f(x)
\]

оно и называется интегралом представления $T^{x}$. Если мера точки $x$ равна 0 и $T^{x}$ не совпадает ни с каким $T^{y}$, то $T^{x}$ не является подпредставлением в $\widetilde{T}$.

Пусть $G$ - группа Ли. Тогда любое унитарное представление группы $G$ может быть разложено в интеграл неприводимых представлений. На практике, чтобы построить такое разложение, обычно необходимо найти подходящее интегральное преобразование.

Лит.: Диксмье Ж., С -алгебры и их представления, пер. с франц., М., $1974 . \quad$ Ю. А. Неретин. ИНТЕГРАЛ ЭНЕРГИИ - см. Динамическая система.

ИНТЕГРАЛЬНАЯ КРИВАЯ - график решения дифференциального уравнения или системы дифференциальных уравнений. В. И. Битюиков.

ИНТЕГРАЛЬНАЯ ПОВЕРХНОСТЬ - см. Дифференциальное уравнение с частными производными второго порядка. ИНТЕГРАЛЬНАЯ ПОКАЗАТЕЛЬНАЯ ФУНКЦИЯ - см. Интегральные функции.

ИНТЕГРАЛЬНОЕ МНОГООБРАЗИЕ - поДМНогообразие, касающееся распределения. $k$-мерное распределение называется и нте гр и руемым (или слоен и м м), если через каждую точку многообразия проходит $k$-мерное И. м. Необходимые и достаточные условия интегрируемости распределения в терминах задающих его 1-форм дает теорема Фробениуса. Для распределения коразмерности 1, заданного $1-$ формой $\alpha$, это условие: $\alpha \wedge d \alpha=0$.

Максимальная размерность И. м. контактного распределения в $M^{2 n+1}$ равна $n$. Такие подмногообразия называются лежандровыми многообразиями.

C. А. Табачников ИHТEГPAЛЬНОЕ УРАВНЕНИЕ - уравнение, в к-ром неизвестная функция содержится под знаком интеграла. В классич. теории область определения $\Omega$ неизвестной функции является либо отрезком прямой или кривой - такие И. у. называют о д ном е р ны м и, либо областью конечномерного пространства или многообразия - такие И. у. называют многомерными; при этом в зависимости от типа рассматриваемого интеграла различают И. у. с интегралами Римана, Лебега, Стилтьеса, Коши и т. д. Рассматриваются И.у., у к-рых $\Omega$ является абстрактным множеством, на нек-рой $\sigma$-алгебре к-рого задана мера; такие И. у. охватывают и различные классы бесконечных систем с бесконечным числом переменных. Чаше всего рассматриваются И. у., в к-рых искомая функция принимает или скалярные значения, или значения из конечномерного пространства; в последнем случае И.у. по существу является системой И.у. Встречаются И. у., в к-рых искомые функщии принимают значения в банаховых или еще более общих пространствах или многообразиях; ниже речь идет лишь об И. у., в к-рых неизвестные функции принимают скалярные значения.

К И. у. сводятся многие задачи математич. физики. Исследование этих И. у. часто оказывается существенно более простой задачей по сравнению с исходной проблемой. Тем самым теория И. у. оказывается одним из наиболее важных методов математич. физики. Сведение многих проблем математич. физики к И. у. осуществляется обычно при помощи Грина функций

По характеру зависимости левой и правой частей И. у. от неизвестной функции различают линейные и нелинейные И.у. Среди л и не й н ы х И.у. наиболее часто встречаются уравнения вида

\[
a(x) u(x)+b(x) \int_{\Omega} k(x, y) u(y) d y=f(x)
\]

здесь $u(x)$ - неизвестная функция, $k(x, y), a(x)$ и $b(x)-$ известные функции, первая из к-рых называется яд ром И. у., а остальные - ко эфф ици е н т а м и. Если интеграл в (1) понимается в смысле Римана или Лебега, то (1) называется интегральным уравнением Фред голь м а. Если интеграл в (1) понимается как несобственный в каком-либо смысле, то (1) называется с и н г у л я р ным линейным И. у.; среди сингулярных И.у. наиболее часто встречаются уравнения, в К-рых интеграл понимается в смысле главного значения Коши

\[
\int_{\Omega} k(x, y) u(y) d y=\lim _{\varepsilon \rightarrow \infty} \int_{\Omega \backslash B(x, \varepsilon)} k(x, y) u(y) d y
\]

[здесь $B(x, \varepsilon)$ - шар с центром в $x$ радиуса $\varepsilon$ ]. И. у. (1) называется однородным, если $f(x)=0$, и неодноро д ным - в противном случае. В частном случае, когда $a(x)=0$ и $b(x)=1$, И.у. (1) называется уравнением пе р вого рода; когда $a(x)=\lambda(\lambda-$ ненулевое число) и $b(x)=-1$, И.у. (1) называется уравнением второго р о д а; в общем случае И.у. (1) часто называют у р а в е нием третьего рода.

Среди многочисленных и самых разнообразных н е л и не й ных И. у. наиболее часто встречаются и нте граль ные уравнения Гаммерштейна

\[
u(x)=\int_{\Omega} k(x, y) f(y, u(y)) d y
\]

и несколько более общие интегральные уравнения Урысона

\[
u(x)=\int_{\Omega} k(x, y, u(y)) d y .
\]

Из других классов нелинейных И.у. отметим уравнения вида

\[
u(x)=g\left(x, u(x), \int_{\Omega} k(x, y, u(x), u(y))\right) d y
\]

и уравнения

\[
\begin{gathered}
u(x)=\sum_{n=0}^{\infty} \int_{\Omega} \ldots \int_{\Omega} k_{n}\left(x, y_{1}, \ldots, y_{n}, u(x), u\left(y_{1}\right), \ldots, u\left(y_{n}\right)\right) \times \\
\times d y_{1} \ldots d y_{n} .
\end{gathered}
\]


\end{document}