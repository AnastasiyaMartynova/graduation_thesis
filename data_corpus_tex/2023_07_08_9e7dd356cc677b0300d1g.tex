\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }
\usepackage{bbold}

\begin{document}
условно сходится, а ряд

\[
\begin{gathered}
\left(\sum_{n=1}^{\infty} \frac{(-1)^{n-1}}{\sqrt{n}}\right)^{2}= \\
=\sum_{n=1}^{\infty}(-1)^{n-1} \sum_{k=1}^{n} \frac{1}{\sqrt{k} \sqrt{n-k+1}}
\end{gathered}
\]

расходится. Если все три Р.-(2), (4), (9)-сходятся, то $a b=c$.

Примером другого правила умножения Р. является суммирование сначала попарных произведений $a_{m} b_{n}$, у к-рых произведение индексов $m n$ имеет фиксированное значение

\[
c_{p}=\sum_{m n=p} a_{m} b_{n},
\]

и определение произведения рядов (2) и (4) как ряда $\sum c_{p}$. Это правило умножения Р. подсказывается формулой умножения Дирихле рядов:

\[
\sum \frac{a_{m}}{m^{x}} \sum \frac{b_{n}}{n^{x}}=\sum \frac{c_{p}}{p^{x}}
\]

Встречаются также Р., члены к-рых $a_{n}$ занумерованы всеми целыми числами $n=0, \pm 1, \pm 2, \ldots$. Они обозначаются:

\[
\sum_{n=-\infty}^{+\infty} a_{n}
\]

Р. (10) наз. с х о д щ и м с я, если сходятся ряды

\[
\sum_{n=1}^{\infty} a_{n-1} \text { и } \sum_{n=1}^{\infty} a_{-n},
\]

\begin{center}
\includegraphics[max width=\textwidth]{2023_07_08_9e7dd356cc677b0300d1g-1}
\end{center}

Числовыми P. более сложной структуры являются кратные ряды, члены $a_{n_{1}} \ldots n_{m}$ к-рых снабжены мультииндексами, где $n_{k}$ - натуральные числа, $k=$ $=1,2, \ldots, m, m=2,3, \ldots$ В теории кратных $\mathrm{P}$. рассматриваются различные тишы частичных сумм: треугольные

\[
s_{n}=\sum_{k_{1}+\ldots+k_{m} \leqslant n} a_{k_{1}} k_{m}
\]

прямоугольные

сферические

\[
s_{n_{1} \ldots n_{m}}=\sum_{k_{1}=1}^{n_{1}} \cdots \sum_{k_{m}=1}^{n_{m}} a_{k_{1} . k_{m}}
\]

\[
s_{\boldsymbol{r}}=\sum_{k_{1}^{2}+\ldots+k_{m}^{2} \leqslant r^{2}} a_{k_{1} \quad k_{m}}, r>0,
\]

и др. В зависимости от выбора типа частичных сумм определяется понятие суммы кратного Р. как соответствующего их предела. В случае $m=2$ кратный Р. наз. двойным рядом. Для кратных Р., в отличие от однократных, члены P. уже могут не определяться заданием множества частичных сумм, т. е., вообще говоря, для того чтобы кратный Р. был определен, необходимо задать как кратную последовательность членов Р., так и множество его частичных сумм.

В математич. анализе находят применение не только сходящиеся Р., но и расходящиеся. Для последних разработаны разнообразные методы суммирования.

В виде сумм числовых Р. записываются многие важные иррациональные постоянные, напр.:

\[
e=\sum_{n=0}^{\infty} \frac{1}{n !}, \quad \pi^{2}=6 \sum_{n=1}^{\infty} \frac{1}{n^{2}},
\]

а также значения определенных интегралов, первообразные подинтегральных функций к-рых не выражаются через элементарные функции:

\[
\begin{gathered}
\int_{0}^{1} \frac{\operatorname{arctg} x}{x} d x=\sum_{n=1}^{\infty} \frac{(-1)^{n-1}}{(2 n-1)^{2}}, \\
\int_{0}^{1} x-x d x=\sum_{n=1}^{\infty} \frac{1}{n^{n}} .
\end{gathered}
\]

Рд

Функциональные ряды. Ф у н к ц и о н а л ь н ы м (о днок р а т ны м) р я до м

\[
\sum a_{n}(x), \quad x \in X,
\]

наз. пара функциональных последовательностей $\left\{a_{n}(x)\right\}$ и $\left\{s_{n}(x)\right\}$, состоящих из числовых функций, определенных на нек-ром множестве $X$ и таких, что

\[
s_{n}(x)=a_{1}(x)+a_{2}(x)+\ldots+a_{n}(x), n=1,2, \ldots .
\]

Как и в случае числовых Р., элементы последовательности $\left\{a_{n}(x)\right\}$ наз. ч л е н а м и Р. (11), а последовательности $\left\{s_{n}(x)\right\}$ - его ч а с т и ч ны м и с у м м ами. Р. (11) наз. с х о д я и и с я на мно жес т в е $X$, если при любом фиксированном $x_{0} \in X$ сходится числовой ряд

\[
\sum a_{n}\left(x_{0}\right)
\]

П р и м е р. Ряд

\[
\sum_{n=0}^{\infty} \frac{z^{n}}{n !}=e^{z}
\]

сходится на всей комплексной плоскости, а ряд

\[
\sum_{n=0}^{\infty} n ! z^{n}, z \in \mathbb{C}
\]

только при $z=0$.

Сумма сходящегося Р. непрерывных, напр. на некром отрезке, функций не обязательно является непрерывной функцией, напр. ряд

\[
1+\sum_{n=1}^{\infty} x^{n-1}(x-1)
\]

сходится на отрезке $[0,1]$, его члены непрерывны на этом отрезке, а сумма

\[
s(x)=\left\{\begin{array}{lll}
0, & \text { если } & 0 \leqslant x<1, \\
1, & \text { если } & x=1
\end{array}\right.
\]

разрывна в точке $x=1$. Ряд условий, при к-рых на функциональные Р. переносятся свойства непрерывности, дифференцируемости и интегрируемости конечных сумм соответственно непрерывных, дифференцируемых или интегрируемых функций, формулируется в терминах равномерной сходимости рядов (см. Равномерно сходящийся ряд).

Р я ды и м е р имых ф ункций. Пусть измеримое по Лебегу подмножество $n$-мерного евклидова пространства $\mathbb{R}^{n}, \mu$ - мера Лебега и члены $a_{k}(x)$ ряда

\[
\sum a_{k}(x), \quad x \in X \subset \mathbb{R}^{n}
\]

являются измеримыми почти всюду конечными на $X$ функциями, принимающими значения из расширенной числовой прямой (т. е. наряду с действительными числами они могут принимать значения $+\infty$ и $-\infty$ ). Если Р. (12) сходится почти всюду на множестве $X$, то его сумма $s(x)$ также является измеримой функцией и, в силу Егорова теоремь, для любого $\varepsilon>0$ существует такой компакт $E \subset X$, что $\mu(X \backslash E)<\varepsilon$, а $\mathrm{P}$., членами к-рого являются сужения $\left.a_{k}(x)\right|_{E}$ функций $a_{k}(x)$ на компакте $E$, сходится на нем равномерно и его суммой является $\left.s(x)\right|_{E}$ - сужение суммы Р. (12) на рассматриваемом компакте.

Пусть $L_{p}(X), 1 \leqslant p \leqslant+\infty,-$ пространство функций $f: X \stackrel{\mathbb{R}}{\rightarrow}$ соответственно с нормой

\[
\|f\|_{p}=\left(\int_{X}|f(x)|^{p} d x\right)^{1 / p}
\]

при $1 \leqslant p<+\infty$ и с нормой

\[
\|f\|_{\infty}=\sup _{x \in X} \operatorname{vrai}|f(x)|
\]


\end{document}