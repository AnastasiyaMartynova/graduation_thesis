\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\DeclareUnicodeCharacter{0131}{$\imath$}

\begin{document}
к $S O_{n}(\mathbb{R})$, т. е. расслоение ориентируемо тогда и только тогда, когда $w_{1}(\xi)=0$.

П е р в й к л а с с Ч ж э н. Дана короткая точная последовательность

\[
0 \longrightarrow \mathbb{Z} \longrightarrow \mathbb{C} \stackrel{\exp }{\longrightarrow} \mathbb{C}^{0} \longrightarrow 0 .
\]

Связывающий гомоморфизм $\delta: H^{1}\left(B ; \mathbb{C}^{0}\right) \rightarrow H^{2}(B ; \mathbb{Z})$ соответствующей когомологич. последовательности сопоставляет с каждым одномерным комплексным расслоением $\xi$ над $B$ двумерный класс когомологий базы $B$, наз. п е р в ы м к л а с с ом Ч ж эня расслоения и обозначаемый $c_{1}(\xi)$. Иными словами, если $g_{\alpha \beta}: u_{\alpha} \cap$ $\bigcap u_{\beta} \longrightarrow \mathbb{C}^{0}$ функции перехода расслоения $\xi$, то выбором произвольных значений логарифмов $\ln g_{\alpha \beta}$ получается двумерный целочисленный коцикл $\left\{k_{\alpha \beta \gamma}\right\}$ :

\[
k_{\alpha \beta v}=\frac{1}{2 \pi i}\left(\ln g_{\alpha \beta}+\ln g_{\beta \nu}+\ln g_{\gamma \alpha}\right)
\]

и $c_{1}(\xi)$ есть по определению клаос когомологий этого коцикла.

С п ин о р на я с т р у т у р а. Имеет место точная последовательность групп

$\stackrel{1}{\longrightarrow} \mathbb{Z}_{2} \longrightarrow \operatorname{Spin}_{n}(\mathbb{R}) \longrightarrow S O_{n}(\mathbb{R}) \longrightarrow 1$

где $\operatorname{Spin}_{n}(\mathbb{R})$ - группа, определяемая в теории Клифборда алгебр. Связывающее отображение $w_{2}: H^{1}(B$; $\left.S O_{n}(\mathbb{R})\right) \longrightarrow H^{2}\left(B ; \mathbb{Z}_{2}\right)$ соответствующей когомологич. ıоследовательности наз. в т о р ы м к л а с с о м III т иф е л я-Уитни. Структурная группа ориентированного векторного расслоения $\xi$ может быть редуцирована к $\operatorname{Spin}_{n}(\mathbb{R})$ тогда и только тогда, когда $w_{2}(\xi)=0$.

К л а с с Әй л е р а. Пусть база $B$ действительного векторного расслоения $\xi=(E, p, B)$ есть гладкое компактное $N$-мерное многообразие с краем $\partial B$ (возможно пустым), и нулевое сечение $i: B \longrightarrow E$ приведено в «общее положение с самим собой». Пусть близкое и изотопное к $i$ вложение $i^{\prime}: B \longrightarrow E$ трансверсально регулярно относительно $i(B) \subset E$. Тогда $X=i^{-1}\left(i^{\prime}(B) \cap\right.$ $\cap i(B))$ будет подмногообразием $B$ и $\partial X \subset \partial B, \operatorname{codim} X=$ $=n=\operatorname{dim} \xi$. Следовательно, $[X] \in H_{N-n}(B, \partial B)$. Двойственный $\kappa[X]$ класс когомологий наз. к л а с с о м Эй л е р а расслоения $\xi$ и обозначается $e(\xi) \in H^{n}(B)$. Расслоение $\xi$ имеет не обращающееся в нуль сечение тогда и только тогда, когда $e(\xi)=0$. Если $B$ связно, $\partial B=\varnothing$, a $\xi$-касательное расслоение, то $\operatorname{dim} X=0$, следовательно, $X$ состоит из конечного числа точек. Класс $[X] \subset H_{0}(B)$ определяется в этом случае целым тислом, к-рое обозначается через $\chi(B)$ и совпадает с эйлеровой характеристикой $B$.

Построение классов ІІтифеля - Уитни и Чжэня на языке теории препятствий (см. [6]-[8]) проводится так. Пусть $\eta: E \stackrel{p}{\longrightarrow} B-C е р p a$ расслоение, и $B-$ связное клеточное разбиение. Тогда гомотопич. тип слоя $F=$ $=p^{-1}(x)$ не зависит от $x \in B$. Если $\pi_{q}(F)$-первая нетривиальная гомотопич. группа слоя $F$ и $B$ односвязно, то первое препятствие к построению сечений $s: B \longrightarrow E$ лежит в групше $H^{q+1}\left(B ; \pi_{q}(F)\right)$. Это препятствие $\chi(\eta)$ инвариантно связано с расслоением $\eta$. Иногда инвари-

\includegraphics[max width=\textwidth, center]{2023_07_09_334f6e8e87e2c7bc8ccfg-1}
лексное векторное расслоение над $B, \operatorname{dim} \xi=n$. Для каждого $1 \leqslant q \leqslant n$ с $\xi$ ассоциируется другое расслоение $\eta^{q}$ со слоем $U_{n} / U_{q-i}$ (к о м п л е к с н ы м м ногоо б р а $з$ и е м ІІ тифе л я). Из точных последовательностей расслоений следует, что $\pi_{i}\left(U_{n} / U_{q-1}\right)=0$ при $i<2 q-1, \pi_{2 q-1}\left(U_{n} / U_{q-1}\right)=\mathbb{Z}$, так что $x\left(\eta^{q}\right) \in H^{2 q}(B)$. Этот класс наз. $q$-м к лас с о м Чж эня расслоения $\xi$, $c_{q}(\xi)=x\left(\eta^{q}\right)$.

Если $\xi$ - действительное расслоение, $F=\mathbb{R}^{n}$, то слоем расслоения $\eta^{q}$ будет $O_{n} / O_{q-1}$. Так как

\[
\begin{aligned}
& \pi_{q-1}\left(O_{n} / O_{q-1}\right)= \begin{cases}\mathbb{Z}_{2}, & \text { если } q \text { четно и } q<n, \\
\mathbb{Z}, & \text { если } q \text { нечетно или } q=n ;\end{cases} \\
& \pi_{i}\left(O_{n} / O_{q-1}\right)=0
\end{aligned}
\]

при $i<q-1$, то класс

\[
\chi\left(\eta^{q}\right) \in\left\{\begin{array}{l}
H^{q}(B), \text { если } q \text { нечетно или } q=n, \\
H^{a}\left(B ; \mathbb{Z}_{2}\right), \text { если } q \text { четно и } q<n .
\end{array}\right.
\]

Клас с ами III ти е ля - Уитни расслое-

ния $\xi$ наз. классы

\[
w_{q}(\xi)=x\left(\eta^{q}\right) \bmod 2 \in H^{q}\left(B ; \mathbb{Z}_{2}\right) .
\]

Впрочем, если $\xi$ неориентированно, то классы $\chi\left(\eta^{q}\right)$ определены корректно лишь с коэфффициентами в $\mathbb{Z}_{2}$.

При $q=n$ многообразие ІІтифеля есть сфера $S^{n-1}$ в действительном случае и $S^{2 q-1}-$ в комплексном. $3 \mathrm{a}-$ дача о построении сечения расслоения $\eta^{n}$ совгадае' с задачей о построении не обращающегося в нуль сечения расслоения $\xi$. Первое препятствие в этом случае наз. к л а с с о м Ә й л е р а $e(\xi)$

\[
e(\xi)=c_{n}(\xi) \in H^{2 n}(B)
\]

в комплексном случае,

\[
e(\xi)=x\left(\eta^{n}\right) \in H^{n}(B)
\]

в действительном ориентированном случае,

\[
e(\xi)=w_{n}(\xi) \in H^{n}\left(B ; \mathbb{Z}_{2}\right)
\]

в действительном неориентированном случае.

Пусть $E_{D^{\prime}}$ и $E_{S}$-пространства расслоений, ассоцинрованных с $\xi$ со слоями: диск $D^{n}$ и сфера $S^{n-1}$. Если $i: B \longrightarrow E_{D-\text { нулевое сечение, то }} e(\xi)=i^{*}(u)$, где $u \in$ $\in H^{n}\left(E_{D}, E_{S}\right)-T$ ома класс Пусть $F$-одно из тел $\mathbb{R}$, $\mathbb{C}, \mathbb{H}$, где $\mathbb{R}$-поле действительных чисел, $\mathbb{C}$-поле комплексных чисел, 너-тело кватернионов. Пусть $h^{*}-$ мультипликативная теория когомологий, обладающая следующим свойством: для каждого конечномерного векторного пространства $V$ над $F$ можно выбрать естественно относительно вложений такой әлемент $\alpha_{V} \in$ $\epsilon^{d}(P(V))$, где $\boldsymbol{P}(V)-$ многообразие всех одномерных подпространств в $V, P(V)=F P \operatorname{dim} V-1 \quad$ и $d=\operatorname{dim}_{\mathbb{R}} F$, что $h^{*}(P(V))=h^{*}(p t)\left[\alpha_{V}\right] /\left(\alpha_{V}^{n}\right), \quad$ где $n=\operatorname{dim} V$. Для случая $\operatorname{dim} V=2$ пусть $\alpha_{V}$ совпадает с фундаментальным классом (ориентацией) многообразия $P(V)$.

Пусть $\xi=(E, p, B)$ - векторное (в смысле $F)$ расслосние над $B$ со слоем $V, \operatorname{dim} \xi=n, \boldsymbol{P}(\xi)-$ п р о е к т ив и з а ц и я этого расслоения, т. е. локально тривиальное расслоение над $B$ со слоем $P(V)$, пространство к-рого $P(E)$ состоит из всех одномерных подпространств в слоях расслоения $\xi$. Над пространством $\boldsymbol{P}(E)$ имеется одномерное расслоение, пространство к-рого состоит из всех пар $(l, x)$, где $l$-одномерное подпространство слоя расслоения $\xi, l \in P(E), x$-точка пространства $l$. Этому расслоению соответствует классифицирующее отображкние $j: \boldsymbol{P}(E) \longrightarrow P(V)$. Пусть $a=j^{*}\left(\alpha_{V}\right), a \in h^{d}(P(E))$. Если снабдить группу $h^{*}(\boldsymbol{P}(E))$ структурой $h^{*}(B)$-модуля с помощью гомоморфизма $\pi^{*}$, где $\pi: P(E) \rightarrow B-$ проекция расслоения $\boldsymbol{P}(\xi)$, то этот модуль свободен и имеет базис $1, a, \ldots, a^{n-1}$. Существуют однозначно определенные классы когомологий $\sigma_{1}(\xi), \quad \sigma_{2}(\xi), \ldots$ $\ldots, \sigma_{n}(\xi), \sigma_{i}(\xi) \in h^{d i}(B)$, для к-рых

\[
\begin{aligned}
a^{n}-\pi^{*}\left(\sigma_{1}(\xi)\right) & a^{n-1}+\ldots+(-1)^{n-1} \pi^{*}\left(\sigma_{n-1}(\xi)\right) a+ \\
& +(-1)^{n} \pi^{*}\left(\sigma_{n}(\xi)\right)=0 .
\end{aligned}
\]

Для $F=\mathbb{R}$ условиям, налагаемым на теорию $h^{*}$, удовлетворяет, напр., теория $H^{*}\left(\cdot ; \mathbb{Z}_{2}\right)$. В этом случае определенные выше Х. к. обозначаются через $w_{1}, w_{2}, \ldots$ $\ldots, w_{n}$ и наз. г л а с с а м и III т и фе л я-У и тни. Для $F=\mathbb{C}$ в качестве $h^{*}$ можно взять обычную теорию когомологий $H^{*}$. Определенные выше классы для $H^{*}$ обозначаются через $c_{1}, c_{2}, \ldots, c_{n}$ и наз. к л а с с а м и Чжәня. Впрочем, при $F=\mathbb{C}$ требуемым условиям удовлетворяет любая ориентированная теория когомологий. При $F=\mathbb{H}$ также можно рассматривать обычную теорию $H^{*}$. В этом случае определенные выше классы


\end{document}