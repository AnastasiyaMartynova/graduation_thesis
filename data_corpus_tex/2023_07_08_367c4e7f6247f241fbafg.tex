\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}

\begin{document}
где $A_{1}$ и $A_{2}$ - самосопряженные операторы в сепарабельном гильбертовом пространстве $H, P_{1}$ - ортопроектор на $H_{1, a c}$, и такой, что

\[
\left\{W_{+}\left(A_{2}, A_{1}\right) x\left\|W_{+}\left(A_{2}, A_{1}\right) x\right\|=\|x\|\right\}=H_{2, a c} .
\]

Здесь $I I_{i, a c}, i=1,2,-$ совокупность всех спектрально абсолютио непрерывных относитөльно $A_{i}$ әлементов $x$, т. е. таких, что спектральная мера $\left\langle E_{A_{i}}(\mu) x, x\right\rangle$ множества $M$ абсолютно непрерывна относительно меры Лөбега $\mu(x)$.

Если оператор $W_{+}\left(A_{2}, A_{1}\right)$ (или аналогично определяемый оператор $\left.W_{-}\left(A_{2}, A_{1}\right)\right)$ существует и является полным. то $A_{i}$, ас-части операторов $A_{i}$ на $H_{i}, a c$ унитарно эквивалентны. Если $A_{1}$ и $A_{2}$ - самосопряженные операторы в $H$ и $A_{2}=A_{1}+c\langle\cdot, f>f$, где $f \in H$ и $c$ вещественно, то $W_{ \pm}\left(A_{2}, A_{1}\right)$ и $W_{ \pm}\left(A_{1}, A_{2}\right)$ существуют и являются нолными.

Лит.: [1] К а т о Т., Теория возмущений линейных операторов, пер. с англ., М., 1972. В. И. Соболев.

ПОЛОВИННОГО ДЕЛЕНИЯ МЕТОД, д и х о то м и и,- 1) Один из методов численного решения уравнений с одним неизвестным. Пусть имөется уравнение $f(x)=0$ с непрерывной на отрезке $[a, b]$ функцией $f(x)$, принимающей на концах отрезка значения разных знаков и имеющей внутри $[a, b]$ единственныйі корень $x_{*}$. Для приближенного нахождения $x_{*}$ отрезок $[a, b]$ делят пополам и вычисляют значение $f\left(x_{1}\right)$ в средней точке $x_{1}=(a+b) / 2$. Если $f\left(x_{1}\right) \neq 0$, то из двух отрезков $\left[a, x_{1}\right]$ и $\left[x_{1}, b\right]$ для последующего деления пополам выбирается тот, на ковцах к-рого значения функции различны по знаку. Возникающая в процессе такого дробления последоватөльность сөредин отрезков $x_{1}, x_{2}, \ldots$ сходится к корню $x_{*}$ со скоростью геометрич. прогрессии:

\[
\left|x_{n}-x_{*}\right| \leqslant(b-a) / 2^{n}, n=1,2, \ldots,
\]

причем в рассматриваемом классе функций оценка (1) неулучшаема. В случає, когда функция $f(x)$ имөет на $[a, b]$ более одного корня, последовательность будет сходиться к одному из них.

\begin{enumerate}
  \setcounter{enumi}{1}
  \item Один из методов минимизации функций одного переменного, Пусть требуется найти минимум
\end{enumerate}

\[
f_{*}=\min _{a \leqslant x \leqslant b} f(x)
\]

унимодальной функции $f(x)$ на отрезке $[a, b]$ и указать точку $x_{*}$, в к-рой он достигаөтся. Тогда отрезок $[a, b]$ делят пополам и вблизи его середины $\bar{x}_{1}=(a+b) / 2$ вычисляют значения функции $f(x)$ в двух точках $x_{1}=\bar{x}_{1}-\varepsilon / 2$ и $x_{2}=\bar{x}_{1}+\varepsilon / 2$, где число $\varepsilon>0$, являющееся параметром метода, достаточно мало. Затем значения $f\left(x_{1}\right)$ и $f\left(x_{2}\right)$ сравнивают и с учетом унимодальности функции $f(x)$ из двух отрезков $\left[a, x_{2}\right]$ и $\left[x_{1}, b\right]$ выбирают тот, к-рый заведомо содержит точку $x_{*}$. Так, өсли $f\left(x_{1}\right) \leqslant f\left(x_{2}\right)$, әто будет отрезок $\left[a, x_{2}\right]$, в противном случае - отрезок $\left[x_{1}, b\right]$. Выбранный отрезок вновь делят пополам, вблизи его середины $\bar{x}_{2}$ берут две точки $x_{1}=$ $=\bar{x}_{2}-\varepsilon / 2$ и $x_{2}=\bar{x}_{2}+\varepsilon / 2$, сравнивают в них значения функции и т. Д. В результате возникает последовательность срединных точек $\left\{\bar{x}_{n}\right\}$, для к-рой

\[
\left|\bar{x}_{n}-x_{*}\right| \leqslant(b-a-\varepsilon) / 2^{n}+\varepsilon / 2, n=1,2, \ldots
\]

За приближения $\kappa f_{*}$ привимают значения $f\left(\bar{x}_{n}\right)$ при достаточно больших $n$.

Название метода объясняется тем. что на каждом следующем шаге описанного алгоритма отрезок, содержащий точку минимума, становится примерно вдвое короче. На классе унимодальных функций П. Д. м. не является наилучшим. Суцествуют более әффективны е методы, позволяющие при том же количестве вы- числений значений функции достигнуть лучшей по сравнению с (2) точности (см., напр., Фибоначчи меmod).

Лum.: [1] Д е м и до в и ч Б. П., М а р о и И. А., Основы вычислительной математики, 3 изд., М., 1966, [2] у а й л д Д.Д ж., Методы поиска әкстремума, пер. с англ., М., 1967.

ПОЛОЖИТЕЛЬНАЯ ВАРИАЦИЯ ФУНКЦИИ одно из двух слагаемых, сумма к-рых есть полное изменение, или вариация функции, на данном отрезке. Пусть $f(x)$ - функция действительного переменного, заданная на отрезке $[a, b]$ и принимающая конечные значения. Пусть $\Pi=\left\{a=x_{0}<x_{1}<\ldots<x_{n}=b\right\}-$ произвольное разбиение отрезка $[a, b]$ и

\[
P_{\Pi}(f)=\sum_{i}^{+}\left[f\left(x_{i}\right)-f\left(x_{i-1}\right)\right],
\]

где суммирование производится по тем номерам $i$, для к-рых разность $f\left(x_{i}\right)-f\left(x_{i-1}\right)$ неотрицательна. Величина

\[
P(f) \equiv P(f ;[a, b])=\sup _{\Pi} P_{\Pi}(f)
\]

наз. поло жительным изменен и е м фу ук ц и и $f$ на отрезке $[a, b]$. Всегда $0 \leqslant P(f) \leqslant+\infty$. Понятие «П. и. Ф.» введено К. Жорданом [1]. См. также Omрицательнал вариация бункции.

Jum.: [1] J or d a n C., "C. r. Acad. sci.", 1881, t. 92, p. 228230; [2] Л е б е г А., Интегрирование и отыскание примития-

ПОЛОЖИТЕЈЬНАЯ КОРРЁЛЯЦИЯ - вид корреляционной зависимости между случайньми величинами, при к-рой условные средние значения одной из них увеличиваются при возрастании значений другой величины. О П. к. между величинами с коррелячии кояффичиентом $\rho$ говорят в том случае, когда $\rho>0$. См. Коррелячия. ПОЛОЖИТЕЛЬНО ОПРЕДЕЛЕННАЯ ФОРМА выражение вида

\[
\sum_{i, k=1}^{n} a_{i k} x_{i} x_{k}
\]

где $a_{i k}=a_{k i}$, принимающее неотрицательные значения при любых действительных значениях $x_{1}, x_{2}, \ldots, x_{n}$ и обрашающееся в пуль лишь при $x_{1}=x_{2}=\ldots=x_{n}=0$. Т. о., П. о. Ф. есть квадратичная форма специального типа. Любая П. о. ф. приводится с помощью линейного преобразования к виду

\[
\sum_{i=1}^{n} x_{i}^{2}
\]

Для того чтобы форма

\[
\sum_{i, k=1}^{n} a_{i k} x_{i} x_{k}
\]

была П. о. ф., необходимо и достаточно, чтобы $\Delta_{1}>0, \Delta_{2}>0, \ldots, \Delta_{n}>0$, где

\[
\Delta_{k}=\left|\begin{array}{cccc}
a_{11} a_{12} & \ldots & a_{1 k} \\
a_{21} a_{22} & \cdots & a_{2 k} \\
\cdot & \cdot & \cdot & \cdot \\
a_{k 1} a_{k 2} & \cdots & a_{k k}
\end{array}\right|
\]

В любой аффинной системе координат расстояние точки от начала координат выражается П. о. ф. от координат точки.

Форма

\[
f=\sum_{i, k=1}^{n} a_{i k} x_{i} \bar{x}_{k}
\]

такая, что $a_{i k}=\bar{a}_{k i}$ и $f \geqslant 0$ для всех значений $x_{1}, x_{2}, \ldots, x_{n}$ и $f=0$ лигь при $x_{1}=x_{2}=\ldots=x_{n}=0$ наз. э р м и т о в о й П. о. ф.

$\mathrm{C}$ понятием П.о. ф. связаны также понятия: 1) по Јо жительно о Іределенной мат-


\end{document}