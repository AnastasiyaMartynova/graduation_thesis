\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}

\begin{document}
где $l=1 / \Sigma, \quad c=\Sigma_{S} / \Sigma, f_{1}=f / \Sigma$. Характеристиками левой части (6) я вляются все прямые $x=x_{0}+\xi \Omega, x_{0} \in D$; вдоль каждой из вих уравнение (6) принимает вид

\[
\frac{\partial \varphi}{\partial \xi}+\Sigma \varphi=\Sigma_{s} \varphi_{0}+f
\]

Если в (6) сделать замену $u=(\varphi(x, \Omega)+\varphi(x,-\Omega)) / 2$, то оно принимает вид

\[
[-l \Omega \nabla]^{2} u+u=c u_{0}+F .
\]

Решение уравнения (9) минимизирует квадратичный фу уционал В ладим и ров а:

\[
\begin{aligned}
G(u) & =(l \Omega \nabla u, l \Omega \nabla u)+(u, u)-\left(c u_{0}, u\right)+ \\
& +\int_{\Gamma \times \Omega}|(\Omega, u)| u^{2} d \Omega d \Gamma-2(u, F),
\end{aligned}
\]

где

\[
(u, v)=\int_{D} \int_{\Omega} u v d x d \Omega
\]

Пусть краевые задачи записаны в операторной форме

\[
L \varphi=S \varphi+f
\]

Х арактерным свойством задач переноса, используемым в численных алгоритмах, является то, что значение $L^{-1} \psi$ находится по заданному $\psi$ прямым методом путем интегрирования (8) вдоль характеристик. Учитывая это, из (11) получают т. н. и н те г p а л ьно е у р а вени е Пайе рл с а

\[
S \varphi=S L^{-1}(S \varphi+f)
\]

для нулевого момента $S \varphi$.

Для решения задач переноса существенное развитие получил м е т о д с фе р и ч е с ких г а р моник (являющийся вариантом метода Галеркина). Приближенное решение ( $P_{n}$-приближение) находят в виде

\[
\varphi^{(n)}(x, \Omega)=\sum_{k=0}^{n}(2 k+1) \sum_{i=-k}^{k} \varphi_{k i}(x) Y_{k i}(\Omega),
\]

где $\varphi_{k i}(x)$ - неизвестные функции, а $Y_{k i}(\Omega)-$ сферич. гармоники $k$-го порядка. Подставляя (13) в (6), умножая результат на $Y_{k i}$ и интегрируя по $\Omega$, получают систему уравнений с частными производными для определения $\varphi_{k i}(x)$. В $P_{1}$-приближениц система имеет вид

\[
\begin{gathered}
\operatorname{div} \varphi_{1}+\Sigma \varphi_{0}=f_{0}, \frac{1}{3} \nabla \varphi_{0}+\varphi_{0}+\Sigma \varphi_{1}=f_{i}, \\
2\left(\varphi_{1}, n\right)-\left.\varphi_{0}\right|_{\Gamma}=0,
\end{gathered}
\]

где $\varphi_{0}=\varphi_{00}, \varphi_{1}=\left(\varphi_{11}, \varphi_{12}, \varphi_{13}\right)$. При $f_{1} \equiv 0$ из (14) получают диффузионное приближение

\[
-\operatorname{div} D \nabla \varphi_{0}+\Sigma \varphi_{0}=f_{0}, \quad 2 D \frac{\partial \varphi}{\partial n}-\left.\varphi_{0}\right|_{\Gamma}=0,
\]

где $D=1 /(3 \Sigma)$. Это- эллиптич. задача, решение к-рой находят, применяя вариационные или сеточные методы.

Для решения одномерных задач развиты аналитич. методы, основанные на разложении решения по обобщенным собственным функциям. Для нахождения значений функционалов от решений сложных многомервых задач применяют Монте-Карло метод.

Широкое распространение получили методы конечноразностных аппроксимаций уравнения переноса. Так, используя квадратурную формулу для области $D$, заменяют интег ральное уравнение (12) системой линейных уравнений. В уравнениях $(4),(5),(6),(8)$ для аппроксимации интегрального оператора применяют квадратурные формулы для сферы. Известны Гаусса квадратурные бормуль для сферы до 35-го алгебраич. порядка точности. В методе характеристик через каждую точку пространственной сетки проводят семейство характеристик по направлениям, соответствующим узлам квадратуры для сферы, и заменяют дифференциальный оператор $L$ в (8) разностным. Разностные уравнения $S_{n}$-м е т о д а получают интегрированием уравнения (6) по сеточной ячейке фазового пространства, предполагая линейность решения в пределах ячейки по независимым переменным. В м ет о д е

\[
\varphi=\sum_{n=1}^{N} g_{n}(\Omega) \varphi_{n}(x)
\]

Если $\varphi_{n}(x)$ заданы, то для определения $g_{n}(\Omega)$ получают систему вырожденных интегральных уравнений; если $\varphi_{n}(x)$ - финитные функции, то получают $м \mathrm{e}-$ тод коне чных элемент тов; если $g_{n}(\Omega)-$ заданные финитные функции и выражение (16) минимизирует функционал $(10)$, то получают так наз. $P_{N J^{-}}$ у р а в н е ни я.

Итерационные методы решения разностных задач переноса обладают своей спецификой; она заключается в том, что сходимость итерационных методов, как правило, замедляется при $c \rightarrow 1(c \leqslant 1)$, а для нахождения следующего приближения $\varphi^{k+1}$ используют только часть информации $о$ предыдущем приближении $\varphi^{k}$ существенно меньшей размерности - запоминают и используют лишь значения $\varphi_{0}^{k}$. В итерадионных методах в качестве промежуточной операции (операции $K$ ) часто входит решение следующей задачи

\[
L \Phi^{k}=S \varphi^{k}+f, \quad \Phi_{0}^{k}=\frac{1}{4 \pi} \int \Phi^{k} d \Omega^{\prime} .
\]

Тогда ошибка $\varphi-\Phi^{k}$ удовлетворяет (11) с независящим от $\Omega$ источником $S\left(\Phi^{k}-\varphi^{k}\right)$ и невязка тожө не зависит от $\Omega$. Это свойство позволяет ускорить сходимость итераций. Пусть задана периодич. задача для уравнения (7) с постоянными коэффициентами, четным по $\mu$ источником и $H=2 \pi$. Применительно к этой задаче ниже рассмотрены следующие итерационные методы. Для уравнения (7) строится сетка с $N$ узлами по $x$ и $M$ угловыми направлениями по $\mu$. Пусть

\[
\begin{gathered}
r(t)=t^{-1} \operatorname{arctg} t, \quad 0 \leqslant r(t) \leqslant 1, \\
\varepsilon_{0}^{k}=\varphi_{0}-\varphi_{0}^{k}=\sum_{n} \varepsilon_{n}^{k} \exp (\operatorname{inx}), \\
\left\|\varepsilon_{0}^{k}\right\|=\max _{n}\left|\varepsilon_{n}^{k}\right| .
\end{gathered}
\]

Для сходящихся итерационных методов $\left\|\varepsilon_{0}^{k+1}\right\| \leqslant q\left\|\varepsilon_{0}^{k}\right\|$, где $0 \leqslant q<1$. Пусть операции $K, Ц$ - цена полной итерации, а $\Delta=Ц-\bigsqcup_{\circ}$. Для различных методов имеют место следующие соотношения.

\begin{enumerate}
  \item П р о с т а я и т е р а ция: $\varphi_{0}^{k+1}=\Phi_{0}^{k}$; для нее $\Delta=0, q=c$.

  \item М е то д Л ю с те р н и ка: для нек-рых $k$ полагают в простой итерации

\end{enumerate}

\[
\varphi_{0}^{k+1}=\Phi_{0}^{k}+\left(\lambda_{1}-1\right)^{-1}\left(\Phi_{0}^{k}-\varphi_{0}^{k}\right)
\]

где $\lambda_{1}>1$ - наибольшее собственное значение задачи $L \varphi=\lambda S \varphi ;$ тогда $\Delta=0(N), q=c r(l)(q \rightarrow 1 \quad$ при $c \rightarrow 1$, $l \rightarrow 0$ ).

\begin{enumerate}
  \setcounter{enumi}{2}
  \item М е тод оценки и те р ационных о тк л о не н и й: $\varphi_{0}^{k+1}=\Phi_{0}^{k}+W_{0}^{k}, \quad$ где $W^{k}-$ решение уравнения
\end{enumerate}

тогда

\[
l \mu \frac{\partial W^{k}}{\partial x}+(1-c) W^{k}=c\left(\Phi_{0}^{k}-\varphi_{0}^{k}\right)
\]

\[
\Delta=O(N M), \quad q=\max \left(c r(l), \frac{\pi \sqrt{2} c^{2}}{12}\right)
\]

$(q \rightarrow 1$ при $c \rightarrow 1, l \rightarrow 0)$.


\end{document}