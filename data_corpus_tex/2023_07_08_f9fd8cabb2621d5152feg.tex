\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{mathrsfs}

\begin{document}
у к-рой $f^{(r)}(\omega, t)$ четна, равна $\lceil\omega(\pi / n-2 t)] / 2$ на $[0$, $\pi / 2 n]$ и равна $-[\omega(2 t-\pi / n)] / 2$ на $[\pi / 2 n, \pi / n]$. Нормы $\left\|f_{n r}(\omega)\right\|_{X}$ допускают явное выражение, нагр.:

\[
\left\|f_{n 0}(\omega)\right\|_{\tilde{C}}=\frac{1}{2} \omega(\pi / n)
\][

\textbackslash left|f\_\{n r\}(\textbackslash omega)\textbackslash right|\textit{\{\textbackslash tilde\{C\}\}=\textbackslash frac\{1\}\{2 n\^{}\{r\}\} \textbackslash int}\{0\}\^{}\{\textbackslash pi\} \textbackslash Phi\_\{r\}(\textbackslash pi-t) \textbackslash omega(t / n) d t, r=1,2, ···,
]

где функции $\Phi_{k}(t)$ задаются на $[0, \pi]$ рекуррентно:

\[
\begin{gathered}
\Phi_{1}(t)=\frac{1}{2}, \\
\Phi_{k}(t)=\frac{1}{2} \int_{0}^{\pi-t} \Phi_{k-1}(u) d u .
\end{gathered}
\]

Решение задач о наилучшем для класса $\tilde{W}^{r} H^{\omega}$ ливейном методе из $\tilde{C}$ в $\mathfrak{N}_{2 n-1}^{T}$ или в $S_{2 n}^{r}$ известно в случае $\omega(\delta)=M \delta \quad(0 \leqslant \delta \leqslant \pi), \quad$ т. е. когда $\quad \tilde{W}^{r} H^{\omega}=M \tilde{W}_{\infty}^{r+1}$. Интерполяционные сплайны $\sigma_{r}(f, t)$ из $S_{2 n}^{r}$ реализуют верхнюю грань $E\left(\tilde{W}^{r} H^{\omega}, S_{2 n}^{r}\right)_{\bar{C}}$ (при любом выпуклом $\omega(\delta))$ лишь в случае $r=1$.

При решении әкстремальных задач на классах функций, заданных на конечном отрезке и не связанных жесткими краевыми условиями, нельзя ждать результатов в столь совершенном, как в периодич. случае, віде: на экстремальных функциях сказывается возмуцающее действие концов промежутка, к-рое усугубляется с увеличением порядка дифференцируемости. Здесь известны нек-рые результаты с точной асимптотикой. Если $M W^{r} H^{\alpha}\left(r=0,1, \ldots ; 0<\alpha \leqslant 1, M W^{0} H^{\alpha}=\right.$ $\left.=M H^{\alpha}\right)$ - класс функций $f(t) \in C^{r}[-1,1]$, у к-рых

$\left|f^{(r)}\left(t^{\prime}\right)-f^{r}\left(t^{\prime \prime}\right)\right| \leqslant M\left|t^{\prime}-t^{\prime \prime}\right|^{\alpha}\left(t^{\prime}, t^{\prime \prime} \in[-1,1]\right)$, то для наилучшего равномерного на $[-1,1]$ приближения подпространством $\mathfrak{P}_{n}^{A}$ алгебраич. многочленов степеви $n-1$ имеют место соотношения

\[
\begin{aligned}
& \lim _{n \rightarrow \infty} n^{\alpha} E\left(M H^{\alpha}, \mathfrak{P}_{n}^{A}\right)_{C}=M \pi^{\alpha} / 2, \\
& \lim _{n \rightarrow \infty} n^{r+\alpha} E\left(M W^{r} H^{\alpha}, \mathfrak{P}_{n}^{A}\right)_{C}== \\
= & \frac{M}{2} \int_{0}^{\pi} \Phi_{r}(\pi-t) t^{\alpha} d t, \quad r=1,2, \ldots,
\end{aligned}
\]

к-рые полезно сравнить с соответствующими результатами в перпооич. случае; пріг $\boldsymbol{\alpha}=1$ правые частіг $(\boldsymbol{E})$ и (7) равны соответственно $M K_{1}$ и $M K_{r+1}$. Отказавшись от многочленов наилучшего приближения, можно усилить эти результаты, существенно улучшив приближение у концов отрезка $[-1,1]$ без потери наилучшеи́ асимптотики на всем промежутке. Напр., для любой $f \in M H^{\alpha}$ существует последовательность алгебраич. многочлевов $p_{n}(f, t) \in \mathfrak{R}_{n}^{A}$ таких, что равномерно по $t \in[-1,1]$ при $n \rightarrow \infty$

\[
\begin{gathered}
\left|f(t)-p_{n}(f, t)\right| \leqslant \frac{M}{2}\left[\left(\frac{\pi}{n} \sqrt{1-t^{2}}\right)^{\alpha}+o\left(n^{-\alpha}\right)\right]= \\
=E\left(M H^{\alpha}, \mathfrak{R}_{n}^{A}\right)_{C}\left[\left(1-t^{2}\right)^{\alpha / 2}+o(1)\right] .
\end{gathered}
\]

Аналогичный факт имеет место для функций классов $M W^{r} H^{1}(r=1,2, \ldots)$ (см. [11]). В задачах приближения сплайнами (наилучшими и ивтерполяционными) классов функций, заданных на отрезке, известны векрые точные (гл. обр., для сплайнов малого порядка) и асимптотически точные результаты (см. [1.5]).

В случае одностороннего приближения (в интегральной метрике) известен ряд точных результатов по оценке погрешности наилучшего приближения полиномами и сплайнами на введенных выше классах функций (см. [19]). При их получении существенно использовались соотношения двойственности для ваилучшего приближения пріп наличіпи ограничений, задаваемых с помощцю конуса.

Отыскание наилучшего аппарата приближения (фиксированной размерности) для данного класса функций $\mathfrak{M}$ приводит к задачам о п о п е $\mathrm{pe}$ ч н и к а х: найти величины (см. (1) ІІ (3))

\[
\begin{aligned}
& d_{N}(\mathfrak{M}, X)=\inf _{\mathfrak{R}_{N}} E\left(\mathfrak{M}, \mathfrak{N}_{N}\right)_{X}, \\
& d_{N}^{\prime}(\mathfrak{M}, X)=\inf _{\mathfrak{N}_{N}} \mathscr{O}\left(\mathfrak{M}, \mathfrak{N}_{N}\right)_{X},
\end{aligned}
\]

где нижние грани берутся по всем подпростравствам $\mathfrak{N}_{N}$ из $\boldsymbol{X}$ (и их сдвигам) размерности $N$, а также указать экстремальные (наилучшие) подпространства, реялизующие эти нижние грани. Оценки сверху для $d_{N}$ и $d_{N}^{\prime}$ дают найденные для конкретных подпространств $\mathfrak{R}_{N}$ величины $E\left(\mathfrak{M}, \mathfrak{\Re}_{N}\right)_{X}$ и $\mathscr{E}\left(\mathfrak{M}, \mathfrak{N}_{N}\right)_{X}$, основная трудность в задаче о поперечнике обычно состоит в получении точных оценок снизу. В ряде ситуацій получить такие оценки удается, привлекая топологич. соображения, в частности теорему Борсука об антиподах (см. [8]). Практически во всех случаях точного репения задачи о наилучшем приближении классов $M \tilde{W}_{p}^{r}$ и $\tilde{W}^{r} H^{\omega}$ периодич. функций подпространствами $\mathfrak{P}_{2 n-1}^{T}$ (тригонометрич. полиномов порядка $\left.n-1\right)$ и $S_{2 n}^{m}$ (сплайнов нек-рого порядка $m$ дефекта 1 по разбиению $k \pi / n)$ найденные точные верхние грани $E(\mathfrak{M}$, $\left.\mathfrak{P}_{N}\right)_{X}$ дают и значения поперечников $d_{N}$ этих классов, причем оказалось, что для периодич. классов $d_{2 n-1}=d_{2 n}$. В частности (см. [7], [8]),

\[
\begin{gathered}
d_{2 n-1}\left(\tilde{W}_{\infty}^{r}, \tilde{C}\right)=d_{2 n}\left(\tilde{W}_{\infty}^{r}, \tilde{C}\right)=d_{2 n-1}\left(\tilde{W}_{1}^{r}, \tilde{L}_{1}\right)= \\
=d_{2 n}\left(\tilde{W}_{1}^{r}, \tilde{L}_{1}\right)=K_{r}{ }^{-r} \\
n, r=1,2, \ldots,
\end{gathered}
\]

а при выпуклом вверх $\omega(\delta)$ и $X=\tilde{C}$ или $\tilde{L}_{1}$

\[
\begin{gathered}
d_{2 n-1}\left(\tilde{W}^{r} H^{\omega}, X\right)=d_{2 n}\left(\tilde{W}^{r} H^{\omega}, X\right)=\left\|f_{n r}(\omega)\right\|_{X}, \\
n=1,2, \ldots ; r=0,1, \ldots
\end{gathered}
\]

Следует отметить, что подпространство $\Re_{2 n-1}^{T}$ является ваилучшим для рассматриваемых классов при всех $r$ и никакое подпространство размерности $2 n$ не дает на этіг классах лучшее приолижение, чем $\vartheta_{2 n-1}^{T}$ (имеющее размерность $2 n-1$ ). Подпространство сплайнов $S_{2 n}^{m}$ является наилучшим для классов $\tilde{W}_{\infty}^{r+1}$ й $\tilde{W}^{r} H^{\omega}$ в $\tilde{C}$ при $m=r$ и для класса $\tilde{W}_{p}^{r+1}$ в $\tilde{L}_{1}$ прп $m \geqslant r$. Јинейные поперечники $d_{N}^{\prime}$ классов $\tilde{W}_{\infty}^{r}$ в $\tilde{C}$ и $\tilde{W}_{1}^{r}$ в $\tilde{L_{1}}$ совпадают с $d_{N}$, они реализуются на подпространствах $\mathfrak{P}_{2 n-1}^{T}$ и $S_{2 n}^{r-1}$ наилучшими линейными методами, о к-рых упоминалось выше. Поперечники $d_{N}$ и $d_{N}^{\prime}$ класса $\tilde{W}_{2}^{r}$ в $\tilde{L}_{2}$ при $N=2 n-1$ и $N=2 n$ равны и реализуются суммами Фурье по тригонометрич. системе. Для поперечников классов функций, заданных на отрезке, в ряде случаев известна точная асимптотика; поперечники $d_{N}$ и $d_{N}^{\prime}$ классов $W_{\infty}^{r}$ в $C[-1,1]$ совпадают и реализуются интерполяционными сплайнами порядка $r-1$ по неравномерному разбиению (см. [8]).

Задачу оценки погрешности приближения на множестве $X^{r} r$-х интегралов от функций из $X$ (не являющемся локально компактным) можно сделать корректной, если оценивать для $f \in X^{r}$ при фиксированном $\gamma>0$ величину

\[
E\left(f, \mathfrak{\Re}_{N}\right)_{X} / \omega\left(f^{(r)}, \gamma\right)_{X}
\]

$\left(\omega(g, \delta)_{X}-\right.$ модуль непрерывности функции $g$ в пространстве $X$ ) или (в случае прибліжения конкретным методом) величину

\[
\left\|f-U_{N} f\right\|_{X} / \omega\left(f^{(r)}, \gamma\right)_{X} .
\]


\end{document}