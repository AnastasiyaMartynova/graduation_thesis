\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}
\usepackage{mathrsfs}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
$\chi_{A}\left(y_{1}, y_{2}, \ldots, y_{l}, t\right)$ удовлетворяет уравнению Шредингера

\[
i \frac{\partial \chi_{a}}{\partial t}=H_{A} \chi_{A},
\]

в к-ром $H_{A}$, действующий в пространстве $L_{2}\left(\mathbb{R}^{3(l-1)}\right)$, задается дифференциальным оператором

\[
H_{A}=-\sum_{k=1}^{l} \frac{1}{2 M_{k}} \Delta_{y_{k}}-E_{A}, \quad E_{A}=\sum_{k=1}^{l} \varepsilon_{j_{k}},
\]

с условием $y_{1}+y_{2}+\ldots+y_{l}=0$. Общее асимптотич. движение задается суперпозицией состояний $\Psi_{A}^{a s}$ :

\[
\Psi_{a s}(t)=\sum_{A} C_{A} \Psi_{A}^{a s}(t) .
\]

Задача рассеяния для уравнения Шредингера (1) состоит в нахождении решения $\Psi(t)$, совпадающего при $t \rightarrow-\infty$ с одним из асимптотич. состояний $\Psi_{a s}^{(-)}(t)$ :

\[
\left\|\Psi(t)-\Psi_{a s}^{(-)}(t)\right\| \rightarrow 0, \quad t \rightarrow-\infty .
\]

При $t \rightarrow+\infty$ решение $\Psi(t)$ вновь становится асимптотич. состоянием с асимптотикой $\Psi_{a s}^{(+)}(t)$ :

\[
\left\|\Psi(t)-\Psi_{a s}^{(+)}(t)\right\| \rightarrow 0, \quad t \rightarrow+\infty .
\]

Результат рассеяния состоит в превращении асимптотич. состояния $\psi_{a s}^{(-)}(t)$ в $\psi_{a s}^{(+)}(t)$ в соответствии с динамикой, задаваемой уравнением (1). Это превращение осуществляется линейным оператором $S$ таким, что

\[
\Psi_{a s}^{(+)}(t)=S \Psi_{a s}^{(-)}(t)
\]

Оператор $S$ явно не зависит от времени, содержит в себе информацию о всех возможных результатах рассеяния и по этой причине называется о пер а т о р о м р ас с е я и я.

Обоснование приведенной постановки задачи рассеяния включает в себя: 1) доказательство существования решения с начальным условием (3) при $t \rightarrow-\infty$; 2) исследование асимптотич. поведения $\Psi(t)$ при $t \rightarrow+\infty$ и доказательство представления (4). Решения этих задач определяют подпространства $\mathscr{f}_{a s}^{(-)}$и $\mathscr{H}_{a s}^{(+)}$в $\mathscr{H}$, к-рые пробегают векторы начальных значений $\Psi_{0}$ при $t=0$, соответствуюшие решениям с описанными асимптотиками при $t \rightarrow \pm \infty$. Если выполнено условие

\[
\mathscr{H}_{a s}^{(-)}=\mathscr{H}_{a s}^{(+)},
\]

то оператор $S$ в формуле (4) становится унитарным. Это условие носит название ус л о в и я ас и м п тот ическо й П о л ноты.

Если потенциалы взаимодействия $v_{i j}(r)$ содержат кулоновскую часть, то приведенные выше конструкции требуют модификации. Она включает в себя изменение асимптотич. динамики векторов $\chi_{A}$. Оператор $H_{A}$ в формуле (2) следует заменить на оператор

\[
\begin{gathered}
H_{A}^{a s}=H_{A}+\frac{1}{|t|} \sum_{i<k=1}^{l} B_{i k}, \\
B_{i k}=q_{i k} M_{i} M_{k}\left[\left(M_{i} \nabla_{y_{k}}-M_{k} \nabla_{y_{i}}\right)^{2}\right]^{1 / 2},
\end{gathered}
\]

где константы $q_{i k}$ пропорциональны произведению

\[
\sum_{m \in \omega_{i}} z_{m} \sum_{n \in \omega_{k}} z_{n}
\]

Все дальнейшие построения остаются без изменений.

Лит.: [1] Меркурьев С. П., Фаддеев Л. Д., Квантовая теория рассеяния для систем нескольких частиц, М., 1985; [2] Н ьюто н Р., Теория рассеяния волн и частиц, пер. с англ., М., 1969.

С. П. Меркурьев, С.Л. Яковлев.

МОДЕЛИРОВАНИЕ - см. Изоморфизм.

МОДИФИЦИРОВАННЫЕ ЦИЛИНДРИЧЕСКИЕ ФУНКции - см. Цилиндрические функции.

модуль вектора - см. Вектор.

Модуль к в т е р н иона - см. Алгебра, Кватернион. МоДуль пе р и од о в - см. Периодическая функиия, Двоякопериодическая функция.

Модуль упругости - см. Упругости математическая теория.

МоДуль эллиптического интеграла - см. Эллиптический интеграл.

МОДУлЯРНАЯ ГPУกпА - әрупnа $\Gamma$ всех дробно-линейных преобразований $\gamma$ вида

\[
z \rightarrow \gamma(z)=\frac{a z+b}{c z+d}, \quad a d-b c \neq 1
\]

где $a, b, c, d$ - целые рациональные числа. М. г. является дискретной группой преобразований верхней комплексной полуплоскости $H=\{z=x+i y \mid y>0\}$ (плоскост и Лобачевского) и допускает представление образующими $T: z \rightarrow z+1$ и $S: z \rightarrow-1 / z$ и соотношениями $S^{2}=(S T)^{3}=1$.

Интерес к М. г. связан с изучением модулярных функций, римановой поверхностью к-рых является факторпространство $H / \Gamma$, отождествляемое с фундаментальной областью $G$ М. г. Исследование подгруппы М. г. представляет интерес в теории модулярных форм. A. А. Панчишкин. МОДУЛЯРНАЯ ФИГУРА - см. Риманова поверхность. МОДУлЯРНАЯ ФОРМА, эллиптическая моду ля рная форма, одного комплексного переменного функция $f$ на верхней полуплоскости $H=\{z \in \mathbb{C} \mid \operatorname{Im} z>0\}$, удовлетворяющая при некотором фиксированном $k$ у с ловию автоморфности :

\[
f\left(\frac{a z+b}{c z+d}\right)=(c z+d)^{k} f(z)
\]

для любого элемента $\left\|\begin{array}{ll}a & b \\ c & d\end{array}\right\|$ группы $S L_{2}(\mathbb{Z})$ целочисленных матриц с определителем $a d-b c=1$, и такая, что

\[
f(z)=\sum_{n=0}^{\infty} a_{n} q^{n}
\]

где $q=\exp (2 \pi i z), z \in H, a_{n} \in \mathbb{C}$. Целое число $k \geqslant 0$ называется в е со м М.Ф.f. Если $a_{0}=0$, то М.Ф. $f$ называется п а ра бо лической модулярной формой. А.А. Панчишкин. МОДУЛЯРНАЯ ФУНКЦИЯ, эл л и птиче ская моду л я р н а я ф у н к ц и я, одного комплексного переменного автоморфная функция комплексного переменного $z=x+i y$, ассоциированная с группой $\Gamma$ всех дробно-линейных преобразований $\gamma$ вида

\[
z \rightarrow \gamma(z)=\frac{a z+b}{c z+d}, \quad a d-b c \neq 1,
\]

где $a, b, c, d$ - целые действительные числа (эта группа называется м о ду л я р н о й). Преобразования группы Г переводят действительную ось в себя, и областью определения М. ф. можно считать верхнюю полуплоскость $\{z: \operatorname{Im} z>0\}$. Группа $\Gamma$ порождается двумя образующими $z \rightarrow z+1, z \rightarrow$ $\rightarrow-1 / z$. Фундаментальная область $G$ модулярной группы изображена на рис. 1; это криволинейный четырехугольник $A B C D A$ с вершинами $A(\infty), \quad B(-1 / 2+i \sqrt{3 / 2})$, $C(i), D(1 / 2+i \sqrt{3} / 2)$, две стороны к-рого $A B$ и $D A$ - отрезки прямых соответственно $x=-1 / 2$ и $x=1 / 2$, а $B D-$ дуга окружности $|z|=1$. Участки границы $A B$ и $B C$ включаются в $G$, а $C D$ и $D A$ не включаются. Образы области $G$ при всевозможных отображениях группы $\Gamma$ покрывают всю полуплоскость $\operatorname{Im} z>0$ без пересечений.

\begin{center}
\includegraphics[max width=\textwidth]{2023_07_08_0409a24bc32bb63c6ca0g-1}
\end{center}


\end{document}