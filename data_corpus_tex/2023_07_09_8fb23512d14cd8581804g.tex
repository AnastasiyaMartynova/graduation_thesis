\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{mathrsfs}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
Jum.: [1] G rothen di e c k A., D i e u d on né J., Elements de géométrie algébrique, t. 1, B.-Hdlb.-N. Y [3] II а ф а р е в и ч И. Р., Основы алгебраической геометрии, M., 1972; [4] X а р т с х о р н Р., Алгебраическая геометрия, пер. с англ., М., 1981; [5] Итоги науки и техники. Алгебра. топология. Геометрия, т. 10, М., 1972, с. 47-112.

СХЕМА ИЗ ФУНКІИОНАЛЬНЫХ ЭЛЕМЕНТОВ математич. модель реальных объектов, связанных с переработкой информации, в к-рых допускается многократное использование промежуточных результатов. K подобным объектам относятся, напр., электронноламповые схемы, сети нейронов, нек-рые виды вычислительных алгоритмов. Это один из основных классов управляющих систем. С. из ф. э. можно рассматривать как автомат без памяти.

Математически С. из Ф. э. можно определить как ориентированный граф без циклов с помеченными ребрами и вершинами, множество вершин к-рого разбито на два подмножества. Вершины одного из них наз. в х о д а м и С. из ф. ә. Им не инцидентны входящие ребра, и каждой из них приписана буква из алфавита переменных $X=\left\{x_{1}, \ldots, x_{n}\right\}$. Вершинам другого подмножества пришисаны буквы из алфавита $\mathscr{E}=\left\{\varphi_{1}()\right.$, ..., $\left.\varphi_{m}()\right\}$ функциональных символов.

Алфавиту $\mathcal{E}$, таким образом, соответствует однозначно множество функций $\overline{\mathscr{E}}=\left\{\varphi_{1}(), \ldots, \varphi_{m}()\right\}$. Некрые вершины графа выделены и объявлены в ы х о д ам и С. из ф. ә. Вершина с входящими в нее (занумерованными) ребрами, к-рой приписан символ $\varphi_{i}$ из $\mathscr{E}$ (местность его равна числу входящих ребер), наз. ф ун к ц он альным эл е ментом $E_{\varphi_{i}}$. Другие концы инцидентных этой верпине входящих ребер суть в х о д ы функционального әлемента $E_{\varphi_{i}}$, а сама вершина есть в ы х о д функционального әлемента $E_{\varphi_{i}}$. Если на входы функционального әлемента $E_{\varphi_{i}}$ подать набор б̈ значений переменных из $X$, то ва выходе $E_{\varphi_{i}}$ (т. е. в вершине $\varphi_{i}$ ) реализуется значение функции $\varphi_{i}$ на этом наборе $\tilde{\sigma}$; таким образом, функциональный элемент $E_{\varphi_{i}}$ реализует функцию $\varphi_{i}$. Всякая С. из ф. ә. также реализует на своих выходах нек-рые функции. Набор функциональных элементов, соответствующий алфавиту $\mathscr{E}$, из к-рого строятся С. из ф. э., наз. б аЗ и с о м $B_{\mathscr{E}}$. Множество всех С. из ф. ә., построенных при помощи функциональных элементов из $B_{g}$, наз. множеством С. из ф. э. в б а з и с е $B_{\mathscr{E}}$. Если $\overline{\mathscr{E}}$ полно, то $B_{\mathscr{E}}$ полон, и С. из ф. э. в $B_{\mathscr{E}}$ можно реализо-

\includegraphics[max width=\textwidth, center]{2023_07_09_8fb23512d14cd8581804g-1}
вать любую функцию. Далее предполагается, что переменные из $X$ принимают значения 0,1 , и $\overline{\mathscr{E}}-$ подмножество функций алгебры логики. Именно такого типа базисы изучены наиболее полно.

В качестве примера С. из ф. э. может служить изображенная на рис. С. из ф. э. в базисе $\{\&, \vee,-\}$. Ee вершина $E_{\&}\left(a_{4}\right)$, ва нем реализуется функция

\[
\left(x_{1} \vee x_{2}\right) \& \overline{x_{1} \& x_{2}}, \quad \text { T. e. } x_{1} \overline{x_{2}} \vee \overline{x_{1}} x_{2} .
\]

Эквивалентное определение С. из ф. ә. можно дать также в терминах равенств. Для рассмотренного на рис. примера такая система может быть записана следующим образом:

\[
\begin{gathered}
a_{1}=x_{1} \vee x_{2}, \quad a_{2}=x_{1} \& x_{2}, \\
a_{3}=\overline{a_{2}}, \quad a_{4}=a_{1} \& a_{3}=x_{1} \overline{x_{2}} \vee \overline{x_{1}} x_{2} .
\end{gathered}
\]

Обычно функциональным элементам из $B_{\&}$ приписываются неотрицательные числа, именуемые в е с а м и (или сло жн о с т я и) ф у нкци о на льны ә л е м е т т в базиса. Под с л о н о с т ь ю С. из ф. э. понимается сумма весов всех функциональных әлементов, присутствующих в этой С. из ф. ә. Минимальная сложность, достаточная для реализации произвольной функции алгебры логики от $n$ переменных С. из ф. ә. в произвольном конечном базисе (с ненулевыми весами), асимштотически равна $\rho 2^{n / n}$ (см. [1]), где $\rho$ - константа для рассматриваемого базиса (см. Синтеза задачи). Минимальная сложность, достаточная для реализации С. из ф. э. системы $F$ функций, зависящих от одних и тех же переменных, асимптотически равна

\[
\rho \frac{\log _{2}|F|}{\log _{2} \log _{2}|F|}
\]

где $|F|$ - число функций $F$, а $\rho$ - константа, вычисляемая по базису.

По числу входов, к-рые могут быть присоединены к выходу произвольного функционального элемента, из класса С. из ф. ә. выделяются т. н. С. из ф. э. б е з в е т в ле в и я вых о дов, или фо р м у лы (к выходу каждого функционального әлемента такой С. из ф. э. может быть присоединен только один вход). В отличие от формул, С. из ф. э. общего вида можно рассматривать как схемы вычислений с запоминанием промежуточных результатов. Для класса формул минимальная сложность, необходимая для реализации произвольной функции алгебры логики от $n$ переменных формулой в произвольном конечном базисе (с ненулевыми весами), асимптотически равна $\rho 2^{n} / \log n$, где $\rho$ - ковстанта, зависящая от базиса (ср. с ковтактными л-схемами, см. Контактная схема). Для базисов, содержащих элементы с нулевыми весами, сложности С. из ф. э. ведут себя иначе (см., напр., [5]).

Кроме того, интерес представляет задача о синтезе схем в бесконечных базисах. Наиболее полно исследован случай, когда элементы базиса реализуют пороговые функции. Функция алгебры логики $f\left(x_{1}, \ldots, x_{n}\right)$ нав. п о р о г о в о й, если существуют действительные числа $w_{1}, \ldots, w_{n}, h$ такие, что

\[
w_{1} x_{1}+\ldots+w_{n} x_{n} \geqslant h
\]

тогда и только когда, когда $f\left(x_{1}, \ldots, x_{n}\right)=1$. Функциональный элемент, реализующий пороговую функцию, наз. п о ро го в ы м э ле ме нт о м. С. изф. э. в базисе из пороговых элементов наз. с х е м а м и и з п о р о г о в ы х э е м е т тов. Обычно рассматриваются два типа базисов из пороговых элементов: 1) веса пороговых элементов равны единице, 2) вес порогового элемента равен сумме модулей всех коэфффициентов $w_{i}$ (шри условии, тто пороговые функции задаются целочисленным неравенством (*)). Для каждого из этих базисов получены асимптотические оценкк сложности схем из пороговых элементов: 1) $2\left(2^{n / n)^{1 / 2}}\right.$, 2) $2^{n / n}$.

Путь между входом и выходом С. из ф. ә. наз. ц еп ь ю. Число верпин цепи, отличных от входа, наз. д л и н о й ц е пи. Максимальная длина цеши в $\mathrm{C}$. из ф. ә. наз. г л у б и н о й $\mathrm{C}$. из ф. э. Минимальная глубина С. из ф. э. (и формулы), достаточная для реализации произвольной функции алгебры логики от $n$ переменных в базисе $\{\&, \vee,-\}$, равна

\[
n-\log _{2} \log _{2} n+O(1) .
\]

К роме весов, функциональным әлементам базиса могут быть приписаны неотрицательные числа, именуемые з а д е р ж к а ми. Под 3 а д е р ж о й це п понимается сумма задержек присутствующих в ней функциональных элементов. Под з а д е р ж к о й С. и ф. э. понимается максимальная задержкка цепей этой


\end{document}