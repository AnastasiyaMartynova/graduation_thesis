\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{CJKutf8}
\usepackage{bbold}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
т. е. циклирование по трем первым (нижним) индексам дает нуль.

Р. т. обладает следующими свойствами:

\begin{enumerate}
  \item $R_{l k, i j}=R_{i j, l k} ;$ 2) $R_{i, l k}^{q}=-R_{i, k l}^{q}$;

  \item $R_{l k, i j}=-R_{k l, i j}, R_{l k, i j}=-R_{l k, j i}$;

  \item $R_{a a, i j}=0, R_{l k, b b}=0$,

\end{enumerate}

если оба индекса одной пары одинаковы, то соответствующая координата равна нулю: $R_{a a,}^{q},=0$;

\begin{enumerate}
  \setcounter{enumi}{4}
  \item для абсолютных производных Р. т. имеет место то жд е с тво Би а н к и
\end{enumerate}

\[
\nabla_{m} R_{k l, i}^{q}+\nabla_{k} R_{l m, i}^{q}+\nabla_{l} R_{m k, i}^{q}=0,
\]

где $\nabla_{m}$ - символ ковариантного дифференцирования по координате $x^{m}$. Аналогичное тождество имеет место и для тензора $R_{l k, i j}$.

Р. т. имеет всего $n^{4}$ координат, где $n-$ размерность пространства, из них $n^{2}\left(n^{2}-1\right) / 12$ существенных, между к-рыми нет тождественных зависимостей, вытекающих из указанных свойств.

В случае $n=2$ Р. т. имеет одну существенную координату $R_{12,12}$, к-рая входит в определение внутренней, или римановой, кривизны поверхности $K=\frac{R_{12}, 12}{\operatorname{det} g_{i j}}$ (см. Гауссова кривизна).

Р. т. был определен Б. Риманом (B. Riemann) в 1861 (опубл. в 1876).

Jит.: [1] Р а ш е в с к и й П. К., Риманова геометрия и тензорный анализ, 3 изд., M., $1967 ;[2]$ \begin{CJK}{UTF8}{mj}月\end{CJK}й 3 \& н х а p т J. П., $\mathrm{F}_{i}$ л и н г е в б е р г B., м е й е р'В., Риманова геометрия в лом, пер. с нем., М., 1971.

РИМАНА ТЕОРЕМА - 1) Р. Т. 0 к о н ф о р м н о м о т о б р а же н и и: каковы бы ни были две односвязные области $G_{1}$ и $G_{2}$ распиренной комплексной плоскости $\overline{\mathbb{C}}$, отличные от $\overline{\mathbb{C}}$, а также от $\overline{\mathbb{C}}$ с какой-либо исключенной из нее точкой, найдется бесконечное число аналитических и однолистных в области $G_{1}$ функций, каждая из к-рых осуществляет взаимно однозначное и конформное отображение $G_{1}$ на $G_{2}$. При этом для любой пары точек $a \in G_{1}, a \neq \infty$, и $b \in G_{2}$ и любого действительного числа $\alpha, 0 \leqslant \alpha \leqslant 2 \pi$, найдется, и притом единственная, функция $f$ әтого класса, для к-рой $f(a)=b, \arg f^{\prime}(a)=\alpha$. У словие $\arg f^{\prime}(a)=\alpha$ гөометрически означает, что каждый бесконечно малый вектор, выходящий из точки $a$, при отображении $w=f(z)$ переходит в бесконечно малый вектор, направление к-рого образует с направлениөм исходного вектора угол $\alpha$.

Р. т. является основной в теории конформных отображений и вообще в геометрич. теории функций комплексного переменного. Вместе со своими обобщениями на многосвязные области она имеет обширные применения в теории функций комплексного переменного, математич. физике, теории упругости, аэро- и гидромеханике, электро- и магнитостатике и т. д. Эта теорема для болөе общего случая односвязных и, вообще говоря, неоднолистных областей над комплексной плоскостью была сформулирована Б. Риманом (B. Riemann, 1851). При этом вместо условий нормировки «f $(a)=b, \arg f^{\prime}(a)=\alpha$ конформного отображения $w=f(z)$, обеспечивающих его единственность, в формулировке Б. Римана для той же цели использовались условия «f $(a)=b, f(\zeta)=\omega »$, где $a \in G_{1}, b \in G_{2}$, а $\zeta$ и $\omega$ - наперед заданные точки границ областей $G_{1}$ и $G_{2}$ соответственно. Последние условия при современном определении понятия односвязной области не всегда корректны. Б. Риман доказывал свою теорему в значительной степени исходя из физич. представлений, к-рые его также убедили в важности этой теоремы для приложений. В современном понимании математич. строгость доказательству Б Римана придал Д. Гильберт (D. Hilbert), обосновавший использованный Б. Риманом в его доказательстве т. н. Дирихле принцип.

См. также Конбормное отображение.

Лum.: [1] Р и м а н Б., Соч., пер с нем., М.- Л., 1948, с. $49-87$; [2] П р и в а л о в И. Й., Введение в теорию функций комплексного переменного, 12 изд., М., 1977; [3] $\Gamma$ о лу уменного, 2 изт, М., 1966 .

\begin{enumerate}
  \setcounter{enumi}{1}
  \item Р.т. о пе р е т а нов е членов р я д а: если ряд, члевы к-рого являются действительными числами, сходится, но не абсолютно, то каково бы ни было число $A$, можно так переставить члены әтого ряда, что сумма получившегося ряда будет равна $A$. Кроме того, члены ряда можно так переставить, что его сумма будет равна одной из наперед заданных бесконечностей со знаком $+\infty$ или $-\infty$, а также так, что его сумма не будет равна ни $+\infty$, ни $-\infty$, но последовательность его частичных сумм будет бесконечно большой, и, наконец, так, что последовательность его частичных сумм не будет иметь ни конечного, ни бесконечного предела (см. $\left.P_{\boldsymbol{\Omega}} \partial\right)$. Л. Д. Кудрлвчев
\end{enumerate}

РИМАНА ТЕТА-ФУНКЦИЯ - суперпозиция тета-функций 1-го порядка $\Theta_{H}(u), u=\left(u_{1}, \ldots, u_{p}\right)$, с полуцелыми характеристиками $H$ и абелевых интегралов 1-го рода, примененная Б. Риманом (B. Riemann, 1857) для решения Якоби проблемь обрацения.

Пусть $F(u, w)=0$ - алгебраич. уравнение, определяющеө компактную риманову поверхность $F$ рода $p$; $\varphi_{1}, \ldots, \varphi_{p}$ - базис абелевых дифберенциалов 1-го рода на $F$ с матрицей периодов размера $p \times 2 p$ :

\begin{center}
\includegraphics[max width=\textwidth]{2023_07_08_acce1153832edf86e4c2g-1}
\end{center}

Пусть

$u(w)=\left(u_{1}\left(w_{1}\right)=\int_{c_{1}}^{w_{1}} \varphi_{1}, \ldots, u_{p}\left(w_{p}\right)=\int_{c_{p}}^{\omega_{p}} \varphi_{p}\right)$

\begin{itemize}
  \item вектор базисных абелевых интегралов 1-го рода, где $\left(c_{1}, \ldots, c_{p}\right)$ - фиксированная система точек на $F$, а $w=$ $=\left(w_{1}, \ldots, w_{p}\right)$ - текущая система точек на $F$. Для любой тета-характеристики
\end{itemize}

\[
H=\left\|\begin{array}{l}
h \\
h^{\prime}
\end{array}\right\|=\left\|\begin{array}{lll}
h_{1} & \ldots & h_{p} \\
h_{1}^{\prime} & \ldots & h_{p}^{\prime}
\end{array}\right\|,
\]

где целые числа $h_{i}, h_{i}^{\prime}$ принимают только значения 0 или 1, можно построить тета-функцию $\Theta_{H}(u)$ с матрицей периодов $W$, причем $\Theta_{H}(u)$ удовлетворяет основным соотнотениям

\[
\left.\begin{array}{c}
\Theta_{H}\left(u+\pi i e_{\mu}\right)=(-1)^{h_{\mu}} \Theta_{H}(u), \\
\Theta_{H}\left(u+e_{\mu} A\right)=(-1)^{h_{\mu}^{\prime}} \exp \left(-a_{\mu \mu}-2 u_{\mu}\right) \cdot \Theta_{H}(u),
\end{array}\right\}
\]

где $e_{\mu}$ есть $\mu$-я вектор-строка единичной матрицы $E$, $\mu=1, \ldots, \quad p$.

Если $z=\left(z_{1}, \ldots, z_{p}\right)$ - нек-рый фиксированный вектор в комплексном пространстве $\mathbb{C}^{p}$, то т е т а - ф у н кц и я $\mathrm{P}$ и м а в а $\Phi_{H}(w)$ представляет из себя суперпозицию

\[
\Phi_{H}(w)=\Theta_{H}(u(w)-z)
\]

В области $F^{*}$, получающейся из $F$ после удаления разрезов вдоль циклов $a_{1} b_{1} \ldots a_{p} b_{p}$ базиса гомологий $F$, Р. т.-. (2) всюду определены и аналитичны При переходе через разрезы Р. т.-ф., вообще говоря, умножаются на мультипликаторы, значения к-рых определяются из основных соотношений (1). Особую роль при этом играет тета-функция 1-го порядка $\Theta(u)=\theta_{0}(u)$ с нулевой характеристикой $H=0$. Именно нули $\eta_{1}, \ldots, \eta_{p}$ соот-


\end{document}