\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}

\def\AA{\mathring{\mathrm{A}}}

\begin{document}
обладает левым С. ф., к-рый каждой группе $G$ сопоставляет ее факторгруппу по коммутанту.

С в о й с т в a С. Ф. Функтор, сопряженный слева к данному функтору, определен однозначно с точностью до изоморфизма функторов. Сопряженный слева функтор унивалентен тогда и только тогда, когда единица сопряжения состоит из мономорфизмов. Он перестановочен с копределами и переводит нулевые объекты и нулевые морфизмы в нулевые объекты и нулевые морфизмы соответственно.

Пусть \& и (5 - полные слева и локально малые слева категории. Функтор $G: \mathbb{E} \rightarrow \Re$ тогда и только тогда обладает сопряженным слева функтором $F: \Re \rightarrow($, когда выполнены следующие условия: a) функтор $G$ перестановочен с пределами; б) для каждого $X \in \mathrm{Ob} \mathfrak{\Re}$ хотя бы одно из множеств $H(X, G(Y)$, $Y \in O b \AA$, непусто; в) для каждого $X \in O b \mathfrak{R}$ существует такое множество $S \subset O b(5$, что всякий морфизм $\alpha: X \rightarrow$ $\rightarrow G(Y)$ представим в виде $\alpha=\varphi G\left(\alpha^{\prime}\right)$, где $\varphi: X \rightarrow G(B)$, $B \in S, \alpha^{\prime}: B \rightarrow Y$.

Переход к двойственным категориям позволяет установить двойственность между понятиями «функтор, сопряженный слева», и «функтор, сопряженный справа», что позволяет выводить свойства сопряженных справа функторов из свойств сопряженных слева функторов.

Понятие С. ф. непосредственно связано с понятием тройки (монады) в категории.

Лum.: [1] Ца ленн о М. III., III ульге й фе р Е. Г., Основы теории категорий, M., 1974; [2] M a c L a n e S., Сategories for the working mathematician, N. Y., 1971 . Цал. Цаленко.

СОПРЯЖЕННЫЙ ЭЛЕМЕНТ к э л е м е н т у $x$ г р у п І ы $G$ - элемент $x^{\prime}$ такой, что

\[
x^{\prime}=g^{-1} x g
\]

для нек-рого элемента $g$ из $G$. Говорят также, что $x^{\prime}$ получается на $x \quad$ т $\mathrm{p}$ а н с ф о р м и $\mathrm{p}$ о в а н и е м при помощи элемента g. Для С. э. используется иногда степенное обозначение: $x g$.

Если $A$ и $B$ два подмножества групшы $G$, то через $A^{B}$ принято обозначать множество

\[
\left\{a^{b} \mid a \in A, \quad b \in B\right\} .
\]

Множество $M^{g}=\{x g \mid x \in M\}$, где $g$ - нек-рый фиксированный элемент из $G$, наз. с о пі р я же н ны м с м в о же с т в о м $M$ в группе $G$. В частности, две подтруппы $U$ и $V$ наз. с о пі р я ж е н ны м и по одг р у Іі па ми, если $U=V g$ для нек-рого $g$ из $G$. Если подгруппа $H=H g$ для любого элемента $g \in G$ (т. е. $H$ содержит все әлементы, сопряженные с ее элементами), то $H$ наз. с а м о с о пі р я ж $е$ н н 0 й п $о$ Д г р у П П о й в $G$, или нормальным делителем.

СОПРЯЖЕННЫХ ГРАДИЕНТОВ МЕТОД - метоД решения системы линейных алгебраич. уравнений $A x=b$ с положительно определенной матрицей $A$. Это прямой и итерационный метод одновременно: при любом начальном приближении он сходится за конечное число итераций, давая точное решение. В С. г. м. матрица системы не меняется в процессе вычислений, на каждой итерации она используется лишь для умножения на вектор. Поэтому порядок систем, решаемых на ЭВМ, может быть высоким, он определяется объемом числовой информации, задающей матрицу.

Структура С. г. м. как прямого метода основана на процессе последовательной $\boldsymbol{A}$-ортогонализации системы векторов, представляющем собой обычный процесс ортогонализации (см. Ортозонализации метод) относительно скалярного произведения $(x, y)=x^{T} A y$. Если $\left\{s_{1}, s_{2}, \ldots, s_{n}\right\}$ есть $A$-ортогональный базис пространства, то точное решение $x^{*}$ системы при любом наталь- ном приближении $x_{0}$ может быть получено из разложения

\[
x^{*}-x_{0}=\sum_{j=1}^{n} \alpha_{j} s_{j}, \quad \alpha_{j}=\frac{\left(r_{0}, s_{j}\right)}{\left(s_{j}, A s_{j}\right)},
\]

где $r_{0}=b-A x_{0}$-невязка $x_{0}$. В С. г. м. $A$-ортогональные векторы $s_{1}, s_{2}, \ldots, s_{n}$ строятся процессом $A$-ортогонализации невязок $r_{0}, r_{1}, \ldots, r_{n-1}$ последовательности приближений $x_{0}, x_{1}, \ldots, x_{n-1}$, вычисляемых по формулам

\[
x_{k}=x_{0}+\sum_{j=1}^{k} \alpha_{j} s_{j}, \quad \alpha_{j}=\frac{\left(r_{0}, s_{j}\right)}{\left(s_{j}, A s_{j}\right)} .
\]

Построенные таким образом векторы $r_{0}, r_{1}, \ldots, r_{n-1}$ и $s_{1}, s_{2}, \ldots, s_{n}$ обладают следующими свойствами:

\[
\left(r_{i}, r_{j}\right)=0, i \neq j ;\left(r_{i}, s_{j}\right)=0, j=1,2, \ldots, i \text {. }
\]

Расчетные формулы С. г. м. даются следующими рекуррентными соотношениями (см. [1]):

\[
\left.\begin{array}{c}
s_{1}=r_{0} ; x_{i}=x_{i-1}+\alpha_{i} s_{i-1}, \alpha_{i}=-\frac{\left(s_{i-1}, r_{i-1}\right)}{\left(s_{i-1}, A s_{i-1}\right)}, \\
r_{i}=r_{i-1}+\alpha_{i} A s_{i-1}, s_{i}=r_{i}+\beta_{i} s_{i-1}, \\
\beta_{i}=-\frac{\left(r_{i}, A s_{i-1}\right)}{\left(s_{i-1}, A s_{i-1}\right)} .
\end{array}\right\}
\]

Процесс заканчивается при нек-ром $k \leqslant n$, для к-рого $r_{k}=0$. При этом $x^{*}=x_{k}$. Момент обрыва процесса определен начальным приближением $x_{0}$. Из рекуррентных соотношений (2) следует, что векторы $r_{0}, r_{1}, \ldots, r_{i}$ являются линейными комбинациями векторов $r_{0}, A r_{0}, \ldots$, $A^{i} r_{0}$. Так как векторы $r_{0}, r_{1}, \ldots, r_{i}$ ортогональны, то обращение в нуль $r_{i}$ возможно лишь тогда, когда векторы $r_{0}, A r_{0}, \ldots, A^{i} r_{0}$ линейно зависимы, напр. когда в разложении $r_{0}$ по базису из собственных векторов $A$ только $i$ компонент отличны от нуля. Этим соображением можно руководствоваться при выборе начального приближения.

С. г. м. относится к классу методов, в к-рых за решение принимается вектор, минимизирующий нек-рый функционал. Для вычисления этого вектора строится итерационная последовательность, сходящаяся к точке минимума. Последовательность $x_{0}, x_{1}, \ldots, x_{n}$ в (2) осуществляет минимизацию функционала $f(x)=(A x$, $x)-2(b, x)$. На $i$-м шаге процесса (2) вектор $s_{i}$ совпадает с направлением скорейшего спуска (градиентом) для поверхности $f(x)=c \quad$ в $(n-i)$-мерном эллипсоиде, представляющем собой сечение поверхности плоскостью, сопряженной направлениям $s_{1}, s_{2}, \ldots, s_{i-1}$.

Описанный процесс С. г. м. или близкие к нему имеют много различных названий: м е т о д Л а ц ц ошіа, метод Хестенс а, ме тод и т. Д. Из всех методов, осуществляющих минимизацию функционала, С. г. м. является наилучшим в стратегич. плане: он дает максимальную минимизацию за $n$ mагов. Однако вычисления (2) в реальных условиях машинной арифметики чувствительны к ошибкам округления, и условия (1) могут быть нарушены. Это препятствует окончанию процесса за $n$ шагов. Поэтому С. г. м. продолжают за $n$ итераций, рассматривая его как бесконечный итерационный процесс минимизации функционала. Известны модификации схемы вычислений (2) более устойчивые к ошибкам округления (см. [3], [4]).

Лит.: [1] Ф а дд е е в.Д. К., Ф а дд е е в а В. Н., Вычислительные методы линейной алгебры, М., $1963 ;$; [2] Б' е р е3 и н И. С., Ж и д ко в Н. П., Методы вычислений, т. 2 , M., 1960; [3] В о е в о д и н В. В., Численные методы алгебры, 966; [4] Б а х в ал о в Н. С., Числениые методы, М.,

СОСТАВНОЙ ИДЕАЛ - идеал кольца или алгебры, не являющиӥся простым идеалом.


\end{document}