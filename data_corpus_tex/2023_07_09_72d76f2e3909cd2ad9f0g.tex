\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
ре м е н н о г о: если $a_{k} \geqslant 0, k=0,1, \ldots$, и для степенного ряда

\[
f(z)=\sum_{k=0}^{\infty} a_{k} z^{k}
\]

с радиусом сходимости 1 имеет место ва действительной оси асимптотич. равенство

\[
f(x)=\sum_{k=0}^{\infty} a_{k} x^{k} \sim \frac{1}{1-x}, \quad x \rightarrow 1,
\]

то для частичных сумм $s_{n}$ имеет место асимптотич. равенство

\[
s_{n}=\sum_{k=0}^{n} a_{n} \sim n, \quad n \rightarrow \infty .
\]

Эта Х.- Л. т., установленная Г. Харди и Дж. Литлвудом [1], является одной из тауберовых теорем.

Jum.: [1] H a rd y G. H., Lit tle w o od J. E., "Proc. Lond. Math. Soc. (2)», 1914, v., 13, p. 174-91; [2] T и T ч м a p II E., Теория функций, пер.' с англ., М.- J.,, 1951.

Е. Д. Соломенцев.

\begin{enumerate}
  \setcounter{enumi}{1}
  \item Х.-Л. т. о не о т риц а те льно ̈й с ум мир у е м о й ф у н п ии-теорема об интегральных свойствах нек-рой функции, связанной с данной. Установлена Г. Харди и Дж. Литлвудом ([1]). Пусть $f(x)$ неотрицательна и суммируема на $[a, b]$ и пусть
\end{enumerate}

\[
\theta(x)=\theta_{f}(x)=\sup _{\xi \in[a, b], \xi \neq x} \frac{1}{x-\xi} \int_{\xi}^{x} f(t) d t .
\]

Тогда:

\begin{enumerate}
  \item если $f \in L_{p}(a, b), 1<p<\infty$, то
\end{enumerate}

\[
\int_{a}^{b} \theta^{p}(x) d x \leqslant 2\left(\frac{p}{p-1}\right)^{p} \int_{a}^{b} f^{p}(x) d x
\]

\begin{enumerate}
  \setcounter{enumi}{1}
  \item если $f \in L(a, b)$, то для всех $\alpha \in(0,1)$
\end{enumerate}

\[
\int_{a}^{b} \theta^{\alpha}(x) d x \leqslant \frac{2(b-a)^{1-\alpha}}{1-\alpha} \int_{a}^{b} f(x) d x
\]

\begin{enumerate}
  \setcounter{enumi}{2}
  \item если $f(x) \ln +f(x) \in L(a, b)$, то
\end{enumerate}

\[
\int_{a}^{b} \theta(x) d x \leqslant 4 \int_{a}^{b} f(x) \ln +f(x) d x+A,
\]

где $A$ зависит только от $b-a$. Здесь

\[
\ln +u= \begin{cases}0, & u<1, \\ \ln u & u \geqslant 1 .\end{cases}
\]

Пусть $f(x)$-периодич. функция с периодом $2 \pi$, суммируемая на $[-\pi, \pi]$, и

\[
M(x)=M_{f}(x)=\sup _{0<\mid t \uparrow \leqslant \pi} \frac{1}{t} \int_{x}^{x+t}|f(u)| d u .
\]

Тогда $M_{f}(x) \leqslant \theta_{1 f_{1}}(x)$, где $\theta_{1 f i}(x)$ построена для $[-2 \pi, 2 \pi]$. Из теоремы для $\theta(x)$ получаются интегральные неравенства для $M(x)$.

Jum.: [1] H a r d y G., L it t l e w o o d J., "Acta math.», 1930,у. 54, p. 81-116; [2] З и г м у н д А., Тригонометрические ряды, пер. с англ., т. 1, М., 1965. А. А. Конюиков.

ХАССЕ ИНВАРРИАНТ - 1) Х. и. $h(A)$ ц е н т р а льн о ̈̈ п р о с т о й а л г е б р ы $A$ над локальным полем $K$ (соответственно над полями $K=\mathbb{R}, \mathbb{C})$ - образ класса алгебры $\boldsymbol{A}$ при канонич. изоморфизме $\boldsymbol{B}$ рауәра группы поля $K$ на группу всех компјексных корней из 1 (соответственно на группы $\{ \pm 1\},\{1\})$. Для циклич. алгебры $A$ с образующими $a, b$ и определяющими соотношениями $a^{n}=x, b^{n}=y, b a=\varepsilon a b$, где $x, y \in K^{*}, \varepsilon \in K-$ первообразный корень степени $n$ из 1, Х. и. $h(A)$ совпадает с норменныл вычетом (символом Гильберта) $(x, y)_{n}$. В частности, Х. и. алгебры кватернионов равен -1 .

Для центральной простой алгебры $A$ над глобальным полем $K$ и любого нормирования $v$ этого поля определяется л о к а л ь н ы й Х. ІІ. $h_{v}(A)$ как Х. и. алгебры $A \otimes K_{v}$ над пополнением $K_{v}$ поля $K$ относительно топологии, определяемой нормированием $v$. Локальные Х. п. однозначно определяют класс алгебры $A$. Они связаны следующими условиями: 1) имеется лишь конечное число нормирований $v$, для к-рых $h_{v}(A) \neq 1$; 2) $\prod_{v} h_{v}(A)=1$ (з а го о в з а и м вос т и). В остальном они могут принимать произвольные значения.

Х. и. предложен X. Хассе [1], [2].

Jum.: [1] H a s s e H., "Math. Ann.", 1931, Bd 104, S. 495534; [2] е г о ж е, там же, 1933, Bd 107, S. 731-60; [3] Алгебраическая теория чисел, пер. с англ., М., $1969 ;$ [4] В е й л ь А., Основы теории чисел, пер. с англ., ї., 1972.

\begin{enumerate}
  \setcounter{enumi}{1}
  \item Х. и., ин в а р и а н т Х а с с е- М ин к о в с ко го, си м в о л $\mathrm{X}$ а с с е, $\varepsilon(f)$ невырожденной квадратичной формы $f \sim a_{1} x_{1}^{2}+\ldots+a_{n} x_{n}^{2}$ над локальным полем $K$ характеристики $\neq 2$ (соответственно над полями $K=\mathbb{R}$, C) - произведение
\end{enumerate}

\[
\prod_{i<j}\left(a_{i}, a_{j}\right)= \pm 1,
\]

где ( , )-кв а дратичный с им вол берта, то есть $(a, b)=1$, если квадратичная форма $a x^{2}+b y^{2}$ представляет 1 в поле $K$ и $(a, b)=-1$ в противном случае. X. и. зависит только от класса эквивалентности формы $f$, а не от выбора диагональной формы в этом классе. Иногда Х. и. определяют как произведение $\prod_{i \leqslant j}\left(a_{i}, a_{j}\right)$, что отличается от приведенного выше определения множителем $(d(f), d(f))$, где $d(f)-$ дискриминант формы $f$.

В случае локального поля $K$ число $n$ переменных, дискриминант и X. и. определяют класс формы $f$. При $n \geqslant 3$ инварианты $d(f)$ и $\varepsilon(f)$ могут принимать любые значения независимо друг от друга; при $n=2$ исключается случай $d(f)=-1, \varepsilon(f)=-1$; при $n=1$ всегда $\varepsilon(f)=1$.

В случае $K=\mathbb{R} X$. и. выражается через сигнатуру, а именно

\[
\varepsilon(f)=(-1)^{s(s-1) / 2}
\]

где $s$ - отрицательный индекс инерции формы $f$. В случае $K=\mathbb{C}$ всегда $\varepsilon(f)=1$.

Для невырожденной квадратичной формы $f$ над глобальным полем $K$ характеристики $\neq 2$ и любого нормирования $v$ поля $K$ определяется л о к а л ь н ы й Х. и. $\varepsilon_{v}(f)$ как Х. и. квадратичной формы $f$, рассматриваемой над пополнением $K_{v}$ поля $K$ относительно топологии, определяемой нормированием $v$. Число переменных, дискриминант, локальные Х. и. и сигнатуры над вещественными пополнениями поля $K$ определяют класс формы $f$.

Необходимые и достаточные условия существования невырожденной квадратичной формы от $n$ переменных над глобальным полем $K$ характеристики $\neq 2$, имеющей заданный дискриминант $d \neq 0$, заданные локальные $\mathrm{X}$. и. $\varepsilon_{v}$ и, для вещественных нормирований $v-$ заданные отрицательные индексы инерции $s_{v}$, состоит в следующем:

a) $\varepsilon_{v} \neq 1$ лишь для конечного числа нормирований $v$;

b) $\prod_{v} \varepsilon_{v}=1$ (следствие квадратичного закона взаимности);

c) $\varepsilon_{v}=1$, если $n=1$ или $n=2$ и $d \in(-1)\left(K_{v}^{*}\right)^{2}$;

d) $\varepsilon_{v}=(-1)^{s_{V}\left(s_{v}-1\right) / 2}$ для каждого вещественного нормирования $v$;

е) $\varepsilon_{v}=1$ для каждого комплексного нормирования $v$;

f) $\operatorname{sgn} d_{v}=(-1)^{s_{v}}$ для каждого вещественного нормирования $v$ (здесь $d_{v}-0$-раз $d$ при изоморфизме $\left.K_{v} \underset{\sim}{\sim}\right)$.

X. и. предложен $\mathrm{X}$. Хассе [1].

\includegraphics[max width=\textwidth, center]{2023_07_09_72d76f2e3909cd2ad9f0g-1}
S. 129-48, 205-24; 1924, Bd 153, S. 12-43, 113-30, $158-62$; [a. o.], 1963; [3] L a m T.-Y., The algebraic theory of quadratic forms, Reading (Mass.), 1973; [4] א а с с е лы с Д ж., Рациональные квадратичные формы, пер. с англ., М., 1982.


\end{document}