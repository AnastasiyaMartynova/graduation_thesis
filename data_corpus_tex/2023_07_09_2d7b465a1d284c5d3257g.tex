\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{mathrsfs}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }
\usepackage{bbold}

\begin{document}
т. 20 , № 5 , с. $1174-1204 ;$ [7] Б и н д е p К. и д р., Методы Монте-Карло в статистической физике, пер. с англ., М., 1982; [8] Б у с л е н к о Н. ПI. и д р., Метод статистических испытаний (метод Монте-Карло), М., $1962 . \quad$ Г. А. Михайлов.

СТАТИСТИЧЕСКОЕ ОЩЕНИВАНИЕ - один из осғовных разделов математич. статистики, посвященный оцениванию по случайным наблюдениям тех или иных характеристик их распределения.

П р и м е р 1. Пусть $X_{1}, \ldots ., X_{n}$ - независимые случайные величины (наблюдения) с обшим распределением $\mathscr{P}$ на прямой, неизвестным наблюдателю. Эмпирическое (выборочное) распределение $\mathscr{P}_{n}^{*}$, приписывающее нагрузку $\frac{1}{n}$ каждой случайной точке $X_{i}$, является оценкой для $\mathscr{P}$. Эмпирич. моменты

\[
a_{v}=\int x^{v} d \mathscr{P}_{n}^{*}=\frac{1}{n} \sum_{i=1}^{n} X_{i}
\]

служат оценками для моментов $\alpha_{v}=\int x^{v} d \mathscr{P} . \mathrm{B}$ частности,

\begin{itemize}
  \item оденка среднеї,
\end{itemize}

\[
\bar{X}=\frac{1}{n} \sum_{i=1}^{n} X_{i}
\]

\begin{itemize}
  \item оценка дисперсии.
\end{itemize}

\[
s^{2}=\frac{1}{n} \sum_{i=1}^{n}\left(X_{i}-\bar{X}\right)^{2}
\]

О с во в в ы п по н я т я я. В общей теории оценивания наблюдение $X$ есть случайный әлемент со

\includegraphics[max width=\textwidth, center]{2023_07_09_2d7b465a1d284c5d3257g-1}
известное распределение к-рого принадлежит заданному семейству распределений $P$. Семейство распределений всегда можно параметризовать и записать в виде $\left\{\mathscr{P}_{\theta}, \theta \in \Theta\right\}$. Здесь форма зависимости от параметра и множество $\Theta$ предполагаются известными. Задача оценивания по наблюдению $X$ неизвестного параметра $\theta$ или значения $g(\theta)$ функции $g$ в точке $\theta$ заключается в том, чтобы построить функцию $\theta *(X)$ от наблюдений, достаточно хороші аппроксимирующую $\theta(g(\theta))$.

С р а в н е н и е о ц е н о к производится следующим образом. Пусть на множестве $\Theta \times \Theta(g(\Theta) \times g(\Theta))$ задана неотрицательная функция потерь $w\left(y_{1} ; y_{2}\right)$, смысл к-рой состоит в том, что употребление оценки $\theta *$ при фактическом значении параметра $\theta$ приводит к потерям $w(\theta * ; \theta)$. Средние потери, функцию риска $R_{w}$ $(\theta * ; \theta)=\mathbf{E}_{\theta} w(\theta * ; \theta)$ принимают за меру качества статистики $\theta$ * как оценки $\theta$ при функции потерь $w$. Тем самым на множество оценок вводится отношение частичного упорядочения: оценка $T_{1}$ предпочтительнее оценки $T_{2}$, если $R_{w}\left(T_{1} ; \theta\right) \leqslant R_{w}\left(T_{2} ; \theta\right)$. В частности, оценка $T$ параметра $\theta$ наз. н е д о п у с т и м о й (по отношению к функции потерь $w$ ), если найдется оценка $T^{\prime}$ такая, что $R_{\varpi}\left(T^{\prime} ; \theta \leqslant R_{\varpi}(T ; \theta)\right.$ для всех $\theta \in \Theta$, причем для какого-нибудь $\theta$ имеет место знак строгого неравенства. При таком способе сравнения качества оценивания многие оценки оказываются несравнимыми, кроме того, выбор функции потерь в значительной стенени произволен.

Иногда удается найти оценки, оптимальные внутри нек-рого более узкого класса оценок. Важный класс образуют несмеценные оценки. Если исходный эксперимент инвариантен относительно нек-рой группы преобразований, естественно ограничиться оценками, не нарушающими симметрию задачи (см. Эквивариантная оценка).

Оценки можно сравнивать по их поведению в «худших» точках: оценка $T_{0}$ параметра $\theta$ наз. м и н и м а кс н о й (см. Минимаксная оуенка) по отношению к функции потерь $w$, если

\[
\sup _{\theta} R_{w}\left(T_{0} ; \theta\right)=\inf _{T} \sup _{\theta} R_{\mathfrak{w}}(T ; \theta)
\]

■ нижняя грань берется по всем оценкам $T=T(X)$. В бейесовской постановке задачи оденивания считается, что неизвестный параметр $\theta$ представляет значения слутайной величины с априорным распределением $Q$ на $\Theta$. В этом слутае наилучпая по отношению к функции потерь $w$ оценка $T_{0}$ определяется соотношением

\[
\begin{aligned}
r_{w}\left(T_{0}\right)= & \mathbf{E}_{w}\left(T_{0} ; \theta\right)=\int_{\Theta} \mathrm{E}_{\theta} w\left(T_{0} ; \theta\right) Q(d \theta)= \\
& =\inf _{T} \int_{\Theta} \mathrm{E}_{\theta} w(T ; \theta) Q(d \theta)
\end{aligned}
\]

и нижняя грань берется по всем оценкам $T=T(X)$. Различают параметрич. задачи оценивания, когда $\Theta$ есть подмножество конечномерного евклидова пространства, и непараметрические. В параметрич. слуqае обычно рассматривают функции потерь вида $l\left(\left|\theta_{1}-\theta_{2}\right|\right), l-$ неотрицательная неубывающая функция на $\mathbb{R}+$. Особую роль играет наиболее часто употребля емая квадратическая функция потерь $\left|\theta_{1}-\theta_{2}\right|^{2}$.

Если $T=T(X)$ - достаточная статистика для семейства $\left\{\mathscr{P}_{\theta}, \theta \in \Theta\right\}$, то часто можно ограничиться оценками $\theta^{*}=h(T)$. Так, если $\Theta \subset \mathbb{R} k, w\left(\theta_{1} ; \theta_{2}\right)=$ $=l\left(\left|\theta_{1}-\theta_{2}\right|\right), l$ - выпуклая функция, и $\theta^{*}-$ какая-нибудь оценка для $\theta$, найдется оценка $h(T)$, к-рая не хуже $\theta^{*}$; если $\theta^{*}$ - несмещонная, $h(T)$ можно выбрать несмешенной (те о ре м а Бл г ку ә л а). Если $T$ - полная достаточная статистика для семейства $\left\{\mathscr{P}_{\theta}\right\}, \theta^{*}$ - несмещенная оценка пля $g(\theta)$, найдется несмещенная оценка вида $h(T)$ с минимальной в классе несмещенных оценок дисперсией (т е о р е м а Л е ман а - III е ф ф е).

Как правило, в параметрич. задачах оценивания предполагается, что әлементы семейства $\left\{\mathscr{P}_{\theta}, \theta \in \Theta\right\}$ аб̆солютно непрерывны по отношению к нек-рой $\sigma-$ конечной мере $\mu$ и существует плотность $\frac{d \mathscr{T} \theta}{d \mu}=$ $=p(x ; \theta)$. Если $p(x ; \theta)$ достаточно гладкая функция $\theta$ и существует информационная матрица Фишера

\[
I(\theta)=\int_{\mathfrak{X}} \frac{\partial p}{\partial \theta}(x, \theta)\left(\frac{\partial p}{\partial \theta}(x, \theta)\right)^{T} \frac{\mu(d x)}{p(x ; \theta)}
\]

задача оценивания наз. $\mathrm{p}$ е г у л я р н о й. Для регулярных задаq точность оценивания ограничивается снизу п е р а в е в с т в ом $\mathrm{K}$ р а м е p a-P a o: ecли $\Theta \subset \mathbb{R} 1$, то для любой оценки $T$

\[
\mathrm{E}_{\theta}|T-\theta|^{2} \geqslant \frac{\left(1+\frac{d b}{d \theta}(\theta)\right)^{2}}{I(\theta)}+b^{2}(\theta), b(\theta)=\mathrm{E}_{\theta} T-\theta .
\]

П ри ме ры зада ч о енивания. 2. Наиболее распространенная постановка, когда наблюдается выборка объема $n: X_{1}, \ldots, X_{n}$ - независимые одинаково распределөнные величины, принимающие значения

\includegraphics[max width=\textwidth, center]{2023_07_09_2d7b465a1d284c5d3257g-1(1)}
распределения $f(x, \quad \theta)$ относительно меры $v, \quad \theta \in \Theta$. В регулярных задачах, если $I(\theta)-$ информация Фишера на одно наблюдение, то информация Фишера всей выборки $I_{n}(\theta)=n I(\theta)$. Неравенство Крамера Рао принимает вид

\[
\begin{gathered}
\mathrm{E}_{\theta}|T-\theta|^{2} \geqslant \frac{\left(1+\frac{d b}{d \theta}\right)^{2}}{n I(\theta)}+b^{2}(\theta), \\
T=T\left(X_{1}, \ldots, X_{n}\right) .
\end{gathered}
\]

2.1. Пусть $X_{j}$ - нормальные случайные величины с плотностью расшределения

\[
(2 \pi)^{-1 / 2} \exp \left\{-2^{-1} \sigma^{-2}(x-a)^{2}\right\} .
\]

Неизвестный параметр $\theta=\left(a, \sigma^{2}\right)$; оценками для $a$ и $\sigma^{2}$ могут служить $\bar{X}$ и $s^{2}$. При этом $\left(\bar{X}, s^{2}\right)-$ достаточная статистика. Оценка $\bar{X}-$ несмещенная, $s^{2}-$ сме-


\end{document}