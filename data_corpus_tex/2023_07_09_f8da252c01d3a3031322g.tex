\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}

\begin{document}
\begin{enumerate}
  \setcounter{enumi}{2}
  \item Пусть дано дифференциальное уравнение (2), где $x$ принадлежит векторному топологич. пространству $E$. Решение $x_{0}(\cdot): t_{0}+\mathbb{R}+\rightarrow E$ әтого уравнения наз. у с т о ї ч и в м П о Л я П у но в у, если для всякой окрестности нуля $U \subset E$ множество тех $x \in E$, для к-рых решение $x(\cdot)$ задачи Коши $(2), x\left(t_{0}\right)=x$ единственно, определено на $t_{0}+\mathbb{R}+$ и при всяком $t \in t_{0}+\mathbb{R}+$ удовлетворяет соотношению $x(t)-x_{0}(t) \in U$, есть окрестность точки $x_{0}\left(t_{0}\right)$ в пространстве $E$. Если, сверх того, найдется окрестность $V_{0} \subset E$ точки $x_{0}\left(t_{0}\right)$ такая, что для всякого решения $x(\cdot)$ уравнения (2), удовлетворяющего условию $x\left(t_{0}\right) \in V_{0}$, имеет место равенство
\end{enumerate}

\[
\lim _{t \rightarrow+\infty}\left(x(t)-x_{0}(t)\right)=0
\]

(равенство

\[
\lim _{t \rightarrow+\infty} e^{\alpha t}\left(x(t)-x_{0}(t)\right)=0
\]

для нек-рого $\alpha>0$ ), то решение $x_{0}(\cdot)$ ваз, а с и м п т от и ч е с к и (соответственно ә к с по н е н ц и а л ьно) у с т о й ч и вы м. Если пространство $E$ нормируемо, то это определение формулируется гак же, как в п. 1, если в качестве нормы $|\cdot|$ взять любую норму, согласующуюся с топологией пространства $E$.

\begin{enumerate}
  \setcounter{enumi}{3}
  \item Пусть дано дифференциальное уравнение (2) на римановом многообразии $U$ (моделььо к-рого служит евклидово или гильбертово пространство) или (более общая ситуация) на финслеровом многообразии $U$ (моделью к-рого служит нормированное гространство); расстояние в $U$ обозначается через $d(\cdot, \cdot)$. Решение $x_{0}(\cdot): t_{0}+\mathbb{R}+\rightarrow U$ этого уравнения наз. у с т о й ч ив Ы м п о Л я п у н о в у, если для всякого $\varepsilon>0$ найдется $\delta>0$ такое, что для всякого $x \in U$, удовлетворяющего неравенству $d\left(x, x_{0}\left(t_{0}\right)\right)<\delta$, ретение $x(\cdot)$ задачи Коши (2), $x\left(t_{0}\right)=x$ единственно, определено на $t_{0}+\mathbb{R}+$ и при всяком $t \in t_{0}+R+$ удовлетворяет неравенству $d\left(x(t), x_{0}(t)\right)<\varepsilon$. Если, сверх того, найдется $\delta_{0}>0$ такое, что для всякого решения $x_{0}(\cdot)$ уравнения (2), начальное значение к-рого удовлетворяет неравенству $d\left(x\left(t_{0}\right), x_{0}\left(t_{0}\right)\right)<\delta_{0}$, имеет место равенство
\end{enumerate}

\section{(неравенство}
\[
\lim _{t \rightarrow+\infty} d\left(x(t), x_{0}(t)\right)=0
\]

\[
\left.\varlimsup_{t \rightarrow+\infty} \frac{1}{t} \ln d\left(x(t), x_{0}(t)\right)<0\right)
\]

то репение $x_{0}(\cdot)$ наз. а с и м п т о т и ч е с к и (соответственно Әк с П о н нци а ль ь о) у с то й ч ив ы м.

Пусть дано диффференциальное уравнение (2) на компактном дифференцируемом многообразии $V^{n}$. $\mathrm{Pe}$ шение этого уравнения наз. ус той ч и в ы м по Л я П уно в у (ас им птоти ч е с к и, әкс поне нци альн о ус той ч и вым), если оно становится таковым в результате наделения многообразия $V^{n}$ нек-рой римановой метрикой. От выбора римановой метрики әто свойство решения не зависит.

\begin{enumerate}
  \setcounter{enumi}{4}
  \item Пусть $E-$ равномерное пространство. Пусть
\end{enumerate}

\[
f_{t}: U \longrightarrow E, t \in G^{+}(G=\mathbb{R} \text { или } G=\mathbb{Z}),
\]

\begin{itemize}
  \item отоб́ражения, определенные на открытом множестве $U \subset E$. Точка $x_{0} \in U$ наз. у с т о й ч и в о й по Л я п уно в у относительно семейства отображений $\left\{f_{t}\right\}_{t \in G+}$, если для всякого окружения $W$ найдется окрестность $V$ точки $x_{0}$ такая, что множество тех $x \in U$, для к-рых $\left(f_{t} x, f_{t} x_{0}\right) \in W$ при всяком $t \in G^{+}$, есть окрестность точки $x_{0}$. Если, сверх того, найдется окрестность $V_{0}$ точки $x_{0}$ такая, что для всякого $x \in V_{0}$ для всякого окружения $W$ найдется $t(x, W) \in G^{+}$такое, что $\left(f_{t} x, f_{t} x_{0}\right) \in W$ прі всяком $t \in t(x, W)+\mathbb{R}+$, то точка $x_{0}$ наз. асимптот и ч е с ки устойчи вой. Если $E$-компактное топологич. пространство, a $f_{t}: \quad U \longrightarrow E, \quad t \in G^{+},-$отображения, заданные на нек-ром открытом множестве $U \subset E$, то точка $x_{0} \in U$ на3. устойчи вой П о Л япунов у (ас имптоти ч ески устой ч и вой) относительно семейства отображений $\left\{f_{t}\right\}_{t \in G+}$, если она становится таковой в результате наделения пространства $E$ той единственной равномерной структурой, к-рая согласуется с топологией пространства $E$.
\end{itemize}

\begin{enumerate}
  \setcounter{enumi}{5}
  \item Пусть $E$-топологич. пространство, $U$-открытое множество в нем. Пусть $f_{t}: U \longrightarrow E, t \in G^{+}$, где $G$ есть $\mathbb{R}$ или $\mathbb{Z},-$ отображения, имеющие неподвижную точку $x_{0}$. Неподвижная точка $x_{0}$ наз. у с т о й ч и в о й II 0 J я І у в о в у относительно семейства отображений $\left\{f_{t}\right\}_{t \in G+}$, өсли для всякой окрестности $V$ точки $x_{0}$ найдется окрестность $W$ той же тотки такая, что $f_{t} W \subset V$ при всяком $t \in G^{+}$. Если, сверх того, найдется окрестность $V_{0}$ точки $x_{0}$ такая, тто $\lim _{t \rightarrow+\infty} f_{t} x=x_{0}$ для всякого $x \in V_{0}$, то неподвижная точка $x_{0}$ наз. а с и м п т о т и ч е с к и у с то й ч и в о й относительно семейства отображений $\left\{f_{t}\right\}_{t \in G+}$

  \item У. по J. ( асимптотическ а я, экспоне вц и а Јьная устойчивость) решения $y_{0}(\cdot)$ уравненпя произвольного порядка $y^{(m)}=g\left(y, \dot{y}, \ldots, y^{(m-1)}, t\right)$ определяется как У. по ЈI. (соответственно асимптотіческая, экспоненциальная устойчивость) решсния $x_{0}(\cdot)=\left(y_{0}(\cdot), \dot{y}_{0}(\cdot), \ldots, y_{0}^{(m-1)}(\cdot)\right)$ соответствующего уравнения 1-го порядка (2), где $x=\left(x_{1}, \ldots, x_{m}\right)$, $f(x, t)=\left(x_{2}, \ldots, x_{m}, g\left(x_{1}, \ldots, x_{m}, t\right)\right)$.

\end{enumerate}

Определения $1,2,4,6,7$ охватывают устоїчивыө движения систем с конечным числом степеней свободы (причем уравнения на многообразиях естественно возникают при рассмотрении механич. систем со связями). Огределения $2-7$ охватывают устойчивые движения в механике сплошной среды и в других разделах физики, устойчивые решения операторных, функционально-дифференциальных (в частности, уравнений с запаздыванием) и др. уравнений.

Исследовани е на устойчпвость равновесия положения автономной спстемы. Пусть в окрестности точки $x_{0} \in \mathbb{R} n$ задано автономное диффференциальное уравнение $\dot{x}=f(x)$, причем фурнкция $f(\cdot)$ непрерывно диффференцируема и обращается в әтой пчке в нуль. Если действитөльные части всех собственных значений производной $d f_{x_{0}}$ отрицательны, то неподвижная точка $x_{0}$ диффференциального уравнения $\dot{x}=f(x)$ экспоненциально устойтива (т е о р е м а Л япунова об устойчнвостй по пе рвопу п р и б ли и е н и ю); для облегчения проверки условия этсй теоремы применяются крітерип устойчивости. Если при тех же условиях хоть одно із собственных значений производной $d f_{x_{0}}$ имеет положительную действительную часть (это условие мокно проверить, не находя самих собственных значений, см. Устойчивости критерии), то неподвижная точка $x_{0}$ диффференциального уравнения $\dot{x}=f(x)$ неустой чива.

П р и м с р. Уравнение колебаний маятника с трением

\[
\ddot{y}+a \dot{y}+b \sin y=0, a>0, b>0 \text {. }
\]

Нижнее положеніе равновесия $y=\dot{y}=0$ әкспоненциально устоӥниво, т. к. корни характеристич. уравнения $\lambda^{2}+a \lambda+b=0$ уравнения в вариациях имеют отрпцательные действительные части. Верхнее положение равновесия $y=\pi, \quad \dot{y}=0$ неустойчиво, т. к. характеристич. уравнение $\lambda^{2}+a \lambda-b=0$ уравнения в вариациях $\ddot{y}+a \dot{y}-b y=0$ имеет положительный корень. Әта неустойчивость имеет место и в отсутствии трения $(a=0)$. Нижнее положение равновесия маятника без


\end{document}