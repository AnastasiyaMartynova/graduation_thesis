\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}

\begin{document}
формулой, в к-рой метрич. тензор пространства Минковского $g_{\alpha \beta}$ заменен на метрич. тензор евклидова пространства $\delta_{\alpha \beta}$. Для того чтобы евклидово действие было конечно, нужно, чтобы поле $F_{\mu \nu}$ убывало на бесконечности. Накладывая естественные условия на скорость его убывания, можно доказать, что $A_{\mu}(x)$ на бесконечности близко чисто калибровочному полю $g^{-1}(x) \partial_{\mu} g(x)$, где $\mathrm{g}(x)$ - функция, принимающая значения в группе и определенная вне нек-рого шара. Это позволяет сопоставить полю $A_{\mu}$ элемент $q(A)$ гомотопич. группы $\pi_{3}(G)$, определяемый функцией $g(x)$, рассматриваемой как отображение сферы большого радиуса $S^{3}$ в $G$ (точнее, следует потребовать, чтобы сфера $S^{3}$ содержала внутри себя шар, вне к-рого определена функция $g(x)$ ). Элемент $q(A)$ определяет топологич. тип поля $A_{\mu}$; поле $A_{\mu}$ топологически нетривиально, если $q(A) \neq 0$. Пусть для определенности $G-$ простая компактная некоммутативная группа Ли; тогда $\pi_{3}(G)=\mathbb{Z}$ и $q(A)$ можно рассматривать как целое число. Его можно аналитически выразить в виде:

\[
q(A)=\frac{1}{64 \pi^{2}} \int d^{4} x \varepsilon^{\mu v \rho \sigma}\left\langle F_{\mu \nu}, F_{\rho \sigma}\right\rangle
\]

при соответствующей нормировке скалярного произведения в \&s (в случае $G=S U(n)$, (S)=su(n) нормированное нужным образом скалярное произведение дается формулой $\langle a, b\rangle=-\mathrm{Sp} a b$; в общем случае следует наложить условие: наибольшая длина простого корня равна двум). Доказывается, что евклидово действие $S(A)$ оценивается снизу числом

и что равенство

\[
\begin{gathered}
8 \pi^{2} e^{-2}|q(A)| \\
S(A)=8 \pi^{2} e^{-2}|q(A)|
\end{gathered}
\]

достигается при условии

\[
F_{\alpha \beta}= \pm \tilde{F}_{\alpha \beta}
\]

где $\tilde{F}_{\alpha \beta}$ - тензор, двойственный к $F_{\alpha \beta}$, знак определяется знаком числа $q(A)$. Уравнение (3) носит название у ра в нения самодуальности (автодуальности) или антисамодуальности (антиавтодуальности) (в зависимости от знака); его решения являются экстремалями евклидова действия. При $q>0$ эти решения называются инст антонами, а при $q<0-$ антиинстантонам и с топологич. числом $q$ (конечно, все они являются И. в смысле данного выше общего определения). Если калибровочное поле $A$ определено на четырехмерном ориентированном римановом многообразии, то (1) можно по-прежнему рассматривать как определение евклидова действия, а (2) - как определение топологич. числа поля $A$. Сформулированные выше результаты остаются справедливыми; калибровочное поле на многообразии можно интерпретировать как связность на главном расслоении над этим многообразием. Если многообразие замкнуто, то $q(A)$ отождествляется с характеристич. числом расслоения. Напряженность поля удобно рассматривать как 2-форму

\[
F=\frac{1}{2} F_{\alpha \beta} d x^{\alpha} \wedge d x^{\beta},
\]

принимаюшую значения в слоях ассоциированного расслоения со слоем (S; она отождествляется с кривизной связности. Уравнение (3) приобретает вид $F= \pm * F$, где * означает переход к двойственной форме. Евклидово действие $S(A)$ с точностью до множителя записывается как скалярный квадрат $(F, F)$ формы $F$. Исследование минимума $S(A)$ при фиксированном топологич. числе $q(A)=\left(32 \pi^{2}\right)^{-1}(F, * F)$ проводится с помощью тождества

\[
(F \pm * F, F \pm * F)=2(F, F) \pm 2(F, * F) \geqslant 0 .
\]

Уравнение (3) конформно инвариантно. Это позволяет свести изучение И. в $\mathbb{R}^{4}$ к изучению И. на сфере $S^{4}$. Переход

216 ИHCTAHTOH к изучению И. на компактном многообразии можно рассматривать также как пространственное обрезание. Изучение числа решений уравнения (3) на сфере $S^{4}$ (и, следовательно, в $\mathbb{R}^{4}$ ) проводится с помощью теории индекса эллиптич. операторов (см. [3]-[5]). В частности, для группы $S U(N), N \geqslant 2$, решения с топологич. числом $q$ образуют семейство, зависящее от $4 N|q|-\left(N^{2}-1\right)$ параметров при $|q| \geqslant \frac{N}{2}$ и от $4 q^{2}+1$ параметров при $1 \leqslant|q| \leqslant \frac{N}{2}$. Калибровочно эквивалентные решения при этом отождествляются. Решения уравнения самодуальности для случая $G=S U(2)$, $q=1$ получены в виде:

\[
A_{\mu}(x)=\frac{\lambda^{2}}{(x-a)^{2}+\lambda^{2}} u^{-1}\left(\frac{x-a}{|x-a|}\right) d_{\mu} u\left(\frac{x-a}{|x-a|}\right),
\]

где $u(n)=n^{0}+i \sigma \boldsymbol{n}$ осуществляет топологич. эквивалентность $S^{3}$ и $S U(2)$; здесь $n^{0^{2}}=n^{2}=1, \sigma-$ матрицы Паули. Параметр $\lambda$ в (4) характеризует размер И., $a$ - его положение. Считая, что группа $S U(2)$ стандартным способом вложена в $S U(N)$, можно рассматривать (4) как одноинстантонное решение для группы $S U(N)$; одноинстантонные решения для произвольной группы $G$ также могут быть получены с помощью вложения группы $S U(2)$ в $G$. Приближенное решение уравнения самодуальности с топологич. числом $q$ можно получить, склеивая $q$ далеких друг от друга одноинстантонных решений. Напр., для $G=S U(N)$ следует рассмотреть поле, к-рое вдали от точек $a_{1}, \ldots, a_{q}$ является чисто калибровочным, а в случае, если точка близка к $a_{i}$, имеет вид:

\[
A_{\mu}(x)=V_{i} \lambda_{i}^{2}\left(\left(x-a_{i}\right)^{2}+\lambda_{i}^{2}\right)^{-1} u^{-1}\left(\frac{x-a_{i}}{\left|x-a_{i}\right|}\right) d_{\mu} u\left(\frac{x-a_{i}}{\left|x-a_{i}\right|}\right),
\]

где $V_{i} \in S U(N),\left|x-a_{i}\right| \ll\left|a_{i}-a_{j}\right|, \lambda_{i} \ll\left|a_{i}-a_{j}\right|, i \neq j$. Можно доказать, что вблизи от построенных только что приближенных решений уравнения самодуальности лежат точные $q$-инстантонные решения; это позволяет придать наглядный смысл параметрам И. Семейство точных $q$-инстантонных решений для группы $S U(2)$, зависящее от $5 q+4$ параметров при $q \geqslant 3$ и от 13 параметров при $q=2$, можно получить с помощью подстановки:

\[
A_{\mu}=\bar{\sigma}_{\mu \nu} \partial_{v} \rho
\]

где

\[
\rho=\sum_{i=1}^{q+1} \lambda_{i}^{2}\left|x-a_{i}\right|^{-2}, \quad \bar{\sigma}_{\mu \nu}=\sigma^{a}\left(\varepsilon_{0 \alpha \mu \nu}-\delta_{a \mu} \delta_{0 v}+\delta_{a v} \delta_{0 \mu}\right)
\]

\begin{itemize}
  \item матрица, определяющая гомоморфизм алгебры Ли группы $S O(4)$ в алгебру Ли группы $S U(2)$. Было получено полное (но не очень явное) семейство $q$-инстантонных решений. Для группы $G=S U(2)=S p(1)$ их конструкция имеет следующий вид. Пусть $M(x)=a+b \hat{x}-$ матричная функция, где $a$ и $b-$ постоянные $((1+q) \times q)$-мерные кватернионные матрицы, $\hat{x}-$ кватернион, отвечающий вектору $x$. Если матрица $R(x)=M^{+}(x) M(x)$ действительна и обратима для каждой точки $x$, то можно построить $q$-инстантонное поле по формуле
\end{itemize}

\[
A_{\mu}(x)=N^{+}(x) \partial_{\mu} N(x),
\]

где $N(x)-(q+1)$-мерный кватернионный вектор-столбец, удовлетворяющий уравнениям

\[
N^{+}(x) M(x)=0, N^{+}(x) N(x)=1 .
\]

Следует отметить, что изучение экстремалей функционала евклидова действия $(2+1)$-мерной теории сводится к изучению экстремалей функционала энергии $(3+1)$-мерной теории. Это позволяет отождествить И. $(2+1)$-мерной калибровочной теории с магнитными монополями ее $(3+2)$-мерного аналога. И. (1+1)-мерной модели с евклидовым действием

\[
S(\boldsymbol{n})=\int \partial_{\mu} n^{a} \partial_{\mu} n^{a} d^{2} x
\]

где $n=\left(n^{1}, n^{2}, n^{3}\right), n^{2}=1$, можно отождествить с рациональными функщиями комплексного переменного


\end{document}