\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
и союзное с ним уравнениө

\[
\psi(x)-\lambda \int_{a}^{b} K(s, x) \psi(s) d s=0
\]

при фиксированном значении параметра $\lambda$ имеют либо лишь тривиальные решения, либо одинаковое конечное число линейно независимых решений: $\varphi_{1}, \ldots$ $\ldots, \varphi_{n} ; \psi_{1}, \ldots, \psi_{n}$.

Т е о е м а 2. Для разрешимости неоднородного уравнения

\[
\varphi(x)-\lambda \int_{a}^{b} K(x, s) \varphi(s) d s=f(x)
\]

необходимо и достаточно, чтобы его правая часть была ортогональна полной системе линейно независимых решений соответствующего однородного союзного уравнения (2):

\[
\int_{a}^{b} f(x) \psi_{j}(x) d x=0, j=1, \ldots, n .
\]

Т е о е ма 3 (а л ь т е рн а ти в а г о л м а). Либо неоднородное уравнение (3) разрешимо, какова бы ни была его правая часть $f$, либо соответствующее однородное уравнение (1) имеет ненулевые решения.

Т е о р е м 4. Множество характеристических чисел уравнения (1) не более чем счетно, с единственной возможнойі предельной точкой на бесконечности.

Для справедливости Ф. т. в функциональном пространстве $L_{2}(a, b)$ достаточно, чтобы ядро $K$ уравнения (3) было интегрируемым с квадратом на множестве $[a, b] \times[a, b]$ ( $a, b$ могут быть и бесконечными). Когда это условие нарушается, уравнение (3) может оказаться нефредгольмовым интегральныл уравнением. Когда параметр $\lambda$ и функции, участвующие в уравнении (3), принимают комплексные значения, тогда взамен союзного уравнения (2) часто рассматривают сопряженное к (1) уравнение

\[
\psi(x)-\bar{\lambda} \int_{a}^{b} \overline{K(s, x)} \psi(s) d s=0 .
\]

В этом случае условие (4) заменяется условием

\[
\int_{a}^{b} f(x) \overline{\psi_{j}(x)} d x=0, j=1, \ldots, n .
\]

Теоремы доказаны Э. Фредгольмом [1].

Jum.: [1] F r e d h o l m E. I., "Acta math.", 1903, v. 27, p. $365-390$. Б. В. Хведелидзе.

ФРЕДГОЛЬМА УРАВНЕНИЕ - интегральное уравнение вида

\[
\int_{a}^{b} K(x, s) \varphi(s) d s=f(x), x \in[a, b],
\]

— Ф. у. 1-г о р о д а, или вида

\[
\varphi(x)-\lambda \int_{a}^{b} K(x, s) \varphi(s) d s=f(x), x \in[a, b],
\]

\begin{itemize}
  \item Ф. у. 2-г о р о д а, если интөгральный оператор
\end{itemize}

\[
K \varphi(x)=\int_{a}^{b} K(x, s) \varphi(s) d s, x \in[a, b],
\]

является виолне непрерывным в нек-ром функциональном пространстве $E$. Предполагается, что свободчый член $f$ и искомая функция $\varphi$ принадлежат пространству $E$ : Важным примером Ф. у. является уравнение, в к-ром ядро $K$ удовлетворяет условию

\[
B^{2}=\int_{a}^{b} \int_{a}^{b}|K(x, s)|^{2} d x d s<\infty,
\]

a $E=L_{2}([a, b])$ Численный параметр $\lambda$ и функции $f, \varphi, K$ могут принимать как действительные, так и комплексныө значения. О Ф. у. 1-го рода см. Интегральное уравнение с симметричным ядром и Некорректные задачи. Ниже рассматриваются лишь Ф. у. 2-го рода.

М етод послед о в т т льных при ближений р е шен ия Ф. у. 2-го ро д а. Этопервый метод, к-рый был предложен для решения уравнения (1). Для формулировки этого метода пусть ураввение (1) записано в виде

\[
\varphi(x)=f(x)+\lambda K \varphi(x), x \in[a, b] .
\]

Предполагается, что ядро $K$ удовлетворяет условию (3), а $E=L_{2}([a, b])$. Пусть начальное приближение искомого решения $\varphi_{0}=f$; если $(n-1)$-е приближени $\varphi_{n-1}$ построено, то

Іри этом

\[
\varphi_{n}=f+\lambda K \varphi_{n-1}
\]

\[
\varphi_{n}=\sum_{m=0}^{n} \lambda^{m} K_{m} f
\]

где $K_{m}$ обозначает $m$-е итерированное ядро ядра $K$. Функция (5) является частичной суммой ряда

\[
\sum_{m=0}^{\infty} \lambda^{m} K_{m} f
\]

к-рый наз. р я д о м ЈІ и у в и л л я- Н е й м а на). Если $|\lambda|<B^{-1}$, то ряд (6) сходится в среднем к решению уравнения (1), ІІ это решение - единственное (см., напр., [5]). Если существует такая положительная постоянная $A$, что

\[
\int_{a}^{b}|K(x, s)|^{2} d s \leqslant A, x \in[a, b],
\]

то ряд (6) сходится абсолютно и равномерно. Вообще говоря, ряд (6) расходится, если $|\lambda| \geqslant B^{-1}$. Именно, это так, если ядро $K$ имеет характеристич. число. Если же ядро не имеөт характеристич. чисел (как, напр., в случае ядра Вольтерра), то ряд (6) сходится при любом значении $\lambda$.

М е тод Фред гольма решения Ф. у. 2 - г о р о д а. Метод последовательных приблиякений дает возможность цостроить решение уравнения (1), вообще говоря, лишь при малых значениях параметра 2. Метод, дающий возможность решить уравнение (1) для любого значения параметра $\lambda$, был впервые предложен Ә. Фредгольмом (E. Fredholm, 1903). В предположении, что ядро $K$ нопрерывно на квадрате $[a, b] \times$ $\times[a, b]$, а свободный члөн и искомое решение непрерывны на сегменте $[a, b]$, ниже кратко описана идея әтого мөтода.

Отрезок $[a, b]$ делится на $n$ равных частей длины $h=(b-a) / n$. Если заменить интеграл в (1) интегральной суммой, то точное уравнение (1) заменяется приближенным

\[
\varphi(x)-\lambda h \sum_{j=1}^{n} K\left(x, s_{j}\right) \varphi\left(s_{j}\right)=f(x), x \in[a, b] .
\]

Полагая в (7) последовательно $x=s_{1}, \ldots, s_{n}$ для определения приближенного значения неизвестной функции $\varphi$ в точках $s_{j}$, получают линейную алгебраич. систему

\[
\varphi_{i}-\lambda h \sum_{j=0}^{n} K_{i j} \varphi_{j}=f_{i}, i=1,2, \ldots, n,
\]

где $f\left(s_{i}\right)=f_{i}, \varphi\left(s_{i}\right)=\varphi_{i}, K\left(s_{i}, s_{j}\right)=K_{i j}$. Разрешимость системы (8) зависит от значения определителя

\begin{center}
\includegraphics[max width=\textwidth]{2023_07_09_d7d5e31992ab74d4cf57g-1}
\end{center}

к-рый является многочленом относительно $\lambda$. Если $\lambda$


\end{document}