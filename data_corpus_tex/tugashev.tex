% \input{preamblw}
%
%
% \begin{document}
%
%
% \tableofcontents
% \newpage

\renewcommand{\firstpage}{260}
\renewcommand{\lastpage}{266}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


\udk{519.712}
\msc{68W40} 

\title{К вопросу об оптимизирующих преобразованиях управляющих алгоритмов
реального времени}% Полный заголовок
{К вопросу об оптимизирующих преобразованиях  \dots}

\author{А.\,А.~Тюгаш\"eв,  А.\,Ю.~Богатов}{Т\,ю\,г\,а\,ш\,\"e\,в~А.\,А.,  Б\,о\,г\,а\,т\,о\,в~А.\,Ю.}

\institute{Самарский государственный аэрокосмический университет им. ак. С.\,П.~Королёва, \\
443086,  г.Самара,  Московское ш.,  34.}

\email{E-mails}{tau797@mail.ru,  artmbogatov@yandex.ru}

\otherinf{ Тюгашёв Андрей Александрович "--- профессор кафедры компьютерных систем;
д.т.н.,  профессор.%; специалист по созданию ИПИ-технологий на проблемно-ориентированном уровне.
\\
Богатов Артём Юрьевич "--- ассистент кафедры компьютерных систем;
аспирант.%,  круг научных интересов: теория графов,  теория алгоритмов,  CALS-технологии.
}


\workabstract{Рассматривается проблема машинно-независимой оптимизации программ,
построенных на управляющих алгоритмах реального времени. Описывается
математическая модель семантики управляющих алгоритмов и основанные на ней
эквивалентные оптимизирующие преобразования.}

\keywords{математическая модель,  управляющий алгоритм,  эквивалентные
оптимизирующие преобразования,  функциональная задача,  система линейных
уравнений,  бинарное дерево}

\engkeywords{mathematical model, control algorithm, equivalent optimizing transformations, functional task, system of the linear equations, binary tree}

\engtitle{To the Question of Optimizing the Real-time Controlling Algorithms}

\engauthor{A.\,A.~Tyugashev,  A.\,Yu.~Bogatov}{T\,y\,u\,g\,a\,s\,h\,e\,v~A.\,A.,  B\,o\,g\,a\,t\,o\,v~A.\,Y\,u.}


\enginstitute{S.\,P.~Korolyov Samara State Aerospace University, \\
34, Moskovskoye sh., Samara,    443086.}

\otherenginfm{Tyugashev Andrey Alexandrovich, Dr. Sci. $($Techn.$)$, Prof., Dept. of Computer
Systems.\\
Bogatov Artyom Yurievich, Assist., Postgraduate Student, Dept. of Computer
Systems.}

\engabstract{The problem of machine-independent optimization is studied for the programs, based on real-time controlling algorithm. This article describes a mathematical model of the controlling algorithm semantics and equivalent optimizing transformations based on it.}


\date{15/I/2009} % Дата поступления статьи в редакцию.
\revisiondate{16/II/2009} % В окончательном виде

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


\maketitle

\textbf{Введение.} В работе рассматриваются оптимизирующие преобразования на
множестве программ,  основанных на управляющих алгоритмах.
В классическом представлении программа описывается с~точки зрения модели
пред- и постусловий: $(A)\pi(B)$.
Программа $\pi $ работает корректно ($(A)\pi (B)\equiv 1$),  если для любого набора данных,
на котором истинен предикат $A$,  после выполнения $\pi $,  получаем выходные данные,
на которых истинен предикат $B$.
Соответственно,  две программы $\pi _1 $ и $\pi _2 $ будут
эквивалентны,  если для любых предикатов $R$ и $Q$ выполняется
$(R)\pi _1 (Q)\to  (R)\pi _2 (Q)$.
Преобразования $\Phi(\pi )$ приводят программу $\pi $ к~оптимальному виду,
если выполнены следующие два условия:
\begin{enumerate}
\item эквивалентность: $\forall $ предикатов $R$,  $Q$ выполняется $(R)\pi (Q){\to } (R)\Phi(\pi )(Q)$;
\item оптимальность: для выбранного критерия оптимальности,  задаваемого предикатом $(K)$,
должно выполняться тождество: $(K)(\Phi(\pi ))\equiv 1$.
\end{enumerate}

Специфика управляющих алгоритмов реального времени заключается в том,
что корректно работающие программы,  построенные на этих алгоритмах, должны
обеспечивать выполнение целевых задач в заданные моменты времени.
В рамках модели пред- и постусловий такую программу можно описать следующим образом:
$$(A(D_0 , t_0 ))\pi (B(D_1 (t_1 ), D_2 (t_2 ), \dots, D_k (t_k ))), $$
где $(A(D_0, t_0 ))$ означает корректное задание начальных условий на момент времени
$t_0 $,
а $(B(D_1 (t_1 ), D_2 (t_2 ), \dots, D_k (t_k )))$ означает,  что в
результате выполнения программы $\pi $ были корректно выполнены целевые
задачи в моменты времени $t_1 , t_2 ,  \dots, t_k $.

Обычно при оптимизации программу переводят в некоторое внутреннее
представление,  для которого находят эквивалентные преобразования,
позволяющие выполнить критерий оптимизации. В настоящей статье используется
представление программ,  предложенное в работе [1].

\textbf{1. Исчисление управляющих алгоритмов реального времени.}
Пусть в управляющем алгоритме выполняются $N$ функциональных задач,
тогда управляющий алгоритм реального времени можно представить
в~виде четвёрки объектов:
$\langle f_i , t_i , \tau _i , \vec l_i \rangle $  ($i=1, 2, \dots, N$),
где $f_i $ "--- функциональная задача,
$t_i $ "--- момент начала выполнения действия,
$\tau _i $ "--- длительность действия,
$\vec l_i $ -- логический вектор,  обуславливающий действие.
Координаты вектора
$\vec l_i $ могут принимать значения из множества $\{1, 0, \rm H\}$,
где $\rm  H$ означает независимость выполнения функциональной задачи от значения
данной логической переменной.
В таблице приведены операции конъюнкции,
дизъюнкции и отрицания для алгебры трёхзначной логики.

\begin{table}[htbp]
\centering
\begin{minipage}{.7\textwidth}
\centering
\textbf{Функции конъюнкция,  дизъюнкция и отрица\-ние в~алгебре трёхзначной логики}\vspace{-.1mm}

\begin{tabular}{c|c|c|c|c}
\hline
~~~~$x$~~~~&
~~~~$y$~~~~&
$x \vee y $ (или)&
$x \wedge y$ (и)&
~$\neg x$ (не)~ \\
\hline
0&
0&
0&
0&
1 \\
\hline
0&
1&
1&
0&
1 \\
\hline
1&
0&
1&
0&
0 \\
\hline
1&
1&
1&
1&
0 \\
\hline
0&
H&
0&
0&
1 \\
\hline
1&
H&
1&
1&
0 \\
\hline
H&
0&
H&
0&
H \\
\hline
H&
1&
H&
1&
H \\
\hline
H&
H&
H&
H&
H \\
\hline
\end{tabular}
\end{minipage}
\end{table}


Далее  на множестве управляющих алгоритмов вводятся нижеследующие операции.

\begin{enumerate}
\item ${\rm CK}$ "--- операция совпадения по концу.
Пусть $T_1 $ и $T_2 $ "--- термы  со временем начала исполнения $t_{T1} $и $t_{T2} $
и~длительностью $\tau _{T1} $и $\tau _{T2} $,  тогда
\begin{equation}
\label{tyug:eq1}
T_1 \;{\rm CK}\;T_2 \quad \Leftrightarrow \quad  t_{T1} +\tau _{T1} =t_{T2} +\tau_{T2}.
\end{equation}

\item ${\rm CH}$ "--- операция совпадения по началу.
Пусть $T_1 $ и $T_2 $ "--- термы со временем начала исполнения $t_{T1} $ и $t_{T2}$,  тогда
\begin{equation}
\label{tyug:eq2}
T_1 \;{\rm CH}\;T_2 \quad \Leftrightarrow \quad t_{T1} =t_{T2} .
\end{equation}

\item $\to $ "--- операция непосредственного следования.
Пусть $T_1 $ и $T_2 $ "--- термы со временем начала исполнения $t_{T1} $ и $t_{T2} $
и~длительностью $\tau _{T1}$ и $\tau _{T2} $,  тогда
\begin{equation}
\label{tyug:eq3}
T_1 \to  T_2 \quad \Leftrightarrow \quad  t_{T1} +\tau _{T1} =t_{T2} .
\end{equation}

\item $+$ "--- операция выбора динамического объекта.
Для термов $T_1 $ и $T_2 $ выражение $T_1 +T_2 $ означает,  что существует два варианта выполнения программы:
по ветке,  реализующей терм $T_1$, и~по ветке,  реализующей терм $T_2 $.

\item $\Rightarrow $ "--- операция <<навешивания>> предиката на терм (создание динамического объекта).
Для терма $T_1 $ и $n$-местной логической функции $\psi (\alpha _1 , \alpha _2,$ $\dots, \alpha _n )$ выражение
\[
\psi (\alpha _1 , \alpha _2, \dots, \alpha _n )\Rightarrow T_1
\]
означает,  что терм $T_1 $ выполняется,  если функция $\psi (\alpha _1
,  \dots, \alpha _n )$ истинна.


\item $\rm IN$, $\rm OUT$ "--- операции <<навешивания>> входных и выходных переменных на терм. Данные операции определяют обязательные входные и выходные переменные.
\end{enumerate}

Для операций на множестве управляющих алгоритмов реального времени
справедливы следующие аксиомы:

1) $T_1 \;{\rm CH}\;T_2 =T_2 \;{\rm CH}\;T_1 , $

2) $T_1 \;{\rm CK}\;T_2 =T_2 \;{\rm CK}\;T_1 , $

3) $T_1 +T_2 =T_2 +T_1 , $

4) $(T_1 \to T_2 )\to  T_3 =T_1 \to (T_2 \to  T_3 ), $

5) $(T_1 \;{\rm CH}\;T_2 )\;{\rm CH}\;T_3 =T_1 \;{\rm CH}\;(T_2 \;{\rm CH}\;T_3 ), $

6) $(T_1 \;{\rm CK}\;T_2 )\;{\rm CK}\;T_3 =T_1 \;{\rm CK}\;(T_2 \;{\rm CK}\;T_3 ), $

7) $(T_1 \to T_2 ){\rm CH}(T_1 \to  T_3 )=T_1 \to (T_2 \;{\rm CH}\;T_3 ), $

8) $(T_1 \to T_2 ){\rm CK}(T_3 \to  T_2 )=(T_1 \;{\rm CK}\;T_3 )\to  T_2 , $

9) $(T_1 \to T_2 )+(T_1 \to  T_3 )=T_1 \to (T_2 +T_3 ), $

10) $(T_1 \to T_2 )+(T_3 \to  T_2 )=(T_1 +T_3 )\to  T_2 , $

11) $(T_1 \;{\rm CH}\;T_2 )+(T_1 \;{\rm CH}\;T_3 )=T_1 \;{\rm CH}\;(T_2 +T_3 ), $

12) $(T_1 \;{\rm CH}\;T_2 )+(T_3 \;{\rm CH}\;T_2 )=(T_1 +T_3 )\;{\rm CH}\;T_2 , $

14) $(T_1 \;{\rm CK}\;T_2 )+(T_1 \;{\rm CK}\;T_3 )=T_1 \;{\rm CK}\;(T_2 +T_3 ), $

15) $(T_1 \;{\rm CK}\;T_2 )+(T_3 \;{\rm CK}\;T_2 )=(T_1 +T_3 )\;{\rm CK}\;T_2 , $

16) $(\alpha _1 =1)\Rightarrow (T_1 \to  T_2 )=((\alpha _1 =1)\Rightarrow T_1
)\to  ((\alpha _1 =1)\Rightarrow T_2 ), $

17) $(\alpha _1 =1)\Rightarrow (T_1 \;{\rm CH}\;T_2 )=((\alpha _1 =1)\Rightarrow
T_1 ){\rm CH}((\alpha _1 =1)\Rightarrow T_2 ), $

18) $(\alpha _1 =1)\Rightarrow (T_1 \;{\rm CK}\;T_2 )=((\alpha _1 =1)\Rightarrow
T_1 ){\rm CK}((\alpha _1 =1)\Rightarrow T_2 ), $

19) $T_1 \;{\rm CH}\;T_1 =T_1 , $

20) $T_1 \;{\rm CK}\;T_1 =T_1 , $

21) $(\alpha \Rightarrow T)+(\neg \alpha \Rightarrow T)=T, $

22) $(\alpha \Rightarrow (\beta \Rightarrow T))=(\alpha \wedge \beta
)\Rightarrow T.$

\textbf{2. Постановка задачи.}
Аналогично тому,  как это было сделано для классических
программ,  задачу поиска эквивалентных оптимизирующих преобразований для
управляющих алгоритмов реального времени можно сформулировать в~рамках
модели пред- и постусловий:
требуется найти преобразование $\Phi(\pi )$
программы $\pi $,  для которого истинны следующие высказывания:
\[
\left\{ \begin{array}{l}
 (A(D_0 , t_0 )\}\pi \{B(D_1 (t_1 ), \dots, D_k (t_k ))\}\to  \\
\qquad \qquad \{A(D_0 , t_0 )\}\Phi(\pi)\{B(D_1 (t_1 ), \dots, D_k (t_k ))); \\
 (K(\Phi(\pi )))\equiv 1. \\
 \end{array} \right.
\]

Первое условие означает,  что программы $\pi $ и $\pi _1 =\Phi(\pi )$  на одних
и~тех же исходных данных будут работать эквивалентно,  в~каждый момент
времени выполняя одинаковые функциональные задачи и формируя одинаковый
набор выходных данных.

Второе условие означает оптимальность программы $\pi _1 =\Phi(\pi )$ в смысле
критерия оптимальности,  задаваемого предикатом $K$.

Будем строить оптимизирующие преобразования для следующих критериев:

\begin{enumerate}
\item минимизация количества операций типа ${\rm CH}$, ${\rm CK}$, $\to$, $+$;
\item минимизация проверок условий.
\end{enumerate}

\textbf{3. Оптимизация по критерию минимума количества операций.}

\textbf{3.1 Алгебраический метод.} Пусть имеется реализация управляющего
алгоритма на некотором проблемно-ориентированном языке программирования.
Синтаксическая редукция описывающих управляющий алгоритм формул,
оставляющая без изменений семантику алгоритма,  очевидно,  приведёт к
уменьшению количества операций в конечной программе.

Пусть алгоритм содержат $N$ функциональных задач.  Пронумеруем их числами от
1 до $N$,  время начала исполнения $i$-той задачи обозначим $t_i $,  а
длительность исполнения $\tau _i $. Каждой операции,  задающей временное
соотношение между термами,  поставим в соответствие линейное уравнение по
правилам (1)--(3).
Причём,  если терм $T_i $  выполняется на таких
реализациях логического вектора $\vec l_i $,  что логическая функция
$\psi _i (\alpha _1 , \alpha _2 , \dots, \alpha _k )\equiv 1$,  то соответствующую часть
уравнения нужно умножить на $\psi _i (\beta _1 , \beta _2 , \dots, \beta _k )$.
Таким образом,  получаем систему линейных алгебраических уравнений относительно
$t_i $ и $\tau _i $ ($i=1, 2, \dots, N)$,
при этом,  как известно,
возможны три случая: cистема несовместна; система совместная,  определённая;
система совместная,  неопределённая.

Первый и второй случаи тривиальны. Несовместность системы,  полученной по
вышеописанным правилам,  означает наличие в управляющем алгоритме
противоречивых условий.
Если решение системы существует и единственно,  то
есть для каждой функциональной задачи можно явно указать время начала и
длительность выполнения,  то,  очевидно,  цель оптимизации по критерию минимума
операций типа ${\rm CH}$, ${\rm CK}$, $\to$, $+$ достигнута.
В случае,  когда система
совместная,  неопределённая,  её решение будет задавать квазиоптимальный
алгоритм по рассматриваемому критерию.
С одной стороны,  решая систему,  мы
исключаем из алгоритма избыточные временные условия,  уменьшая тем самым
количество операций типа ${\rm CH}$, ${\rm CK}$, $\to$, $+$.
С другой стороны,  полученное
решение нельзя считать оптимальным,  так как пока неизвестно, зависит ли
сложность решения от выбора главных и свободных переменных,  и если зависит,
то как.

\textbf{3.2. Метод формальных преобразований.} Задачу оптимизации
управляющего алгоритма по критерию минимума операций типа ${\rm CH}$, ${\rm CK}$, $\to$, $+$
 можно решить,  применяя к формулам,  задающим алгоритм,  аксиомы 1)--22).

Рассмотрим пример. Пусть в алгоритме присутствует формула
\[
((f_1 \to  f_2 )\,{\rm CH}\,(f_1 \to  f_3 ))\,{\rm CK}\,(f_4 \to  (f_2 \,{\rm CH}\,f_3 )).
\]

Применяя аксиомы 7), 8), эту  формулу можно преобразовать к виду $$(f_1 \;{\rm CK}\;f_4
)\to  (f_2 \;{\rm CH}\;f_3 ),$$  при этом произошло уменьшение количества операций в
2 раза.

В качестве внутреннего представления оптимизируемых формул управляющего
алгоритма будем рассматривать их двоичные деревья синтаксического разбора.
Оптимизация двоичных деревьев проводится в два этапа: 1) идентификация
двоичных поддеревьев; 2) применение к поддеревьям аксиомы сжатия,  если это
возможно.

Существует несколько способов идентификации одинаковых поддеревьев. Первый
подход "--- метод прямого сравнения,  т.\,е. для каждого узла сравниваем все
поддеревья.
Второй подход "--- метод простых чисел.
Все узлы двоичного дерева
нумеруются простыми числами,  операции ${\rm CH}$, ${\rm CK}$, $\to$, $+$ получают коды 2,
3,  5,  7.
Узлы,  соответствующие одной и~той же операции или одной и~той же
функциональной задаче, нумеруются одинаковыми числами.
Для идентификации поддерева производится перемножение кодов всех узлов,
входящих в него.
Некоммутативные операции $\to  $ и $+$ для различения левого и правого
поддеревьев в случае равенства произведений используют приписывание знака
минус (<<$-$>>).
Недостатком метода простых чисел является быстрое
переполнение. Поэтому вместо него возможно применение метода <<целых>>
чисел,  в котором узлы"=поддеревья кодируются всеми целыми числами.
Однако при
этом необходимо иметь в памяти таблицу,  описывающую каждый подузел с
информацией о его левом,  правом поддереве и месте в дереве.

Рассмотрим алгоритм проведения эквивалентных преобразований на бинарных
деревьях. Этот алгоритм начинает обработку с самых маленьких поддеревьев,  а
затем,  поднимаясь выше,  охватывает все большее количество узлов. Поддеревья
соединяются с помощью узла "--- операции,  причем в конструкции $D_1 f D_2
$(поддерево,  над которым в текущий момент работает алгоритм) поддеревья $D_1
$ и $D_2 $ уже отработаны,  поэтому по ним надо спуститься максимум на
один-два уровня. Таким образом,  процесс эквивалентных преобразований носит
индуктивный характер. Базисом индукции является исходное двоичное дерево
представления алгоритма $D_0 $. Шаги индукции проводятся применением аксиом
исчисления управляющих алгоритмов. На каждом шаге индукции получаем
эквивалентное предыдущему двоичное дерево $D_j $. Последовательность
$D_0, D_1, \dots, D_k $ длины $k+1$ назовем выводом $D_0 \to  D_k $.

\textbf{4. Оптимизация графовых представлений управляющего алгоритма.}
При моделировании сложных распределённых систем довольно продуктивным является
графовый подход,  основанный на представлении логики функционирования
моделируемой системы в виде специальной графовой структуры.
Примерами реализации данного подхода могут служить визуальный язык {\tt GraphPlus} [2]
и~система автоматической разработки {\tt ГРАФКОНТ} [6]. Ниже показана возможность
оптимизации графового представления управляющих алгоритмов в нотации системы
{\tt {\tt ГРАФКОНТ}}.

\begin{figure}[t!]
\centering

\includegraphics{tyug.eps}

\small
Логико-временные схемы,  реализующие один и тот же управляющий алгоритм

\vspace{-5mm}
\end{figure}

На рисунке представлены две логико-временные схемы управляющих алгоритмов.
Очевидно,  что обе схемы семантически эквивалентны. Но во второй схеме на
одну проверку условия меньше и,  соответственно,  отсутствует ветвь,
содержащая дублирующее исполнение функциональной задачи $f_2$ в случае,  когда
$a_2=0$.
Таким образом,  возможность оптимизирующего преобразования,  переводящего
первую схему во вторую,  связана с проверкой значения переменной $a_1$,
при этом  функциональная задача $f_2$ не зависит от $a_1$.
Подобные оптимизирующие преобразования связаны с понятием веса логических переменных.
Назовём весом логической переменной количество логических векторов,
обуславливающих выполнение данного управляющего алгоритма,  в которых
значение этой переменной равно 0 или 1. Приведём алгоритм оптимизирующих
преобразований:
\begin{enumerate}
\item обходим граф исходной логико-временной схемы; для каждой вершины,  соответствующей функциональной задаче, выписываем полный логический вектор;
\item для каждой функциональной задачи сравниваем логические вектора и формируем результирующий вектор по следующему  принципу: если во всех логических векторах значение $i$-той переменной не меняется,  записываем в результирующий вектор это значение,  иначе "--- записываем значение $\rm H$;
\item считаем вес для каждой логической переменной;
\item сортируем массив логических переменных в порядке убывания весов;
\item синтезируем оптимальную логико-временную схему по следующему \linebreak принципу: на $j$-том шаге изображаем вершины для функциональных задач,  зависящих ровно от $j$ первых логических переменных; изображаем соответствующее количество вершин,  соответствующих $j$-той логической переменной.
\end{enumerate}

\textbf{Заключение.} Алгебраический метод синтаксической редукции формул
позволяет проверить корректность задания временных и логических условий
управляющего алгоритма,  но данный подход приводит к квазиоптимальному
решению.

Метод формальных преобразований обеспечивает оптимальность решения,  но
недостатком данного подхода является его высокая вычислительная сложность.

Приведённый выше метод оптимизации графовых представлений управляющего
алгоритма применим для логико-временных схем c одним входом и без временных
связей. Кроме того,  в разобранном случае значения логических переменных не
меняются на протяжении работы управляющего алгоритма. В общем случае
логические переменные,  фигурирующие в логико-временной схеме,  могут
относиться к одному из трех типов:
\begin{enumerate}
\item $\alpha (t) $, то есть актуально значение логической переменной в текущий момент времени $t$,  и с течением времени значение $\alpha $ может меняться;
\item $\alpha (t_0 ) $, когда значение переменной может меняться,  но интересует ее значение в указанный момент времени $t_0 $;
\item $\alpha _{\text{стат}} $, когда значение логической переменной не претерпевает изменений в процессе исполнения управляющего алгоритма.
\end{enumerate}

Таким образом,  рассмотренный случай получается из общего путём фиксации
момента времени $t$,  следовательно,  предложенный алгоритм оптимизации
графовых представлений может быть максимально обобщён.

\begin{thebibliography}{9}
\RBibitem{tu1}
\book Алгебра. Языки. Программирование
\by Глушков~В.\,М.,  Цейтлин~Г.\,Е.,  Ющенко~Е.\,Л.
\publaddr Киев
\publ Наукова думка
\yr 1989
\finalinfo 376~с

\RBibitem{tu2}
\by Востокин~С.\,В.
\book Графическая объектная модель параллельных процессов и ее применение в задачах численного моделирования
\publaddr Самара
\publ СНЦ РАН
\yr  2007
\finalinfo 286~с


\RBibitem{tu3}
\by  Калентьев~А.\,А.
\book Автоматизированный синтез алгоритмов асинхронного управления техническими системами с множеством дискретных состояний
\publaddr Самара
\publ CГАУ
\yr 1998
\finalinfo 204~с


\RBibitem{tu4}
\by   Касьянов~В.\,Н.
\book Оптимизирующие преобразования программ
\publaddr М.
\publ Наука
\yr  1988
\finalinfo 335~с


\RBibitem{tu5}
\by    Лавров~С.\,С.
\book Программирование. Математические основы,  средства,  теория
\publaddr СПб.
\publ БХВ-Петербург
\yr 2001
\finalinfo 320~с

\RBibitem{tu6}
\by Тюгашёв~А.\,А.
\paper Интегрированная среда для проектирования управляющих алгоритмов реального времени
\jour Извест. РАН. Теория и системы управления
\yr 2006
\issue 2
\pages 128--141

\RBibitem{tu7}
\by Тюгашёв~А.\,А.
\paper Проблема неоднозначности при порождении логико-временной структуры управляющего алгоритма по многовходовой модели реального времени
\bookinfo Сб. тр. Третьей международн. молодежн. шк.-сем \inbook БИКАМП-01
\yr 2001
\publaddr СПб.
\publ ГУАП
\pages 51--58

\RBibitem{tu8}
\by Успенский~В.\,А.,  Семенов~А.\,Л.
\book  Теория алгоритмов: основные открытия и приложения
\publaddr М.
\publ Наука
\yr 1987
\finalinfo 288~с
\end{thebibliography}

\makeengtitlem
\lfoot[\thepage]{}
\rfoot[]{\thepage}


% \end{document}
