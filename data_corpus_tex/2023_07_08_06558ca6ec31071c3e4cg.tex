\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}
\usepackage{mathrsfs}

\begin{document}
Если $\alpha$ трансцендентен над $F$, то $c$ является изоморфизмом.

\begin{enumerate}
  \setcounter{enumi}{3}
  \item Пусть $F$ - дифференциальное поле, $a_{1}, \ldots, a_{n} \in F$ и $\left(\eta_{1}, \ldots, \eta_{n}\right)$ - фундаментальная система нулей уравнения $y^{(n)}+a_{1} y^{(n-1)}+\ldots+a_{n} y=0$, к-рая порождает расширение Пикара - Вессио поля $F$. Группа Галуа $G\left(F<\eta_{1}, \ldots, \eta_{n}>/ F\right)$ содержится в $\mathrm{SL}_{K}(n)$ тогда и только тогда, когда уравнение $y^{\prime}+a_{1} y=0$ имеет нетривиальный нуль в $F$.В частности, если $F=C(x)-$ дифференциальное поле рациональных функций одного комплексного переменного с диффференцированием $d / d x$ и $B v=y^{\prime \prime}+x^{-1} y^{\prime}+\left(1-v^{2} x^{-2}\right) y-$ дифференциальный многочлен Бесселя, то группа Галуа соответствующего $\mathrm{P}$. совпадает с $\mathrm{SL}_{K}(2)$ при $v-1 / 2 \notin \mathbb{Z}$. Если $v-{ }^{1} /_{2} \in \mathbb{Z}$, то группа Галуа совпадает с $K^{*}$.
\end{enumerate}

Для любого натурального $n$ можно построить Р. дифференциальных полей $\xi \supset F$ такое, что $G(\xi / F) \approx \mathrm{GL}_{K}(n)$. Существует соответствие Галуа между множеством дифференциальных подполей сильно нормального Р. и множеством алгебраич. подгрупп его группы Галуа.

Как и в обычной теории Галуа, в дифференциальном случае рассматриваются две общие задачи.

а) Прямая задача: задано сильно нормальное расширение $\mathscr{G}$ дифференциального поля $F$. Определить өго группу Галуа.

б) Обратная задача: заданы дифференциальное поле $F$ и алгебраич. группа $G$. Описать множество сильно нормальных Р. поля $F$, группа Галуа к-рых изоморфна группе $G$ (в частности, определить, не пусто ли оно). Существует другой способ обобщения нормальности на случай Р. дифференциальных полей и построения дифференциальной теории Галуа, использующий методы дифференциальной геометрии [4].

Jum.: [1] R i t t J. F., Differential algebra, N. Y., 1950;
[2] K o c $\mathrm{h}$ i $\mathrm{E}$. R., Differential algebra and algebraic groups, N. Y., 1973; [3] К а п л а н с к и й И., Введение в дифференциальную алгебру, пер. с англ., М., 1959; [4] Р о $\mathrm{m} \mathrm{m}$ а r е t J. F., Differential Galo is theory, N. Y.-L.-P., 1983 .

РАСІІИРЕНИЕ м о д у л я Миюбой модуль $X$, содержащий данный модуль $A$ в качестве подмодуля. Обычно, говоря о Р. модуля $A$, фиксируют фактормодуль $X / A$, т. е. р а с ши иен й м модуля с по мо ь ю мо д ул я $B$ наз. точную последовательность

\[
0 \longrightarrow A \longrightarrow X \longrightarrow B \longrightarrow 0
\]

Такой модуль $X$ всегда существует (напр., прямая сумма $A$ и $B$ ), но не определяется модулями $A$ и $B$ однозначно. Как в теории модулей, так и в ее приложениях возникает потребность в обозрении всех различных Р. модуля $A$ с помощью $B$. С этой целью в классе всех Р. модуля $A$ с помощью $B$ вводится отношение әквивалентности, а на классах әквивалентных Р.- операция умножения (см. Бәра умножение), относительно к-рой множество классов эквивалентности Р. модуля $A$ над кольцом $R$ образует абелеву группу $\operatorname{Ext}_{R}^{\prime}(A, B)$. Эта конструкция обобщается и на $n-\kappa$ р а т н ы е $\mathrm{P}$. м од у л я $A$ с по м о щ ю $B$, т. е. на точные последовательности вида

\[
0 \longrightarrow A \longrightarrow X_{n-1} \longrightarrow \ldots \longrightarrow X_{0} \rightarrow B \longrightarrow 0
\]

к-рым соответствует группа $\operatorname{Ext}_{R}^{n}(A, B)$. Группы

\[
\operatorname{Ext}_{R}^{n}(A, B), \quad n=1,2, \ldots,
\]

являются производными функторами функтора $\operatorname{Hom}_{R}(A, B)$ и вычисляются с помощью проективной резольвенты модуля $A$ или инъективной резольвенты модуля $B$. Расширение $X$ модуля $A$ наз. с у щ е с тв е н н ы м, если для любого подмодуля $S$ модуля $X$ из $S \cap A=0$ следует $S=0$. Всяпий модуль обладает максимальным существенным Р., являющимся минимальным инъективным модулем, содержащим данный.

Лит. см. при ст. Расширение группы.

B. E. Говоров. РАСІІИРЕНИЕ о п е р а т о р a - линейный оператор, график к-рого содержит график данного линейного оператора. Тот факт, что оператор $B$ есть Р. оператора $A$, записывается в виде $A \subset B$. Обычные задачи теории Р.: максимально расширить оператор, сохраняя определенное свойство, или изучить Р. оператора, обладающие нек-рым дополнительным свойством.

Пусть, напр., дан изометрич. оператор $A$ в гильбертовом пространстве $H$ с областью определения $D(A) \subset H$ и областью значений $R(A) \subset H$; тогда изометрические $\mathrm{P}$. оператора $A$ находятся во взаимно однозначном соответствии с изометрич. отображениями из $H_{+}=D(A)^{\perp}$ в $H_{-}=R(A)^{\perp}$. В частности, $A$ имеет унитарные $\mathrm{P}$, когда размерности $H_{+}$и $H_{-}$совпадают.

Р а сширения симмет рических опеp а т о р о в. Наиболее развитой (и важной для приложений) является теория самосопряженных Р. симметрич. операторов в гильбертовом пространстве. Оператор $T$ является симметрическим тогда и только тогда, когда $T \subset T^{*}$, где $T^{*}$ - сопряженный к $T$ оператор. Поэтому область определения любого симметрического Р. оператора $T$ содержится в $D\left(T^{*}\right)$, и это Р. есть с уж е н и е оператора $T^{*}$. Тем самым описание симметрических Р. сводится к нахождению их областей определения. Подпространство $L \subset D\left(T^{*}\right)$ является областью определения нек-рого симметрического Р. оператора $T$ тогда и только тогда, когда $\left(T^{*} x, y\right)=\left(x T^{*} y\right)$ для любых $x, y \in L$. Оказывается, что

\[
D\left(T^{*}\right)=D(T) \dot{+} N_{+}+N_{-},
\]

где $N_{ \pm}=\operatorname{Ker}\left(T^{*} \mp i\right)$ - д е фе к т ны е по д п р о с тр а н с т в а (их размерности $n_{ \pm}=\operatorname{dim} N_{ \pm}$наз. д ефе к т ны м и ч с л а м и), и симметрические Р. оператора $T$ находятся во взаимно однозначном соответствии с изометрич. отображениями из $N_{+}$в $N_{-}$: каждому такому отображению $V$ соответствует Р. оператора $T$ с областью определения $D(T)+\Gamma_{V}$, где $\Gamma_{V}-$ график оператора $V$. Самосопряженные Р. соответствуют унитарным операторам $V$ и, следовательно, существуют тогда и только тогда, когда дефектные числа равны.

Области определения Р. симметрич. операторов удобно описывать с помощью т. н. (абстрактных) граничных условий. метрич. оператора $T$ наз. всякий линейный функционал на $D\left(T^{*}\right)$, непрерывный относительно нормы $\langle x\rangle=$ $=\left(\|x\|^{2}+\left\|T^{*} x\right\|^{2}\right)^{1 / 2}$ и равный нулю на $D(T) ;$ г р а н и чш ы м у с ло в и е м наз. уравнение $f(x)=0$, где $f-$ граничное значение. Граничные значения определяются своими значениями на $N_{+}+N_{-}$. Если дефектные числа симметрич. оператора $T$ конечны, то всякое его симметрич. расширение $\tilde{T}$ определяется семейством граничных условий, то есть $D(\tilde{T})=\bigcap_{i=1}^{k} \operatorname{Ker} f_{i}$, где $f_{i}-$ граничные значения. Семейства граничных значений, определяющие самосопряженные Р . оператора $T$ с дефектными числами $n_{+}=n_{-}=n$, описываются следующим образом. Пусть $\varphi_{1}, \ldots, \varphi_{n}-$ ортонормированный базис в $N_{+}$, а $\psi_{1}, \ldots, \psi_{n}-$ в $N_{-}$и пусть для $1 \leqslant i \leqslant n$

\[
\begin{aligned}
& f_{i}(x)=\left(T^{*} x, \varphi_{i}\right)-\left(x, T^{*} \varphi_{i}\right), \\
& g_{i}(x)=\left(T^{*} x, \psi_{i}\right)-\left(x, T^{*} \psi_{i}\right) .
\end{aligned}
\]

Тогда всякое самосопряженное расширение $\tilde{T}$ оператора $T$ определяется граничными условиями

\[
D(\tilde{T})=\bigcap_{i=1}^{k} \operatorname{Ker}\left(f_{i}-\sum_{j=1}^{n} \theta_{i j} g_{j}\right),
\]

где $\left\|\theta_{i j}\right\|-$ унитарная $(n \times n)$-матрица.

В нек-рых случаях удается установить существование самосогряженных Р. (и найти нек-рые из них), не решая трудной задачи нахождения дефектных подпрост-


\end{document}