\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }
\usepackage{bbold}

\begin{document}
мутативно. Понятие П. семейства объектов двойственно понятию копроизедения семейства объектов.

IІроизведением пустого семейства объектов является правый нуль (т е р м и н а л ь н й о б ь е к т) категории. В большинстве категорий структуризованных множеств (категории множеств, груіп, толологич. пространств и т. д.) понятие П. семейства объектов совпадает с понятием декартова (прямого) П. этих объектов. Тем не менее такое совпадение не является обязательным: в категории периодических абелевых групп П. семейства групп $G_{i}$, $i \in I$, есть периодич. часть декартова П. этих групп, к-рая в общем случае отличается от самого декартова П.

В категориях с нулевыми морфизмами для любого произведения $P=\prod_{i \in I}^{\times} A_{i}\left(\pi_{i}\right)$ существуют такие однозначно определенные морфизмы $\sigma_{i}: A_{i} \rightarrow P, \quad i \in I$, что $\sigma_{i} \pi_{i}=1_{A_{i}}, \sigma_{i} \pi_{j}=0$ при $i \neq j$. Если $I$ конечно, то в абелевой категории $\pi_{1} \sigma_{1}+\ldots+\pi_{n} \sigma_{n}=1$ и П. семейства объектов $A_{1}, \ldots, A_{n}$ совпадает с их копроизведением.

\includegraphics[max width=\textwidth, center]{2023_07_08_80b236ac3ffe0c2c79c1g-1}
IІРОИЗВОДНАЯ - одно из основных понятий математич. анализа. Пусть действительная функция $f(x)$ действительного переменного $x$ определена в нек-рой окрестности точки $x_{0}$ п существует конечный или бесконечный предел

\[
\lim _{x \rightarrow x_{0}} \frac{f(x)-f\left(x_{0}\right)}{x-x_{0}}
\]

Этот предел п наз. п р о и з в д н о й от фу ун кц и и $f(x)$ в точке $x_{0}$. Если положить $y=f(x)$,

\[
x-x_{0}=\Delta x, f(x)-f\left(x_{0}\right)=f\left(x_{0}+\Delta x\right)-f\left(x_{0}\right)=\Delta y,
\]

то предел (*) запишется так:

\[
\lim _{\Delta x \rightarrow 0} \frac{\Delta y}{\Delta x}
\]

Используют также обозначения $f^{\prime}\left(x_{0}\right), \frac{d f\left(x_{0}\right)}{d x}, y^{\prime}$, $\frac{d y}{d x}, \frac{d}{d x} f\left(x_{0}\right)$ и нек-рыө другие.

Операцию вычисления П. наз. ди фф ф е р е н ц ир о в а н и м. Если производная $f^{\prime}\left(x_{0}\right)$ конечна, то функцию $f(x)$ наз. д и фф е р е вц и р у е м о й в т $о$ ч к е $x_{0}$. Функцию, дифференцируемую в каждой точке нек-рого множества, наз. дифферөнцируемой на этом множестве. Дифференцируемая функция всегда непрерывна. Однако существуют непрерывные функции, ғе имеющие П. во всех точках заданвого промежутка (см. Недифференцируемая функция).

Пуеть функция дифференцируема в нек-ром промөжутке. Ее производная $f^{\prime}(x)$ может оказаться при этом разрывной функцией. Однако по классификации Бәра (см. Бэра классы) она всегда является функцией 1-го класса и обладает свойством Дарбу: приняв два значения, принимает и все промежуточные.

Обобщением понятия П. является понятие П. по множеству. Пусть действительная функция $f(x)$ определена на нек-ром множестве $E$ действительных чисел, $x_{0}-$ предельная точка этого множества, $x_{0} \in E$, и существует конечный или бесконечный предел

\[
\lim _{x \rightarrow x_{0}, x \in E} \frac{f(x)-f\left(x_{0}\right)}{x-x_{0}},
\]

к-рый и наз. п р о и з в о дн о й о т фу унци и $f(x)$ п о м н о же с т в у $E$ в точке $x_{0}$ и обозначают символом $f_{E}^{\prime}\left(x_{0}\right)$. П. функции по множеству есть обобщение понятия П. Разновидностями этого обобщения являются понятия односторонней производной, производного числа, аппроксимативной произвоной. Данное определение П. (и өго обобщение), а также простейшие ее свойства почти без изменений распространяются на комплексные функции в вектор-функции действительного или комплексного переменного. К роме того, существуют понятия П. скалярной функции точки евклидова пространства $\mathbb{R} n$ (см. Граддент), П. функции множества по мере (в частности, по площади, по объему и т. п.), понятие П. распространяют на вектор-функции точки абстрактного пространства (см. Дифференцирование отображения).

О геометрич. и механич. истолковании П., о простейших правилах дшфференцирования, о П. высших порядков, о частных П., а также лит. см. в ст. Дифференциальное исчисление.

IРОИЗВОДНОЕ МНОЖЕСТВО - совокупность $M^{\prime}$ всех предельных точек множества $M$ в топологич. пространстве. Множество $M$, совпадающее со своим П. м., наз. с о в е р шा е в н Ы м. М. И. Войцеховский.

ПРОИЗВОДНОЕ ПРАВИЛО в Ы в о д а д л я д а нн о г о и с ч и с л е н и я - вывода правило, заключение к-рого выводимо из его посылок в рассматриваемом исчислении. Напр., в высказываний исчислении правило вывода

\[
\frac{A \supset B, B \supset C}{A \supset C}
\]

является П. П., поскольку в этом исчислении имеет место выводимость из посылок:

\[
A \supset B, B \supset C \vdash A \supset C .
\]

Всякое П. П. является допустимым правилом, но не всякое допустимое правило является П. П. Напр., подстановки правило в исчислении высказываний является допустимым правилом, но не производным.

Лит.: [1] К лини $\begin{array}{ll}\text { пер. с англ., М., 1957. } & \text { С. Н. Артемов. }\end{array}$ ПРОИЗВОДНОЕ ЧИСЛО, п р о и з в о д н о е ч и сл о Д и и, - понятие теории функций действительного переменного. В е р х п и м п р а в ы м П. ч. $\Lambda_{\alpha}$ наз. верхний предел отношения $\frac{f\left(x_{1}\right)-f(x)}{x_{1}-x}$ при $x_{1} \rightarrow x$, где $x_{1}>x$. Аналогично определяют ни жне е п р а во е $\lambda_{\alpha}$, ве р хне е $\Lambda_{g}$ и ни жн е е $\lambda_{g}$ л е в ы е П. ч. Если $\Lambda_{\alpha}=\lambda_{\alpha}\left(\Lambda_{g}=\lambda_{g}\right)$, то $f(x)$ имеет в точке $x$ одностороннюю правую (левую) производную. Обыкновенная производная существует, если все четыре П. ч. конечны и совпадают. П. ч. были введены У. Дини [1]. Как показалІ Н. Н. Лузин, если все четыре П. ч. конеqны на нек-ром множестве, то функция имеет обычную производную всюду на этом множестве, кроме точек множества меры нуль.

Jum.: [1] D i n i U., Fondamenti per la teorica delle funzioni di variabili reali, pisa, 1878, [2] С а к с С., Теория интеграла,
пер. с англ., М., 1949. пер. с англ., М., 1949 ВВТОМОРФИЗМ в э р г о д и ч ес к о й т е о р и и - преобразование $T_{X}$, определяющееся по автоморфизму $T$ пространства $c$ мерой $(M, \mu)$ и измеримому подмножеству положительной меры $X \subset M$, почти все точки к-рого под действием итераций $T$ снова пошадают в $X$. Для каждой такой точки $x$ определяют $T_{X}(x)$ как ту точку траектории $T^{n} x$, в к-рой эта траектория впервые после $x$ возвращается в $X$ (согласно Пуанкаре теореме о возвращении, условие, чтобы почти все точки из $X$ со временем снова возвращались в $X$, автоматически выполняется, если $\mu(M)<\infty)$. Преобразование $T_{X}$ оказывается автоморфизмом (точнее, автоморфизмом по $\bmod 0$ ) пространства $X$ с индуцированной на нем мерой (последняя есть мера $\mu$, рассматриваемая только на подмножествах $X ;$ если $\mu(X)<\infty$, то обыкновенно эту меру еще нормируют).

Обратно, если $U_{n \geqslant 0} T^{n} X=M$ (это условие автоматически выполняется, если автоморфизм $T$ әргодичен),


\end{document}