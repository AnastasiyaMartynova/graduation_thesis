\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}

\begin{document}
сопряженности д. о. $L_{0}^{\prime}$ в $L_{2}(\mathbb{R} n)$. Формально самосопряженный д. o. $l(x, D)$ имеет вид

\[
-\sum_{k, i=1}^{n} D_{k} a_{k j}(x) D_{j}+q(x), \quad x \in \mathbb{R}^{n},
\]

с действительными коэффициентами, и $L_{0}^{\prime}$ ограничен снизу; имеет вид (5), является әллиптическим, $a_{k j}-$ постоянные, $q(x) \geqslant-Q(|x|), Q(r)$ монотонно не уо́ывает и интеграл

\[
\int^{\infty} Q^{-1 ; 2}(r) d r=\infty
\]

имеет постоянные и действительные коэффициенты; имеет ограниченные козффициенты, а главный членэллиптич. типа с деӥствительными и постоянными коэффициентами.

Пусть д. о. $L_{0}$ имеет конечные индексы дефекта $n_{ \pm}=$ $=\operatorname{dim} H_{ \pm}$, что является характерным для обыкновенных д. о. В этом случае числа $n_{ \pm}$совпадают с размерностями подпространств решений уравнений $l(u)= \pm i u$ из $L_{2}(a, b)$. Поэтому $n_{ \pm}<m$ и вычисление индексов дефекта д. о. связано с качественной теорией и асимптотич. методами обыкновенных дифференциалыных уравнений.

IІусть $n=1, x \in(a, b)$. Если $n_{+} \neq n_{-}$, то д. о. $L_{0}^{\prime}$ не имеет ни одного самосопряжкенного расширения. Если $n_{+}=n_{-}=k$, то для самосопряженности расширений д. о. L'́ надо задавать $k$ граничных условий и онІ полностью оюисаны. Граничныс условия принимают простої вид, когда выражение $L_{0}^{\prime}$ имеет два регулярных конца или имеет один регулярный конец, но $m=$ $=2 k, n_{ \pm}=k$. Конец $a$ паз. рег у ля р н ым, если $a>-\infty$ и $\frac{1}{a_{m}(x)}, a_{j}(x), 0 \leqslant j \leqslant m-1$ суммируемы на $[a, \beta]$ при любом $\beta<b$.

Имеются примеры д. о. с частными производными в $L_{2}\left(\mathbb{R}^{n}\right), \quad n \geqslant 3$, с разрывными коәфффициентами и с конечными индексами дефекта, но их теория еще слабо развита. В терминах граничных условий ошисаны не все 'самосопряженные расиирения симметрич. Д. о. с частными шроизводными в ограниченноі́ области, но оиисаны разиообразные расширения с заданными свойствами.

ІІусть $l$ - формально самосопряженный әллиптический д. о. четного порядка $m=2 k$ с действительными коэффициентами, $C_{k}^{\infty}\left(\Omega_{n}\right)-$ множество всех функций, имеющих ироизводные любого шорядка в ограниченной замкнутой области $\bar{\Omega}_{n}$ іл удовлетворяющих краевым условиям типа Цирихле $D^{\alpha} u=0, x \in \Gamma,|\alpha| \leqslant k-1$. Тогда д: о., определенный выражением $l$ с областью определения $C_{k}^{\infty}\left(\Omega_{n}\right)$, является симметрическим, а его замыкание $L_{m}$ - самосопрязкенным. Имеются другие примеры конкретных самосопряженных краевых условий для д. о., из них наиболее полно изучены Д. о. 2-го иорядка с краевыми условиями типа Дирихле, Неїмана и третьего рода.

Спектральны ї анализ с амосопряж е н ного д. о. Всякиї самосоиряженный д. о. $L$ допускает спектральие разложение вида

\[
L=\int_{-\infty}^{+\infty} \lambda d E_{\lambda},
\]

где $E_{\lambda}$ - разложение единицы (ортогональное семейство проекторов). Однако общая формула не дает непосредственного разложения по собственным функциям конкретных самосопряженных д. о., и поэтому важно уметь семейство $E_{\lambda}$ выразить с помощью собственных функций. Если самосопряженныї л. о. $L$ пмеет дискретный спектр $\left\{\lambda_{k}\right\}$ с соответствующими ортонорми- рованными собственными функциями $\left\{\varphi_{k}(x)\right\}$, то $E_{\lambda}-$ интегральный оператор с (спектральным) ядром

\[
E(x, y, \lambda)=\sum_{\lambda_{k} \in(0, \lambda]} \varphi_{n}(x) \varphi_{n}(y)
\]

При наличии непрерывного спектра у д. о. вопрос становится сложным: для непрерывного спектра нет собственных функцийі из $L_{2}\left(\Omega_{n}\right)$. Однако имеют место следующие результаты.

Пусть $L$ - самосопряженный обыкновенный д. 0. вида (1) в $L_{2}(-\infty, \infty), \varphi_{1}(x, \lambda), \ldots, \varphi_{n}(x, \lambda)-$ фундаментальная система решениї уравнения $l u=\lambda u$. Тогда существует монотонная матричная функция $\sigma(\lambda)=\left\|\sigma_{i j}(\lambda)\right\|_{i, j=1}^{m}$ (спектральная мера) такая, что разложение единицы $E_{\lambda}$ д. о. $L$ задается ядром

\[
E(x, y, \lambda)=\int_{0}^{\lambda} \sum_{i, j=1}^{m} \varphi_{i}(x, \lambda) \varphi_{j}(y, \lambda) d \sigma_{i j}(\lambda) .
\]

Далее, для любой функции $f(x)$ из $L_{2}(-\infty, \infty)$ интеграл

\[
\left\{F_{j}(\lambda)\right\}=\left\{\int_{-\infty}^{+\infty} \varphi_{j}(x, \lambda) f(x) d x\right\}
\]

сходится в пространстве вектор-функций $L_{2}(-\infty, \infty$; $d \sigma(\lambda))$, порожденном мерой $\sigma(\lambda)$, и обратно, интеграл

\[
\int_{-\infty}^{+\infty} \sum_{i, i=1}^{m} F_{i}(\lambda) \varphi_{j}(x, \lambda) d \sigma_{i j}(\lambda)
\]

сходітся к $f(x)$ в $L_{2}(-\infty, \infty)$. Если выражение (1) имеет один регулярныї конец $a$ и $m=2 k$, а индексы дефекта $n_{ \pm}=k$, то функции $\varphi_{1}(x, \lambda), \ldots, \varphi_{k}(x, \lambda)$ выбираются так, чтобы они образовали фундаментальную систему в классе решений уравнения $i u=\lambda u$, удовлетворяющих краевым условиям в $a$, и в этом случае порядок спектральной меры будет равен $k$.

Пусть $L$ - самосопряженный эллиптич. д. о. в $L_{2}\left(\Omega_{n}\right)$. Тогда его разложение единицы $E$ - интегральный оператор с ядром $E(x, y, \lambda)$ и существует неубывающцая функция $\rho(\lambda)$ такая, что для любых чисел $\lambda_{1}$ и $\lambda_{2}$ имеет место

\[
E\left(x, y, \lambda_{1}\right)-E\left(x, y, \lambda_{2}\right)=\int_{\lambda_{1}}^{\lambda_{2}} \varphi(x, y, \lambda) d \rho(\lambda),
\]

при этом при каждом $\lambda$ существует конечная или бесконечная система $\left\{\varphi_{j}(x, \lambda)\right\}$ решений уравнения $l u=$ $=\lambda u \quad$ и

\[
\varphi(x, y, \lambda)=\sum_{j} \varphi_{j}(x, \lambda) \varphi_{j}(y, \lambda) .
\]

Для оператора Шрёдингера $L u \equiv-\Delta u+q(x) u, x \in \mathbb{R}^{3}$, при условии $|q(x)| \leqslant c(1+|x|)^{-2-\varepsilon}$ ядро $E(x, y, \lambda)$ явно выражается через решения задачи теории рассеяния.

Для произвольных самосопряженных д. о. с частными производными также справедливы формулы (10), $(11)$, в этом случае $\left\{\varphi_{j}(x, \lambda)\right\}$ могут быть обобщенными функциями, но конечного порядка.

Характер сходимости разложкения по собственным функциям д. о. и асимитотич. свойства спектрального ядра помогают в обосновании метода Фурье при решении уравнений математич. физики. Для обыкновенных д. о. имеет место следующий окончательный результат - те о ре ма ра вносходи мости: разложения заданной суммируемой функции по собственным функциям д. о., ограниченного снизу, и интеграл Фурье сходятся или расходятся в точкс одновременно. Для д. о. с частными Іроизводными вогрос становится сложным.

К а ч е с т в ен ная те о рия с пект р а д. о. занимается изучением природы спектра в зависимости от поведения коәффициентов, геометрии области и гранитных услівиї̈.

Имеется серия признаков дискретности спектра д. о. Наиболее общим является следующий критерий и его обобщения: если $q(x) \geqslant 1$, то для дискретности


\end{document}