\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
высказывание обретает точную формулировку в следующей т е о р е м е Р о л ь ф с е н а [12]: два изотопных зацепления объемлемо изотопны, если их соответствующие компоненты эквивалентны (как ориентированные узлы). В силу этого изучение изотопии зацеплений редуцируется к объемлемой изотопии.

Другим отношением әквивалентности, изучаемым в У. т., является ко н ко р д а н т нос т ь, или к об о р д и з м. Локально плоские вложения $i_{0}$ и $i_{1}$ многообразия $X$ в многообразие $Y$ наз. к о н к о р д а н тн ы м и, если существует такое локально плоское вложение

\[
i: X \times[0,1] \rightarrow Y \times[0,1]
\]

что $i(x, 0)=\left(i_{0}(x), 0\right), \quad i(x, 1)=\left(i_{1}(x), 1\right)$ для $\quad$ всех $x \in X$. Если $X$ есть несвязная сумма нескольких әкземпляров окружности, а $Y$ есть $\mathbb{R}^{3}$ или $S^{3}$, то получается определение конкордантности зацеплений. Связная сумма вносит во множество классов конкордантных узлов структуру абелевой групшы. Нуль этой групшы есть класс, содержащий тривиальный узел. Узлы, конкордантные тривиальному, наз. с р е 3 а н н ы м и. Противоположным к классу узла $k$ является класс узла, получаемого так: нужно сменить ориентацию узла $k$ и взять его образ при отражении от любой плоскости. О строении группы классов конкордантных узлов см. Узлов кобордизм. Для того чтобы построить срезанный узел, проще всего взять тривиальное двухкомпонентное зацепление, приклеить к любой из компонент ленточку, как-либо заузлить ее и пропустить через исходное зацепление, а затем приклеить ко второй компоненте. После этого нужно удалить все внутренние точки ленты, а также ее граничные точки, по к-рым производилось приклеивание. $\mathrm{B}$ результате получится срезанный узел (см., напр., рис. 2). Обобщение этого построения
\includegraphics[max width=\textwidth, center]{2023_07_09_f2ddd4443782e9ebd505g-1(2)}

Pис. 2. приводит к понятию л е нт о ч но го 3 а ц е п л ен и я [1]. Имеется гипотеза, что всякий срезанный узел является ленточным. Из изотопности зацеплений не следует их конкордантность (действительно, всякий узел изотопен тривиальному, но не всякий является срезанным). Однако изотопные зацепления конкордантны, если все их соответствующие компоненты попарно конкордантны [12].

Изучаются и другие отнотения әквивалентности между зацеплениями, напр. гомотопия и $I$-эквивалентность (см. [8]).

Aппарат У. т. Фундаментальная әруппа $G(L)$ дополнительного пространства $M(L)$ наз. г р у П п о й з а ц е пл е н и я $L$. Это- важнейший алгебраич. инвариант зацепления. В случае узлов и нераспадающихся зацеплений внешность $M(L)$ асферична и потому ее гомотопический тип определяется группой $G(L)$. Зацепление тривиально тогда и только тогда, когда его группа свободна [8]. Торич. узлы әквивалентны тогда и только тогда, когда они имеют изоморфные группы. Однако утверждение о том, что группа определяет тип зацепления, неверно даже для узлов [1]. Имеется несколько алгоритмов выписывания копредставления группы зацепления по его диаграмме. Наиболее широко известно к о п р е д с т а в л ен и е В и р т и нге р а. О свойствах групп зацеплений см.

\begin{center}
\includegraphics[max width=\textwidth]{2023_07_09_f2ddd4443782e9ebd505g-1(1)}
\end{center}

Более сильным инвариантом зацеплений является гр уппов а я с и с те м а $\left\langle G(L) ; T_{1}, \ldots, T_{\mu}\right\rangle$, к-рая состоит из грушпы зацепления $G(L)$ и классов сопряженности ее подгрупп $T_{i}$, порожденных классами меридиана и параллели $i$-й компоненты. Групповая система узла определяет топологич. тип его дополнения. Полным алгебраич. инвариантом узла является тройка $(G, T, m)$, состоящая из группы узла $G$, периферич. подгруппы $T \subset G$ и меридиана $m \in T:$ два узла әквивалентны тогда и только тогда, когда имеется изоморфизм между их группами, сохраняющий периферич. подгруппы и переводящий меридиан одного узла в меридиан другого или в его обратный. Известны и классифицирующие инварианты узлов, представляющие собой группы. Один такой инвариант (см. [11]) сопоставляет узлу $k$ свободное произведение групп нек-рых обмоток связной суммы узла $k$ и узла Листинга 4.. В әтом определении вместо узла 4I можно взять и нек-рые другие узлы, и при этом получающаяся группа, хотя и изменится, но будет по-прежнему полным инвариантом.

Найден (см. [5]) более естественный полный алгебраич. инвариант узла. Пусть узел $k$ задан своей регулярной проекцией на плоскость, и все переходы занумерованы числами от 1 до $n$. Пусть, далее, в нек-рой двойной точке сходятся переходы с номерами $p, q, r$, расположенные, как на рис. 3, и $\Gamma(k)$ - дистрибутивный группоид, заданный образующими $a_{1}, \ldots, a_{n}$ (их число равно числу переходов), связанными соотношениями $a_{p} \circ a_{q}=a_{r}$ (по одному для каждой двойной точки). Доказано (см. [5]), что, во-первых, группоид $\Gamma(k)$ является инвариантом узла,

\begin{center}
\includegraphics[max width=\textwidth]{2023_07_09_f2ddd4443782e9ebd505g-1}
\end{center}

Pис. 3. т. е. не зависит от выбора проекции, и, во-вторых, узлы с изоморфными группоидами әквивалентны.

Группа узла, групповая система, группоид - достаточно сложные алгебраич. объекты, и их различение qасто оказывается непростым делом. В вычисления удобны а б е л е вы и н в а р и а т т ы узлов и зацеплений, к-рые устроены проще (напр., они описываются средствами коммутативной алгебры) и в то же время достаточно информативны. Важнейший из абелевых инвариантов - м о д у л ь А л е к с а п д е р а, определяется так. Гомологий групnа внешности $H_{1}(M(L))$ зацепления кратности $\mu$ есть свободная абелева группа ранга $\mu$ (это - следствие Александера двойственности), и потому накрытие $p: \tilde{M}(L) \rightarrow M(L)$, отвечающее коммутанту группы $G(L)$, имеет $\mathbb{Z}^{\mu}$ в качестве группы накрывающих преобразований. Если $x_{0} \in M(L)-$ базисная точка, а $\tilde{X}_{0}=p^{-1}\left(x_{0}\right)$, то группа $H_{1}(\tilde{M}(L)$, $\tilde{X}_{0}$ ) имеет естественную структуру модуля над $\Lambda_{\mu}=$ $=\mathbb{Z}\left[\mathbb{Z}^{\mu}\right]$ (кольцом целочисленных лорановских полиномов от $\mu$ переменных), к-рый и наз. м о д у л е м А л е к с а н д е р а зацепления $L$. В случае $\mu=1$ модуль $H_{1}\left(\tilde{M}(L), \quad \tilde{X}_{0}\right)$ изоморфен прямой сумме $H_{1}(\tilde{M}(L))$ и $\Lambda_{1}$; поэтому при изучении узлов модулем Александера принято называть $\Lambda_{1}$-модуль $H_{1}(\tilde{M}(L))$. Матрица, задающая копредставление модуля Александера, наз. м а т р и цей А л е к с а н д е р а. Для нахождения матрицы Александера по копредставлению группы $G(L)$ используется с в о б о д н о е д и ф фе ренциально е ис числ ение Фок с а [1]. Модуль Александера стандартным образом (см. Александера инварианты) задает возрастающую цепочку идеалов кольца $\Lambda_{\mu}$ (к-рые наз. ә л е м е н т а р в ы м и и д е а л а м и) и последовательность целочисленных многочленов $\Delta_{1}\left(t_{1}, \ldots, t_{\mu}\right), \Delta_{2}\left(t_{1}, \ldots, t_{\mu}\right), \ldots$, определенных с точностью до единиц кольца $\Lambda_{\mu}$. Первый многочлен $\Delta_{1}\left(t_{1}, \ldots, t_{\mu}\right)$ наз. м но г о ч л е н о м А ле кс а н д р а. Модуль Александера определяет также к л а с с йд е а ло в III т е й ни ц а С м и т a [8]. С его помощью удается, напр., доказать необратимость нек-рых узлов (узел $k \subset S^{3}$ наз. о б т и м м, если существует сохраняющий ориентацию гомеоморфизм $S^{3}$ на себя, переводящий $k$ в $k$ с обраще-


\end{document}