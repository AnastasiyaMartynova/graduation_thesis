\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
Множество $S$ векторных полей $F$ класса $C^{1}$ на замкнутом дифференцируемом многообразии $V^{n}$ наделяется $C^{1}$-топологией. Векторное поле $F \in S$ индуцирует динамич. систему $f^{t}$ (действие (класса $C^{1}$ ) группы $R$ ) на $V^{n}$. Для всякого $k \in\{1, \ldots, n\}$ показатель Ляпунова $\lambda_{n-k+1}(F, x)$, по ошределению, равен правой тасти равенства (2).

Имеют место утверждения:

а) при всяком $k \in\{1, \ldots, n\}$ функция $\lambda_{k}(\cdot): S \times V^{n} \longrightarrow \mathbb{R}$ принадлежит второму классу Бәра [4];

б) для всякого $F \in S$, для всякого инвариантного относительно динамич. системы $f t$, индуцированной векторным полем $F$, распределения вероятностей на $V^{n}$ ( $\sigma$-алгебра к-рого содержит все борелевские множества) почти всякая точка $x$ такова, что У. в в. уравнения $\dot{x}=F(x)$ вдоль траектории $\left\{f^{t} x\right\}$ есть правильная линейная система дифференциальных уравнений (см. [5], [6]);

в) для всякого $m \in \mathbb{N}$ пусть $S^{(m)}$ обозначает множество векторных полей класса $C^{m}$ на $V^{n}$, наделенное

\includegraphics[max width=\textwidth, center]{2023_07_09_ace0432c8b29f9c6874fg-1(1)}
на $V^{n}, \sigma$-алгебра к-рого содержит все борелевские множества, и пусть $S_{P}^{(m)}$ обозначает подпространство пространства $S^{(m)}$, состоящее из тех векторных полей, для к-рых распределение $P$ инвариантно относительно индуцированных Імяи динамич. систем; тогда (см. [7]):

\begin{enumerate}
  \item для всяких $m \in \mathbb{N}, k \in\{1, \ldots, n\}$ функция
\end{enumerate}

\[
\sum_{i=1}^{k} \int_{V^{n}} \lambda_{i}(\cdot, x) d P(x): S_{P}^{(m)} \rightarrow \mathbb{R}
\]

(фазовое среднее суммы старших показателей Ляпунова У. в в.) полунепрерывна сверху,

\begin{enumerate}
  \setcounter{enumi}{1}
  \item для всякого $m \in \mathbb{N}$ в пространстве $S_{P}^{(m)}$ имеется всюду плотное множество типа $G_{\delta}$, во всякой точке к-рого функция
\end{enumerate}

\[
\int_{V^{n}} \lambda_{k}(\cdot, x) d P(x) \cdot S_{P}^{(m)} \rightarrow \mathbb{R}
\]

нешрерывна (для всякого $k \in\{1, \ldots, n\}$ ), т. е. в пространствах $S_{P}^{(m)}$ типична непрерывность фазовых средних показателей Ляпунова $\mathbf{y}$. в в.

Лum.: [1] Л я п у н о в А. М., Собр. соч., т. 2, М.- Ј., $1956 ;$ [2] У и т т е к е p E. T., Аналитическая динамика, пер. с англ., М.- Л , 1937; [3] П о н т р я г и ш Ј. С., Обыкновенные дифференциальные уравнения, 4 изд., М., 1974, [4] М и лли о н Ш и к о в В. М., "Диффференц. уравнения", 1983 , т 19 , $\mathfrak{N}_{2} 2$, с. $215-20$, [5] о с е л е д е I В И., «Тр. Моск. матем об-ва", "Матем. сб.", 1968, т 77 , с. $163-73$; [7] е г о ж е, «Дифференц. "уравнения", 1978, т. 14, 수 4 , c. $759-60$.

\begin{center}
\includegraphics[max width=\textwidth]{2023_07_09_ace0432c8b29f9c6874fg-1(3)}
\end{center}

\[
D^{*} x(t) \subset F(t, x(t)),
\]

где $D^{*} x(t)-$ к о н т и н г е в ц и я вектор-функции $x(t)$, т. е. множество всех частичных пределов отношения

\[
\left[x\left(t^{\prime}\right)-x(t)\right] /\left(t^{\prime}-t\right)
\]

при $t^{\prime} \rightarrow t$, а $F(t, x)$ - заданное непустое множество в $\mathbb{R}^{n}$, зависящее от $t$ и $x$ (см. [1], [2]). В случае когда множество $F(t, x)$ ограничено, замкнуто, выпукло и является полунепрерывной сверху относительно включения функцией точки $(t, x)$, У. в к. равносильно у р а в-

\includegraphics[max width=\textwidth, center]{2023_07_09_ace0432c8b29f9c6874fg-1(2)}
аналогично У. в к., но с использованием отношения $\left[x\left(t^{\prime}\right)-x\left(t^{\prime \prime}\right)\right] /\left(t^{\prime}-t^{\prime \prime}\right) \quad$ при $\quad t^{\prime} \longrightarrow t, \quad t^{\prime \prime} \longrightarrow t$

см. [3]) и диядеренциальпому включению

\[
d x / d t \in F(t, x)
\]

(см. [4]). Свойства У. в к. подобны свойствам дифференциальных включений.

JIum. [1] M a r c h a u d A., «Bull. Soc. math France», 1934, t. $62, \mathrm{p} .1-38$; [2] Б а р б а ш и н Е. А., А ли м о в Ю И., «Изв. высших учебн. заведений. Математика», 1962, № 1, с. $3-13 ;$ [3] Z a re m b a S. C., "Bull. Sci. math.», 1936, ser."2, t. 60, № 5, p. $139-60 ;[4]$ W a ż e w s k i T., «BuII. Acad. Polon. sci.,
Ser. math... 1961, v. 9 , № 12, p. $865-67$. A. $\Phi$ Duлunnos.

УРАВНОВЕIIEHНOE MНOभКEТВО-множество $U$ действительного или комілексного векторного пространства $X$ такое, что из $x \in U$ и $|\lambda| \leqslant 1$ следует $\lambda x \in U$. Примером $У$. м. может служить единичный шар нормированного векторного пространства и вообще окрестность нуля $U$ базы окрестностей нуля топологич. векторного пространства. Эти окрестности нуля являются, кроме того, поглощающими множествами, т. е. такими, что для любого $x \in X$ существует $\alpha>0$ такое, что $x \in \lambda U$ при $|\lambda| \geqslant \alpha$. Если $U-$ выпуклое поглощающее $У$. м., то функционал $p_{U}(x)=\inf _{x \in \lambda U}|\lambda|$ является полунормой, т. е. обладает свойствами

\[
p_{U}(x+y)=p_{U}(x)+p_{U}(y), \quad p_{U}(\lambda x)=|\lambda| p_{U}(x)
\]

У. м. наз. такґе ц е н т р и р о в а н н ы м и.

\begin{center}
\includegraphics[max width=\textwidth]{2023_07_09_ace0432c8b29f9c6874fg-1}
\end{center}

УРНОВАЯ СХЕМА - одна из простейших моделей теории вероятностей. Описание У. с. таково: рассматривается некиї сосуд - урна - c шарами белого и черного цвета. Из урны наугад извлекается один шар, а затем он возвращается в урну вместе с $c$ парами того же цвета, что и вынутый шар, и $d$ шарами другого цвета. После перемешивания шаров в урне процедура повторяется любое нужное число раз. Предполагается, что первоначально урна содержала $a>0$ и $b>0$ белых и черных паров соответственно. Числа $c$ и $d$ - параметры У. с.- могут быть и отрицательными.

У. с. дает удобную возможность вычисления нек-рых основных вероятностей через условные вероятности. При различных значениях параметров $c$ и $d$ получаются многие известные схемы теории вероятностей: при $c=0$, $d=0$ - схема случайного выбора с возвращением (см. Бернулли испьтания), при $c=-1, d=0$ - схема случайного выбора без возвращения, при $c=-1, d=-1-$ модель диффузии. Эренфестов, при $c>0, d=0-$ урновая схема Пойа и т. д. Эти частные случаи служат моделями многих реальных явлений или методов их исследования. Так, напр., У. с. Поӥа пспользуется пля описания әпидемиї, при к-рых осуществление к.-л. событий увеличивает вероятность их последующего появления. В рамках У. с. могут быть ввсдены многие распределения теории вероятностей, такие, как биномиальное, гипергеометрическое, геометрическое, Поӥа. Для описания предельных случайных шроцессов, возникающих в У. с., применяются отрицательное бипомиальное распределение и распределение Пуассона.

Лum [1] Ф е л л е р В, Введение в теорию вероятностей и ее приложения, пгер. с англ, 2 изд., т. $1-2$, М., 1967.

A. В Iрохоров. УРОВНЯ ЛИНИЯ ф у кци и Г р і н - множество точек

\[
L_{\lambda}=\left\{z \in D: G\left(z, z_{0}\right)=\lambda=\text { const }\right\}, 0 \leqslant \lambda<\infty,
\]

где $G\left(z, z_{0}\right)$ - функция Грина области $D$ комплексної плоскости с полюсом в точке $z_{0} \in D$. Если область $D$ односвязна, то структура этого множества легко выясняется при конформном отображении $D$ на круг $|\zeta|<1$, переводящем точку $z_{0}$ в $\zeta=0$. Функция Грина инвариантна при әтом отобраякении, а У. л. функции Грина круга $|\zeta|<1$ с полюсом в $\zeta=0$, т. е. $\log \frac{1}{|\zeta|}$, служат окружности $|\zeta|=$ const. Таким образом, в случае односвязной области У. л. $G\left(z, z_{0}\right)=\lambda$ являются простые замкнутые кривые, совпадающие при $\lambda=0$ с границей области $D$ и стремящиеся при $\lambda \rightarrow+\infty$ к точке $z_{0}$. Если область $D m$-связна и граница ее состоит из жордановых кривых $C_{v}$, $v=1, \ldots, m$, то: если $\lambda>0$ достаточно велико, то У. Ј. является жордановой кривой; при $\lambda \rightarrow+\infty$ соответствуюгцая У. Ј. стремится к точке $z_{0}$, а при убывании $\lambda$ эта кривая удаляется от $z_{0}$; если $m>1$, то при нек-ром


\end{document}