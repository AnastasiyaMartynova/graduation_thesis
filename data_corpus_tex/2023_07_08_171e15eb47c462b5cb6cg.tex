\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}

\begin{document}
скает аналитич. продолжение в сектор $|\arg \lambda|<\pi / 2+\varphi$ и в любом секторе $|\arg \lambda| \leqslant \pi / 2+\psi, \quad \psi<\varphi$, допускает оценку

\[
\left\|S_{n}(\lambda, A)\right\| \leqslant|\lambda|^{\alpha-n-1} M(\psi)
\]

Обратно, пусть резольвента $S_{n}(\lambda, B)$ оператора $B$ определена в секторе $|\arg \lambda|<\pi / 2+\psi$ и справедливо неравенство

\[
\left\|S_{n}(\lambda, B)\right\| \leqslant \lambda^{\alpha-n-1} M .
\]

Тогда существует аналитическая в секторе $|\arg z|<\psi$ полугруппа $T(z)$ роста $\alpha$, для производящего оператора $A$ к-рой $S_{n}(\lambda, A)=S_{n}(\lambda, B)$.

Полугруппа-распределение. В соответствии с общей концепцией теории распределений (обобценных бункций) можно отказатъся от того, чтобы оператор-функция $T(t)$ была определена при каждом $t>0$, а потребовать лишь вычислимость интегралов $\int_{-\infty}^{+\infty} T(t) \varphi(t) d t$ для всех $\varphi$ из пространства $D(\mathbb{R})$ всех финитных бесконечно дифференцируемых функций. Тогда возникает определение: по лугруп пой - р а сп р е деление м в банаховом пространстве $X$ наз. линейное непрерывное отображение $T(\varphi)$ пространства $D(\mathbb{R})$ в пространство $L(X)$ всех линейных ограниченных операторов в $X$, обладающее свойствами a) $T(\varphi)=0$, если $\operatorname{supp} \varphi \subset$ $\subset(-\infty, 0) ;$ б) для $\varphi, \psi$ ив пространства $D+(\mathbb{R})$, состоящего из всех функций из $D(\mathbb{R})$ с носителями в $(0, \infty)$, $T(\varphi * \psi)=T(\varphi) T(\psi)$, где свертка

\[
\varphi * \psi=\int_{-\infty}^{\infty} \varphi(t-s) \psi(s) d s
\]

(полугрупповое свойство); в) если $T(\varphi) x=0$ для всех $\varphi \in D+(\mathbb{R})$, то $x=0 ;$ г) линейная оболочка объединения всех значений $T(\varphi) x, \varphi \in D+(\mathbb{R}), x \in X$, плотна в $X$; д) для любого $y=T(\psi) x$ с $\psi \in D+(\mathbb{R})$ существует такая нешрерывная на $(0, \infty)$ функция $u(t)$ со значениями в $X$, что $u(0)=y \quad$ и

\[
T(\varphi) y=\int_{0}^{\infty} \varphi(t) u(t) d t
\]

для всех $\varphi \in D(\mathbb{R})$.

Инфините зима льны й опе ра тор $A_{0}$ полугруппы-распределения определяется так: если существует дельта-послөдовательность $\left\{\rho_{n}\right\} \subset D+(\mathbb{R})$ такая, что $T\left(\rho_{n}\right) x \rightarrow x$ и $T\left(-\rho_{n}^{\prime}\right) x \rightarrow y$ при $n \rightarrow \infty$, то $x \in D\left(A_{0}\right)$ и $y=A_{0} x$. Инфинитезимальный оператор допускает замыкание $A=\overline{A_{0}}$, к-рое наз п р о и з в о д ящи и оператором полугруппы-ра сп р е д е л е в и я. Множество $\bigcap_{n=1}^{\infty} D\left(A_{0}^{n}\right)$ плотно в $X$ и содержит $T(\varphi) X$ при любой $\varphi \in D^{+}(\mathbb{R})$.

Замкнутый линейный оператор $A$ с плотной в $X$ областью определения является производящим оператором полугруппы-распределения тогда и только тогда, когда найдутся числа $a, b \geqslant 0, c>0$ и натуральное $m$ такие, что при $\operatorname{Re} \lambda \geqslant a \ln (1+|\lambda|)+b$ существует резольвента $R(\lambda, A)$ и выполнено неравенство

\[
\|R(\lambda, A)\| \leqslant C(1+|\lambda|)^{m} .
\]

Если $A$ - замкнутый линейный опсратор в $X$, то множество $\bigcap_{n=1}^{\infty} D\left(A^{n}\right)$ можно превратить в пространство Фреше $X_{\infty}$, введя в нем систему норм

\[
\|x\|_{n}=\sum_{k=0}^{n}\left\|A^{k} r\right\|
\]

Сужение $A_{\infty}$ оператора $A$ ва $X_{\infty}$ оставляет $X_{\infty}$ инвариантным. Если $A$ - производящий оператор полугруппы-распределения, то $A_{\infty}-$ производящий оператор полугруппы класса $C_{0}$ (непрерывный при $t \geqslant 0, T(0)=I$ ) в пространстве $X_{\infty}$. Обратно, если $X_{\infty}$ плотно в $X$, оператор $A$ имеет непустое резольвентное множество и является производящим оператором полугруппы класса $C_{0}$ в $X_{\infty}$, то $A$ - производящий оператор полугруппыраспределения в $X$.

Полугруппа-распределение имеет экспоненциальный порядок роста не выше $q, 1 \leqslant q<\infty$, если при нек-ром $\omega>0$ отображение $\exp \left(-\omega t^{q}\right) T(\varphi)$ непрерывно в топологии, индуцированной на $D^{+}$пространством $S(\mathbb{R})$ быстро убывающих функций. Для того чтобы замкнутый линейный оператор был производящим оператором такой полугруппы-распределения, необходимо и достаточно, чтобы он имел резольвенту $R(\lambda, A)$, для к-рой выполнено (6) в области

$\left\{\lambda: \operatorname{Re} \lambda \geqslant[\alpha \ln (1+|\operatorname{Im} \lambda|+\beta)]^{1-1 / q}, \operatorname{Re} \lambda>\omega\right\}$, где $\alpha, \beta>0$. В частности, если $q=1$, то полугруппа наз. ә к с п о н е н ц и а л ь н о й и неравенство (6) выполняется в нек-рой полуплоскости. Имеется характеристика полугрупп указанных типов в терминах свойств оператора $A_{\infty}$. Для полугруппы-распределений изучены вопросы гладкости и аналитичности.

Полугруппа операторов в (отделимом) локально выпуклом пространстве $X$. Определение сильно непрерывной полугруппы непрерывных в $X$ операторов $T(t)$ остается таким же, как и в банаховом пространстве. Аналогично класс $C_{0}$ выделяется свойством $T(t) x \rightarrow x$ при $t \rightarrow 0$ и любом $x \in X$. Полугруппа наз. л о к а л ьв о э к в и н е п р е р ы в н о й (принадлежит классу $\left.l C_{0}\right)$, если семейство операторов $T(t)$ равностепенно непрерывно, когда $t$ пробегает любой конечный промежуток из $(0, \infty)$. В бочечном пространстве полугруппа класса $C_{0}$ всегда локально эквинепрерывна.

Полугруппа наз. а к в и н ө п р е р ы в н о й (принадлежит классу $\left.u C_{0}\right)$, если семейство $T(t), 0 \leqslant t<\infty$, равностепенно непрерывно.

Инфинитезимальный и производящий операторы полугрупіы определяются так же, как и в банаховом случае.

В дальнейшем предполагается, что пространство $X$ секвенциально полно. Для полугрупп класса $l C_{0}$ производящий оператор $A$ совпадаөт с инфинитезимальным, его область определөния $D(A)$ плотна в $X$ и, более того, плотно в $X$ множество $\bigcap_{n=1}^{\infty} D\left(A^{n}\right)$. Полугруппа $T(t)$ оставляет $D(A)$ инвариантной и

\[
\frac{d T}{d t} x=A T(t) x=T(t) A x, 0 \leqslant t<\infty, x \in D(A) .
\]

Если $A$ - производящий оператор полугруппы класca $u C_{0}$, то при $\operatorname{Re} \lambda>0$ опредөлена резольвента $R(\lambda, A)$ и она является преобразованием Лапласа от полугруппы.

Линейный оператор $A$ является производящим оператором полугруппы класса $u C_{0}$ тогда и только тогда, когда он замкнут, имеет плотную в $X$ область определения и существует такая последоватөльность положительных чисел $\lambda_{k} \rightarrow \infty$, что для любого $\lambda_{k}$ определена резольвента $R\left(\lambda_{k}, \quad A\right)$ и семейство операторов $\left[\lambda_{k} R\left(\lambda_{k}, A\right)\right]^{n}, k, n=1,2, \ldots$, равностепенно непрерывно. При этом полугруппа может быть построена по формуле

\[
\begin{gathered}
T(t) x=\lim _{k \rightarrow \infty}\left(\exp \left[-\lambda_{k}-\lambda_{k}^{2} R\left(\lambda_{k}, A\right)\right] t\right) x, \\
t \geqslant 0, x \in X .
\end{gathered}
\]

В ненормируемом локально выпуклом пространстве производящий опера'ор полугруппы класса $l C_{0}$ может не иметь резольвенты ни в одной точке. Пример: $A=\frac{d}{d s}$ в пространстве $C^{\infty}$ бесконечно дифференцируемых функций от $s$ на $\mathbb{R} \quad B$ качестве оператора, заменяющего резольвенту, может быть взят непрерывный оператор, умножение к-рого на оператор $A-\lambda I$ справа и слева «мало» отличается от единичного оператора


\end{document}