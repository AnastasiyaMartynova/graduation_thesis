\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
ски и В. Г., Гамк р е ли д з е Р. В., Мищ ен к о Е. Ф., Математическая теория оптимальных процессов, 2 изд., М., 1969; 1967, т. $7, \mathcal{N}_{2} 2$, с. $259-83 ;[4]$ К р о т о в В. Ф., "Автоматика и телемеханика», 1963, т. 24, № 5 , с. $581-98$. обычо то же, что оптимизачия вьчислительны $x$ алао ритмов. Иногда, однако, эти понятия трактуются различно. Напр., можно говорить об оптпмальности конкретного сеточного метода решения краевой задачи на нек-ром классе задач, имея в виду, что он требует вычисления правой части в минимальном числе точек. При рассмотрении вопросов об оптимизации вычислительного алгоритма изучается дополнительно ряд других моментов: способы решения возникающей системы сеточных уравнений, программная реализация этих способов и Т. П. H. С. Бахвалов. ОПТИМИЗАЦИЯ ВЫЧИСЛИТЕЛЬНЫХ АЛГОРИТ МОВ - выбор оптимального вычислительного алгоритма при решении прикладных задач или при разработке систем стандартных программ. При решении конкретной задачи оптимальная тактика поведения может состоять в том, чтобы не оптимизировать метод решения, а подключиться к стандартной программе или воспользоваться простейшим методом, составление программы для к-рого не потребует много усилий.

Теоретич. постановка вопроса об О. в. а. имеет следуюшее основание. При выборе метода решения задачи исследователь ориентируется на нек-рые ее свойства и выбирает алгоритм решения в зависимости от них, причем алгоритм будет применимым и при решении других задач, обладающих этими свойствами. Поәтому при теоретич. исследовании алгоритмов вводят в рассмотрение нек-рый класс задач $P$, обладающих определенными свойствами. При выборе метода решения исследователь располагает нек-рым множеством $M$ методов решения. При применении метода $m$ к решенпю задачи $p$ репение будет получено с нек-рої погрешностью $\varepsilon(p, m)$. Величина

\[
E(P, m)=\sup _{p \in P}|\varepsilon(p, m)|
\]

\includegraphics[max width=\textwidth, center]{2023_07_08_40f67ade2adf19fa416eg-1(1)}
с. е задач $P$, а

\[
E(P, M)=\inf _{m \in M} E(P, m)
\]

\includegraphics[max width=\textwidth, center]{2023_07_08_40f67ade2adf19fa416eg-1(2)}
п о г р ш н о т и методов із множества $M$. Еслі существует метод такой, что

\[
E\left(P, m_{0}\right)=E(P, M)
\]

то әтот метод наз. о п т и м а л ь н ы. Такая схема рассмотрения проблемы О. в. а., восходящая А. Н. Колмогорову [2], изучаст множество задач вычисленія пнтеграла

\[
I(f)=\int_{0}^{1} f(x) d x
\]

при условии $\left|f^{(n)}\right| \leqslant A, M$ - множество всевозможных квадратур

\[
I(f) \approx \sum_{j=1}^{N} C_{j}^{\top} f\left(x_{j}\right)
\]

каждая квадратура задается совокупностью $2 N$ чисел $C_{j}$ и $x_{j}$. Задача о минимальном количестве информации (см. [2], [3]), необходимой для запоминания функции из данного класса с требуемой точностью, также может быть уложена в әту схему Р Расматривается [4] более сложная постановка проблемы, где трудоемкость реализации алгоритма в определенном смысле увязывается с объемом исиользуемой памяти. Оптимальные алгоритмы построены для незначительного тисла задач типа [1]. Однако для большого числа вычислительных задач построены методы, по своим асімптотич. характеристикам близкие к оптимальным (см. [5]-[8]).

Исследование характеристик оптимальных на классах вычислительных алгоритмов решения задач (см. [5], [7]) складывается из двух частей: построение конкретных методов решения с возможно лучшими характеристиками и получение оценок снизу характеристик вычислительных алгоритмов (см. [2] - [4], [9]). По существу первая часть вопроса является основной проблемой теории численных методов і в большинстве случаев рассматривается независимо от проблемы оптимизации. Получение оценок снизу обычно сводится к оценке

\includegraphics[max width=\textwidth, center]{2023_07_08_40f67ade2adf19fa416eg-1}
пространств; иногда оно проводится независимо, но с использованием техники, аналогичной технике получения указанных оценок.

Вычислительные алгоритмы условно делят на п а сс и в ы е и а к т и в ы е. В первом случае алгоритм решения задачи не зависит от получаемой в ходе репения задачи информации, а во втором - зависит. При вычислении интеграла используемая информация о функции есть обычно информация о ее значениях в $N$ точках. В случае пассивного алгорітма интеграл вычисляется по формуле

\[
I(f) \approx \sum_{j=1}^{N} C_{j} f\left(P_{j}\right),
\]

где веса $C_{j}$ и $P_{j}$ из области $\Omega$ определения $f$ задаются заранее. Активные алгорітмы вычисления интеграла укладываются в следуюшую схему: задается точка $P_{1} \in \Omega$ п функциіг

$Q_{q}=\Phi_{q}\left(Q_{1}, \ldots, Q_{q-1} ; y_{1}, \ldots, y_{0-1}\right), q=2, \ldots, N$, $S_{N}\left(Q_{1}, \ldots, Q_{N} ; y_{1}, \ldots, y_{N}\right)$,

где $y_{j}, S_{N}$ - числа, $Q_{j} \in \Omega$. Последовательно вычисляют

$f\left(P_{1}\right), P_{2}=\Phi_{2}\left(P_{1} ; f\left(P_{1}\right)\right), f\left(P_{2}\right)$,

$P_{3}=\Phi_{3}\left(P_{1}, P_{2} ; f\left(P_{1}\right), f\left(P_{2}\right)\right), \ldots, f\left(P_{N}\right)$

и полагают

\[
I(f) \approx S_{N}\left(P_{1}, \ldots, P_{N} ; f\left(P_{1}\right), \ldots, f\left(P_{N}\right)\right) .
\]

В случае выпуклых классов подшнтегральных функций, центрально симметритных относительно функціи $f \equiv 0$, оптимальная оценка на классе пассивных алгоритмов совпадает с оптимальної оценкой на глассе актівных алгорштмов (см. [10], [11]).

В практике численного интегрирования активные алгоритмы типа алгоритмов интегрирования с автоматич. выбором шага (см. [10]) показали свое превосходство над пассивными алгоритмами. Это подтверясдает общепринятую точку зрения о том, что формальная схема О. в. а. зачастую не охватывает специфики реальных задач. При решении задач оптимизации (в частности, задач минимизации) пассивные алгоритмы практически не употребляются (см. [12], [13]). Выше за единицу трудоемкости алгоритма неявно принимается трудоемкость вычисления одного значения функции из нек-рого класса $F$. Возможны и другие подходы к оценке оптимальности характеристик алгоритма. Напр., за единицу трудоемкости принимается трудоемкость вычисления нек-рого функционала $l(f)$ из множества функционалов $L(f)$. В этом случае оценка снизу трудоемкости алгоритмов производится с помощью теории поперечников (см. [14], [15]). Трудоемкость алгоритма складывается не только из трудоемкости получения информации об исходных данных, но и из трудоемкости обработки получевной информации. В настоящее время, по-видимому, нельзя привести примеров классов реальных вычислительных задач, где бы были иолучены оценки свизу трудоемкости алгоритмов, отличные от информационных


\end{document}