\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}
\usepackage{mathrsfs}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
5 Aufl., B., 1923; [3] C a r t a n E., "Ann. Soc. pol. math.», 1923, t. 2. p. $171-221 ;$ [4] e ro ж e, "Bull. Soc. math. de Fran-
ce», 1924 t. 52, p. $205-41$; [5] e r o ж e, "Acta math.", 1926, t. 48 , p. $1-42 ;$; 6$]$ В а г н е р B. В., "Тр. Семин. по вект. и тен3. анализу», 1950 , в. 8 , c $11-72 ;$ [7] E h r es m a n n C., B Kн.: Coll. de Topologie. Bruxelles, 1950, P., 1951 , p. $29 \frac{5}{5}$; [8] Ј̈ а п т е в Г. Ф., "Тр. Моск матем об-ва", 1953 , т. 2, с. $275-382 ;[9]$ Н о м и д з у К., Группы Ли и дифференциальная геометрия, пер. с англ., М., $1960 ;$ [10] Л и х н е р о в и ч А., геометрия, пер. с англ., М.; $1960 ;[10]$ Л и х н е р о в и ч А.,
Теория связностей в целом и группы голономий, пер. с франц., М., 1960 ; [11] Ј у м и с т е Ю. Г., в кн.: Итоги науки. Сер. Математика, в. 21 - Алгебра. Топология. Геометрия. 1969 , М., 1971, с. 123-68. СГУІЕНИЯ ТОЧКА, н а к о п л е н и я т о ч к а,то же, что предельная точка. М. И. Войчеховский СДВИГ - аффџнное преобразование плоскости, при к-ром каждая точка смещается по направлению оси $O x$ на расстояние, пропорциональное ее ординате. В декартовой системе координат С. задается соотношениями

\[
x^{\prime}=x+k y, y^{\prime}=y, k \neq 0 \text {. }
\]

При С. сохраняются площади и ориентация.

C. пространсттва по направлению оси $O x$ задается соотношениями

\[
x^{\prime}=x+k z, y^{\prime}=y, z^{\prime}=z, k \neq 0 .
\]

При С. пространства сохраняются объемы и ориента$\begin{array}{ll}\text { ция. } & \text { А. Б. Иванов. }\end{array}$

СДВИГА ОПЕРАТОР - оператор $T_{t}$, зависящий от параметра $t$ и действуюший на нек-ром множестве Ф отображений $\varphi: A \rightarrow E$ (где $A$ - абелева полугруппа, $E$ - множество) по формуле

\[
T_{t} \varphi(\cdot)=\varphi(\cdot+t)
\]

(говорят также, что $T_{t}-$ о п е а т о р с д в и г а н a $t$ ). В качестве $A$ часто фигурируют $\mathbb{R}, \mathbb{R}+$ (тогда $T_{t}$ - сдвиг в том или ином пространстве функций действительного переменного), $\mathbb{Z}$ или $\mathbb{N}$ (тогда $T_{t}-$ сдвиг в том или ином пространстве последовательностей). Множество $E$, а соответственно этому и множество Ф, обычно наделено нек-рой структурой (векторного, векторного топологического, нормированного, метрического, вероятностного пространства).

С. о. используется, в частности, в теории динамич. систем (см. Сдвигов динамическая система, Бернулли автоморфизм). Употребляется также термин «С. о. по траекториям диффференциальных уравнений» (см. Kouu onepamop).

B. М. миллионциков.

СДВИГА ПАРАМЕТР - параметр $\theta, \theta \in \Theta \subset R^{k}$, семейства функций $\left\{\varphi_{\theta}(\cdot)\right\}$, заданных на $R^{k}$ по следуюшему правилу:

\[
\varphi \theta(\cdot)=\varphi(\cdot-\theta) \text { для любого } \theta \in \Theta \text {, }
\]

где $\varphi(\cdot)$ - нек-рая заданная функция на $R^{k}$.

Л. Лит.: [1] И б р а г и м о в ИА А., Х а с ь мин с ки й

\begin{itemize}
  \item З., Асимгтотичская теор. С. Никулин.
\end{itemize}

СДВИГИ ПОЛУГРУПП - преобразования полугрупп, удовлетворяющис специальным условиям: і р а в ы м с д в и го м полугруппы $S$ наз. преобразование $\rho$ такое, что для любых $x, y \in S$ имеет место $(x y) \rho=x(y \rho)$; левый сдвиг определяется симметричным образом, при этом ради удобства левые сдвиги чаще записывают как левые операторы. Таким образом, л е в ы й с д в и г полугруппы $S$ - такое ее преобразование $\lambda$, что для любых $x, y \in S$ имеет место $\lambda(x y)=(\lambda x) y$. Соответственно последовательность выполнения двух левых сдвигов в произведении (см. Преобразований полуәруппа) осуществляется сирава налево. Произведение двух левых (правых) С. П. само будет левым (правым) С. п., так что множество $\Lambda(S)$ (соответственно $P(S)$ ) всех левых (правых) С. п. $S$ будет подполугруппой симметрич. полугруппы $\mathscr{\sigma} s$. Для произвольного элемента $a \in S$ преобразование $\lambda_{a}\left(\rho_{a}\right)$, затанное формулой $\lambda_{a} x=a x\left(x \rho_{a}=\right.$ $=x a$ ) является левым (правым) сдвигом, соответствующим әлементу $a$. Он наз. в н у т р е н н и ле в ы м (п р а в ы м) с д в и г о м. Множество $\Lambda_{0}(S)$ (соответственно $P_{0}(S)$ ) всех внутренних левых (правых) С. п. $S$ есть левый идеал в $\Lambda(S)$ (правый идеал в $P(S)$ ). Левый сдвиг $\lambda$ и правый сдвиг $\rho$ полугруппы $S$ наз. с в я з а н ны м и, если $x(\lambda y)=(x \rho) y$ для любых $x$, $y \in S ;$ в әтом случае пару $(\lambda, \rho)$ наз. б и с д в и г о м $S$. Для любого $a \in S$ пара $\left(\lambda_{a}, \rho_{a}\right)$ есть бисдвиг; он наз. В н у т р е н им би с д в и го м, соответствующим әлементу $a$. В полугруппах с единицей и только в них всякий бисдвиг внутренний. Множество Т $(S)$ всех бисдвигов полугруппы $S$ есть подполугруппа декартова произведения $\Lambda(S) \times \mathrm{P}(S)$; она наз. с д в и г ово й обо лоч к о й полугруппы $S$. Множество $\mathrm{T}_{0}(S)$ всех внутренних бисдвигов есть идеал в Т $(S)$; он наз. в н у т р е н н е й ч а с т в ю $\mathrm{T}(S)$. Отображение $\tau: S \rightarrow \mathrm{T}_{0}(S)$, заданное формулой $\tau(a)=\left(\lambda_{a}\right.$,

\includegraphics[max width=\textwidth, center]{2023_07_08_b6ae0629cde4104759efg-1(2)}
н о п ч е с к и м. Полугруппа $S$ наз. с л а б о р ед у к т и в н о й, если для любых $a, b \in S$ из того, что $a x=b x$ и $x a=x b$ для всех $x \in S$, следует $a=b ;$ т. е. канонич. гомоморфизм полугруппы $S$ есть изоморфизм. Если $S$ слабо редуктивна, то T $(S)$ совпадает с идеализатором $\mathrm{T}_{0}(S)$ в $\Lambda(S) \times \mathrm{P}(S)$, т. е. с наибольшей подполугруппой из $\Lambda(S) \times \mathrm{P}(S)$, содержащей $\mathrm{T}_{0}(S)$ в качестве идеала.

С. П. и, в частности, сдвиговые оболочки играют существенную роль при изучении идеальных расширений полугрупп. Роль сдвиговой оболочки при этом в известной етепени аналогична роли голоморђа әруппы в теории групп.

Лит.: [1] Клифффо р д А., П р е с то н Г., Алгебраическая теория полугрупп, пер. с англ., т. $1, \mathrm{M} ., 1972 ;$ [2] P e t-
r i c h M., Introduction to semigroups. Columbus, Ohio, 1973;

\begin{center}
\includegraphics[max width=\textwidth]{2023_07_08_b6ae0629cde4104759efg-1}
\end{center}

СДВИГОВ ДИНАМИЧЕСКАЯ СИСТЕМА - динамическая система $f^{t}$ (или, в иных обозначениях,$\left.f(t, \cdot)\right)$

\includegraphics[max width=\textwidth, center]{2023_07_08_b6ae0629cde4104759efg-1(1)}
метрич. пространство), наделенном к о м п а к т о о т к р ы т ой топо логи й й (т. е. топологией равномерной сходимости на отрезках), заданная формулой

\[
f^{t} \varphi=T_{t} \varphi,
\]

где $T_{t}$ - сдвига оператор на $t$, то есть

\[
T_{t} \varphi(\cdot)=\varphi(\cdot+t) \text {. }
\]

Таким образом, траектория точки $\varphi$ в С. Д. с. есть множество всех сдвигов функции $\varphi$, т. е. всех функций вида $\varphi(t+\tau)$ переменного $\tau \in \mathbb{R}$, а замыкание этой траектории - множество всех функций вида

\[
\tilde{\varphi}(\tau)=\lim _{k \rightarrow \infty} \varphi\left(t_{k}+\tau\right)
\]

где предел равномерен на каждом отрезке. С. д. с. наделяется нормированными инвариантныли мерами, существующими в силу тсоремы Боголюбова - Крылова (инвариантные меры Боголюбова - Крылова сосредоточены на компактах).

С. Д. с. используется в теории динамич. систем, главным образом - для построения примеров (при этом в качестве $S$ обычно берут $\mathbb{R}$; п р и м е р М а р к о в а не строго эргодич. системы на компакте, каљкдая траектория к-рой всюду плотна, и др.), а также в теории неавтономных систем обыкновенных дифференциальных уравнений, где в качестве $S$ берется обычно $\mathbb{R} n$ или нек-рое пространство отображений $\mathbb{R}^{n} \rightarrow \mathbb{R} n$ (в теории линейных однородных неавтономных систем обычно берется $S=\operatorname{Hom}(\mathbb{R} n, \mathbb{R} n))$.

См. также Особые показатели, Центральнье показатели.

B. М. Миллионшиков.

СЕГМЕНТ - 1) С. н а п л о с к о с т І - плоская фигура, заключенная между кривой и ее хордой. Пло-


\end{document}