\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
ИНВАРИАНТНАЯ РЕГУЛЯРИЗАЦИЯ - реГУЛЯрИЗацИЯ, сохраняющая инвариантность относительно некоторой группы преобразований. В более узком смысле термин «И. р.» применяется в квантовой теории поля для обозначения сохраняющей калибровочную инвариантность регуляризации интегралов, отвечающих диаграммам Фейнмана. Существование И. р. является достаточным условием возможности построения калибровочно инвариантной квантовой теории. Если это условие не выполнено, то возможно появление квантовых аномалий, нарушающих калибровочную инвариантность на квантовом уровне. В качестве И. p. применяют размерную регуляризацию, высших ковариантных производных метод, переход к калибровочной теории в решетчатом пространстве-времени. $\quad$ A. А. Славнов. ИНВАРИАНТНОЕ ИНТЕГРИРОВАНИЕ - вИД ИНТеГРИрования для функций, аргументом которых являются элементы группы или точки однородного пространства (любую точку такого пространства можно перевести в другую заданным действием группы). И. и. согласовано с действием группы: значение интеграла не меняется при заменах переменных, отвечающих этому действию, а якобиан замены равен 1.

И. и.- стандартный прием для построения функционального интеграла, служащего эффективным средством изучения калибровочных полей, различных моделей квантовой теории поля.

Если пространство аргументов $X$ является многообразием (то есть допускает введение локальных координат $x_{1}, \ldots, x_{n}$ ), И. и. функции $f(x)$ сводится к вычислению интеграла от дифференциальной формы $f \cdot \omega$, где $\omega=\rho(x) d x_{1} \wedge \ldots \wedge d x_{n}$. Условие согласования имеет вид:

\[
\int_{X} f \cdot \omega=\int_{X} T_{g} f \cdot \omega
\]

здесь $T_{g}$ означает оператор сдвига на $X$ с помощью $g \in G$.

\[
T_{g} f(x)=f\left(g^{-1} x\right)
\]

Пусть $X=G$ - топологич. группа, действующая на себя левыми сдвигами. И. и. сушествует тогда и только тогда, когда $G$ локально компактна (в частности, на бесконечномерных группах И. и. не существует). Для подмножества $A \subset G$ И. и. характеристич. функшии $\chi_{A}$ (равной 1 на $A$ и 0 вне $A)$ задает ле в ую ме ру Хаара $\mu(A)$. Определяющим свойством этой меры является ее инвариантность при левых сдвигах: $\mu\left(g^{-1} A\right)=\mu(A)$ для всех $g \in G$. Левая мера Хаара на группе определена однозначно с точностью до положительного скалярного множителя. Если известна меpa Хаара $\mu$, то И. и. функции $f$ дается формулой

\[
\int_{G} f(g) d \mu(g) .
\]

Аналогичными свойствами обладает правая мера $\mathrm{X}$ a a a. Существует непрерывный гомоморфизм (отображение, сохраняюшее групповое свойство) $\Delta_{G}$ группы $G$ в группу (относительно умножения) положительных чисел, для $\mathbf{K}$-рого

\[
\begin{aligned}
d \mu_{l}(g h) & =\Delta_{G}(h) d \mu_{l}(g), d \mu_{r}(h g)=\Delta_{G}(h) d \mu_{r}(g), \\
d \mu_{r}(g) & =\text { const } \cdot \Delta_{G}(g) d \mu_{l}(g)=\text { const } \cdot d \mu_{l}\left(g^{-1}\right),
\end{aligned}
\]

где $d \mu_{r}$ и $d \mu_{l}-$ правая и левая меры Хаара. Функцию $\Delta_{G}(g)$ называют м одуле м группы $G$. Если $\Delta_{G} \equiv 1$, то группа $G$ называется унимодулярной; в этом случае правая и левая меры Хаара совпадают. Компактные, полупростые и нильпотентные (в частности, коммутативные) группы унимодулярны. Если $G-n$-мерная группа Ли и $\theta_{1}, \ldots, \theta_{n}-$ базис в пространстве левоинвариантных 1-форм на $G$, то левая мера Хаара на $G$ задается $n$-формой $\omega=\theta_{1} \wedge \ldots \wedge \theta_{n}$. В локальных координатах

\[
\theta_{i}=\sum_{j} \theta_{i j}(x) d x_{j}, \quad \omega=\operatorname{det}\left\|\theta_{i j}\right\| d x_{1} \wedge \ldots \wedge d x_{n} .
\]

Для вычисления форм $\theta_{i}$ можно воспользоваться любой матричной реализацией группы $G$ : матричная $1-ф о р м а g^{-1} d g$ левоинвариантна, а ее коэффициенты являются левоинва- риантными скалярными 1-формами, из к-рых и выбирается искомый базис. Напр., полная матричная группа $G L(n, R)$ унимодулярна и мера Хаара на ней задается формой $(\operatorname{det} g)^{-n} \hat{i j} d g_{i j}$.

Пусть $X=G / H$ - однородное пространство, для к-рого локально компактная группа $G$ является группой преобразований, а замкнутая подгруппа $H$ - стабилизатором нек-рой точки. Для того чтобы на $X$ существовало И. и., необходимо и достаточно, чтобы для всех $h \in H$ выполнялось равенство $\Delta_{G}(h)=\Delta_{H}(h)$. В частности, это верно в случае, когда $H$ компактна или полупроста.

Полной теории И. и. на бесконечномерных многообразиях не сушествует.

Лит.: [1] В е йл ь А., Интегрирование в топологических группах и его применения, пер. с франц., М., 1950; [2] К и р и лл о в А. А., Элементы теории представлений, 2 изд., М., 1978; [3] Славн о в А. А., Ф а д е е в Л. Д., Введение в квантовую теорию калибровочных полей, 2 изд., М., 1988. А. А. Кириллов.

ИНВАРИАНТНОЕ МНОТООБРАЗИЕ - инвариантное множество в фазовом многообразии $M$, являюшееся подмногообразием $M$. Основные примеры: а) И. м. $N$ автономного обыкновенного дифференциального уравнения $d x / d t=V(x)$ на многообразии $M$ (фазовая кривая уравнения, проходящая через любую точку $p \in N$, целиком лежит в $N$; б) И. м. $N$ диффеоморфизма $A: M \rightarrow M$ многообразия $M(N$ удовлетворяет условию $A(N)=N)$. Подмногообразие $N$ является И. м. уравнения $d x / d t=V(x)$ (используют также термины «И. м. векторного поля $\boldsymbol{V}$ », «И. м. фазового потока

\includegraphics[max width=\textwidth, center]{2023_07_08_c81984b7c9579485d5dag-1}
только тогда, когда вектор поля $\boldsymbol{V}$ в каждой точке $p \in N$ касается $N$.

См. также Динамическая система. М. Б. Севрюк. ИНВАРИАНТНОЕ МНОЖЕСТВО - такое ПОдМНОЖествО $N$ фазового пространства некоторой системы, что если состояние системы $x(t)$ в начальный момент времени $t=t_{0}$ принадлежит $N$, то $x(t)$ принадлежит $N$ во все предыдушие и последующие моменты времени $t$. Любая фазовая кривая этой системы либо целиком лежит в $N$, либо не пересекается с $N$. Термин «И. м.» обычно употребляется только для автономных систем, когда закон, определяющий эволюцию системы, не зависит явно от времени. ИHBAPИАНTHOE ПOДПPOCTPAHCTBO - см. Неприводимое представление.

ИНВАРИАНТНОЕ РАСПРЕДЕЛЕНИЕ ВЕРОЯТНОСТЕЙ - см. Эргодическая теорема.

ИНВАРИАНТНОСТИ ГPУППА - см. Инвариантность.

ИНВАРИАНТНОСТЬ - фундаментальное физическое понятие, выражающее независимость физических закономерностей от конкретных ситуаций, в которых они устанавливаются, и от способа описания этих ситуаций. Понятие И. применяется также к физич. величинам, значения к-рых не зависят от способа описания.

И. формулируется как обобщение данных опыта и является физич. закономерностью. Среди прочих физич. закономерностей свойства И. выделены тем, что относятся к наиболее широкому кругу явлений, отражают наиболее общие и глубокие свойства физич. объектов. Поэтому иногда их называют принци пам и И. В ряде случаев понятие И. возникает только в определенных теоретич. рамках, и для его формулировки необходимо ввести принципиально ненаблюдаемые величины. Так, описание калибровочной И. происходит в терминах потенциалов поля (наблюдаемы их производные - напряженности) и фаз волновых функций (наблюдаемы квадраты их модулей вероятности).

Изменение условий наблюдения часто эквивалентно изменению способа описания явления: смена места и времени наблюдения - сдвигу начала отсчета координат и времени, замена частиц на античастицы - операции зарядового со-


\end{document}