\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }
\usepackage{mathrsfs}
\usepackage{bbold}

\begin{document}
где $p_{0}, p$ - начальная и переходная плотности распределения. Это выражение сходно с функцией правдоподобия для последовательности независимых наблюдений и при соблюдении условий регулярности (гладкость по $\left.\theta \in \Theta \subset R^{k}\right)$ можно построить теорию оценивания и проверки гипотез, аналогичную соответствующей теории для независимых наблюдений.

Более сложная ситуация возникает, если $x(t)-$ марковский процесс с непрерывным временем. Пусть $x(t)$ - однородный марковский процесс с конечным числом состояний $N$ и диффференцируемыми вероятностями перехода $P_{i j}(t)$. Матрица вероятностей перехода определяется матрицей $Q=\left\|q_{i j}\right\|, q_{i j}=P_{i j}^{\prime}(0), q_{i}=-q_{i i}$. Пусть в начальный момент $x(0)=i_{0}$ независимо от $Q$. Выбирая какую-нибудь матрицу $Q_{0}=\left\|q_{i j}^{0}\right\|$, находят

\includegraphics[max width=\textwidth, center]{2023_07_09_ac332bf9521450ef7396g-1}
$\times \exp \left\{t_{v}\left(q_{i_{n}}-q_{i_{v}}-q_{i_{v}}^{0}+q_{i_{n}}^{0}\right)\right\}$.

Здесь статистики $n(x), t_{v}(x), j v(x)$ определяются следующим образом: $n$ - это число скачков $x(t)$ на интервале $[0, T), \tau_{v}-$ момент $v$-го скачка, $t_{v}=\tau_{v+1}-\tau_{v}$, $z_{v}=x\left(\tau_{v}\right)$. Из указанного выражения выводятся оценки максимального правдоподобия для параметров $q_{i j}$ : $: q_{i j}^{*}=\frac{m_{i j}}{\mu_{i}}$, где $m_{i j}-$ число переходов из $i$ в $j$ на отрезке $[0, T)$, а $\mu_{i}$ - время, проведенное процессом $x(t)$ в состоянии $i$.

П р и м е 9. Пусть $x(t)$ - рождения и гибели процесс с постоянными интенсивностями размножения $\lambda$ и гибели $\mu$. Это значит, что $q_{i, i+1}=i \lambda, q_{i, i-1}=i \mu, q_{i i}=$ $=1-i(\lambda+\mu), q_{i j}=0$, если $|i-j|>1$. В этом примере число состояний бесконечно. Пусть $x(0) \equiv 1$. Отношение правдоподобия

\[
\begin{gathered}
\frac{d P_{\lambda \mu}^{T}}{d P_{\lambda_{0}}^{T}, \mu_{0}}(x)= \\
=\left(\frac{\lambda}{\lambda_{0}}\right)^{B}\left(\frac{\mu}{\mu_{0}}\right)^{D} \exp \left\{-\left(\lambda+\mu-\lambda_{0}-\mu_{0}\right) \int_{0}^{T} x(s) d s\right\} .
\end{gathered}
\]

Здесь $B$ - общее число рождений (скачков размера $+1), \quad D$ - смертей (скачков размера -1). Оденки максимума правдоподобия для $\lambda$ и $\mu$ :

\[
\lambda_{T}^{*}=\frac{1}{B} \int_{0}^{T} x(s) d s, \quad \mu_{T}^{*}=\frac{1}{D} \int_{0}^{T} x(s) d s .
\]

Пусть $x(t)$ - дифффуионный процесс с коэффициентом сноса $a$ и коэффициентом диффузии $b$, так тто $x(t)$ удовлетворяет следующему стохастическому дифференциальному уравнению

$d x(t)=a(t, x(t)) d t+b(t, x(t)) d w(t), x(0)=x_{0}$, где $w$ - винеровский процесс. Тогда при определенных ограничениях

\[
\begin{gathered}
\frac{d P_{a, b}^{T}}{d P_{a_{0} b}^{T}}(x)=\exp \left\{-\int_{0}^{T}\left(b(t, x(t))^{-1}(a(t, x(t))-\right.\right. \\
-a_{0}(t, x(t)) d x(t)+\frac{1}{2} \int_{0}^{T}\left(b(t, x(t))^{-1}(a(t, x(t)-\right. \\
\left.-a_{0}(t, x(t))^{2} d t\right\}
\end{gathered}
\]

(здесь $a_{0}$ - фиксированный коэфффициент).

П ри м е р 10. Пусть

\[
d x(t)=a(t, x(t) ; \theta) d t+d w,
\]

$a$ - известная функция, $\theta$ - неизвестный действи. тельный параметр. Если обозначить через $\mu$ меру Винера, то функция правдоподобия

$\frac{d P_{\theta}^{T}}{d \mu}=\exp \left\{\int_{0}^{T} a(t, x(t) ; \theta) d x-\frac{1}{2} \int_{0}^{T} a^{2}(t, x(t) ; \theta) d t\right.$,

и при условиях регулярности выполняется н е р а в е нс т в о K р а м е р а - $\mathrm{P}$ а о: для оценки $\tau$ со смещением $\Delta(\theta)=E_{\theta} \tau-\theta$

\[
\mathrm{E}_{\theta}|\tau-\theta|^{2} \geqslant \frac{\left(1+\frac{d \Delta}{d \theta}\right)^{2}}{\mathrm{E}_{\theta} \int_{0}^{T}\left[\frac{\partial}{\partial \theta} a(t, x(t) ; \theta)\right]^{2} d t}+\Delta^{2}(\theta) .
\]

Если зависимость от $\theta$ линейная, оценки максимального правдоподобия

\[
\left.\theta_{T}^{*}=\int_{0}^{T} a(t, x(t)) d t\left(\int_{0}^{T} a^{2}(t, x(t)) d t\right)\right)^{-1} .
\]

Jium.: [1] Г р е н а нд е р У., Случайные процессы и статистические выводы, пер. с англ., М., 1961; [2] X е н н а н Э.,
Анализ временных рядов, пер. с англ., М., 1964; [3] G r е n a nd e r U., R os e n b a t t M., Statistical analysis of stationary time series, Stockh., 1956; [4] G r e n a n d e r U., Abstract inference, N. Y., 1981; [5]' Р о з а н о в Ю. А., Га́уссовские бесконечномерные распределения, М., 1968 (Тр. матем. ин-та им. Стеклова, т. 108); [6] И бр а гим о в И. А., Р о з а н о в Ю. А., Гауссовские случайные процессы, М., 1970; [7] Бр и л Л и н д ж е p Д., Временные ряды, пер. c англ., M.,
1980 ; [8] B i l l i n g s le y P., Statistical inference for Markov processes, Chi., 1961; [9] ЈI и п ц е p P. III., III и р я е в А. Н., Статистика случайных процессов, М., $1974 ;[10]$ Я г ло м А. M., 1981; [11] А н д е р с о н Т., Статистический анализ временных рядов, пер. с англ., М., 1976 . И. А. Ибрагимов.

СТАТИСТИЧЕСКИЙ АНАЛИЗ СЈУЧАЙНЫХ ПРОЦЕССОВ - раздел математич. статистики и теории случайных процессов, посвященный исследованию и решению статистических задач случайных процессов.

СТАТИСТИЧЕСКИЙ АНСАМБЛЬ - принятое в статистич. физике название фазового пространства ( П р ос т р а н с т в а с о с т о я н и й) какой-нибудь физич. системы вместе с нек-рым способом усреднения физич. величин (т. н. н а б л ю д а е м ы х), связанных с этой системой. В случае классич. системы с фазовым пространством $\Omega$ наблюдаемые - действительные функции, определенные на $\Omega$, а их усреднение происходит с помощью интегрирования по нек-рой вероятностной мере $\mu$ на пространстве $\Omega$. В случае квантовой системы, описываемой векторами гильбертова пространства $\mathscr{H}$, наблюдаемые задаются самосопряженными операторами, действующими в $\mathscr{H}$, и усреднение вводится с помощью нек-рого положительного и нормированного функционала $\rho$, определенного на алгебре $\mathfrak{A}(\mathscr{H})$ ошераторов, действующих в $\mathscr{H}$ (такие функционалы на $\mathfrak{A}(\mathscr{H})$ наз. с о с т о я н и я м и). Обычно состояние задается в виде

\[
\rho(A)=\operatorname{Sp} \hat{\rho}(A), \quad A \in \mathfrak{Y}(\mathscr{H}),
\]

где $\hat{\rho}-$ положительный оператор в $\mathscr{H}$ такой, что $\operatorname{Sp} \hat{\rho}=1$ (оператор $\hat{\rho}$ наз. м а т р и це й П ло тн о с ти с ос то я н и я $\rho$ ).

Если задана эволюция физич. системы во времени (д и на мик а с и с т е мы), т. е. (в классич. случае) - группа $\Gamma_{t}: \Omega \rightarrow \Omega, t \in \mathbb{R}$, взаимно однозначных преобразований фазового пространства в себя, порождаемая гамильтоновыми уравнениями движения с некрой функцией Гамильтона $H(\omega), \omega \in \Omega$ (ә н е р г и я с и с т е м ы), или (в квантовом случае) - группа унитарных преобразований $U_{t}: \mathscr{H} \rightarrow \mathscr{H}, t \in \mathbb{R}$, гильбертова пространства в себя, порождаемая оператором Гамильтона $\hat{H}$ (о п е р а т о р о м ән е р г и и с и с те мы), то естественно определяется эволюция во времени любого С. а., определенного для этой системы

\[
\left.\begin{array}{l}
\mu_{t}(C)=\mu\left(\Gamma_{t}^{-1} C\right), \quad C \subset \Omega \text { (классич. случай), } \\
\rho_{t}(A)=\rho\left(U_{t} A U_{t}^{-1}\right) \quad \text { (квантовый случай). }
\end{array}\right\}
\]


\end{document}