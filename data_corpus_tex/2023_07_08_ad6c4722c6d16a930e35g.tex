\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}

\begin{document}
ничных свойств функций. Если пытаться упростить решение аппроксимационной задачи переходом к области более простой структуры, то возникает проблема возможности продолжения функции $f$ из области $Q \subset \mathbb{R} m$ в нек-рую содержащую $Q$ канонич. область $Q_{1}$ (напр., в параллелепипед или на все пространство $\mathbb{R}^{m}$ ) с сохранением тех или иных гладкостных свойств (см. Продолжения теоремы). Этот круг вопросов тесно связан с вложения теоремами, а также с проблемами, возникающими при решении краевых задач математич. физики.

Увеличение числа независимых переменных, естественно, усложняет и аппарат приближения, ибо увеличивается его размерность при том же, напр., порядке полиномов. Алгебраич. многочлен степени $n_{1}$, ..,$n_{m}$ соответственно по переменным $t_{1}, \ldots, t_{m}$ имеет вид

\[
P_{n_{1}, \ldots, n_{m}}\left(t_{1}, \ldots, t_{m}\right)=\sum a_{k_{1}, \ldots, k_{m}} t^{k_{1}} \ldots t^{k_{m}}
\]

$\left(a_{k_{1}}, \ldots, k_{m}-\right.$ действительные коэффициенты, суммирование ведется по $k_{v}, v=1, \ldots, m$, от 0 до $n_{v}$ ), так что, напр., подпространство многочленов степени 3 по каждому из $m$ переменных имеет размерность $4^{m}$. Иногда фиксируется суммарная степень многочлена $n$, тогда суммирование в (1) распространено на индексы, удовлетворяющие неравенству $0 \leqslant k_{1}+\ldots+k_{v} \leqslant n$. Тригонометрический действительный полином порядка $n_{1}, \ldots, n_{m}$ по переменным $t_{1}, \ldots, t_{m}$ может быть записан в виде

\[
\begin{gathered}
T_{n_{1}, \ldots, n_{m}}\left(t_{1}, \ldots, t_{m}\right)= \\
=\sum a_{k_{1}, \ldots, k_{v}} \exp \left(i \sum_{v=1}^{m} k_{v} t_{v}\right),
\end{gathered}
\]

где комплексные коэффициенты $a_{k_{1}}, \ldots, k_{m}$ с индексами противоположного знака комплексно сопряжены, а суммирование ведется по $k_{v}, v=1, \ldots, m$, от $-n_{v}$ до $n_{v}$. Этот полином можно также представить в виде линейной комбинации всевозможных произведений вида $\varphi_{k_{1}}(t) \varphi_{k_{2}}\left(t_{2}\right) \ldots \varphi_{k_{m}}\left(t_{m}\right)$, где $\varphi_{k_{v}}\left(t_{v}\right)$ есть либо $\sin k_{v} t_{v}\left(0 \leqslant k_{v} \leqslant n_{v}\right)$, либо $\cos k_{v} t_{v} \quad\left(0 \leqslant k_{v} \leqslant n_{v}\right)$. Широкое применение находят многомерные сплайны, «склеенные» до определенной гладкости из «кусков» алгебраич. многочленов $m$ переменных. В случае $m=2$ наиболее простой вид имеют сплайны, склеенные из многочленов по прямым, параллельным осям координат. В качестве аппарата приближения применяются также функции $g\left(t_{1}, \ldots, t_{m}\right)$, являющиеся полиномами или сплайнами лишь по нек-рым из переменных. Для приближения непериодич. функций, заданных на всем пространстве $\mathbb{R}^{m}$ (или на неограниченном множестве из $\mathbb{R} m$ ), могут применяться целые функции экспоненциального типа, представимые в виде суммы абсолютно сходящегося степенного ряда

\[
\begin{gathered}
G_{n_{1}, \ldots, n_{m}}\left(t_{\mathbf{i}}, \ldots, t_{m}\right)= \\
=\sum_{k_{v} \geqslant 0, v=1, \ldots, m} a_{k_{1}, \ldots, k_{m}} t_{1}^{k_{1}} \ldots t_{m}^{k_{m}}
\end{gathered}
\]

при условии, что для любого $\varepsilon>0$ при всех комплексных $t_{1}, t_{2}, \ldots, t_{m}$ $\left|G_{n_{1}, \ldots, n_{m}}\left(t_{1}, \ldots, t_{m}\right)\right| \leqslant M_{\varepsilon} \exp \sum_{v=1}^{m}\left(n_{v}+\varepsilon\right)\left|t_{v}\right|$,

где константа $M_{\varepsilon}$ зависит только от $\varepsilon$ (см. [1]). Следует заметить, что в отличие от полиномов функция (2) определяется бесконечным числом параметров.

В многомерном случае справедлива т е о р е м а В е й е $\mathrm{p}$ шा т $\mathrm{p}$ а с с а о возможности шриблизить непрерывную на ограниченном замкнутом множестве $Q \subset \mathbb{R}^{m}$ функцию $f(f \in C(Q))$ или непрерывную ва всем пространстве $\mathbb{R} m$ с периодом $2 \pi$ по каждому переменному функцию $f\left(f \in \tilde{C}\left(\mathbb{R}^{m}\right)\right)$ алгебраическими (соответственно тригонометрическими) полиномами с любой наперед заданной степенью точности. Аналогичный факт имеет место и в пространствах $L_{p}(Q)$ и (в периодич. случае) $\tilde{L}_{p}\left(\mathbb{R}^{m}\right) \quad(1 \leqslant p<\infty)$. На линейные нормированные пространства функций $m$ переменных распространяются общие факты и теоремы о свойствах наилучшего приближения, о существовании, единственности и характеристич. свойствах функции наилучшего приближения, а также общие соотношения двойственности для приближения выпуклым множеством и, в частности, подпространством (см. [3], [4]). Однако получение конкретных реализаций этих теорем с учетом конкретной метрики и специфики приближающего подпространства в многомерном случае сопряжено с большими трудностями.

Более полно исследованы вопросы связи порядка убывания наилучших приближений функций многих переменных алгебраическими и тригонометрич. полиномами, а также целыми функциями с гладкостными свойствами приближаемой функции.

Пусть $Q$ - произвольное открытое множество в $\mathbb{R} m$ (в частности, $Q=\mathbb{R}^{m}$ ), e - единичный вектор из $\mathbb{R}^{m}$, $h>0$ и $Q_{h e}-$ множество точек $t \in Q$ таких, что отрезок $[\boldsymbol{t}, \boldsymbol{t}+h \boldsymbol{e}] \subset Q$. Если $f \in L_{p}(Q), 1 \leqslant p \leqslant \infty$, то величина

\[
\omega_{\boldsymbol{e}}(f ; \delta)_{L_{p}(Q)}=\sup _{h \leqslant \delta}\|f(\boldsymbol{t}+h \boldsymbol{e})-f(\boldsymbol{t})\|_{L_{p}\left(Q_{h} \boldsymbol{e}\right)}
\]

наз. мо дуле м не п ре ры вност п фун кц и и $f\left(t_{1}, \ldots, t_{m}\right)$ в ме т $\mathbf{p}$ и к е $L_{p}(Q)$ п о нап р а в лени ю $\boldsymbol{e}$. Величину

\[
\omega(f ; \delta)_{L_{p}(Q)}=\sup _{\boldsymbol{e}} \omega_{e}(f ; \delta)_{L_{p}(Q)}
\]

наз. м о дуле м в е п ре $\mathrm{p}$ ы в нос т и функции $f$ в $L_{p}(Q)$.

В периодич. случае для наилучших приближений $\left.E_{n_{1}}, \ldots, n_{m}(f)_{\tilde{L}_{p(\mathbb{R}}}\right) \quad$ тригонометрич. полиномами $\tilde{T}_{n_{1}}, \ldots, n_{m}$ функции $f \in \tilde{L}_{p}\left(\mathbb{R}^{m}\right)$, имеюцей частные (вообще говоря, обобщенные по Соболеву) производные

\[
D_{v}^{r} v f=\frac{\partial^{r} v}{\partial t_{v}^{r} v} f \in \tilde{L}_{p}(\mathbb{R} m)
\]

$\left(r_{v} \geqslant 0-\right.$ целые, $\left.\quad D_{v}^{0} f=f, \quad v=1, \ldots, m\right)$, справедливы оценки

\[
\begin{aligned}
& \tilde{E}_{n_{1}, \ldots, n_{m}}(f)_{\tilde{L}_{p}\left(\mathbb{R}^{m}\right)} \leqslant \\
& \leqslant M \sum_{v=1}^{m} n_{v}^{-r} v \omega_{e_{v}}\left(D_{v^{r} f ; n_{v}^{-1}}^{{ }^{r}}\right)_{p\left(\mathbb{R}^{m}\right)},
\end{aligned}
\]

где $\boldsymbol{e}_{v}$ - единичный вектор, направленный вдоль $t_{v}$, а константа $M$ не зависит от $f$ и $n_{v}$. Для функции $f$ из $\tilde{L_{p}}\left(\mathbb{R}^{m}\right)$, имеюцей обобщенные смешанные и несмешанные производные

\[
D^{r} f=\frac{\partial r}{\partial t_{1}^{r_{1}} \ldots \partial t_{m}^{r_{m}}} f \in \tilde{L}_{p}\left(\mathbb{R}^{m}\right)
\]

$\left(\boldsymbol{r}=\left(r_{1}, \ldots, r_{m}\right)\right)$ порядка $r=r_{1}+\ldots+r_{m}$, имеют место неравенства

\[
\begin{gathered}
\tilde{E}_{n, \cdots, n}(f)_{\tilde{L}_{\mu}\left(\mathbb{R}^{m}\right)} \leqslant \\
\leqslant \frac{M I}{u^{r}} \sum_{r_{1}+}+r_{m=r} \omega\left(D_{\left.f ; n^{-1}\right)} \tilde{L}_{p\left(\mathbb{R}^{m}\right) .} .\right.
\end{gathered}
\]

Если

\[
\omega\left(D^{r} f ; \delta\right)_{\tilde{L}_{p}\left(\mathbb{R}^{m}\right)} \leqslant K \delta^{\alpha}, \quad 0<\alpha \leqslant 1
\]


\end{document}