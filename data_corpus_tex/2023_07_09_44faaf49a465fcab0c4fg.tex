\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{mathrsfs}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
$=x_{[0, \infty)}=\left(x_{t}\right)_{t \geqslant 0}$ со значениями в $E$, и пусть $N_{t}\left(N_{t-}\right)-$ минимальная $\sigma$-алгебра в $\Omega$, относительно к-рої измеримы функции $x_{s}=x_{s}(\omega)$ при $s \leqslant t$ (при $s<t$ ) и $N=N_{\infty}$. Любая функция $\alpha_{t}(\omega)$ на $(0, \infty) \times \Omega$ со значениями $\alpha_{t}(\omega) \in A\left(x_{t}-(\omega)\right)$, прогрессивно измеримая относительно семейства $\left\{N_{t-}\right\}$, наз. (естественной) с т р атеги ей. Из определения следует, что $\alpha_{t}(\omega)=$ $=\alpha\left(x_{[0, t)}\right)$, где $x_{[0, t)}=\left(x_{s}\right)_{0 \leqslant s<t}$. Если $\alpha_{t}(\omega)=\alpha\left(t, x_{t-}\right)$, где $\alpha(t, x)$-борелевская функция на $(0, \infty) \times E$ со свойством $\alpha(t, x) \in A(x)$, то стратегия $\alpha$ наз. м а pко вск о й, а если $\alpha(t, x)=\alpha(x)$, то с т а ци он а $\mathbf{p}-$ н о й. Классы естественных, марковских и стационарных стратегий обозначаются соответственно $\mathfrak{A}_{E}, \mathfrak{A}_{M}$ и $\mathfrak{A}_{s}$. Ввиду возможности измеримого выбора из $A(x)$ класс $\mathfrak{H}_{S}$ (следовательно, $\mathfrak{H}_{M}$ и $\mathfrak{H}_{E}$ ) не пуст. Если $q$ огранитена, то по любым $x \in E$ и $\alpha \in \mathfrak{A}_{E}$ строится единственная вероятностная мера $\mathrm{P}_{x}^{\alpha}$ на $(\Omega, N)$ такая, что $\mathrm{P}_{x}^{\alpha}\left\{x_{0}=x\right\}=1$ и при любых $t^{\prime} \geqslant t \geqslant 0, \Gamma \in \mathscr{B}(E)$

\[
\left.\begin{array}{c}
\mathrm{P}_{x}^{\alpha}\left\{\tau>t^{\prime} \mid N_{t}\right\}= \\
\left.=\exp \left[\int_{t}^{t^{\prime}} q\left(\alpha\left(x_{[0, s)}^{*}\right), s, x_{t}, x_{t}\right) d s\right] \text { (п. н. } \mathrm{P}_{x}^{\alpha}\right), \\
\mathrm{P}_{x}^{\alpha}\left\{x_{\tau} \in \Gamma \mid N_{t}(\tau)\right\}= \\
\left.=-\frac{q\left(\alpha\left(x^{*}[0, \tau)\right), \tau, x_{t}, \mathrm{r}\right)}{q\left(\alpha\left(x_{[0, \tau)}^{*}\right), \tau, x_{t}, x_{t}\right)} \text { (п. н. } \mathrm{P}_{x}^{\alpha}\right),
\end{array}\right\}
\]

где $\tau=\tau(t, \omega)$-момент первого скатка после $t, N_{t}(\tau)$ минимальная $\sigma$-алгебра в $\Omega$, содержащая $N_{t}$, относительно к-рой $\tau$ измеримо, $x_{u}^{*}=x_{t}$ при $0 \leqslant u \leqslant t$ и $x_{u}^{*}=x_{t}$ при $u>t$. Слутайный процесс $\left\{\left(x_{t}\right)_{t \geqslant 0}, P_{x}^{\alpha}\right\}$ и есть у.с.м.п. Марковское свойство у.с.м.п. состоит в том, что при известном «настоящем» $x_{t}$, «прошлое» $\left(x_{u}\right)_{u<t}$ входит в правые части (1) лишь через стратегию $\alpha$. При произвольной стратегии $\alpha \in \mathfrak{A}_{E}$ процесс $\left(x_{t}\right)_{t \geqslant 0}$, вообще говоря, не марковский, но если $\alpha \in \mathfrak{A}_{M}$, то он будет марковским процессом, а если $\alpha \in \mathfrak{P}_{S}$ и $q$ не зависит от $t$, то-однородным марковским с плотностью вероятности скачка из $x$ в $\Gamma$, равной $q(\alpha(x)$, $x, \Gamma)$. Управление процессом состоит в выборе стратегии.

Типичная задача управления-максимизация функционала

\[
v^{\alpha}(x)=\mathrm{E}_{x}^{\alpha}\left[\int_{0}^{T} f^{\alpha} t\left(t, x_{t}\right) d t+g\left(x_{T}\right)\right],
\]

где $f^{a}(t, x)$ и $g(x)$ - ограниченные борелевские функ-

\includegraphics[max width=\textwidth, center]{2023_07_09_44faaf49a465fcab0c4fg-1(1)}
C. помощью перехода к другим функциям $f$ и $g$ и введения фиктивных состояний к виду (2) приводится более широкий класс функционалов, содержащих также члены $h^{\alpha_{\tau}}\left(\tau, x_{\tau-}, x_{\tau}\right)$, $\tau-$ момент скачка, и допускающих обрыв или остановку процесса. Фу нк ци е й в ы и г р ыш а (це н о й) наз. функция

\[
v(x)=\sup _{\alpha \in \mathfrak{a}_{E}} v^{\alpha}(x), \quad x \in E .
\]

Стратегия $\alpha$ наз. $\varepsilon-0$ пт и м а ль ной, если $v^{\alpha}(x) \geqslant$ $\geqslant v(x)-\varepsilon$ при всех $x \in E$, и (п. н. $\mu$ ) $\varepsilon-0$ п т и м а льн о й, если то же верно при потти всех $x$ относительно меры $\mu$ на $E$. 0-оптимальные стратегии наз. о пт имальн ы м и. Пусть в модели, полученной из первоначальной схемы сокращением промежутка $[0, \infty)$ до $[t, \infty)$, символы $\mathfrak{A}(t) \mathrm{E}_{t, x}^{\alpha}$ и $v(t, x)$ имеют тот же смысл, что $\mathfrak{P}, \mathrm{E}_{x}^{\alpha}$ и $v(x)$ в первонатальной модели. Рассматривая скачки как последовательные шаги в управляемой марковской цепи с дискретным временем, можно установить существование (п. н. $\mu$ ) $\varepsilon$-оптимальных стратегий в классе $\mathfrak{A}_{E}$ и получить свойство изме римости $v$ в форме: $\{(t, x): v(t, x)>C\}$ - аналитич. множество. Это позволяет применить идеи динамического програлмирования и вывести соотношение

$v(t, x)=\sup _{\alpha \in \Omega_{E}(t)} \mathrm{E}_{t, x}^{\alpha}\left[\int_{t}^{\tau_{1}} f^{\alpha_{s}}\left(s, x_{s}\right)+v\left(\tau_{1}, x_{\tau_{1}}\right)\right]$,

где $\tau_{1}=\min \left(\tau(t), t^{\prime}\right), 0 \leqslant t<t^{\prime} \leqslant T$ (вариант принципа Беллмана). При $t^{\prime} \downarrow t$ из (4) и (1) получается Беллмана уравнение

\[
-\frac{\partial v(t, x)}{\partial t}=
\]

$=\sup _{a \in A(x)}\left[f^{a}(t, x)+\int_{E} v(t, y) q(a, t, x, d y)\right]$ (І.. в.).

Функция выигрыша $v(t, x)$ - единственная ограниченная абсолютно непрерывная по $t$ функция на $[0, T] \times E$, удовлетворяющая (5) и условию $v(T, x)=g(x)$. Уравнение (5) можно решать методом последовательных приближений. При $\alpha \in \mathfrak{A}_{M}$ из Колмогорова уравнения для марковского процесса $\left(x_{t}\right)_{t \geqslant 0}$ следует, что если супремум в (5) достигается при измеримой функции $a=\alpha(t, x)$, то марковская стратегия $\alpha$ оптимальна. Так устанавливается существование марковских оптимальных стратегий в полунепрерывных моделях (в к-рых $A, q, f$ и $g$ удовлетворяют определевным условиям компактвости и непрерывности), в частности в конечных моделях (с ковечными $E$ и $A$ ). В произвольных борелевских моделях с помощью теорем измеримого выбора выводится существование (п. н. $\mu$ ) $\varepsilon$-оптимальных марковских стратегий при любом $\varepsilon>0$. В счетных моделях получа-

\includegraphics[max width=\textwidth, center]{2023_07_09_44faaf49a465fcab0c4fg-1}
частично переносятся на случаи, когда $T=\infty$ и функции $f$ и $g$ не ограничены, но в общей ситуации достаточность марковских стратегий не доказана.

В однородных моделях, где $g$ и $f$ не зависят от $t$, наряду с (2) рассматривают функционалы

\[
\begin{gathered}
v^{\alpha}(x)=\mathrm{E}_{x}^{\alpha} \int_{0}^{\infty} e^{-\lambda t} f^{\alpha} t\left(x_{t}\right) d t, \\
v^{\alpha}(x)=\lim _{T \rightarrow \infty} \mathrm{E}_{x}^{\alpha} \frac{1}{T} \int_{0}^{T} f^{\alpha} t\left(x_{t}\right) d t,
\end{gathered}
\]

причем ставится вопрос о достаточности класса $\mathfrak{A}_{s}$. Если $\lambda>0$ и борелевская функция $f^{a}(x)$ огравичена, то уравнение (5) для функционала (7) обращается в

\[
\lambda v(x)=\sup _{a \in A(x)}\left[f^{a}(x)+\int_{E} v(y) q(a, x, d y)\right] ;
\]

оно совпадает с уравнением Беллмана для аналогичной задачи с дискретным временем и имеет единственное ограниченное решение. Если супремум в (8) достигается при $a=\alpha(x), \alpha \in \mathfrak{N}_{s}$, то $\alpha$ оптимальна. Точно так же с заменой $\mathfrak{A}_{M}$ на $\mathfrak{A}_{S}$ переносятся на случай фувкционала (6) и результаты о существовании $\varepsilon$-оптимальных стратегий в моделях разных типов. При критерии (7) законченные результаты получены лишь для конечных и специального вида эргодич. у.с.м.п. и подобны случаю дискретного времени: можно выбрать $\alpha \in \mathfrak{N}_{S}$ и функцию $g$ на $E$ так, чтобы $\alpha$ была оптимальна по критерию (2) сразу при всех $T$ и, следовательно, оптимальна по критерию (7).

Управляемый диффузионный процесс (у.д.І.) - непрерывный управляемый случайный процесс в $d$-мерном евклидовом пространстве $E_{d}$, допускающий стохастический дифференциал по отношению к нек-рому винеровскому процессу, не зависящему от управлевий. Теория у.д.п. возникла как обобщение теории управления детерминированным движением нек-рой точки $x_{t} \in E_{d}$, уравнение движения к-рой имеет вид $d x_{t}=$ $=b\left(\alpha_{t}, s+t, x_{t}\right) d t, t \geqslant 0$, где $\alpha_{t}-$ управляющий параметр.


\end{document}