\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
М., 1977; [2] 3 о й т е н д е й к Г., Методы вовможных направлений, пер. с англ., М., $1963 ;$ [3] П ше н и ч н ы й Б. Н., Д ан и ли и ю Ю. М., Численные методы в әкстремалыных задачах, М., 1975 ; 2963 т. $3, N$ ㅇ. 4 , $643-54$.

СРАВНЕНИЕ - соотнотение между целыми числами $a$ и $b$ вида $a=b+m k$, означающее, что их разность $a-b$ делится на заданное целое положительное число $m$, наз. м о д у л е м с р а в не н и я; при этом $a$ наз. вычетом целого числа $b$ по модулю $m$. Для выражения сравнимости чисел $a$ и $b$ по модулю $m$ употребляется символ

\[
a \equiv b(\bmod m)
\]

Если разность $a-b$ не делится на $m$, то $a$ и $b$ наз. несравнимыми по модулю $m$ и для выражения несравнимости $a$ и $b$ употребляется символ

\[
a \neq b(\bmod m) \text {. }
\]

Наличие С. $a \equiv b(\bmod m)$ эквивалентно тому, что $a$ и $b$ имеют одинаковые остатки при делении на $\mathrm{m}$.

Отношение сравнимости по фиксированному модулю $m$ является отношением әквивалентности: оно рефлексивно, т. к. $a \equiv a(\bmod m)$, симметрично, т. к. из $a \equiv$ $\equiv b(\bmod m)$ следует $b \equiv a(\bmod m)$, и транзитивно, т. к. из $a \equiv b(\bmod m)$ и $b \equiv c(\bmod m)$ следует, что $a \equiv c(\bmod m)$.

\includegraphics[max width=\textwidth, center]{2023_07_09_591418b56d7ef62ebae7g-1}
ство всех целых чисел на непересекающиеся классы эквивалентности $A, B, C, \ldots$ Два целых числа лежат в одном и том же классе тогда и только тогда, когда они сравнимы по модулю $m$. Эти классы наз. к л а с с а м и в т ч е т о в по мо д у л ю $m$. Каждое целое число сравнимо по модулю $m$ лишь с одним из чисел 0,1 , ..., m-1; числа $0,1, \ldots, m-1$ лежат в различных классах, поэтому имеется в точности $m$ классов вычетов, а числа $0,1, \ldots, m-1$ образуют множество представителей этих классов. Любое множество из $m$ целых чисел, взятых по одному из каждого класса вычетов, наз. По о по й сй т е м о й вы ч е тов по моД у л ю $m$.

С. с одним и тем же модулем можно складывать, вычитать и перемножать подобно обычным равенствам, т. е. из

$a \equiv b(\bmod m)$ и $c \equiv d(\bmod m)$

следует, что

$a \pm c \equiv b \pm d(\bmod m)$ и $a c \equiv b d(\bmod m)$.

Обе части С. можно умножить на одно и то же целое число, обе части С. можно разделить на их общий делитель, если последний взаимно прост с модулем С. Если же общий делитель числа, на $\kappa$-рое делятся обе части С., и модуля С. $m$ есть $d$, то после деления на $d$ получается С. по модулю $\mathrm{m} / \mathrm{d}$.

Операции сложения, вычитания и умножения $\mathrm{C}$. индуцируют подобные же операции над классами вычетов. Так, если $a$ и $b$ произвольные элементы из классов вычетов $A$ и $B$ соответственно, то $a+b$ всегда лежит в одном и том же классе вычетов, наз. с у м м о ӥ $A+B$ классов $A$ и $B$. Аналогично определяется разность $A-B$ и произведение $A \cdot B$ двух классов вычетов $A$ и $B$. Классы вычетов по модулю $m$ образуют по сложению абелеву группу порядка $m$. Нулевым элементом этой группы является класс, состоящий из всех целых чисел, кратных числу $m$; обратным к классу $A$ является класс $A$, состоящий из всех әлементов класса $A$, взятых со знаком минус. Более того, классы вычетов по модулю $m$ образуют кольцо относительно определенных выше операций сложения, вычитания и умножения.

Пусть $F\left(x_{1}, x_{2}, . ., x_{n}\right)$ - многочлен от $n$ переменных с целыми коәффициентами. Выражение вида

\[
F\left(x_{1}, x_{2}, \ldots, x_{n}\right) \equiv 0(\bmod m)
\]

наз. алгебраическим сравнением. Решением сравнения (\textit{) наз. всякий набор $a_{1}, a_{2}, \ldots, a_{n}$ целых значений неизвестных $x_{1}, x_{2}, \ldots, x_{n}$ такой, что число $F\left(a_{1}, a_{2}\right.$, ..., $\left.a_{n}\right)$ сравнимо с нулем по модулю $m$. Если $a_{i}, 1 \leqslant$ $\leqslant i \leqslant n$, лежат соответственно в классах вычетов $X_{i}$ по модулю $m$, то любой другой набор $a_{i}^{\prime}, \quad 1 \leqslant i \leqslant n$, где $a_{i}^{\prime} \in X_{i}$, также будет решением С. Поэтому принято наз. решением С. (}) также сам набор классов вычетов, представителями к-рых являются $a_{1}, a_{2}, \ldots, a_{n}$, т. е. решение алгебраического уравнения $F\left(x_{1}, x_{2}, \ldots\right.$, ..., $\left.x_{n}\right)=0$ в кольце классов вычетов по модулю $m$. Прй таком соглашении С. (*) будет иметь столько решений, сколько наборов классов вычетов полной системы по модулю $m$ удовлетворяют уравнению $F\left(x_{1}\right.$, $\left.x_{2}, \ldots, x_{n}\right)=0$. Аналогично определяется число решений С. более общего вида, а также систем С.

С. 1-й степени

\[
a x \equiv b(\bmod m)
\]

если $a$ взаимно просто с $m$ (что обозначается $(a, m)=1$ ), имеет единственное решение. Классы вычетов по модулю $m$, элементы к-рых взаимно просты с $m$, образуют абелеву группу по умножению. Единицей этой групшы является класс $E$, содержащий число 1, и обратным к классу $A$ является класс $A^{-1}$, содержащий решение $x$ C.

\[
a x \equiv 1(\bmod m), \text { где } a \in A .
\]

Классы вычетов по модулю $m$, элементы к-рых взаимно просты с $m$, наз. Іі р и в е д е н н ы м и к л а с с ам и, а любая система чисел, взятых по одному из каждого приведенного класса, наз. п р и в е д е н н о й с и с т е м о й в ы ч т о в. Приведенная система вычетов состоит из $\varphi(m)$ әлементов, где $\varphi(m)-$ Әйлера функция, указывающая количество чисел в множестве $1,2, \ldots, m$, взаимно простых с $m$.

Строение мультипликативной группы приведенных классов вычетов по модулю $m$ в значительной степени выясняют теоремы Эйлера и Ферма, показывающие, что порядок любого ее элемента делит величину $\varphi(m)$.

Т е о р е м а Э й л е р а. Если $a$ и $m$ взаимно просты, To

\[
a^{\varphi(m)}=1(\bmod m)
\]

(см. Эйлера теорема).

Частный случай этой теоремы для простого модуля т е о р е м а Ф е р м а: если $p-$ простое число и $a$ не делится на $p$, то

\[
a^{p-1} \equiv 1(\bmod p)
\]

(см. Ферма малая теорема).

Важным понятием для изучения мультипликативной группы приведенных классов вычетов является понятие первообразного корня по модулю $m$. При $(a, m)=1$ существуют положительные целые $\gamma$ с условием $a \vartheta \equiv$ $\equiv 1(\bmod m)$, напр. $\gamma=\varphi(m)$. Наименьшее из них наз. показателем, к-рому принадлежит ч и с л о $a$ по модулю $m$.

Числа, принадлежащие показателю $\varphi(m)$ (если такие существуют), наз. П е р в о о б р а з ны м и к о рн я м И по м о д у л ю $m$. Если $g$ - первообразный корень по модулю $m$ и $\gamma$ пробегает полную систему вычетов по модулю $\varphi(m)$, то $g \vartheta$ пробегает приведенную систему вычетов по модулю $\mathrm{m}$. Таким образом, если $(a, m)=1$, то при нек-ром $\gamma$ из множества $0,1, \ldots$ $\varphi(m)-1$ имеет место С. $a \equiv g \gamma(\bmod m)$. Такое $\gamma$ наз. инд е к с ом чи с ла $a$ п о мод улю $m$ п р и о с н о в а н и и $g$ и обозначается символом ind $a$ или, более точно, ind $g$. Свойства индекса во многом аналогичны свойствам логарифма. Первообразные корни существуют только для модулей $m>1$ вида $2,4, p^{\alpha}$, $2 p^{\alpha}$, где $p \geqslant 3$ - простое число и $\alpha \geqslant 1$ - делое. В әтих случаях мультипликативная группа приведенных клас-


\end{document}