\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
чайные величины $\left(f\left(x_{i}\right)\right.$ - плотность вероятности случайной величины $\left.X_{i}\right)$, то

$\mathrm{P}\left\{R_{i}=k\right\}=\frac{1}{n}, i=1,2, \ldots, n$,

$\mathrm{P}\left\{R_{i}=k, R_{j}=m\right\}=\frac{1}{n(n-1)}, i \neq j, k \neq m$,

$\left.\mathrm{E}\left\{R_{i}\right\}=\frac{n+1}{2}, \mathrm{D}\left\{R_{i}\right\}=\frac{n^{2}-1}{12}, i=1,2, \ldots, n,\right\}$

для любого $k=1,2, \ldots, n$.

При выполнении условия (1) существует совместная плотность вероятности $q\left(x_{i}, k\right), k=1,2, \ldots, n$, случайных величин $X_{i}$ и $R_{i}$, к-рая выражается формулой

\[
\begin{gathered}
q\left(x_{i}, k\right)= \\
=\frac{(n-1) !}{(k-1) !(n-k) !}\left[F\left(x_{i}\right)\right]^{k-1}\left[1-F\left(x_{i}\right)\right]^{n-k} f\left(x_{i}\right),
\end{gathered}
\]

где $F\left(x_{i}\right)$ - функция распределения случайной величины $X_{i}$. Из (2) и (3) следует, что условная плотность вероятности $q\left(x_{i} \mid R_{i}=k\right)$ случайной величины $X_{i}$ при условии, что $R_{i}=k \quad(k=1,2, \ldots, n)$, выражается формулой

\[
\begin{gathered}
q\left(x_{i} \mid R_{i}=k\right)= \\
=\frac{n !}{(k-1) !(n-k) !}\left[F\left(x_{i}\right)\right]^{k-1}\left[1-F\left(x_{i}\right)\right]^{n-k} f\left(x_{i}\right) .
\end{gathered}
\]

Последняя формула позволяет проследить внутреннюю связь, существующую между вектором наблюдений $X$, P. в. $R$ и вектором порядковых статистик $X^{(\cdot)}$, так как (4) есть не что иное, как плотность вероятности $k$-й порядковой статистики $X_{(n k)}, k=1,2, \ldots, n$. Кроме того, из (3) следует, что условное распределение ранга $R_{i}$ выражается формулой

\[
\begin{gathered}
\mathrm{P}\left\{R_{i}=k \mid X_{i}\right\}= \\
=\frac{(n-1) !}{(k-1) !(n-k) !}\left[F\left(X_{i}\right)\right]^{k-1}\left[1-F\left(X_{i}\right)\right]^{n-k} .
\end{gathered}
\]

И, наконец, при допущении о существовании моментов $\mathrm{E}\left\{X_{i}\right\}$ и D $\left\{X_{i}\right\}$ и выполнении (1) из (2) и (3) следует, что коэфффициент корреляции $\rho\left(X_{i}, R_{i}\right)$ между $X_{i}$ и $R_{i}$ равен

$\rho\left(X_{i}, R_{i}\right)=\sqrt{\frac{12(n-1)}{(n+1) D\left\{X_{i}\right\}}} \int_{-\infty}^{\infty} x_{i}\left[F\left(x_{i}\right)-\frac{1}{2}\right] d F\left(x_{i}\right)$.

В частности, если $X_{i}$ подчиняется равномерному распределению на отрезке $[0,1]$, то

\[
\rho\left(X_{i}, R_{i}\right)=\sqrt{\frac{n-1}{n+1}} .
\]

В случае, если $X_{i}$ подчиняется нормальному распределениіо $N\left(a, \sigma^{2}\right)$, то

\[
\rho\left(X_{i}, R_{i}\right)=\sqrt{\frac{3(n-1)}{\pi(n+1)}},
\]

причем $\rho\left(X_{i}, R_{i}\right)$ не зависит от параметров нормального закона.

Jum.: [1] H o e f f d i n g W , в кH : Proc. 2 Berkeley symposium on mathematical statistics and probability, Berkeley - Los Ang., 1951, p. 83-92, [2] $\Gamma$ a е к Я , ШІ и д а к З., Теория ранговых критериев, пер. с англ., М., 1971; [3] Т а р а с е н к о Ф. II.,
Непараметрическая статистика, Томск, 1976. М. С. Никулин.

РАНГОВАЯ СТАТИСТИКА - статистика, построенная по вектору рангов. Если $R=\left(R_{1}, \ldots, R_{n}\right)-$ ранаов вектор, построенный по случайному вектору наблюдений $X=\left(X_{1}, \ldots, X_{n}\right)$, то любая статистика $T=T(R)$, являющаяся функцией от $R$, наз. р а н $\Gamma$ ов о й с т а т и с т и к о й. Классич. пример Р. с. дает

\includegraphics[max width=\textwidth, center]{2023_07_08_02cac97ecdda4d6a1053g-1}
ции К е н а л л а между векторами $R$ и $1=(1$, $1, \ldots, 1)$, к-рый определяется по формуле

\[
\tau=\frac{1}{n(n-1)} \sum_{i \neq j} \operatorname{sign}(i-j) \operatorname{sign}\left(R_{i}-R_{j}\right)
\]

В классе всех Р. с. особое положение занимают т. н. линейные Р. с., к-рые определяются следующим образом. Пусть $A=\|a(i, j)\|$ - произвольная квадратная матрица порядка $n$. Тогда статистика

\[
T=\sum_{i=1}^{n} a\left(i, R_{i}\right)
\]

наз. Л ине й но й $\mathrm{p}$ ан $\mathrm{r} о$ в о й с т а т и с т и кой. Напр., к о эффици инт $\rho$ ра нговой кор е еля ци и Спи рм е а между векторами $R$ и 1 , определяемый по формуле

\[
\rho=\frac{12}{n(n-1)} \sum_{i=1}^{n}\left(i-\frac{n+1}{2}\right)\left(R_{i}-\frac{n+1}{2}\right),
\]

является линейной P.с.

Линейные Р. с., как правило, просто устроены в вычислительном отношении и их распределения вероятностей нетрудно находить. Именно поэтому в теории Р. с. играет большую роль понятие проекции Р. с. в семейство линейных Р. с. Если $T$ - нек-рая Р. с., построенная по случайному вектору $X$, относительно распределения вероятностей к-рого высказана гипотеза $H_{0}$, то п р о е к ц и е й $\widehat{T}=\widehat{T}(R)$ Р. с. $T$ в семейство линейных Р. с. наз. такую линейную Р. с., что $\mathrm{E}\left\{(T-\widehat{T})^{2}\right\}$ минимально при справедливости $H_{0}$. Как правило, проекция $\widehat{T}$ достаточно хорошо аппроксимирует Р. с. $T$, и разность $T-\hat{T}$ пренебрежимо мала, когда $n \rightarrow \infty$. При справедливости гипотезы $H_{0}$, согласно к-рой компоненты $X_{1}, \ldots, X_{n}$ случайного вектора $X$ суть независимые случайные величины, проекция $\hat{T}$ Р. с. $T$ определяется по формуле

\[
\widehat{T}=\frac{n-1}{n} \sum_{i=1}^{n} \hat{a}\left(i, R_{i}\right)-(n-2) \mathrm{E}\{T\},
\]

где $\hat{a}(i, j)=\mathrm{E}\left\{T \mid R_{i}=j\right\}, 1 \leqslant i, j \leqslant n$ (см. [1]).

Существует внутренняя связь между Р. с. $\tau$ и $\rho$. Как показано в [1], при справедливости гипотезы $H_{0}$ проекция $\hat{\tau}$ коэффициента корреляции Кендалла $\tau$ в семейство линейных Р. с. с точностью до постоянного множителя совпадает с коэффициентом ранговой корреляции Спирмена $\rho$, а именно:

\[
\hat{\tau}=\frac{2}{3}\left(1+\frac{1}{n}\right) \rho \text {. }
\]

Из этого равенства следует, что коәффициент корреляции $\operatorname{corr}(\rho, \tau)$ между $\rho$ и $\tau$ равен

\[
\operatorname{corr}(\rho, \tau)=\sqrt{\frac{D \hat{\tau}}{D \tau}}=\frac{2(n+1)}{\sqrt{2 n(2 n+5)}},
\]

т. е. при больших $n$ P. c. $\rho$ и $\tau$ асимптотически эквивалентны (см. [2]).

Лum.: [1] $\Gamma$ а е к Я., Ш и д а к 3., Теория ранговых нритеpиев, пер. с aнгJI., M., 1971; [2] K e n d a 11 M. G., Rank correlation methods, 4 ed., L., 1970.

РАНГОВЫЙ КРИТЕРИЙ - статистический крітерий, основанный на ранговой статистике. Напр., критерии Вилкоксона и Манна - Уитни являіотся ранговыми. Р. к. инвариантны относительно группы $G$ всех преобразований, задаваемых непрерывными строго возрастаюгции функциями и, следовательно, являются инвариантными относительно изменений параметров сдвига и масштаба. Кроме того, во многих задачах статистич. проверки гипотез наиболее мощные инвариантные критерии хорошо аппроксимируются Р. к.

См. также Непараметрический критерий.

Лит.: [1] Г а е к Я., ІІ и д а к 3 , Теория ранговых критериев, пер. с англ., М., 19̀71; [2] Ј̈ м м а н Э., Проверка статистических гипотез, пер. с англ., 2 изд , М., 1979. M C. Никулињ.

РАНДОМИЗАЦИИ КРИТЕРИИ, п е р е с т а н ов о к к р и т е р и й,- статистический критерий, предназначенный для проверки гишотезы о симметричности


\end{document}