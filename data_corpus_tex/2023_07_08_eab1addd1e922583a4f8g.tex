\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
Лебегов подход к определению Р. (в отличие от индуктивного подхода) позволяет в случае любых рассматриваемых пространств геометризовать понятие $\mathrm{P}$. посредством сравнения исходного топологич. пространства с простейшими геометрич. образованиями - полиәдрами. Грубо говоря, пространство $n$-мерно тогда и только тогда, когда оно сколь угодно мало отличается от $n$-мерного полиэдра. Точнее, имеет место т е о р е м а А л е к с а д д р в а об $\omega-0$ т о б р а же н и я х: тогда и только тогда $\operatorname{dim} X<n$, когда для любого конечного открытого покрытия $\omega$ пространства $X$ существует $\omega$-отображение пространства $X$ на не более чем $n$-мерный, $n=0,1,2, \ldots$ (компактный) полиэдр. Особую наглядность сформулированная теорема приобретает в случае компактов: для компакта $X$ тогда и только тогда $\operatorname{dim} X \leqslant n$, когда для любого $\varepsilon>0$ существует є-отображение комнакта на не более чем $n$-мерный полиэдр. Если еще $X$ лежит в евклидовом или гиль-

\includegraphics[max width=\textwidth, center]{2023_07_08_eab1addd1e922583a4f8g-1}
менить $\varepsilon$-сдвигом (т е о р е м а А ле к с а нд р о в а об $\varepsilon-0$ т об р а жения и и $\varepsilon-c$ д в и г а х).

Следующее утверждение позволяет выяснить, какова P. пространства, посредством его сравнения со всевозможными $n$-мерными кубами: тогда и только тогда $\operatorname{dim} X \geqslant n$, когда пространство обладает существенным отображением на $n$-мерный куб, $n=0,1,2, \ldots$ (т е орема А лександ рова осуществен ных о тоб р а ен ия х).

Этой теореме можно придать следующую форму. Тогда и только тогда $\operatorname{dim} X \leqslant n$, когда для любого замкнутого в $X$ множества $A$ и любого нешрерывного отображения $f: A \rightarrow S^{n}$ в $n$-мерную сферу существует непрерывное шродолжение $F: X \rightarrow S^{n}, \quad n=0,1,$. , отображения $f$.

Следующая характеристика Р. указывает на роль этого понятия в вопросах существования решений систем уравнений: тогда и только тогда $\operatorname{dim} X \geqslant n, n=$ $=1,2, \ldots$, когда в $X$ существует такая система дизъюнктных пар замкнутых множеств $A_{i}, B_{i}, i=1, \ldots, n$, что для любых нешрерывных на $X$ функций $f_{i}$, удовлетворяющих условию $\left.f_{i}\right|_{A_{i}}>0,\left.f_{i}\right|_{B_{i}}<0, i=1, \ldots, n$, найдется точка $x \in X$, в к-рой $f_{i}(x)=0, i=1, \ldots, n($ т е орем а Отто-Эйлен бе р га-Хеммингсен a п е р е го род к а х).

Одно из важнейших свойств Р. выражает т е о р е м а с уммы если пространство $X$ есть конечная или счетная сумма своих замкнутых подмножеств размерности $\leqslant n$, то и $\operatorname{dim} X \leqslant n, n=0,1, \ldots$ В этой теореме мокно условие конечности или счетности суммы заменить условием ее локальной конечности. Аналогичное теореме суммы утверждение для большой и малой индуктивных P. не выполняется уже в классе бикомпактов. Следуюшие утверждения принадлежат к числу основных обџих фактов теории Р. и позволяют сводить рассмотрение любых пространств к рассмотрению бикомгактов. Для любого нормального Іоространства

a) $\operatorname{dim} \beta X=\operatorname{dim} X$, Ind $\beta X=\operatorname{Ind} X$, где $\beta X-$ максимальное бикомпактное расширение Стоуна -. Чеха пространства $X$; в то же время неравенство ind $\beta X>$ $>$ ind $X=$ Ind $X$ возможно;

б) существует бикомпактное распирение $b X$ пространства $X$, вес к-рого $w(b X)$ равен весу $w X$, и размерность $\operatorname{dim} b X$ равна размерности $\operatorname{dim} X$; аналогичное утверждение верно и для большой индуктивной $\mathrm{P}$. Особенно интересен случай счетного веса пространства, т. к. в этом случае распирение $b X$ метризуемо.

Утверждение б) может быть усилено слөдующим предложением: для любого $n=0,1, \ldots$ и любой бесконечной мощности существует бикомпакт $\prod_{\tau}^{n}$ веса $\tau$ и размерности $\operatorname{dim} \prod_{\tau}^{n}=n$, содержащий гомеоморфный образ любого нормального пространства $X$ веса $\leqslant \tau$ и размерности $\operatorname{dim} X \leqslant n$ (теорема об универсальном бикомпакте данного веса и размерности). Аналог ичное утверждение верно и для большой индуктивной Р. При этом в качестве $\prod_{\aleph_{0}}^{0}$ можно взять канторово совершенное множество, а в качестве $\prod_{\aleph_{0}}^{1}$ - менгеровскую универсальную кривую.

Казалось бы, что Р. должна обладать свойством монотонности: $\operatorname{dim} A \leqslant \operatorname{dim} X$, если $A \subset X$. Это так, если a) множество $A$ замкнуто в $X$ или сильно паракомпактно, или б) пространство $X$ метризуемо (и даже совершенно нормально). Однако уже для подмножества $A$ наследственно нормального пространства $X$ может быть $\operatorname{dim} A>\operatorname{dim} X$ и Ind $A>$ Ind $X$. Но всегда ind $A \leqslant$ sind $X$ при $A \subset X$.

Одним из важнейших вопросов теории $\mathrm{P}$. является поведение Р. при непрерывных отображениях. В слуqае замкнутых отображений (к ним принадлежат и все непрерывные отображения бикомшактов) ответ дается формулами B. Гуревича (W. Hurewicz), полученными им первоначально в классе пространств со счетной базой.

Ф о рмула щх ихамерность отображений: если отображение $f: X \rightarrow Y$ непрерывно и замкнуто, то

\[
\text { кратность } f+\operatorname{dim} X-1 \geqslant \operatorname{dim} Y \text {, }
\]

где кратность $f=\sup \left\{\left|f^{-1} y\right|: y \in Y\right\}$.

Формула Гуревича для понижа ющи и азме рность отображен ий: для непрерывного замкнутого отображения $f: X \rightarrow Y$ на паракомпакт $Y$ выполняется неравенство

\[
\operatorname{dim} X-\operatorname{dim} f \leqslant \operatorname{dim} Y
\]

где

\[
\operatorname{dim} f=\sup \left\{\operatorname{dim} f^{-1} y: y \in Y\right\} .
\]

Для произвольного нормального пространства $Y$ эта формула, вообе говоря, неверна.

В случае непрерывных отображений конечномерных компактов установлено, что непрерывное отображение $f$ размерности $\operatorname{dim} f=k$ является суперпозицией $k$ непрерывных отображений размерности 1 (это - уточнение формулы (1) и аналог того факта, что $k$-мерный куб есть произведение $k$ отрезков).

В случае открытых отображений можно показать, что образ нульмерного бикомпакта нульмерен и в то же время гильбертов кирпич есть образ одномерного компакта, даже если соответствующее отображение ! имеет размерность $\operatorname{dim} f$, равную нулю. Однако в случае открытого отображения $f: X \rightarrow Y$ бикомпактов $X$ и $Y$ кратности $\leqslant \aleph_{0}$ выполняется равенство $\operatorname{dim} X=$ $=\operatorname{dim} Y$.

Поведение Р. при взятии топологич. произведения оиисывают следующис утверждения:

a) существуют такие конечномерные компакты $X$ и $Y$, что $\operatorname{dim} X \times Y<\operatorname{dim} X+\operatorname{dim} Y$;

б) если оцин из сомножителей произведения $X \times Y$ бикомпактен или метризуем, то $\operatorname{dim} X \times Y \leqslant \operatorname{dim} X+$ $+\operatorname{dim} Y$

в) существуют такие нормальные пространства $X$ и $Y$, что $\operatorname{dim} X \times Y>\operatorname{dim} X+\operatorname{dim} Y$.

В случае бикомпактных $X$ и $Y$ всегда Ind $X \times Y<\infty$, если Ind $X<\infty$ и Ind $Y<\infty$, но может быть Ind $X \times Y>$ Ind $X+\operatorname{Ind} Y$. Если же бикомпакты $X$ и $Y$ совертенно нормальны или одномерны, то Ind $X \times Y \leqslant \operatorname{Ind} X+$ + Ind $Y$.

Наиболее содержательна теория Р. прежде всего в классе метрич. пространств со счетной базой и затем


\end{document}