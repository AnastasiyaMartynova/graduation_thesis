\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\DeclareUnicodeCharacter{0131}{$\imath$}

\begin{document}
рабатываюгая натуральные числа в натуральные) наз. э л е м е н т о м по т о к а П, символически $\alpha \in \Pi$, если для всякого $n$ кортеж $\langle\alpha(0), \ldots, \alpha(n-1)\rangle$ является узлом П. В приложения х встречается также понятие оснащенного П. $\mathrm{O}$ с н а щ $е$ п ны й по т о к $Г$ состоит из потока П и әффективного правила $\Gamma_{\Pi}$ (т. н. Д о п о лн и т е льно го за к о на П.), приписывающего кажкдому узлу $\pi$ потока П нек-рый объект $\Gamma_{\mathrm{II}}(\pi)$. Каждому элементу потока II при этом естественным образом соответствует последовательность объектов, задаваемых законом $\Gamma_{\Pi}$.

В языке формального интуиционистского математич. анализа П. задается функцией - свонм законом П. С этой цельо рассматривается стандартное примитивно рекурсивное взаимно однозначное соответствие меясду кортежами натуральных чисел и натуральными числами. Пусть при этом кортежу $く>$ соответствует 0 , операция соединения двух кортежей в один задается на их номерах примитивно рекурсивной функцией $x * y, \hat{x}$ означает номер кортежа с единственным членом $x$. У тверждение, что кортеж с номером $x$ является узлом П., задаваемого $a$, записывается в виде $a(x)=0$. Тогда формула $\operatorname{Spr}(a)$, выражаюшая понятие «функция $a$ задает П.», записывается в виде

\[
\begin{gathered}
a(0)=0 \& \forall x y(a(x * y)=0 \supset a(x)=0) \& \\
\forall x(a(x)=0 \supset \exists y(a(x * \widehat{y})=0)) .
\end{gathered}
\]

Наконец, если через $\bar{\alpha}(n)$ обозначить номер кортежа $\langle\alpha(0), \ldots, \alpha(n-1)\rangle$, где $n-$ длина кортежа, то формула $\alpha \in a$ (« $\alpha$ есть элемент П.,заданного $a »)$ записывае'ся в виде $\forall x a(\bar{\alpha}(x))=0$.

В основаниях математики употребляются такле обобщепия понятия П., в к-рых используются кортежи не натуральных чисел, а более слонных объектов, напр. кортежи, составленные из свободно становящихся последовательностей

Лum.: [1] Г е й т и н $\boldsymbol{\Gamma}$ А., Интуиционизм. Внедение, пер. с англ., М., $1965 . \quad$ А. Г. Драгалин.

ПОТОК В СЕТИ - функция, сопоставляющая дугам данной сети (ориентированного графа) нек-рые числа. Каждое число интерпретируется как интенсивность потока нек-рого груза по данной дуге. П. в с. являются удобной моделью при исследовании ряда ироблем в транспорте, связи и др. областях, связапных с движением грузов, информации и т. д. Многие задачи о потоках являются задатами линейного программирования и могут решаться обцции методами этой теории. Однако в большинстве случаев задачи о нотоках цопускают әффективное решение методами теории графов.

Пусть каждой дуге $(x, y)$ сети $N$ приписано неотрицательное действительное число $c(x, y)$ - пропускная способность дуги $(x, y)$. Говорят, что поток $f(x, y)$ является стационарным потоком величины $v$ из вершины $r$ в вершину $s$, удовлетворяющшм ограничениям пропускных способностей дуг, если

\[
f^{-}(r)-f^{+}(r)=v,
\]

$f^{-}(x)-f^{+}(x)=0$ при $x \neq r, s$, $f^{-}(s)-f^{+}(s)=-v$

$0 \leqslant f(x, y) \leqslant c(x, y)$ для любой дуги $(x, y)$,

здесь $f-(x)=\sum_{y} f(x, y)$-поток, выходящий из вершины $x, \mathbf{a} f^{+}(x)=\sum_{y} f(y, x)$ - поток, входяций в вершину $x$. В $а$ а даче о максимальном потоке между двумя вершинами требуется построить стационарный поток из вершины $r$ в вершину $s$, имеющий максимально возможную величину $v$. Для решения этой задачи существуют достаточно эффективные алгоритмы. Пусть $X-$ подмножество вершин сети $N$ такое, что $r \in X, s \notin X$. Тогда множество дуг $(x, y)$ таких, что $x \in X$, $y \notin X$, ваз. р а 3 р е з о м. Пропускной способностью разреза ваз. величина $\sum_{x \in X, y \notin X} c(x, y)$. Справедлива следующая т е о р м а о м а к с и м а л ь ном потоке и минима льном ра з р е е е: максимальная величина потока равна минимальной пропускной способности разрезов. В приложениях часто используется т е о р м а $о$ це ло ч и с ле н н ос т и: если пропускная способность дуг целочисленна, то существует целочисленный максимальный (стационарный) поток.

К задаче о максимальном потоке между двумя вершинами сводится ряд задач: задача о максимальном II. в с. с несколькими источниками и стокамı; задача о максимальном П. в с., имеющей неотрицательные ограничения на потоки по дугам как сверху, так и снизу; задача о максимальном потоке в неориентированных и смешаншых сетях; задача о максимальном потоке в сети с пропускными способностями дуг и вершин и др.

Теорема о максимальном потоке и минимальном разрезе выявила общую основу ряда результатов, полученных ранее в теории графов и комбинаторике. Оказалось, что как следствия этой теоремы могут быть получены: теорема о максимальном паросочетании в грабe двудольном, теорема о различных представителях теоремы о $k$-связности графов (см. Граба связность), теорема о покрытии частично упорядоченного множества наименыпим числом цепей и др. Сведе́ние различных задач к задаче о максимальном потоке является важным методом теории графов и комбинаторики.

В ряде задач о П. в с. каждой дуге $(x, y)$ сопоставляется число $a(x, y)$ - стоимость перевозки единицы груза ІІо дуге $(x, y)$ и требуется найти поток, удовлетворяющий оиределенным ограничения м и минимизирующий общую стоимость потока. 3 а д а ч а о по т о к е м ин и м а л ь о о й с т о и м о с т и состоит в нахождении стационарного Іотока из вершины $r$ в вершину $s$, удовлетворяющего ограничениям пропускных способностей дуг, причем такого, что величина его равна заданному числу $v$, а стоимость минимальна.

В г р а н с п о р тн о й 3 а д а ч е сеть является двудольным графом. Вершины одной доли $S_{1}, \ldots, S_{m}$ интерпретируются как пункты отіравления нек-рого груза, вершины другой $T_{1}, \ldots, T_{n}$ - как пункты назначения. Каждый пункт отиравления $S_{i}$ имеет олределенное предложение $b_{i}$, и каждый пункт назначения $T_{j}$ имеет определенный спрос $c_{j}$. Известна стоимость $a_{i j}$ перевозки единицы груза из $S_{i}$ в $T_{j}$. Задача состоит в отыскании потока минимальной стоимости, удовлетворяющего спрос во всех пунктах назначения.

Рассматриваются также многопродуктовые потоки и потоки, изменяюгиеся во времени.

\includegraphics[max width=\textwidth, center]{2023_07_08_58357a75f189188d3b0cg-1}
ПОТОЧЕЧНАЯ СХОДИМОСТЬ - один из видов сходимости последовательности функций (отображений). Пусть $f_{n}: X \rightarrow Y, n=1,2, \ldots$, где $X$ - нек-рое множество, а $Y$ - топологич. пространство, тогда П. с. означает, что для любого элемента $x \in X$ последовательность точек $y_{n}=f_{n}(x), n=1,2, \ldots$, сходится в пространстве $Y$. Важным подклассом класса поточечно сходящихся последовательностей являются равномерно сходящиеся последовательности. Л. Д. Кудрявцев.

ПОТОЧЕЧНОЙ СХОДИМОСТИ ТОПОЛОГИЯ одна из топологий пространства $F(X, Y)$ отображений множества $X$ в топологич. пространство $Y$. Направление $\left\{f_{\alpha}\right\}_{\alpha \in \Re} \subset F(X, Y)$ поточечно сходится к $f \in F(X, Y)$, если $\left\{f_{\alpha}(x)\right\}_{\alpha \in \text { श }}$ сходится при любом $x \in X$ к $f(x)$ в топологии пространства $Y$. Базу окрестностей точки $f_{0} \in F(X$, $Y)$ образугт множества вида $\left\{f \mid f\left(x_{i}\right) \subset v_{f_{0}\left(x_{i}\right)}, i=1, \ldots\right.$ $\ldots, n\}$, где $x_{1}, \ldots, x_{n}$ - конечный набор точек из $X$ и


\end{document}