\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}

\begin{document}
ПОЛОС МЕТОД - метод приближенного решения одномерных интегральных ураннений Фредгольма 2-го рода, основанный на специальном способе замены ядра на вырожденное, на получении резольвенты вырожденного уравнения и последующем уточнении приближенного решения с помощью быстросходящегося итеративного алгоритма.

Пусть исходное интегральное уравнение записано в виде

\[
\varphi(x)-\lambda \int_{a}^{b} K(x, s) \varphi(s) d s=f(x) .
\]

Для построения вырожденного ядра в П. м. квадрат

\[
\{a \leqslant x \leqslant b, a \leqslant s \leqslant b\}
\]

разбивается на $N$ полос

\[
\begin{gathered}
\left\{\frac{b-a}{N} i \leqslant x \leqslant \frac{b-a}{N}(i+1), a \leqslant s \leqslant b\right\}, \\
i=0,1, \ldots, N-1 .
\end{gathered}
\]

В каждой полосе $(i)$ функция $K(x, s)$ приближается в среднем квадратическом или равномерно функцией вида

\[
K_{i}(x, s)=C_{i}(x)+P_{i}(x) Q_{i}(s) .
\]

В простейшем случае

\[
K_{i}(x, s)=K\left(\xi_{i}, s\right), \xi_{i} \in\left[\frac{b-a}{N} i, \frac{b-a}{N}(i+1)\right]
\]

При помощи функции $K_{i}(x, s)$ можно образовать вырожденное ядро:

\[
\begin{aligned}
& K_{N}(x, s)=\sum_{i=0}^{N-1} \hat{C}_{i}(x)+\hat{P}_{i}(x) Q_{i}(s), \\
& \hat{p}_{i}(x)= \begin{cases}P_{i}(x), & x \in\left[\frac{b-a}{N} i, \frac{b-a}{N}(i+1)\right] \\
0, & x \notin\left[\frac{b-a}{N} i, \frac{b-a}{N}(i+1)\right] .\end{cases} \\
& \hat{C}_{i}(x)= \begin{cases}C_{i}(x), & x \in\left[\frac{b-a}{N} i, \frac{b-a}{N}(i-1)\right] \\
0, & x \notin\left[\frac{b-a}{N} i, \frac{b-a}{N}(i+1)\right] .\end{cases}
\end{aligned}
\]

Решение уравнения с вырожденным ядром (2) аппроксимирует решение (1), вообще говоря, тем лучше, чем больше число полос $N$ и чем лучше аппроксимация $K(x, s)$ в каждой полосе. Приближенное решение $\varphi_{0}(x)$ можно еще улучшить с помощью итөративного алгоритма:

\[
\begin{gathered}
\varphi_{k}(x)-\lambda \int_{a}^{b} K_{N}(x, s) \varphi_{k}(s) d s= \\
=f(x)+\lambda \int_{a}^{b}\left[K(x, s)-K_{N}(x, s)\right] \varphi_{k-1}(s) d s .
\end{gathered}
\]

Итерация (3) сходится к решению (1) в среднем квадратическом или равномерно при условии достаточной близости ядер $K(x, s)$ и $K_{N}(x, s)$.

Лит.: [1] П о ло жи й Г. Н., Ч а л е н к о П. И., "Доповіді АН УРСР», 1962, Nㅏ 4, с. 427-31.

ПОЛОСА - совокупность точек А. Б. Бажушинский. щих между двумя параллельными прямыми әтой плоскости. Координаты точек $x, y$ полосы удовлетворяют неравенствам $C_{1}<A x+B y<C_{2}$, где $A, B, C_{1}, C_{2}-$ нек-рые постоянные, причем $A$ и $B$ одновременно не равны нулю. Преобразование $w=e^{z}$ конформно отображает полосу $0<y<\pi$ комплексной плоскости $z=x+i y$ на верхнюю полуплоскость комплексной $\begin{array}{ll}\text { плоскости } \boldsymbol{w} & \boldsymbol{C} \text { Э- }\end{array}$

ПОЛI0СА, по в е р х нос т на я по ло с а (в узком смысле), однопараметрическое семейство касательных плоскостей к поверхности. В общем смысле п о л о с о й наз. объединение кривой $l$ и вектора $m$, ортогонального в каждой точке кривой ее касатөльному вектору. Пусть в евклидовом пространстве $\mathbb{R}_{3}$ кривая $l$ задана уравнением $\boldsymbol{r}=\boldsymbol{r}(s)$, где $s-$ өстөственный параметр кривой, $\boldsymbol{r}(s)$ - радиус-вектор точки кривой. Вдоль $l$ задается вектор-функция $\boldsymbol{m}=\boldsymbol{m}(s)$, где $\boldsymbol{m}(s)-$ единичный вектор, ортогональный касательному вектору $\boldsymbol{t}=d \boldsymbol{r} / d s$ в соответствующих точках кривой. В этом случае говорят, что вдоль кривой $l$ задана поверхностная полоса $\Phi=\{l, \boldsymbol{m}\}$ с нормалью $\boldsymbol{m}(s)$. Вектор $\boldsymbol{\tau}=$ $=[m, t]$ наз. В е к т о р о м ге о де 3 и ч е с к о й н о р м а л и полосы Ф; вместе с векторами $t$ и $m$ вектор т образует трехгранник Френө для П. Относвтельно подвижного трехгранника Френе для П. записываются деривационные формулы Френе:

\[
\begin{gathered}
\frac{d t}{d s}=k_{g} \tau+k_{n} m ; \quad \frac{d \tau}{d s}=-k_{g} t+x_{g} m ; \\
\frac{d m}{d s}=-k_{n} t+x_{g} \tau,
\end{gathered}
\]

где $k_{g}$ (геодезич. кривизна П.), $k_{n}(s)$ (нормальная кривизна П.), $x_{g}(s)$ - (геодезич. кручение П.) - скалярные функции параметра $s$.

Если в каждой точке кривой $l$ вектор $\boldsymbol{m}$ колливеарен вектору главной нормали кривой $l$, то $k_{g}=0$ и в этом Если в каждой точке вектор $m$ коллинеарөн бинормали кривой, то в этом случае $k_{n}=0$, а П. наз. а с и м п т от ич ч с к о й по ло с о й.

Лum.: [1] Б л я ш к е В., Введение в дифференциальную ПОЛУГЕОДЕЗИЧЕСКИЕ КООРДИНАТЫ, $г$ е одезиче ские нормальны е ко о р пин аты,- координаты $x^{1}, \ldots, x^{n}$ в $n$-мерном римановом пространстве, характеризующиеся тем, что координатные линии, соответствующие $x^{1}$, являются геодезич, линиями, на к-рых $x^{1}$ шграет роль нормального параметра, а координатные поверхности $x^{\mathrm{1}}=\mathrm{const}-$ ортогональны этим геодезическим. В П. к. квадрат линейного элемента имеөт вид

\[
d s^{2}=\left(d x^{1}\right)^{2}+\sum_{i, j=2}^{n} g_{i j} d x^{i} d x^{j} .
\]

П. к. можно ввести в достаточно малой окрестности любой точки произвольного риманова пространства. В двумерных римановых пространствах в ряде случаөв (напр., для регулярных поверхностей строго отрицательной кривизны) возможно введение П. к. в целом.

В двумерном случаө квадрат линейного элемента в П. к. принято записывать в виде

\[
d s^{2}=d u^{2}+B^{2}(u, v) d v^{2}
\]

Полная (гауссова) кривизна может быть найдена по формуле:

\[
K=-\frac{1}{B} \frac{\partial^{2} B}{\partial u^{2}} .
\]

При изучении двумерных римановых многообразий знакоопределенной кривизны важную роль играют специальные П. к.- г е о д е з и ч е с к и е по л я рны е к о о р ди н а ты $(r, \varphi)$. В этом случае все координатные геодезич. линии $\varphi=$ const пересекаются в одной точке (п о л ю с е), а $\varphi$ является углом между координатными линиями $v=0$ и $\varphi=$ const. Линия $r=$ = const наз. ге о д е 3 ич е с ко й ок у у ж вс т ю ю. Квадрат ливейного элемента в окрествости полюса в геодезических полярных координатах имеет вид

\[
\begin{gathered}
d s^{2}=d r^{2}+r^{2}\left\{1-\frac{K_{0}}{3} r^{2}-\right. \\
\left.-\frac{1}{\hbar}\left(K_{1} \cos \varphi+K_{2} \sin \varphi\right) r^{3}+o\left(r^{3}\right)\right\} d \varphi^{2},
\end{gathered}
\]


\end{document}