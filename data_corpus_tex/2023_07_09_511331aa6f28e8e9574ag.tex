\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }
\usepackage{CJKutf8}
\usepackage{mathrsfs}

\begin{document}
ТОРИЧЕСКИЙ УЗЕЛ т и п а $(p, q)$ - кривая в $\mathbb{R}^{3}$, к-рая в цилиндрич. коордиватах $r, z, \theta$ задается уравнениями

\[
r=2+\cos t, z=\sin t, \theta=p t / q,
\]

где $t \in[0,2 \pi q]$. Здесь $p$ и $q-$ взаимно простые натуральные числа. Т. у. лежит на поверхности незаузленного тора $(r-2)^{2}+z^{2}=1$, пересекая меридианы тора в $p$ точках, а параллели - в $q$ точках. Т. у. типов $(p, 1)$ и $(1, q)$ тривиальны. Простейший нетривиальный Т. у. т р и л и с тви к (см. рис. 1), имеющий тип $(2,3)$. Группа Т. У. типа $(p, q)$ имеет копредставление $\left|a, b: a^{p}=b^{q}\right|$, а многочлен Александера равен

\[
\left(t^{p q}-1\right)(t-1)\left(t^{p}-1\right)^{-1}\left(t^{q}-1\right)^{-1} .
\]

Все Т. у. являются Нейвирта узлами. Род Т. у. равен $(p-1)(q-1) / 2$.

Другая конструкция Т. у. использует особенность в нуле алгебраич. гиперповерхности

\[
V=\left\{\left(z_{1}, z_{2}\right) \in \mathbb{C}^{2}, z_{1}^{p}+z_{2}^{q}=0\right\} .
\]

Если $p$ и $q$ взаимно просты, то пересечение $V$ с достаточно малой сферой $S^{3} \subset \mathbb{C}^{2}$ является узлом в $S^{3}$, эквивалент-

\begin{center}
\includegraphics[max width=\textwidth]{2023_07_09_511331aa6f28e8e9574ag-1(1)}
\end{center}

Puc. 1.

\begin{center}
\includegraphics[max width=\textwidth]{2023_07_09_511331aa6f28e8e9574ag-1}
\end{center}

Рис. 2. ным Т. У. типа $(p, q)$. В случае, когда $p$ и $q$ не взаимно просты, это пересечение также лежит на незаузленном торе $T^{2} \subset S^{3}$, но состоит из нескольких компонент. Получающееся зацепление наз. т о р и ч е с к и м 3 ац е п л е ви ем т и п а $(p, q)$ (см. рис. 2 , где $p=3$, $q=6)$.

Лum.: [1] К р о у э л л Р $\mathrm{P}_{\dot{\prime}}$ Ф о к с Р., Введение в теорию узлов, пер. с англ., М., 1967; [2] М и л н о р Д ж., Особые точки комплексных гиперповерхностей, пер. с англ., М., 1971.

ТОРОИДАЛЬНАЯ ГАРМОНИКА - функция точки на торе, появляющаяся при решении уравнения Лапласа методом разделения переменных в тороидальных координатах $(\sigma, \tau, \varphi)$. Гармонич. функция $h=h(\sigma, \tau, \varphi)$, являющаяся решением уравнения Лапласа, записывается в виде ряда

\[
\begin{gathered}
h=\sqrt{\operatorname{ch} \tau-\cos \sigma} \sum_{j, k=0}^{\infty}\left[A_{j k} P_{j-1 / 2}^{(k)}(\operatorname{ch} \tau)+\right. \\
\left.+B_{j k} Q_{j-1 / 2}^{(k)}(\operatorname{ch} \tau)\right]\left(a_{k} \cos k \sigma+b_{k} \sin k \sigma\right) \times \\
\times\left(c_{j} \cos j \varphi+d_{j} \sin j \varphi\right),
\end{gathered}
\]

где $P_{j-1 / 2}^{(k)}, Q_{j-1 / 2}^{(k)}-$ присоединенные функции Лежандра с полуцелым индексом. Полагая здесь $\tau=\tau_{0}$, получают Т. г., или, иначе, по о в е р х н о с т н у ю Т. Г., в отличие от членов ряда (*), зависящих от трех переменных $(\sigma, \tau, \varphi)$, к-рые иногда наз. П р о с т р а н с т в е нн ы м и Т. Г.

Ряд (*) используется при решении краевых задач в тороидальных координатах с учетом разложения

\[
\frac{1}{\sqrt{\operatorname{ch} \tau-\cos \sigma}}=\frac{\sqrt{2}}{\pi} \sum_{k=0}^{\infty} Q_{k-1 / 2}(\operatorname{ch} \tau) \cos k \sigma
\]

где $Q_{k-1 / 2}-$ функции Лежандра 2-го рода.

Лum.: [1] Т их о нов А. Н., С а ма р с к и й А. А. Уравнения математической физики, 5 изд., М., 1977; [2] М о р с Ф. М., Ф е ш б а х Г., Методы теоретической физики, пер. с англ., т. 1-2, М., 1960 . Е. Д. Соломенцев. ТОРОИДАЛЬНЫЕ КООРДИНАТЫ - числа $\sigma, \tau$ и $\varphi$, связанные с декартовыми прямоугольными координатами $x, y$ и $z$ формулами:

\[
\begin{gathered}
x=\frac{a \operatorname{sh} \tau}{\operatorname{ch} \tau-\cos \sigma} \cos \varphi, y=\frac{a \operatorname{sh} \tau}{\operatorname{ch} \tau-\cos \sigma} \sin \varphi, \\
z=\frac{a \sin \sigma}{\operatorname{ch} \tau-\cos \sigma},
\end{gathered}
\]

где $-\pi \leqslant \sigma \leqslant \pi, \quad 0 \leqslant \tau<\infty, \quad 0 \leqslant \varphi<2 \pi$. Координатные поверхности: $\sigma=\mathrm{const}-$ сферы с центром $(0,0$, $a \operatorname{ctg} \sigma)$ радиуса $a /|\sin \sigma| ; \tau=$ const - торы с осевой окружностью в плоскости $O x y$, центром в начале координат и радиусом $a$ cth $\tau$ и окружностью поперечного сечения радиуса $a /$ sh $\tau, \quad \varphi=$ const - полуплоскости $y / x=\operatorname{tg} \varphi$. Система Т. к.- ортогональная.

Коэфффициенты Ламе:

\[
\begin{gathered}
L_{\sigma}=L_{\tau}=\frac{a^{2}}{(\operatorname{ch} \tau-\cos \sigma)^{2}}, \\
L_{\varphi}=\frac{a^{2} \operatorname{sh}^{2} \tau}{(\operatorname{ch} \tau-\cos \sigma)^{2}} .
\end{gathered}
\]

Оператор Лапласа:

\[
\begin{gathered}
\Delta f=\frac{(\operatorname{ch} \tau-\cos \sigma)^{3}}{a^{2} \operatorname{sh} \tau}\left[\frac{\partial}{\partial \sigma}\left(\frac{\operatorname{sh} \tau}{\operatorname{ch} \tau-\cos \sigma} \frac{\partial f}{\partial \sigma}\right)+\right. \\
\left.+\frac{\partial}{\partial \tau}\left(\frac{\operatorname{sh} \tau}{\operatorname{ch} \tau-\cos \sigma} \frac{\partial f}{\partial \tau}\right)+\frac{1}{\operatorname{sh} \tau(\operatorname{ch} \tau-\cos \sigma)} \frac{\partial^{2} f}{\partial \varphi^{2}}\right] .
\end{gathered}
\]

Д. Д. Соколов.

TOPC - то же, что развертывающаяся поверхность

TОТАЛЬНОЕ МНОЖЕСТВО - множество $\Sigma$ линейных функционалов на векторном пространстве $E$, разделяющее точки $E$, т. е. такое, что для любого ненулевого вектора $x \in E$ найдется функционал $f \in \Sigma$ с $f(x) \neq 0$.

ТОчЕчНА ОІІеНКА значения к-рой сут точи во множестве значений оце ниваемой величины.

Пус'ьь по реализации $x=\left(x_{1}, x_{2}, \ldots, x_{n}\right)^{\top}$ случаїного вектора $X=\left(X_{1}, X_{2}, \ldots, X_{n}\right)^{\top}, \quad$ принимающего значения в выборочном пространстве (\begin{CJK}{UTF8}{mj}犬\end{CJK}, $\left.\mathscr{B}, P_{\theta}\right)$, $\theta=\left(\theta_{1}, \theta_{2}, \ldots, \theta_{k}\right)^{\top} \in \Theta \subset \mathbb{R} k$, надлежит оценить геизвестный параметр $\theta$ (или нек-рую функцию $g(\theta)$ ). Тогда лгобая статистика $T_{n}=T_{n}(X)$, осуществляющая отображение множества $\mathfrak{X}$ в $\Theta$ (или в множество значений функции $g(\theta))$, наз. т о ч е ч н о й о це н к о й параметра $\theta$ (оцениваемой функции $g(\theta)$ ). Важными характеристиками T. o. $T_{n}$ являются ее математич. ожидание

\[
\mathrm{E}_{\vartheta}\left\{T_{n}\right\}=\int_{\mathfrak{X}} T_{n}(x) d P_{\theta}(x)
\]

и дисперсионная матрица (ковариационная матрица)

\[
\mathrm{E}_{\theta}\left\{\left(T_{n}-\mathrm{E}\left\{T_{n}\right\}\right)\left(T_{n}-\mathrm{E}\left\{T_{n}\right\}\right)^{\top}\right\} \text {. }
\]

Вектор $d(X)=T_{n}(X)-g(\theta)$ наз. в е к т о р о м о ІІІ ибок Т. о. $T_{n}$. Если

\[
b(\theta)=\mathrm{E}_{\theta}\{d(X)\}=\mathrm{E}_{\theta}\left\{T_{n}\right\}-g(\theta)
\]

\begin{itemize}
  \item нулевой вектор при всех $\theta \in \Theta$, то говорят, тто $T_{n}$ является н е с м е е н н о й о ц е н к й й функции $g(\theta)$ или что $T_{n}$ лишена систематит. ошибки, в противном случае Т. о. $T_{n}$ наз. с м е щ е н н о й, а вектор $b(\theta)$ - с м е ен и е м или с и с т е м а тиче с к о й о пі и б к о й Т. о. Качество Т. о. определяется с помощью функции риска.
\end{itemize}

Лum.: [1] К р а м е р Г., Математические методы статистики, пер. с англ., 2 изд., М., 1975; [2] И б р а г и м о в И. А., М. C. Никулия.

TОЧЕЧНАЯ РЕIIETКА в $\mathbb{R}^{n}$ с б а з И с о м [ $\left[e_{1}\right.$, $\left.\ldots, e_{n}\right]$ - множество $\Lambda=\mathbb{Z} e_{1}+\ldots+\mathbb{Z} e_{n}$ точек $\boldsymbol{a}=$ $=g_{1} \boldsymbol{e}_{1}+\ldots+g_{n} \boldsymbol{e}_{n}$, где $g_{1}, \ldots, g_{n}$ - целые числа.


\end{document}