\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
имеет конечную размерность $n$, и в нек-ром базисе $e=\left(e_{1}, e_{2}, \ldots, e_{n}\right)$ векторы

\[
\boldsymbol{u}=\sum_{i=1}^{n} u^{i} \boldsymbol{e}_{i}, \boldsymbol{v}=\sum_{i=1}^{n} v^{i} \boldsymbol{e}_{i}, \quad \boldsymbol{w}=\sum_{i=1}^{n} w^{i} \boldsymbol{e}_{i},
\]

то величины

\[
a^{i j k}=\left|\begin{array}{ccc}
u^{i} & v^{i} & w^{i} \\
u^{j} & v^{j} & w^{j} \\
u^{k} & v^{k} & w^{k}
\end{array}\right|=3 ! u^{[i} \quad v^{j} \quad w^{k]}, 1 \leqslant i, j, k \leqslant n,
\]

наз. координатами Т. $[\boldsymbol{u}, \boldsymbol{v}, \boldsymbol{w}]$ в базисе $e$. Эти координаты кососимметричны по любой паре своих индексов, при замене базиса в $A$ изменяются, как координаты трижды контравариантного тензора. Среди этих координат $C_{n}^{3}$ существенных. Два Т. наз. равными, если в каком-либо базисе $A$ равны их координаты. Класс равных T. наз. с в о б о д ны м T.

При наличии в $A$ скалярного произведения на Т. распространяется ряд метрич. понятий векторной алгебры. М е р о й Т. $[\boldsymbol{u}, \boldsymbol{v}, \boldsymbol{w}]$ наз. трехмерный объем параллелепипеда - множкества концов векторов вида $\boldsymbol{h}=x \boldsymbol{u}+y \boldsymbol{v}+z \boldsymbol{w}$, где $0 \leqslant x, y, z \leqslant 1$, отложенных от общего начала. В случае когда $\operatorname{dim} A=3$, мера Т. равна модулю смешанного произведения векторов $\boldsymbol{u}, \boldsymbol{v}, \boldsymbol{w}$. С к а л я р ным п р о и з в е де ни е м двух Т. наз. число, равное произведению мер сомножителей на косинус угла между несущими их плоскостями. Скалярное произведение является билинейной формой от координат сомножителей. Если $\operatorname{dim} A=4$, то Т. $[u, v, w]$ может быть отождествлен с вектором пространства $A$, наз. векторным произведением векторов $\boldsymbol{u}, \boldsymbol{v}, \boldsymbol{w}$.

Т. в т ен 3 о рн ом иссчи сл ени и есть любой контравариантный кососимметрич. тензор валентности 3 (т. е. тензор тиша $(3,0)$ ). Каждый такой тензор может быть представлен в виде суммы нескольких тензоров, к-рым соответствуют Т. с различными несущими их плоскостями.

См. также Бивектор, Внешнее произведение, Поливектор, Плюккеровы координаты. Л. П. Купуов.

ТРИГОНОМЕТРИЧЕСКАЯ СИСТЕМА - одна иЗ важнейших ортогональных систем функций. Функции T. c.

$1, \cos x, \sin x, \ldots, \cos n x, \sin n x, \ldots$

ортогональны на любом отрезке вида $[a-\pi, a+\pi]$, a функции

\[
\frac{1}{\sqrt{2 \pi}}, \frac{\cos x}{\sqrt{\pi}}, \frac{\sin x}{\sqrt{\pi}}, \ldots, \frac{\cos n x}{\sqrt{\pi}}, \frac{\sin n x}{\sqrt{\pi}}, \ldots
\]

ортонормированы на этом отрезке. 'Т. с. полна и замкнута в пространстве $L_{p}[-\pi, \pi]$ при $1 \leqslant p<\infty$, а также в

\includegraphics[max width=\textwidth, center]{2023_07_09_07cf9b8242aa3877f838g-1}
ций. Эта система образует базис в пространстве $L_{p}[-\pi$, $\pi]$ при $1<p<\infty$. Ряды по Т. с. изучаются в теории тригонометрических рядов.

Наряду с Т. с. широкое применение находит комплексная тригонометрич. система $\left\{e^{i n x}\right\}_{n=-\infty}^{\infty}$. Функции этих систем связаны друг с другом формулами Эйлера.

Jіит.: [1] Б а p и Н. К., Тригонометрические ряды, М., 1961, [2] 3 и г м у н Д.А., Тригонометрические ряды, т. 1-2,

ТРИГОНОМЕТРИЧЕСКАЯ СУММА - конечная сУмма $S$ вида

\[
S=\sum_{x=1}^{P} e^{2 \pi i F(x)}
\]

где $P \geqslant 1, \quad P$ - целое число, $F(x)$ - действительная функция $x$. Т. с. также наз. и более общие суммы $S^{\prime}$ вида

\[
\left.S^{\prime}=\sum_{x_{1}=1}^{P_{1}} \ldots \sum_{x_{r}=1}^{P_{r}} \Phi\left(x_{1}, \ldots, x_{r}\right) e^{2 \pi i F\left(x_{1}\right.}, \ldots, x_{r}\right),
\]

где $F\left(x_{1}, \ldots, x_{r}\right)$ - действительная функция, а $\Phi\left(x_{1}, \ldots, x_{r}\right)$ - произвольная комплекснозначная функция.

Если $F(x)$ - многочлен, то $S$ наз. с у м м о й $\mathrm{B}$ е йл я; если многочлен $F(x)$ имеет вид

\[
F(x)=\frac{a_{n} x^{n}+\ldots+a_{1} x}{q},\left(a_{n}, \ldots, a_{1}, q\right)=1,
\]

то $S$ наз. р а ци она Јг ной т р игоном т т ич. с у м мо й; если $P=q$, то $S$ газ. по лно й т р и г он о м е т р и ч. с у м м о й; если $r=1, \Phi\left(x_{1}\right)=1$ при простом $x_{1}$, и $\Phi\left(x_{1}\right)=0$ при составном $x_{1}$, то $S$ наз. т $\mathrm{p}$ игоном е т рич. с уммой с просты ми числ а м и; если $r_{\geqslant 1} 1, \Phi\left(x_{1}, \ldots, x_{r}\right)=1, F\left(x_{1}, \ldots, x_{r}\right)-$ многочлен, то $S^{\prime}$ наз. К р а т ной с у м м о й $\mathrm{B}$ е йл я. Основной проблемой в теории Т. с. является проблема разыскания верхней грани модуля $S$ и $S^{\prime}$.

Лum. - [1] В и н о г р а д о в И. М., Избр. тр., М., 1952; [2] е г о ж е, Метод тригонометрических сумм в теории чиселі, ческих сумм, М., 1976, [4] X у а JI o - г с н, Метод тригонометрических сумм и его применения в теории чисел, пер. с нем., M., 1964; [5] Т и т ч м а p шI $\mathrm{F}$. $\mathrm{K}$, Теория дзета-фушкции Римана, пер. с англ., М , 1953, 16] А $\mathrm{n}$ х и п о в Г. И., К а р ац у 6 а А. А., ч у б а р и к о в В. Н., Кратные тригономег-
рические суммы, М., 1980

ТРИГОНОМЕТРИЧЕСКИЕ ФУНКЦИИ - класс элементарных функций: синус, косинус, тангенс, котангенс, секанс, косеканс. Обозначаются соответственно: $\sin x, \cos x, \operatorname{tg} x, \operatorname{ctg} x, \sec x, \operatorname{cosec} x$.

Тригономет ти ческие функции дейс т вительного а рг умент а. Пусть $A$ - точка окружности с центром в начале координат и радиусом, равным единице, $\alpha-$ угол между осью абсцисс и вектором $O A$, отсчитываемый от пологкительного направления оси абсцисс (рис. 1). При этом если отсчет ведется против часовой стрелки, то величина угла считается положительной, a если по часовой стрелке отрицательної, т. е. $\alpha-$

\includegraphics[max width=\textwidth, center]{2023_07_09_07cf9b8242aa3877f838g-1(1)}
полярный угол тотки $A$.

Если $\left(x_{\alpha}, y_{\alpha}\right)$ - прямоугольные декартовы координаты точки $A$, то Т. Ф. синус и косинус определяются формулами

$\sin \alpha=y_{\alpha}, \quad \cos \alpha=x_{\alpha}$.

Остальные Т. ф. могут быть определены формулами

$\operatorname{tg} \alpha=\frac{\sin \alpha}{\cos \alpha}, \operatorname{ctg} \alpha=\frac{\cos \alpha}{\sin \alpha}, \sec \alpha=\frac{1}{\cos \alpha}$, $\operatorname{cosec} \alpha=\frac{1}{\sin \alpha}$.

Все Т. ф. - периодические функции. Графики Т. ф. даны на рис. 2 .

\begin{center}
\includegraphics[max width=\textwidth]{2023_07_09_07cf9b8242aa3877f838g-1(2)}
\end{center}


\end{document}