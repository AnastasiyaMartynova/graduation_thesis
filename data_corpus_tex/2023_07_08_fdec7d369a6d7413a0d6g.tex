\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\graphicspath{ {./images/} }

\begin{document}
П. отображекия (функции). Достаточно общим понятием П., в определенном смысле охватывающим все основные слуqаи, является П. отображения по фильтру. П р е д е л филь т р а. Пусть $X$ - топологич. пространство, $\quad X \neq \varnothing, \mathfrak{l}=\{U\}$ - его база топологии, $₹-ф$ и л ь т р на $X$ (т. е. такое непустое семейство непустых подмножеств пространства $X$, что для любых $A^{\prime} \in \mathfrak{F}, A^{\prime \prime} \in F$ существует такое $A \in F$, что $\left.A \subset A^{\prime} \cap A^{\prime \prime}\right)$. Точка $x_{0}$ наз. п р е д е ло м ф и ль т р а $\mathfrak{F}$ или его предельной точкой, если фильтр $\mathfrak{F}$ сильнее фильтра $\mathfrak{B}\left(x_{0}\right)$, являющегося локальной базой топологии в точке $x_{0}$, т. е. для любого $U \in \mathfrak{B}\left(x_{0}\right)$ существует такое $A \in F$, что $A \subset U$.

Пусть $\mathbb{N}$ - множество натуральных чисел с дискретной топологией. Фильтр на множестве $\mathbb{N}$, состоящий из всевозможных дополнений к конечным подмножествам множества $\mathbb{N}$, наз. н а т у $\mathrm{p}$ а л ь н ы м ф и л ь тр о м и обозначается $\mathfrak{F}_{\mathbb{N}}$. Он не имеет П. в $\mathbb{N}$. Тот же фильтр на множестве $X=\mathbb{N} U\{+\infty\}$, в к-ром локальная база $\mathfrak{B}(+\infty)$ состоит из всевозможных множеств $\boldsymbol{A}_{n}=\{m: m \in \mathbb{N}, m>n \in \mathbb{N}\}$, а $\mathfrak{B}(n)$ при $n \in \mathbb{N}$ - из одной точки $n$, имеет своим П. бесконечность $+\infty$. Единственность П. фильтра топологич. пространства связана с отделимостью точек пространства: для того чтобы любой фильтр топологич. пространства имел не более одного П., необходимо и достаточно, чтобы пространство было хаусдорфово.

Пусть $X$ - нек-рое множество, $Y$ - топологич. пространство, $\varphi$ - отображение $X$ в $Y, F$ - фильтр на $X$. Точку $b \in Y$ наз. п р е д л л м о тоб р а же ни я $\varphi$ по фи ль т р у $\mathfrak{F}$ и пишут

\[
\lim _{\mathfrak{F}} \varphi(x)=b,
\]

если фильтр $\varphi(\mathfrak{F})$, состоящий из всевозможных множеств $\varphi(A), A \in \widetilde{F}$ имеет своим П. в пространстве $Y$ точкy $b$.

Если $X=\mathbb{N}$ - множество натуральных чисел, $\varphi-$ отображение $\mathbb{N}$ в топологич. пространство $Y, \varphi(n)=$ $=y_{n} \in Y, n \in \mathbb{N}, \mathfrak{F}_{\mathbb{N}}$ - натуральный фильтр, то П. отображения $\varphi$ по фильтру $\mathfrak{F}_{\mathbb{N}}$ в пространстве $Y$ совпадает с обычным П. последовательности $\left\{y_{n}\right\}_{\text {в }} Y$.

Если в $X=\mathbb{N} \times \mathbb{N}, \mathfrak{F}-$ фильтр на $X$, являющийся произведением двух натуральных фильтров $\mathfrak{F}_{\mathbb{N}}$, т. е. состоящий из всевозможных множеств вида $A \times B$, где $A \in \mathfrak{F}_{\mathbb{N}}, B \in \mathfrak{F}_{\mathbb{N}}, \varphi-$ отображение $\mathbb{N} \times \mathbb{N}$ в топологич. пространство $Y: \varphi(n, m)=y_{n m} \in Y, n \in \mathbb{N}, m \in \mathbb{N}$, то П. отображения $\varphi$ по фильтру $\mathfrak{F}$ в пространстве $Y$ совпадает с обычным П. двойной последовательности $\left\{y_{n m}\right\}$ в $Y$.

Пусть әлементами множества $X$ являются, в свою очередь, множества $x$, состоящие из какого-либо разбиения $\tau=\left\{t_{i}\right\}_{i=0}^{i=n}$ нек-рого отрезка $[a, b] ; a=t_{0}<t_{1}<\ldots<$ $<t_{n-1}<t_{n}=b$ и каких-то точек $\xi_{i} \in\left[t_{i-1}, t_{i}\right], i=1,2, \ldots$, то есть

\[
x=\left\{\tau ; \xi_{1}, \ldots, \xi_{n}\right\} .
\]

Пусть $A_{\eta}$ (для любого $\eta>0$ ) - подмножество множества $X$, состоящее из всех әлементов $x \in X$, у к-рых мелкости входящих в них разбиений $\tau=\left\{t_{i}\right\}_{i=0}^{i=n}$ меньше $\eta$, то есть

\[
\max _{i=1,2, \ldots, n}\left(t_{i}-t_{i-1}\right)<\eta .
\]

Система $\mathfrak{F}=\left\{A_{\eta}\right\}$ является фильтром. Всякая действительная функция $f$, определенная на отрезке $[a, b]$, порождает отображение $\varphi_{f}$ множества $X$ в числовую ось $\mathbb{R}$ по формуле

\[
\begin{gathered}
\varphi_{f}(x)=\sum_{i=1}^{n} f\left(\xi_{i}\right)\left(t_{i}-t_{i-1}\right), x=\left(\tau ; \xi_{1}, \ldots, \xi_{n}\right), \\
\tau=\left\{t_{i}\right\}_{i=0}^{i=n} .
\end{gathered}
\]

Таким образом, $\varphi_{f}(x)$ является значением интегральной суммы Римана функции $f$, соответствующим әлементу $x \in X$.

П. отображения $\varphi_{f}$ в $\mathbb{R}$ по фильтру $\mathfrak{F}$ совпадает с обычным П. интегральных сумм Римана функции $f$ при условии, что мелкости рассматриваемых разбиений стремятся к нулю. Это совпадение имест место в том смысле, что оба П. одновременно существуют или нет, а если существуют, то равны и являются значениями интеграла Римана от функции $f$ по отрезку $[a, b]$

П е дел отображения топологичес ких прост т а н т в по фи ль т р у. Пусть

\includegraphics[max width=\textwidth, center]{2023_07_08_fdec7d369a6d7413a0d6g-1}
на $E, \varphi$ - отображение $E$ в $Y$. Точку $b \in Y$ наз. п $\mathrm{p} \mathrm{e}$ делом отображения $\varphi$ в точке $a \in X$ по фи л ь т р у $F$, если $a$ является П. фильтра $\mathfrak{F}$, а $b$ является П. фильтра $\varphi(\mathfrak{F})$. В этом случае пишут

\[
b=\lim _{\mathfrak{F}} \varphi(x) \text {. }
\]

Если $\mathfrak{P}(a)$ - база окрестностей в точке $a, E=$

\includegraphics[max width=\textwidth, center]{2023_07_08_fdec7d369a6d7413a0d6g-1(1)}
колотых окрестностей» $U(a) \backslash\{a\}$ точки $a, U(a) \in \mathfrak{B}(a)$, то предел $\lim _{\mathfrak{F}} \varphi(x)$ совпадает с обычным пределом $\lim _{x \rightarrow a} \varphi(x)$ отображения $\varphi$ в точке $a$, то есть обобщает классическое определение П. отображения, сформулированное в терминах окрестностей. Непосредственным обобщением понятия П. последовательности является П. направленного множества в топологическом пространстве, т. е. такого частично упорядоченного множества, у к-рого за каждыми двумя элементами имеется следующий. В терминах П. по направленным множествам можно также сформулировать понятие П. отображения одного топологич. пространства в другое (см. Направленность, Сходимость).

Предел последовательности множе с тв. Топологиче ский предел. Пусть $A_{n}, n=1,2, \ldots$, - множества топологич. пространства . В е р хиит топологическим предел о м $\overline{\mathrm{lt}} A_{n}$ последовательности $\left\{A_{n}\right\}$ наз. множество точек $x \in X$, каждая окрестность к-рых пересекается с бесконечным числом множеств $A_{n}$, а ни ж н и м т ополо гическим предел о м иt $A_{n}$-множество точек, каждая окрестность к-рых содержит точки почти всех $A_{n}$. Очевидно, $\underline{\text { it }} A_{n} \subset \overline{\mathrm{lt}} A_{n}$. Если $A=$ $=\mathrm{lt} A_{n}=\overline{\mathrm{lt}} A_{n}$, то последовательность $\left\{A_{n}\right\}$ наз. сходящейся, а множество $A$ ее топологическим П. и пишут $\boldsymbol{A}=\mathrm{lt} \boldsymbol{A}_{n}$. Верхпий и пижний топологические П. последовательности являются замкнутыми множествами.

Т е о рет и к о-мн оже с т венны й п р д е л. Имеется понятие П. последовательности множеств, не связанное с топологией. Последовательность множеств $A_{n}, n=1,2, \ldots$, наз. с х о д я щ е й с я, если существует такое множество $A$, называемое ее п р е де ло м и обозначаемое

\[
\boldsymbol{A}=\lim \boldsymbol{A}_{n},
\]

что каждая его точка принадлежит всем множествам $A_{n}$, начиная с нек-рого номера, и каждая точка из объединения всех множеств $A_{n}$, не принадлежащая $A$, coдержится лишь в конечном числе множеств $A_{n}$. Множество $A$ является П. последовательности $\left\{A_{n}\right\}$ тогда и только тогда, когда оно является одновременно ее вер $x$ ним и нижним пределом.

Лum.: [1] И л ь и н В. А., П о з н я к Э. Г., Основы математического анализа, 3 изд., ч. 1, М., 1971; [2] Ил ь и н В. А., С а д о в н и ч и й B. А., С е н д о в Б. Х., Математический анализ, М., 1979; [3] К у д р я в ц е в Ј. Д., Курс математи-

\includegraphics[max width=\textwidth, center]{2023_07_08_fdec7d369a6d7413a0d6g-1(2)}
М., Курс математического анализа, О озд., т. , М.,


\end{document}