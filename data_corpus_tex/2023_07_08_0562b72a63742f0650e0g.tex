\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}

\begin{document}
Если ряд в (1) сходится для всех $\omega$, достаточно близких к $\omega_{0}$, и

\[
\lim _{\omega \rightarrow \omega_{0}} \sigma(\omega)=s,
\]

то последовательность $\left\{s_{n}\right\}$ наз. суммируемой к пределу $s$ полуне пре рывным методом суммир о в а в и я, определенным последовательностью функций $\left\{a_{k}(\omega)\right\}$. Если $\left\{s_{n}\right\}-$ последовательность частичных сумм ряда

\[
\sum_{k=0}^{\infty} u_{k}
\]

то ряд (2) в этом случае наз. суммируемым полунепрерывным методом к сумме $s$. П. м. с. при $\omega_{0}=\infty$ является аналогом матричного метода суммирования, определенного матрицей $\left\|a_{n k}\right\|$, причем целоqисленный параметр $n$ заменен непрерывным параметром $\omega$. Последовательность функций $a_{k}(\omega)$ в этом случае наз. п о л у н еп $\mathrm{p}$ е ры в н 0 й ма т $\mathrm{p}$ иц е й.

П. м. с. может задаваться преобразованием непосредственно ряда в функцию с помощью заданной последовательности функций, напр. $\left\{g_{k}(\omega)\right\}$ :

\[
\gamma(\omega)=\sum_{k=0}^{\infty} g_{k}(\omega) u_{k} .
\]

В әтом случае ряд (2) наз. суммируемым к сумме $s$, если

\[
\lim _{\omega \rightarrow \omega_{0}} \gamma(\omega)=s
\]

где $\omega_{0}$ - точка сгущения множества $E$ изменения параметра $\omega$, а ряд в (3)предполагается сходящимся для всех $\omega$, достаточно близких к $\omega_{0}$.

П. м. с. в нек-рых случаях являются более удобными, чем методы суммирования, определенные обычными матрицами, т. к. позволяют использовать аппарат теории функций. Примерами П. м. с. являются Абеля метод суммирования, Бореля метод суммирования, Линделёбба метод суммирования, Миттаг-Лефблера метод суммирования. Класс П. м. с. составляют методы с полунепрерывными матрицами вида

\[
a_{k}(\omega)=p_{k} \omega^{k} / \sum_{l=0}^{\infty} p_{l} \omega^{l},
\]

где в знамевателе стоит целая функция, не сводящаяся к многочлену.

Условия регулярности П. м. с. аналогичны условиям регулярности матричного метода суммирования. Напр., условия

\[
\sum_{k=0}^{\infty}\left|a_{k}(\omega)\right| \leqslant M
\]

для всех $\omega$, достаточно близких к $\omega_{0}$,

\[
\begin{gathered}
\lim _{\omega \rightarrow \omega_{0}} a_{k}(\omega)=0, k=0,1,2, \ldots, \\
\lim _{\omega \rightarrow \omega_{0}} \sum_{k=0}^{\infty} a_{k}(\omega)=1
\end{gathered}
\]

являются необходимыми и достаточными, чтобы П. м. с., определенный преобразованием (1) последовательности $\left\{s_{k}\right\}$ в функцию, был регулярным (см. Рееулярности признаки).

Лит.: [1] Х а р д и Г., Расходящиеся ряды, пер. с англ., М., 1951; [2] К у к Р., Бесконечные матрицы и пространства B u kma n $\mathrm{k}$., Theorie der Limitierungsverfahren, 2 Aufl., B. - Hdlb. - N.Y., 1970 .

ПОЛУНОРМА - конечная неотрицательная функция $p$ на векторном пространстве $E$ (над полем действительных или комплексных чисел), подчиненная условиям:

\[
\begin{gathered}
p(\lambda x)=|\lambda| p(x), \\
p(x+y) \leqslant p(x)+p(y)
\end{gathered}
\]

для всех $x, y \in E$ и скаляров $\lambda$. Примером П. служит норма; отличие заклютается в том, что для П. допустимо $p(x)=0$ при $x \neq 0$. Если на векторном пространстве задана полунорма $p$, а на его подпространстве - линейный функционал $f$, подчиненный условию $|f(x)| \leqslant p(x)$, то его можно продолжить на все пространство с сохранением этого условия (т е о р е м а X а н а - Б а наx a). В математич. анализе наиболее употребительны отделимые топодогические векторные пространства, базис окрестностей нуля в к-рых можно составить из выпуклых множеств. Такие пространства наз. л о к а л ьн о в ы п у к л ы м и. В этих пространствах базис может быть описан неравенствами $p(x)<1$, где $p$ - непрерывные П. В то же время в практике математич. анализа встречаются и такие топологич. векторные пространства (в том числе и с метризуемой топологией), на к-рых нет нетривиальных непрерывных П. Простейший пример такого рода - пространство $L_{q}(0,1)$, где $0<$ $<q<1$

Лит.: [1] Б у р б а к п Н., Топологические векторные пространства, пер. с франц., М., 1959, [2] Р у д и н У., Функ циональный анализ, пер с англ., М., 1975.

ПОЛУОГРАНИЧЕННЫИ ОПЕРАТОР - симлетрический оператор $S$ в гильбертовом пространстве $H$, для к-рого существует такое число $c$, что

\[
(S x, x) \geqslant c(x, x)
\]

для всех векторов $x$, лежащих в области определения $S$. П. о. $S$ всегда имеет полуограниченное самосопряженное расширение $A$ с той же нижней гранью $c$ (т е о p eм а $\Phi$ р и д р и х с а). В частности, их индексы дефекта совпадают.

Лum.: [1] Р и с с Ф., С ё к е Ф а л ь в и - Н а д ь Б., ЛеКции по функциональному анализу, пер. с франц 22 изд., М.,
В. И. Ломоносов.

ПОЛУОПРЕДЕЛЕННАЯ ФОРМА - квадратичная форла над упорядоченным полем, представляюшая либо только неотрицательные, либо только неположительные әлементы поля. В первом случае квадратичная форма наз. не о т р и ц а т е л в н о й $(q(x) \geqslant 0$ для всех значений $x)$, во втором - н е п о л ожи т е ль но й к в а д р а тичной фо р мо й $(q(x) \leqslant 0)$. Чаще всего П. ф. рассматриваются вад полем $\mathbb{R}$ действительных чисел. Над полем $\mathbb{C}$ аналогично определяются полуопределенные (неотрицательные и неположительные) эрмитовы квадратичные формы (см. Эрмитова борма).

Если $b$ - симметрическая билинейная или әрмитова форма, причем $q(x)=b(x, x)$ является П. ф., то и форму $b$ также иногда называют полуопределенной (неотрицательной или неположительной). Если $q-$ квадратичная или эрмитова П. ф. в векторном пространстве $V$, то $N=\{x \in V \mid q(x)=0\}$ является подпространством, совпадаюгим с ядром формы $b$, причем на $V / N$ естественным образом индуцируется положительно определенная или отрицательно определенная форма. O. А. Иванова.

ПОЛУПЛОСКОСТЬ - совокупность точек плоскости, лежащих по одну сторону от нек-рой прямой этой плоскости. Координаты точек П. удовлетворяют неравенству $A x+B y+C>0$, где $A, B, C$ - нек-рые постоянные, причем $A$ и $B$ одновременно не равны нулю. Если сама прямая $A x+B y+C=0$ (г р а ш и ц а П.) причисляется к П., то говорят о 3 а м к н у т о й П. На комплексной плоскости $z=x+i y$ рассматриваются верхня я полу плоскость нижня я пол у плос кость $y=\operatorname{Im} z<0, \pi \mathbf{e}-$ в а я полуплос кость по лу плос кость комплексной плоскости $z$ конформно отображается на круг $|w|<1$ с помоцью дробно-линейной функции

\[
w=e^{i \theta} \frac{z-\beta}{z-\bar{\beta}}
\]

где $\theta$ - произвольное действительное число, a $\operatorname{Im} \beta>0$. ECO-3


\end{document}