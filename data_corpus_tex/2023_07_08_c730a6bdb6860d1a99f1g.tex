\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}

\begin{document}
$\mathrm{SL}(n, K)$ играет группа всех матриц из $\mathrm{GL}(n, K)$ с приведенной нормой 1 . Группы $\mathrm{SL}(n, K)$ и $\mathrm{SL}+(n, K)$ не всегда совпадают, но если $k$ - глобальное поле, то это так (см. Кнезера - Tитса гипотеза).

Исследование нормального строения П. л. г. над произвольным кольцом $K$ связано с развитием алгебрачческой $K$-теории. Над кольцами $K$ общего типа группа GL $(n, K)$ может быть весьма насыцена нормальными подгруппами. Напр., если $K$ - коммутативное кольцо без делителей нуля и с конечным числом образующих, то группа $\mathrm{GL}(n, K)$ финитно аппроксимируема, т. е. для каждого ее элемента $g$ существует нормальная подгрупша $N_{g}$ конечного индекса, не содержащая $g$. В случае ${ }^{g} K=\mathbb{Z}$ задача описания нормальных подгрупп группы $\mathrm{GL}(n, \mathbb{Z})$ фактически эквивалентна конгруэнц-проблеме для группы $\mathrm{SL}(n, \mathbb{Z})$, поскольку

\[
[\mathrm{CL}(n, \mathbb{Z}): \mathrm{SL}(n, \mathbb{Z})]=2 .
\]

а всякая нескалярная нормальная подгруппа группы $\mathrm{SL}(n, \mathbb{Z})$ при $n>2$ является конгруэнц-подгрупой.

Имеется глубокая аналогия между строением П. л. г. и строением других классич. групा, к-рая простирается далее на простые алгебраические группы и групाшы Ли.

Лит.: [1] А р т и н Э., Геометрическая алгебра, пер. с англ., М., 1969; 2] Д в е д о нн е Ж., Геометрия классических групп, пер. с франц., М., 1974; [3] Б а с с Х., Алгебраи-

ПОЛНАЯ МЕРА - мера $\mu$ на $\sigma$-алгебре $\Sigma$, для к-рой равенство $|\mu|(A)=0$ влечет за собой $E \in \Sigma$ для всякого $E \subset A$. Здесь $|\mu|$ - полная вариация $\mu$ (для положительной меры $|\mu|=\mu$ ). $\quad$. П. Терехин.

ПОЛНАЯ НЕУСТОЙИВОСТЬ - свойство динамической системы. Динамич. система наз. в п о лн е н е у с т о й ч и в о й, если все ее точки - блуждающие (см. Блуждающая точка).

Для того чтобы динамич. система, заданная на $\mathbb{R} n$, была глобальн о в ы п я мля е мо й (т. е. чтобы существовал гомеоморфизм $\mathbb{R} n \rightarrow \mathbb{R} n \times \mathbb{R}$, отображающий каждую траекторию системы на нек-рую прямую $\{a\} \times \mathbb{R}$, где точка $a \in \mathbb{R}^{n}$ зависит от траектории), необходимо и достаточно, чтобы система была вполне неустойчивой и не имела седла в бесконечности (те о р е м а Н е мы ц к о го, см. [1]).

Лит.: [1] Н е мыц к и й В. В., С т е пано в В. В., Катественная теория дифференциальных уравнений, 2 изд., М. - Л., $\begin{array}{ll}1949 . & \text { В. М. Миллиощиков. }\end{array}$

ПОЛНАЯ ПОДКАТЕГОРИЯ - подкатегория है категории $\Re$ такая, что для любых объектов $A, B$ из (5) выполняется равенство

\[
H_{\Theta}(A, B)=H_{\mathfrak{\kappa}}(A, B) .
\]

Таким образом, П. п. однозначно определяется классом своих объектов. Обратно, всякий подкласс класса объектов категории $\Re$ однозначно определяет П. П., для к-рой он служит классом объектов: в эту подкатегорию входят те и только те морфизмы, начала и концы к-рых принадлежат выделенному подклассу. В частности, П. П., соответствующая единственному объекту $A$, состоит из множества $H_{\Re}(A, A)$.

Многие важные классы подкатегорий (рефлективные и корефлективные подкатегории, многообразия и т. п.) являются П. П.

М. ШІ. Цаленко.

ПОЛНАЯ ПРОБЛЕМА с о б с т в е н н Ы ч е н и й - задача вычисления всех (в отличие от частичной проблемы) собственных значений квадратной матрицы, обычно действительной или комплексной. Часто помимо собственных значений требуется еце и построение базиса из собственных или корневых векторов матрицы.

Решение П. П. собственных значений матрицы $A$ сводится к построению характеристич. многочлена $\varphi_{A}$ этой матрицы и вычислению его корней, действи- тельных или комплексных (последнее обусловливает невозможность нахождения собственных значений конечным вычислительным процессом). Для каждого собственного значения $\lambda$ соответствующие собственные векторы могут быть определены из однородной системы линейных уравнений $(A-\lambda E) x=0(E-$ единичная матрица). При вычислениях над полем комплексных чисел достаточным условием существования базиса из собственных векторов является простота спектра, а необходимое и достаточное условие состоит в том, чтобы алгебраич. кратность каждого собственного значения $\lambda$ (т. е. его кратность как корня характеристич. многочлена $\varphi_{A}$ ) совпадала с его геометрич. кратностью, под к-рой понимается дефект матрицы $A-\lambda E$. При необходимости вычисления корневых векторов высоты, превосходящей единицу, приходится рассматривать однородные системы вида

\[
(A-\lambda E)^{k} x=0, k \in N, k>1 .
\]

Примерно по этой схеме строились и численные методы решения П. п. собственных значений, практиковавшиеся до кон. 1940-х гг. В 1930-х гг. были разработаны высокоэффективные (по количеству арифметич. операций) алгоритмы вычисления характеристич. многочлена матрицы по ее коэффициентам. Так, в методе Данилевского построение характеристич. многочлена матрицы порядка $n$ выполняется с затратой $\sim n^{3}$ мультиплигативных операций (см. [1], [2]).

Методы этой группы получили название прямых, или точных, по той причине, что, проводимые в точной арифметике, они дают точные значения коэффициентов характеристич. многочлена. Их поведение в условиях реальных вычислений, сопровождающихся ошибками округлений, не могло быть проверено для задач сколько-нибудь значительного порядка до появления цифровых ЭВМ. Такая проверка произошла в 1950-х гг., в результате чего прямые методы были полностью вытеснены из численной практики. Катастрофич. неустойчивость вычисления собственных значений, связанная с этими методами, имеет две основные причины. Вопервых, коэффициенты характеристич. многочлена в большинстве точных методов прямо или косвенно определяются как компоненты решения системы линейных уравнений, матрица к-рой составлена по столбцам из векторов $v, A v, A^{2} v, \ldots, A^{n-1} v$, где $v-$ начальный вектор метода. Такая матрица обычно очень плохо обусловлена, что видно хотя бы из того, что длины еe столбцов, как правило, весьма различны, причем тем болыше, чем больше $n$. Тем самым коэффициенты характеристич. многочлена в общем случае вычисляются с очень большими ошибками. Во-вторых, сама по себе задача вычисления корней многочлена зачастую оказывается численно неустойчивой. В этом отношении показателен пример (см. [3]): если у многочлена

$p(x)=(x-1)(x-2) \ldots(x-19)(x-20)=x^{20}-210 x^{19}+\ldots$ изменить коэфффициент при $x^{19} \mathrm{c}-210$ на $-210+2^{-23}$, то у возмущенного многочлена $\tilde{p}(x)$ появится пять пар комплексно сопряженных корней; у одной из этих пар мнимые части достигают $\pm i \cdot 2,81, \ldots$.

Подобная чувствительность собственных значений матрицы к изменениям коэфффициентов характеристич. многочлена обычно не сопровождается сравнимой чувствительностью в отношении элементов самой матрицы. Так, если указанный многочлен $p(x)$ является характеристич. многочленом симметрич. матрицы $A$, то изменения порядка $2^{-23}$ в любом элементе матрицы приводят самое большее к изменениям того же порядка в ее собственных значениях.

Современные численные методы решения П. ш. собственных значений находят собственные значения без предваритөльного вычисления характеристиq. много-


\end{document}