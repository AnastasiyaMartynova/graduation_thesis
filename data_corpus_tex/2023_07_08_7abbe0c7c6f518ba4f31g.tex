\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}

\begin{document}
то необходимо и доста'очно существование такой непрерывной функции $\varphi(t)$, что при $\omega_{1}>\omega$

\[
\begin{gathered}
\int_{0}^{\infty} \varphi(t) e^{-\omega_{1} t} d t<\infty, \\
\left\|R^{n}(\lambda, A)\right\| \leqslant \\
\leqslant \frac{1}{(n-1) !} \int_{0}^{\infty} t^{n-1} e^{-\lambda t} \varphi(t) d t, \lambda>\omega, n=1,2, \ldots
\end{gathered}
\]

При этом $\|T(t)\| \leqslant \varphi(t)$. Выбирая различные функции, удовлетворяющие (3), можно выделять различные подклассы суммируемых полугрупп. Если $\varphi(t)=M e^{\omega t}$, то получается класс $C_{0}$ и из (4) вытекает (1). Если $\varphi(t)=$ $=M t-e^{\omega t}, 0 \leqslant \alpha<1$, то из (4) получается условие $\left\|R^{n}(\lambda, A)\right\| \leqslant \frac{M \Gamma(n-\alpha)}{(n-1) !(\lambda-\omega)^{n-\alpha}}, \lambda>\omega, n=1,2, \ldots$

Полугруппа со степенными особенностями. Если в предыдущем примере $\alpha \geqslant 1$, то в (4) интегралы при $n \leqslant$ $\leqslant \alpha-1$ расходятся. В соответствии с этим производящий оператор соответствующих полугрупп может не иметь резольвенты ни при каких $\lambda$, т. е. иметь спектр, совпадающий со всей комплексной плоскостью. Однако для таких операторов, начиная с нек-рого $n$, существуют функции $S_{n}(\lambda, A)$, совпадающие в предыдущих случаях c $R^{n+1}(\lambda, A)$. Оператор-функция $S_{n}(\lambda, A)$ наз. р ез о л ь в е н т о й п о р я д к а $n$, если она аналитична в нек-рой области $G \subset \mathbb{C}$ и при $\lambda \in G$

\[
\begin{gathered}
S_{n}(\lambda, A) A x=A S_{n}(\lambda, A) x, x \in D(A), \\
S_{n}(\lambda, A)(A-\lambda I)^{n+1} x=x, x \in D\left(A^{n+1}\right),
\end{gathered}
\]

и из того, что $S_{n}(\lambda, A) x=0$ при всех $\lambda \in G$, следует, что $x=0$. Если $\bar{D}\left(A^{n+1}\right)=X$, то оператор может иметь единственную резольвенту порядка $n$, для к-рой имеется максимальная область аналитичности, наз. р е з о л ьв е н т ны м множ е с т в о м по о я д к а $n$.

Пусть для сильно непрерывной полугруппы $T(t)$ выполняется неравенство

\[
\|T(t)\| \leqslant M t-\alpha_{e} \omega t
\]

при $\alpha \geqslant 1$. Тогда өе производящий оператор $B$ имеет резольвөнту порядка $n$ при $n>\alpha-1$, причөм

\[
\begin{aligned}
& S_{n}(\lambda, B) x=\frac{1}{n !} \int_{0}^{\infty} t^{n} e^{-\lambda t} T(t) x d t, \operatorname{Re} \lambda>\omega, \\
& \left\|\frac{d^{k} S_{n}(\lambda, B)}{d \lambda k} x\right\| \leqslant \\
& \leqslant \frac{M \Gamma(k+n+1-\alpha)}{n !(\operatorname{Re} \lambda-\omega)^{k+n+1-\alpha}}, \operatorname{Re} \lambda>\omega, k=0,1, \ldots
\end{aligned}
\]

Обратно, пусть оператор $B$ имеет при Re $\lambda>0$ резольвенту $S_{n}(\lambda, B)$ порядка $n$, для к-рой выполнено (5) іри $n>\alpha-1$. Тогда существует единственная полугруппа $T(t)$ с оценкой

\[
\|T(t)\| \leqslant M t-\alpha_{e} \omega t
\]

для производящего оператора $A$ к-рой $S_{n}(\lambda, A)=$ $=S_{n}(\lambda, B)$.

Гладкая полугруппа. Если $x \in D\left(A_{0}\right)$, то функция $T(t) x$ непрерывно дифференцируема и

\[
\frac{d T(t)}{d t} x=A_{0} T(t) x=T(t) A_{0} x .
\]

Существуют полугруппы класса $C_{0}$, для к-рых при $x \notin D\left(A_{0}\right)=D(A)$ функции $T(t) x$ недифференцируемы при всех $t$. Однако для важных классов полугрупп наблюдается явление повышения их гладкости с увеличением $t$. Если $T(t) x, t>t_{0}$, диффференцируемы при любом $x \in X$, то из полугруппового свойства следует, что $T(t) x$ дважды дифференцируема при $t>2 t_{0}$, трижды - при $t>3 t_{0}$ и т. д. Поэтому если $T(t) x$ дифференцируемы при любых $t>0$ и $x \in X$, то $T(t) x$ бесконечно дифференцируемы. Для того чтобы для полугруппы класса $C_{0}$ при нек-ром $t_{0} \geqslant 0$ функции $T(t) x$ были дифференцируемы при всех $x \in X$ и $t>t_{0}$, необходимо и достаточно существование таких чисел $a, b, c>0$, что резольвента $R(\lambda, A)$ определена в области

\[
\operatorname{Re} \lambda>a-b \ln |\operatorname{Im} \lambda|
\]

И в этой области

\[
\|\operatorname{Re}(\lambda, A)\| \leqslant c|\operatorname{Im} \lambda| .
\]

Для того чтобы $T(t) x$ были бескокечно диффференцируемыми при всех $x \in X$ и $t>0$, необходимо и достаточно, чтобы для каждого $b>0$ нашлись такие $a_{b}, c_{b}$, что резольвента $R(\lambda, a)$ определена при

и

\[
\operatorname{Re} \lambda>a_{b}-b \ln |\operatorname{Im} \lambda|
\]

\[
\|R(\lambda, A)\| \leqslant c_{b}|\operatorname{Im} \lambda| .
\]

Достаточные условия: если при нек-ром $\mu>\omega$

\[
\varlimsup_{\tau \rightarrow \infty} \ln |\tau|\|R(\mu+i \tau, A)\|=t_{0}<\infty,
\]

то $T(t) x$ дифференцируемы при $t>t_{0}$ и $x \in X$, если $t_{0}=0$, то $T(t) x$ бесконечно диффференцируемы при всех $t>0$ и $x \in X$.

Иногда о гладкости полугруппы можно судить по ее поведению в нуле; напр., если для каждого $c>0$ существует такое $\delta_{c}$, что при $0<t<\delta_{c}$

\[
\|I-T(t)\| \leqslant 2-c t \ln t^{-1},
\]

то $T(t) x$ бесконечно дифференцируемы при всех $t>0$, $x \in X$.

Имеются условия гладкости суммируемых полугрупп и полугрупп степенного роста. Если полугруппа степенного роста $\alpha$ бесконечно дифференцируема при $t>0$, то функция

\[
\frac{d T(t)}{d t} x=A T(t) x
\]

часто также имест степенной рост:

\[
\|A T(t)\| \leqslant M_{1} t-\beta^{\omega t} .
\]

Между числами $\alpha$ и $\beta$ в общем случае нет жесткой связи, и число $\beta$ может служить для более детальной классификации бесконечно дифференцируемых полугрупп степенного роста.

Аналитическая полугруппа. Важный класс полугрупшы, связанный с уравнениями с частными производными параболич. тџпа, состоит из полугрупп $T(t)$, допускающих аналитич. продолжение в нек-рый сектор комплексной плоскости, содержащий положительную полуось. Для того чтобы полугруппа класса $C_{0}$ обладала этим свойством, необходимо и достаточно, чтобы в нек-рой правой полуплоскости Re $\lambda>\omega$ для резольвенты выполнялось неравенство

\[
\|\operatorname{Re}(\lambda, A)\| \leqslant M|\lambda-\omega|^{-1} .
\]

Также необходимо и достаточно, чтобы полугруппа была сильно диффференцируемой и чтобы для ее провзводной имелась оценка

\[
\left\|\frac{d T}{d t}\right\| \leqslant M t^{-1} e^{\omega t} .
\]

Наконец, из неравөнства

\[
\varlimsup_{t \rightarrow 0}\|I-T(t)\|<2
\]

также следует аналитичность $T(t)$.

Если полугруппа $T(t)$ допускает аналитич. продолжение $T(z)$ в сектор $|\arg z|<\varphi \leqslant \pi / 2$ и имеет степенной рост в нуле, $\|T(z)\| \leqslant c|z|^{\alpha}, \alpha>0$, то резольвента $S_{n}(\lambda, A)$ порядка $n>\alpha-1$ ее производящего оператора $A$ допу-

$\star 15$ Математическая энц., т. 4


\end{document}