\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{mathrsfs}
\usepackage{bbold}

\DeclareUnicodeCharacter{0131}{$\imath$}

\begin{document}
У. с. как математич. модели исходных физич. У. с., изучаемых в кибернетике, обладают рядом характерпых черт. Прежде всего они являются обтектами дискретної природы. Дискретными являются и схема, и ее координаты, и информация, и функция, и время. Для У. с., требующих специальных кибернетич. рассмотрений, характерна большая сложность. Эта сложность проявляется в том, что У. с. может иметь большое количество элементов, сложную структуру их связей, большую и сложно организованную память (и тем самым сложную информацию), сложное функциони рование. Реальные объекты можно рассматривать как У. с., вообще говоря, многими способами. Фиксация У. с. зависит от выбора әлементарных У. с. и может осуществляться в различных «масштабах". Таким образом, У. с. обладағот свойством относительности. Возможность выбора элементарных У. с. в разных масштабах (на различных уровнях) позволяет во многих случаях рассматривать исходную У. с. как иерархию У. с., в к-рой элементарные У. с. более высокого уровня являются У. с. относительно элементарных У. с. более низкого уровня. В этом случае говорят, что У. с. обладает свойством иерархичности. Наконец, как правило, У. с. описывает исходный объект не вполне точно, а с нек-рой погрешностью. Благодаря этому она выступает как век-рое приближение к объекту.

Существуют попытки расширить трактовку понятия У. с., включив в него и непрерывные объекты. В результате этого появляются «непрерывные» У. с., а также «непрерывно-дискретные» У. с. Впрочем, родственные объекты изучаются давно в теории динамич. систем и в теории регулирования. Между дискретными и непрерывными У. с. существует внешнее сходство, но больше имеется различий. Коренное отличие состоит в том, что дискретные У. с. описывают информационные процессы, а непрерывные - «әнергетические» процессы.

Исследование У. с. ведется на специальных классах У. с., на модельных объектах. Выбор типов модельных об́ъектов основан на следующих соображениях: a) иx число должно быть небольшим, б) они должны быть непохожими друг на друга, в) они должны охватывать наиболее часто встречающиеся типы, г) они должны давать возможность исследовать на основе данного набора типов другие типы У. с. Ниже приведен список типов модельных объектов.

\begin{enumerate}
  \item Формулы алгебры логики в данном базисе и реализующие булевы бункции (в частности, дизъюнктивные нормальные формы).

  \item Схемы из фуунциональных элементов в данном базисе и реализующие системы булевых функций.

  \item Контактные схемы, реализующие матрицы из булевых функциї.

  \item Автоматы в нек-ром автоматном базисе и реализующие преобразования «входных" последовательностей в "выходные» (см. Автомат конечный).

  \item Операторные алгоритмы, реализующие вычислимые функции.

\end{enumerate}

Для данных модельных объектов характерно то, что каждая У.с. $U$ полностью определяется своей схемой $\Sigma$ и функцией $\Phi$, т. е. парой $(\Sigma, \Phi)$. Здесь $U=(\Sigma, \Phi)$. С другой стороны, для каждого класса $\mathfrak{l}$ модельных объектов существует функция $\varphi$ такая, что $\Phi=\varphi(\Sigma)$, т. е. функция однознатно определяется схемой. Более того, функция $\varphi$ является вычислимой функцией. В этом случае каждый из модельных классов может быть охарактеризован другим способом. Пусть $\widetilde{S}=\{\Sigma\}$ и $\mathscr{F}=\{\Phi\}$, где $(\Sigma, \Phi) \in \mathfrak{U}$. Класс Ш полностью определен заданием множеств $\widetilde{S}$ и $\mathfrak{J}$, а также заданием алгоритма $A$, вычисляющего $\varphi$.

Основная проблематика теории У. с. группируется вокруг трех проблем (см. [4]): проблема синтеза (см. Синтеза задачи), проблема әквивалентных преобразо- ваний У. с., проблема надежности У. с. (см. Надежиость и контроль управляюиих систем). Сопоставление результатов, полученных по әтим проблемам пля различных классов модельных объектов, показывает, что имеется четкая закономерность в формулировках соответствующих теорем, наблюдается сходство методов доказательств упомянутых теорем; доказательства теорем для более сложных У. с. часто используют соответствующие теоремы для более простых классов.

Лum. [1] Я б л о н с к и й С. В , «Проблемы кибернетики", мышление, жизнь, М., 1964 , [3] Б у с л е і к о Н. П., «Изв. АН СССР. Сер. технич кибернетика», 1963 , 군. 5 . $7-18 ;$ [4] Л я п у н о в А. А., Я б ло І с к и Й С. В., «Проблемы ки-
бернетики», 1963 , в. 9, с 5-22.

УПРАВЛЯКШАЯ ФУНКІИЯ, у п р а в л е н и е,функция $u(t)$, входящая в дифференциальное уравнение $\dot{x}=f(t, x, u), x\left(t_{0}\right)=x_{0}, \quad f: \mathbb{R} \times \mathbb{R}^{n} \times \mathbb{R}^{m} \longrightarrow \mathbb{R}^{n}, \quad(1)$ значения к-рой в каждый момент времени могут выбираться произвольным образом.

Обычно на область изменения $u(t)$ при каждом $t$ налагается ограничение

\[
u(t) \in U,
\]

где $U$ - заданное замкнутое множество в $\mathbb{R}^{m}$. Управление наз. д о п у с т и м ы м, если при каждом $t$ оно удовлетворяет ограничению (2). Различные допустимые управления $u(t)$ определяют соответствующие различные траектории $x(t)$, исходящие из начальной точки $x_{0}$.

Если задан функционал

\[
J(x, u)=\int_{t_{0}}^{t_{1}} f^{0}(t, x, u) d t
\]

и граничные условия на правом конце траектории в момент времени $t_{1}$ :

\[
x\left(t_{1}\right) \in X_{1},
\]

где $X_{1}$ - нек-рое множество в $\mathbb{R}^{n}$ (в частном случаеточка), то можно поставить вопрос об определении оптимального управления $u(t)$, доставляющего оштимальное значение функционала к задаче (1)-(4). Вопросы, связанные с определением оптимальной У. Ф., являются предметом теории оптимального управления и вариационного исчисления (см. [1], [2]).

В отличие от переменных $x=\left(x^{1}, \ldots, x^{n}\right)$, называемых фа зовыми пе ременны ми (или фа зовым и к о о р д и н а т а м и), уıравления $u=\left(u^{1}, \ldots\right.$, $u^{m}$ ) входят в уравнение (1) без своих производных. Поэтому (1) имеет смысл не только при непрерывном, но и при кусочно непрерывном (и даже измеримом) управлении $u(t)$. Причем при каждом допустимом кусочно непрерывном управлении $u(t)$ уравнение (1) определяет непрерывную, кусочно гладкую траекторию $x(t)$.

В большинстве практич. задач оптимальное управление существует в классе кусочно непрерывных функций. Однако встречатотся задачи, в к-рых оптимальное управление не является кусочно непрерывным, а принадлежит классу измеримых по Лебегу функций. В этих задачах оптимальнос уіравление имеет бесконечное число точек разрыва, сгущающихся к нек-рой точке, напр. к точке входа на особый режим $k$-го порядка, $k>1$ (см. Оптимальный режим особый).

Необходимые условия, сформулированные в теории оптимального управления в виде принципа максимума Понтрягина, доказаны для самого общего случая, в к-ром исследуемая на оптимальность У. ф. предполагается измеримой (в частности, она может быть кусочно непрерывной или непрерывной).

Согласно принципу максимума для оптимальности управления $u(t)$ (в случае минимизации функционала (3)) необходимо, чтобы при каждом $t$ управление $u(t)$ доставляло максимум функций Гамильтона

\[
H(t, x, \psi, u)=\sum_{i=0}^{n} \psi_{i} f^{l}(t, x, u)
\]


\end{document}