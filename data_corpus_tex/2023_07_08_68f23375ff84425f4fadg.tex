\documentclass[10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[version=4]{mhchem}
\usepackage{stmaryrd}
\usepackage{bbold}
\usepackage{mathrsfs}

\begin{document}
$\alpha_{j}$ - целые, $|\alpha|=\alpha_{1}+\ldots+\alpha_{n}, \quad \xi^{\alpha}=\xi_{1}^{\alpha_{1}} \ldots \xi_{n}^{\left.\alpha_{n}\right)}$, то $p(x, D)$ совпадает с дибференциальным оператором, получаемым, если в выражение для $p(x, \xi)$ вместо $\xi$ подставить вектор $D=\frac{1}{i} \frac{\partial}{\partial x}$.

Часто используется класс символов $p(x, \xi) \in C^{\infty}(\Omega \times$ $\times \mathbb{R} n)$, удовлетворяющих условиям

\[
\begin{aligned}
\left|\partial_{\xi}^{\alpha} \partial_{x}^{\beta} p(x, \xi)\right| \leqslant & C_{\alpha, \boldsymbol{\beta}, \boldsymbol{x}}(1+|\xi|)^{m-\rho(\alpha)+\delta(\boldsymbol{\beta})}, \\
& x \in \mathscr{K}, \xi \in \mathbb{R} n,
\end{aligned}
\]

где $\alpha, \beta-$ мультииндексы, $\partial_{x}=\frac{\partial}{\partial x}, \quad \partial \xi=\frac{\partial}{\partial \xi}$, $\mathscr{K}$-компакт в $\Omega$. Этот класс обозначается $S_{\rho, \delta}^{m}$ (или $\left.S_{\rho, \delta}^{m}\left(\Omega \times \mathbb{R}^{n}\right)\right)$.

Обычно предполагается, что $0 \leqslant \rho \leqslant 1,0 \leqslant \delta \leqslant 1$. Через $L_{\rho, \delta}^{m}$ (или $L_{\rho, \delta}^{m}(\Omega)$ ) обозначается класс операторов (также называемых П. о. в $\Omega$ ) вида $p(x, D)+K$, где $p \in S_{\rho}^{m} \delta$, а $K$ - интегральный оператор с бесконечно дифференцируемым ядром, т. е. оператор вида

\[
K u(x)=\int K(x, y) u(y) d y,
\]

где $K(x, y) \in C^{\infty}(\Omega \times \Omega)$. Функцию $p(x, \xi)$ по-прежнему наз. символом П. о. $p(x, D)+K$, хотя теперь она определена уже не однозначно, а с точностью до символов, принадлежащих $S^{-\infty}=\bigcap_{m \in \mathbb{R}} S_{1,0}^{m}$. Оператор $A \in L_{\rho, \delta}^{m}$ наз. П. о. п о р я д к а н в ы ш е $m$ и ти па $\rho, \delta$. Описанный выше дифференциальный оператор принадлежит классу $L_{1,}^{m}$. Наименьшее возможное значение $m$ часто наз. по р я д к о м П. о. Классы $S_{\rho, \delta}^{m}, L_{\rho, \delta}^{m}$ часто наз. к л а с с а м и Х ё р м а н д е р а.

Можно задавать П. о. в $\Omega$ с помощью двойных символов, т. е. в виде

\[
P u=(2 \pi)^{-n} \iint e^{i(x-y) \cdot \xi_{a}}(x, y, \xi) u(y) d y d \xi .
\]

При $a(x, y, \xi)=p(x, \xi)$ эта формула переходит в (1). Обычно предполагается, что $a(x, y, \xi) \in S_{\rho, \delta}^{m}(\Omega \times \Omega \times$ $\left.\times \mathbb{R}^{n}\right)$, то есть

\[
\left|\partial_{\xi}^{\alpha} \partial_{x}^{\beta^{\prime}} \partial_{y}^{\beta^{\prime \prime}} a(x, y, \xi)\right| \leqslant
\]

$\leqslant C_{\alpha, \beta^{\prime}, \beta^{\prime \prime}, x}(1+|\xi|)^{m-\rho|\alpha|+\delta\left|\beta^{\prime}+\beta^{\prime \prime}\right|}, x, y \in \mathscr{K}$

где $\mathscr{K}$ - компакт в $\Omega$. Если $0 \leqslant \delta<\rho \leqslant 1$, то построенный класс операторов вида (3) (со всевозможными функциями $\left.a \in S_{\rho}^{m}, \delta\right)$ совпадает с классом $L_{\rho, \delta}^{m}(\Omega)$. При этом символ $p(x, \xi)$ (определенный с точностью до символов из $S^{-\infty}$ ) имеет следующее асимптотич. разложение:

\[
\left.p(x, \xi) \sim \sum_{\alpha} \frac{1}{\alpha !} \partial_{\xi}^{\alpha} D_{y}^{\alpha} a(x, y, \xi)\right|_{y=x}
\]

где $\alpha !=\alpha_{1} ! \ldots \alpha_{n} !$ и суммирование ведется по всем мультииндексам. Эта запись означает, что разность между $p(x, \xi)$ и частью суммы, взятой по тем $\alpha$, для к-рых $|\alpha| \leqslant N$, является символом, принадлежащим $S_{\rho, \delta}^{m-(\rho-\delta) N}$, т. е. символом, порядок к-рого не выше наибольшего из порядков оставшихся членов.

П. о. $P$ продолжается по непрерывности или с помощью двойственности до оператора $P: \delta^{\prime}(\Omega) \rightarrow D^{\prime}(\Omega)$, где $D^{\prime}(\Omega)$ и $\mathscr{E}^{\prime}(\Omega)$ - пространства обобщенных функций и обобщенных функций с компактным носителем в $\Omega$ соответственно. Если $\delta<1$, то при этом П. 0 . обладает следующим свойством п с е в д о л о к а л ьн о с т и: если $u \in \delta^{\prime}(\Omega) \cap C^{\infty}\left(\Omega^{\prime}\right), \quad$ где $\Omega^{\prime} \subset \Omega, \quad$ то $P u \in C^{\infty}\left(\Omega^{\prime}\right)$. Другая форомулировка свойства псевдолокальности: ядро $K(x, y)$ (в смысле Шьарца) оператора $P$ бесконечно дифференцируемо по $x, y$ при $x \neq y$.

К л а с с и ч е с к и й П.о. порядка $m$ в $\Omega-\Pi .0$. $P \in L_{1,0}^{m}$, символ к-рого $p(x, \xi)$ допускает асимптотич. разложение

\[
p(x, \xi) \sim \sum_{j=0}^{\infty} \chi(\xi) p_{m-j}(x, \xi),
\]

где $\chi(\xi) \in C^{\infty}\left(\mathbb{R}^{n}\right), \chi(\xi)=1$ при $|\xi| \geqslant 1, \chi(\xi)=0$ при $|\xi| \leqslant 1 / 2, p_{m-j}(x, \xi) \in C^{\infty}(\Omega \times(\mathbb{R} n \backslash 0))$ и положительно однородна по $\xi$ порядка $m-j$

\[
p_{m-j}(x, t \xi)=t^{m-j} p_{m-j}(x, \xi), x \in \Omega, \xi \in \mathbb{R}{ }^{\prime} Q .
\]

Примером классического П. о. является дифференциальный оператор (с гладкими коәффициентами). Функция $p_{m}(x, \xi)$ наз. г л а в н Ы м с и м в о ло м классического П. о.

П. о. в $\Omega$ наз. с о б с т в н ны м (или П. о. с с обственным носителе м, или П. о. с комп а к т ны нос и т е ле м), если проекции носителя его ядра при проектировании $\Omega \times \Omega$ на каждый сомножитель являются собственными отображениями. Собственный П. о. $P$ отображает $C_{0}^{\infty}(\Omega)$ в $C_{0}^{\infty}(\Omega)$ и продолжается по непрерывности до отображений $C^{\infty}(\Omega) \rightarrow C^{\infty}(\Omega), \delta^{\prime}(\Omega) \rightarrow \delta^{\prime}(\Omega)$ и $D^{\prime}(\Omega) \rightarrow D^{\prime}(\Omega)$, он может быть записан в виде (1) с символом $p(x, \xi)=$ $=e^{-i x \cdot \xi} P\left(e^{i x} \xi\right)$, где экспонента в скобках рассматривается как функция от $x$, а $\xi$ является параметром.

Пусть $A, B$ - два П. о. в $\Omega$, из к-рых один является собственным. Тогда имеет смысл их произведение (композиция) $C=A B$. Важную роль в теории П. о. играет теорема о композиции: если $A \in L_{\rho, \delta}^{m_{1}}$, $B \in L_{\rho, \delta}^{m_{2}}, 0 \leqslant \delta \leqslant \rho \leqslant 1$, то $C=A B \in L_{\rho, \delta}^{m_{1}+m_{2}}$. Если при этом $\delta<\rho, c(x, \xi), a(x, \xi), b(x, \xi)$ - символы операторов $C, A, B$, то

\[
c(x, \xi) \sim \sum_{\alpha} \frac{1}{\alpha !}\left[\partial_{\xi}^{\alpha} a(x, \xi)\right]\left[D_{x}^{\alpha} b(x, \xi)\right] .
\]

В частности, если $A, B$ - классические П. о. порядков $m_{1}, m_{2}$, то $C$ - классический П. о. порядка $m_{1}+m_{2}$ с главным символом $c_{m_{1}+m_{2}}(x, \xi)=a_{m_{1}}(x, \xi) b_{m_{2}}(x, \xi)$, где $a_{m_{1}}(x, \xi), b_{m_{2}}(x, \xi)$ - главные символы операторов $A$ и $B$.

Если $P \in L_{\rho, \delta}^{m}, 0 \leqslant \delta \leqslant \rho \leqslant 1$, то существует и единствен с о п р я ж е н н ы й П. о. $P^{*} \in L_{\boldsymbol{\rho}}^{m} \delta$, для к-рого $(P u, v)=\left(u, P^{*} v\right), u, v \in C_{0}^{\infty}(\Omega)$, где $(u, v)=\int u(x) \overline{v(x)} d x-$ скалярное произведение $u$ и $v$ в $L_{2}(\Omega)$. Если при этом $\delta<\rho$ и $p^{*}(x, \xi)$ - символ П. о. $P^{*}, \mathrm{a} p(x, \xi)-$ символ $P$, то

\[
p^{*}(x, \xi) \sim \sum_{\alpha} \frac{1}{\alpha^{\prime}} \partial_{\xi}^{\alpha} D_{x}^{\alpha} \overline{p(x, \xi)}
\]

Таким образом, собственные П. о. при $\delta \leqslant \rho$ образуют алгебру с инволюцией, задаваемой переходом к сопряженному оператору. Произвольные П.о. образуют модуль над этой алгеброй.

Те орема об ограниченности П. о. классов Хёрмандера в $L_{2}$-нормах в наиболее точной форме состоит в следующем (см. [8]): пусть $\Omega=\mathbb{R} n$, оператор $P$ имеет вид (3) с двойным символом $a(x, y, \xi)$, удовлетворяющим оценкам (4), где числа $m, \rho, \delta$ удовлетворяют условиям

\[
0 \leqslant \rho \leqslant 1,0 \leqslant \delta \leqslant 1, m \leqslant 0, \rho-\delta-\frac{m}{n} \geqslant 0 ;
\]

тогда оператор $P$ продолжается до ограниченного оператора $P: L_{2}\left(\mathbb{R}^{n}\right) \rightarrow L_{2}\left(\mathbb{R}^{n}\right)$. В частности, при условии (5) ограничены в $L_{2}(\mathbb{R} n)$ П. о. вида (1) с символами, удовлетворяющими оценкам (2) равномерно по $x$ (т. е. с постоянными $C_{\alpha, \beta, \mathscr{K}}=C_{\alpha}, \beta$, не зависящими от $\left.\mathscr{K}\right)$ ). Отсюда следует, напр., ограниченность в $L_{2}\left(\mathbb{R}^{n}\right)$ операторов $P \in L_{\rho, \delta}^{0}$, если $0 \leqslant \delta \leqslant \rho<1$ и ядро оператора $P$ имеет компактвый носитель (или оценки символа опять-


\end{document}